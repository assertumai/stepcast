Прогон завершился отказом. В контексте — status.json с состоянием всех работ.

Определи, что именно не сошлось: дефект реализации, неверная спека, кривой
промпт или проблема окружения. Верни разбор по схеме с одной гипотезой причины
и конкретным следующим действием.
