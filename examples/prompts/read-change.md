Разбери OpenSpec change `${inputs.change}` и составь план реализации.

В контексте: proposal, design и tasks этого изменения.

Верни план по схеме: список задач, для каждой — затрагиваемые файлы, порядок
и признак того, чем считать задачу выполненной. Не начинай реализацию.
