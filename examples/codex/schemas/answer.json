{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "properties": {
    "word": { "type": "string", "description": "Одно слово — ответ на вопрос" },
    "confidence": { "type": "integer", "minimum": 0, "maximum": 100 }
  },
  "required": ["word", "confidence"],
  "additionalProperties": false
}
