# docs/status.md собирается скриптом scripts/status.mjs из docs/status.base.md
# и фрагментов openspec/changes/*/status.md — разрешать конфликт слияния в
# сгенерированном файле руками нечего, содержимое живёт в источниках. Слияние
# объединением может дать неверный результат (например, две версии одной
# строки таблицы рядом); неисправленный результат ловит `npm run status:check`
# в составе `npm run check` до коммита.
docs/status.md merge=union
