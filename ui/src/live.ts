/**
 * `useLive` переехал в сервис `ui/src/services/live.ts` — контекст ядра, а не
 * хук компонента (design.md `cordis-kernel-browser`, Решение 10). Подписка
 * теперь живёт эффектом области сервиса, закрывается снятием этой области, а
 * не размонтированием компонента. Ничто больше не импортирует этот файл.
 *
 * Файл остаётся заглушкой, а не исчезает: физическое удаление файла,
 * отслеживаемого git, вне прав исполняющей работы
 * (`openspec/changes/agent-cannot-clean-up/design.md` — Non-Goal «Право
 * удалять файлы у агента» назван явно и намеренно) — а без git тело,
 * несущее прежнюю логику, осталось бы вторым, расходящимся источником
 * правды рядом с `ui/src/services/live.ts`.
 */
export {};
