// Экран «Прогоны» переехал в `ui/src/screens/runs.tsx` — строку состава
// `screen-runs`, применяемую плагином `ui/src/plugins/screens.tsx` по ответу
// демона (`ui-screens`, «Экран витрины — строка состава с двумя половинами»).
// Файл остаётся заглушкой: `rm` в песочнице недоступен, а трекнутый файл без
// экспортов линт и типизация проходят как обычный модуль.
export {};
