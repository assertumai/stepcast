## 1. Разбор составного состояния наружу

- [x] 1.1 Вынести из `src/core/anchor/composite.ts` разбор идентификатора составного состояния (`splitCompositeAnchorId`) и сверку разобранного с действующим составом; внутренний код якоря звать тот же разбор, второго не заводить.
- [x] 1.2 Тест: разбор возвращает корень и части в каноническом порядке; идентификатор чужого состава и идентификатор не той формы отличимы друг от друга по причине отказа.

## 2. Наложение по репозиториям (`src/core/run/apply.ts`)

- [x] 2.1 Завести один помощник «наложить дифф между двумя состояниями по репозиториям»: для `git`-якоря — прежние один `git diff` и один `git apply --3way` в `cwd`, для `composite` — по вызову на корень и на каждую различающуюся часть, каждый в её каталоге и без приставки пути.
- [x] 2.2 Перед наложением корневого патча сверять его пути с объявленным составом и отказывать названной причиной, если патч двигает запись gitlink объявленного каталога.
- [x] 2.3 Снять откат наполовину наложенного на составной якорь: `createAnchorer` с `nested`, снятый до первого наложения, восстанавливает корень и каждую часть; `ApplyOptions` получает `nestedRepos`.
- [x] 2.4 Перевести на помощник все три входа — `applyLane`, наложение прогона целиком и `--job` — и убрать из них отказы `anchor_kind === 'composite'`.
- [x] 2.5 Тесты наложения на настоящем составном прогоне: правки в корне и в части ложатся каждая в свой репозиторий; конфликт во второй части возвращает первую и корень; состояние чужого состава отказывает названной причиной; однорепозиторный прогон накладывается прежним путём.

## 3. Обход дорожки по репозиториям (`src/core/lanes/merge.ts`, `tree.ts`)

- [x] 3.1 `tree.ts`: коммит и откат, адресованные репозиторию (коммит «если есть что коммитить», `resetToCommit` на каталог части), и чтение сообщения `HEAD` для диагностики обрыва.
- [x] 3.2 Вычислять затронутые репозитории дорожки из её состояний (`tree_before` первого шага и последний непустой `tree_id`), не трогая дерева и не заглядывая в поле `repos` пункта.
- [x] 3.3 `MergeLanesOptions` получает объявленные команды проверки вложенных репозиториев (`--check` остаётся командой корня); ядро конфигурации по-прежнему не читает.
- [x] 3.4 Предполётные проверки до первого наложения: у каждого затронутого репозитория есть команда проверки; иначе отказ, называющий репозиторий и дорожку.
- [x] 3.5 Предполётная диагностика обрыва: `HEAD` репозитория несёт `<слаг>: …` пункта без проставленного исхода — отказ, называющий репозитории с коммитом и без него.
- [x] 3.6 Перестроить обход дорожки в порядок «запомнить коммиты → наложить во все → проверить все → `finishItem` → коммитить снизу вверх по гитлинкам, репозиторий очереди последним из тех, кого корень не ведёт»; проверки в детерминированном порядке, остановка на первой красной, причина называет репозиторий.
- [x] 3.7 Откат по репозиториям: каждый затронутый возвращается к своему записанному коммиту, снимок очереди восстанавливается как прежде.
- [x] 3.8 Снять отказ «файл очереди лежит внутри объявленного вложенного репозитория»; репозиторий очереди коммитить последним, даже если дорожка его дифом не задела.

## 4. Команда и отчёт

- [x] 4.1 `src/cli/commands/merge-lanes.ts`: собрать объявленные команды проверки вложенных репозиториев из конфигурации и передать в ядро; коды возврата не менять.
- [x] 4.2 Строка отчёта сведённой дорожки называет затронутые репозитории; коды возврата и итоговая строка остаются прежними.
- [x] 4.3 `src/cli/commands/run.ts`: итог составного прогона снова предлагает `stepcast apply`, а не причину отказа.

## 5. Проверки

- [x] 5.1 Тесты сведения составного прогона: дорожка, правящая корень и часть, даёт коммит в каждом, репозиторий очереди коммитится последним, отметка `done` — в последнем коммите.
- [x] 5.2 Тесты отката: красная проверка второго затронутого репозитория откатывает оба, незатронутый объявленный репозиторий не тронут, игнорируемое переживает откат.
- [x] 5.3 Тесты отказов: затронутый репозиторий без объявленной команды проверки; расхождение составов; движение gitlink; обрыв между коммитами (и отсутствие ложного срабатывания на пункте со статусом `done`).
- [x] 5.4 Тесты неизменности однорепозиторного пути: сведение без объявленного состава даёт ровно один коммит, прежние причины и прежние коды возврата.
- [x] 5.5 Тест очереди внутри вложенного репозитория: сведение проходит, отметка попадает в коммит этого репозитория, и он коммитится последним.
- [x] 5.6 Тест чистоты корня после сведения: корень, ведущий затронутую часть записью gitlink, подхватывает её коммитом и остаётся чист — в том числе когда очередь лежит в другом вложенном репозитории.
- [x] 5.7 Тест исполнения проверки в каталоге своего репозитория: команда, зелёная только в своём каталоге, у корня и у части.
- [x] 5.8 Тест терпимости предполётной проверки команд: негодная дорожка и дорожка без пункта, затронувшие репозиторий без объявленной проверки, обхода не роняют.

## 6. Документы

- [x] 6.1 `docs/run-layout.md` и `docs/pipeline-format.md`: снять ограничение «`stepcast apply` и `merge-lanes` недоступны составному прогону», описать наложение и сведение по репозиториям.
- [x] 6.2 `docs/backlog.md`: сколько коммитов у сведённой дорожки, где лежит файл очереди и почему её репозиторий коммитится последним.
- [x] 6.3 Фрагмент `openspec/changes/merge-lanes-per-repo/status.md` и пересборка `docs/status.md` командой `npm run status:build`.
- [x] 6.4 `npm run check` зелёный.
