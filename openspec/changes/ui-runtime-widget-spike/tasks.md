## 1. Падающие проверки

- [x] 1.1 Тест в новом `test/ui-widgets.test.ts`: состав виджетов проекта —
  файл `<проект>/.stepcast/widgets/<id>.tsx` попадает в перечень; вложенный
  `sub/deep.tsx` и файл `notes.md` — нет; отсутствующий каталог виджетов и
  проект без пути в `projects.json` дают пустой состав, а не исключение.
  Обязан падать: перечисления виджетов сегодня нет вовсе.
- [x] 1.2 Тест компиляции: `.tsx` с хуком и JSX компилируется в ES-модуль с
  экспортом по умолчанию; в результате остались импорты `react` и
  `react/jsx-runtime`, а тела React внутри нет; типы сняты.
- [x] 1.3 Тест кеша: повторная компиляция того же файла без правки не зовёт
  компилятор второй раз; правка файла (изменившая `mtime` или размер)
  обесценивает запись; кеш ограничен по числу записей и вытесняет давние.
- [x] 1.4 Тест ошибки компиляции: файл с синтаксической ошибкой даёт результат
  с путём, строкой, колонкой и текстом, а не исключение наружу.
- [x] 1.5 Тест обхода каталога: идентификатор с `..`, с разделителем пути, с
  видом абсолютного пути; символическая ссылка из каталога виджетов на файл
  снаружи; чужое расширение — каждый случай отклонён, содержимое не отдано.
- [x] 1.6 Тест перечня переходников в `test/ui-widgets.test.ts`: каждое
  объявленное реэкспортируемое имя действительно экспортируется установленной
  версией `react` / `react-dom` / `react/jsx-runtime`; отсутствующее имя роняет
  проверку.
- [x] 1.7 Тест отпечатка в `test/ui-watcher.test.ts`: появление, правка и
  удаление файла виджета меняют часть отпечатка и дают новое значение состава;
  такт, на котором сдвинулся только идущий прогон, оставляет **тот же объект**
  состава виджетов; правка очереди улучшений не пересобирает состав виджетов.
- [x] 1.8 Тест маршрутов в `test/ui-server.test.ts`: `/widgets/<key>/<id>.js`
  отдаёт модуль с типом `text/javascript`; непроходящий файл — 200, модуль
  ошибки и заголовок-пометка; неизвестный ключ, неизвестный `<id>` и путь под
  `/widgets/` сверх двух объявленных форм — 404 одинаковым ответом.
- [x] 1.9 Тест переходников: `/widgets/runtime/<имя>.js` отдаётся для каждого
  имени перечня и ни для какого другого; отданный текст читает опубликованный
  страницей объект и отказывает названной ошибкой, когда его нет.
- [x] 1.10 Тест события: поток `/api/events` присылает событие о виджетах при
  правке файла и не присылает его на такте, где сдвинулся только обзор;
  версия в событии меняется вместе с файлом.
- [x] 1.11 Тест жизненного цикла компилятора: после запроса виджета и закрытия
  сервера служебного процесса компилятора не остаётся, а процесс теста
  завершается сам; сервер, получивший компилятор снаружи, его не останавливает.
- [x] 1.12 Тест чтения без записи: после компиляции и отдачи виджета в каталоге
  проекта не появилось ни файла, ни каталога, а файл виджета не изменился.
- [x] 1.13 Тест изоляции серверов: два сервера витрины в одном процессе с
  разными корнями прогонов отдают виджеты своих проектов, и результат одного не
  попадает в ответ другого.

## 2. Компилятор и зависимость

- [x] 2.1 Замерить компиляцию представительного виджета `esbuild` и
  `esbuild-wasm`: холодный первый вызов, тёплый вызов, вес установки,
  поведение при отсутствующем платформенном бинарнике. Замер — скрипт в
  `scripts/`, чтобы число воспроизводилось, а не пересказывалось.
- [x] 2.2 Принять решение по критерию из `design.md` (Решение 1) и добавить
  выбранный пакет в `dependencies`; обновить `package-lock.json`.
- [x] 2.3 Ленивый динамический импорт компилятора; отказ импорта — названная
  ошибка в ответе и строка в логе демона, без падения демона.

## 3. Виджеты на стороне демона

- [x] 3.1 `src/ui/widgets.ts`: каталог виджетов проекта — перечисление
  `*.tsx` верхнего уровня, отпечаток каждого файла, разрешение пути с
  проверкой сегментов, границы каталога и приведением к реальному пути (приём
  `resolveJournalPath`, `src/ui/file.ts:62`).
- [x] 3.2 Компиляция одного файла без разрешения импортов (Решение 2) и разбор
  ошибки компилятора в структуру «файл, строка, колонка, текст».
- [x] 3.3 Кеш результата по «путь + `mtime` + размер» с пределом числа записей;
  заводится сервером, а не модулем (Решение 11).
- [x] 3.4 `src/ui/widgetRuntime.ts`: закрытый перечень имён и тексты
  модулей-переходников, читающих опубликованный страницей объект; названный
  отказ при его отсутствии.

## 4. Наблюдатель и поток событий

- [x] 4.1 Третья часть отпечатка `widgets` в `src/ui/watcher.ts:73` и её сбор в
  `fingerprint` (`:100`) — `mtime` и размер каждого файла виджета по проектам с
  известным путём.
- [x] 4.2 `currentWidgets()` и пересборка состава только по своей части
  отпечатка, с возвратом того же объекта при неизменной части (`poll`, `:186`).
- [x] 4.3 Событие `widgets` в `handleEvents` (`src/ui/server.ts:774`) по тому же
  правилу, что `backlog`: только когда наблюдатель отдал не то же значение.

## 5. Маршруты демона

- [x] 5.1 Маршрут `/widgets/<projectKey>/<id>.js`: 200 с модулем,
  200 с модулем ошибки и заголовком-пометкой, 404 одинаковым ответом на все
  причины отсутствия.
- [x] 5.2 Маршрут `/widgets/runtime/<имя>.js` по закрытому перечню; любой иной
  путь под `/widgets/` — 404, перечисления каталогов нет.
- [x] 5.3 Подъём и остановка компилятора в `createUiServer`
  (`src/ui/server.ts:835`) рядом с `registryCache` (`:849`): лениво при первом
  запросе, остановка в `close()` — и только тем, кто поднял (Решение 12).

## 6. Витрина

- [x] 6.1 Import map в `ui/index.html` до первого модульного скрипта;
  публикация экземпляров `react`, `react-dom`, `react/jsx-runtime` в
  `ui/src/main.tsx` до первой отрисовки.
- [x] 6.2 `ui/src/widgetHost.tsx`: загрузка модуля по адресу с версией, граница
  ошибок вокруг карточки со сбросом при смене версии, карточка ошибки
  компиляции с путём и строкой, карточка неразрешённого импорта.
- [x] 6.3 Подписка на событие `widgets` в `ui/src/live.ts` и переимпорт только
  при смене версии.
- [x] 6.4 Маршрут и пункт `MENU` в `src/ui/routes.ts`, экран
  `ui/src/pages/Widgets.tsx`: карточки по проектам, пустое состояние с
  указанием каталога. Комментарий модуля называет экран лесами спайка
  (Решение 13).
- [x] 6.5 Проксирование `/widgets` на демон в `vite.config.ts` рядом с `/api`
  (Решение 14); при необходимости — записи в `ui/tsconfig.json` и в список
  `server.fs.allow`.

## 7. Образец и документация

- [x] 7.1 Виджет-образец в `examples/widgets/` этого репозитория: хук, JSX,
  видимая реакция на действие — тот файл, копией которого в
  `<проект>/.stepcast/widgets/` проверяется замена на лету, и `docs/widgets.md`
  говорит, куда его копировать. В самом `.stepcast/widgets/` образец не лежит:
  этот путь вне объявленных `edit_paths` прогона — строка добавлена в
  `.stepcast/config.yml` и действует со следующего прогона.
- [x] 7.2 `docs/widgets.md`: файл виджета и каталог, адрес и форма запроса,
  доступные имена и их перечень, событие замены и версия, контракт ошибки,
  границы отдачи файлов, требование к браузеру.
- [x] 7.3 В `docs/widgets.md` — решение о компиляторе с числами замера (2.1),
  критерий, по которому оно принято, и упомянутый третьим кандидатом
  `oxc-transform`.
- [x] 7.4 В `docs/widgets.md` — раздел «чего в спайке нет»: проверки типов
  виджета, локальных импортов и зависимостей, песочницы, слоёв и props; рост
  памяти вкладки при частой замене; отмена свойства «демон — не файловый
  сервер» и чем она ограничена.
- [x] 7.5 `README.md`: строка о виджетах в обзоре возможностей.

## 8. Зелёная проверка

- [x] 8.1 Проверить, что комментарии `src/ui/assets.ts:8` и `vite.config.ts:9`
  о демоне, который «не становится файловым сервером», приведены в
  соответствие: свойство отменено названно и с указанием границ.
- [x] 8.2 `npm install --no-audit --no-fund && npm run check` — зелёный.
