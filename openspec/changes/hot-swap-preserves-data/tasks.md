## 1. Устойчивое опознание вклада и окно замены

- [x] 1.1 `SlotEntry` получает поле опознания от внутреннего `seq` (`ui/src/slots.ts:93`, `:189`): уникально в пределах страницы, не зависит ни от позиции, ни от имени владельца (Решение 4)
- [x] 1.2 `<Slot>` ключует списочные вклады и звенья цепочки опознанием вместо `${entry.owner}:${index}` (`ui/src/slots.tsx:64`, `:104`)
- [x] 1.3 Окно замены `SlotsService.batch(fn)`: уведомления, накопленные внутри, сливаются в одно по выходе; окно переживает `await` внутри себя и не теряет уведомления при отказе внутри него (Решение 3)
- [x] 1.4 Тесты реестра (`ui/test/slots.test.tsx`): опознание не меняется от снятия соседа, два вклада одного владельца различимы, повторно внесённый получает новое; окно даёт ровно одно уведомление

## 2. Единица замены в ядре витрины

- [x] 2.1 Сервис состава браузерных строк (`ui/src/services/plugins.ts`): строки по идентификатору, у каждой — версия, область и состояние `active` / `stale` / `failed` (Решения 1, 6)
- [x] 2.2 Имя сервиса — в занятых ядром (`KERNEL_SERVICE_NAMES`, `ui/src/kernel.ts:65`), с переводом отказа, называющим принадлежность; сервис заводится `createBrowserKernel()` рядом со `slots`, `live`, `screens`
- [x] 2.3 Швы в `BrowserKernelOptions`: загрузчик модуля половины и приёмник стилей, по умолчанию — `import(pluginModuleHref(...))` и `<style data-plugin="<id>">` в `document.head` (`ui/src/services/styles.ts`, Решение 8)
- [x] 2.4 Сверка состава: новая строка применяется, исчезнувшая снимается, разошедшаяся версия заменяется; замены одной строки последовательны — два обращения не снимают и не применяют её вместе
- [x] 2.5 Порядок замены (Решение 2): импорт модуля → снятие прежней области → применение новой, целиком внутри окна замены (1.3)
- [x] 2.6 Состояния строк ложатся в `Diagnostic` (`ui/src/kernel.ts:92`) и в полосу диагностик наравне с отказами сборки, с дублированием в `console.error`

## 3. Стили половины

- [x] 3.1 Имя экспорта стилей — в общем модуле (`src/ui/widgetRuntime.ts`, рядом с `WIDGET_ERROR_EXPORT`), чтобы сборщик и хост брали его из одного места (Решение 7)
- [x] 3.2 Хост применяет стиль эффектом области строки и снимает disposer'ом; приёмник по умолчанию ставит и убирает один элемент на строку, подставной — накапливает вызовы

## 4. Доставка браузерной половины (незакрытое `user-plugins-from-files`)

- [x] 4.1 Отпечаток каталога плагина и версия из него (`src/ui/plugins.ts`): рекурсивный обход с пропуском `node_modules`, `.git` и точечных каталогов, свёртка тем же приёмом, что `fingerprintVersion` (`src/ui/widgets.ts:91`) — задача 4.1 `user-plugins-from-files`, Решение 12
- [x] 4.2 Состав браузерных строк у демона: регистрация половины через уже готовый шов `onDirectoryRow` (`src/core/plugins/load.ts:92`), передача его из `currentDaemonKernel` (`src/ui/kernel.ts:108`) — задача 2.6 там же
- [x] 4.3 Компилятор витрины учится собирать бандл (`bundle: true`, `write: false`, внешние имена React) рядом с прежним преобразованием одного файла; CSS выхода уходит в экспорт стилей (3.1); кеш по отпечатку, без записи на диск (`src/ui/widgets.ts`, Решение 13)
- [x] 4.4 `pluginModuleHref(id, version)` в `src/ui/routes.ts` рядом с `widgetModuleHref` и форма адреса `/plugins/<id>.js?v=` в диспетчере (`src/ui/server.ts:201`): безопасный сегмент, границы каталога плагина по реальному пути, только строка действующего состава, иначе 404; ошибка сборки — 200 и исполняемый модуль с разобранной ошибкой
- [x] 4.5 Часть отпечатка «плагины» у наблюдателя и событие `plugins` в `/api/events` по правилу «только при отличии от отправленного» (`src/ui/watcher.ts`)
- [x] 4.6 Состав плагинов — часть снимка `live` (`ui/src/services/live.ts`), откуда его читает сверка состава (2.4); отдельного запроса за составом не заводится (Решение 11)
- [x] 4.7 Тесты демона (`test/ui-plugins.test.ts`): отдача собранной половины, локальный импорт внутри плагина, стили экспортом, ошибка сборки модулем, 404 у отключённой строки и у небезопасного сегмента, версия по отпечатку каталога, событие `plugins` при подключении и по правке

## 5. Браузерные тесты замены

- [x] 5.1 `ui/test/hotSwap.test.tsx`, подписка и данные: замена не открывает нового источника событий и не закрывает прежний, за данными сервисов ядра не уходит ни один запрос — счёт по `fakeEventSources` (`ui/test/support/live.ts`) и по подставленному `fetch` (Решение 10)
- [x] 5.2 Соседи: область второй строки не снималась и не применялась заново, опознание её вкладов не изменилось; замена дала одно уведомление реестра
- [x] 5.3 Каскад: зависимая область снята и применена заново после замены сервиса, цепочка из трёх перезапущена целиком, новая редакция без сервиса оставляет зависимых снятыми и названными в отказах
- [x] 5.4 Отказы: отказ загрузки оставляет прежнюю редакцию и назван обеими версиями; отказ применения оставляет строку без вкладов; прежняя редакция не применяется заново; следующая версия снимает состояние отказа
- [x] 5.5 Стили: сняты вместе со строкой, три замены подряд не копят их, подставной приёмник видит ровно одну действующую редакцию

## 6. Документация

- [x] 6.1 `docs/ui-plugins.md`: раздел «Что переживает замену, а что нет» — граница по владению (Решение 9), порядок замены, одно изменение состава, каскад зависимых и перезапуск их состояния, три состояния строки, стили экспортом; заменить оговорки «— `hot-swap-preserves-data`» на описанное поведение
- [x] 6.2 `docs/plugins.md`, «Известные ограничения»: ограничение «изменённый код витрина не подхватывает» снято для браузерной половины и остаётся для серверной — с названным способом (перезапуск демона) и ссылкой на незакрытые задачи 4.2—4.3 `user-plugins-from-files`; названо, что замены копят модули во вкладке
- [x] 6.3 `docs/widgets.md`: чем виджет отличается от браузерной половины плагина — хост с версией против области строки, и что из гарантий замены на виджеты не распространяется
- [x] 6.4 `npm run check` зелёный целиком
