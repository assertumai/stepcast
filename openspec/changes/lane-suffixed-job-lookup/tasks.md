## 1. Пространство подстановки `job.lane`

- [x] 1.1 Добавить пространство `job` с именем `lane` в перечень известных пространств и в модель подстановок (`src/core/pipeline/model.ts`, `src/core/pipeline/interpolate.ts`); раскрывается при разборе документа, наравне с `params` и `project`, отложенным NOT является
- [x] 1.2 Класть значение обвязки `lane` в область тела работы в `expandPipeline` (`src/core/pipeline/expand.ts`) — обвязка раскрывается там раньше сборки области тела, порядок менять не требуется; сделать это в обеих ветвях: работа, подключённая через `uses`, и работа, объявленная в пайплайне телом
- [x] 1.3 Отказ разбора на `${job.lane}` в работе без объявленной метки: сообщение называет работу и то, что дорожка не объявлена; ни один путь не должен давать пустую строку
- [x] 1.4 Убедиться, что ключ `lane` внутри документа с `kind: job` по-прежнему отклоняется `rejectWiringKeys`, и что `${job.lane}` доступен в теле работы, в её шагах и в тексте файлов промптов
- [x] 1.5 Тесты (`test/expand.test.ts`): раскрытие `${job.lane}` в теле работы и в файле промпта, сборка имени `plan-${job.lane}` в `context_upstream`, отказ у работы без метки, `lane` внутри файла работы по-прежнему отклоняется

## 2. Метка `all` и словарь дорожек

- [x] 2.1 Отклонять `lane: all` в `stepcast lint` рядом с проверкой kebab-case (`src/core/lint.ts`), с сообщением, называющим работу и причину резервирования
- [x] 2.2 Собрать множество меток `lane`, объявленных пайплайном, в одном месте — рядом с разбором графа (`src/core/graph.ts` либо `src/core/lint.ts`), чтобы им пользовались все проверки задачи 3, а не каждая своим проходом
- [x] 2.3 Тесты (`test/lint.test.ts`): отказ на `lane: all`, слаг `all-lanes` меткой остаётся допустимым

## 3. Проверка «работа дорожки не адресует чужую дорожку»

- [x] 3.1 Отклонять ошибкой запись `needs` работы с меткой `X`, называющую работу с меткой `Y ≠ X`; сообщение называет обе работы, обе дорожки, файл и точечный путь поля
- [x] 3.2 То же для записи `context_upstream` — рядом с существующей `checkContextUpstream`, отдельной диагностикой: «работа не выше по графу» и «работа чужой дорожки» — разные причины и чинятся по-разному
- [x] 3.3 То же для подстановки `${jobs.<id>...}`, где `<id>` — работа с меткой `Y ≠ X`; проверка идёт по той же карте подстановок, что и правила `lint-unknown-job-reference`, и потому охватывает подстановки из файлов промптов с указанием файла и позиции
- [x] 3.4 Отклонять ошибкой сегмент пути подстановки, равный метке `Y ≠ X`; сегмент, равный собственной метке работы, ошибкой не является
- [x] 3.5 Не применять правило к работе без метки `lane`: `slots`, `merge` и `finalize` адресуют все дорожки по назначению
- [x] 3.6 Тесты (`test/lint.test.ts`): слот чужой дорожки в подстановке, `needs` на работу соседней дорожки, `context_upstream` на неё же, выход работы соседней дорожки, собственная дорожка во всех четырёх местах без ошибки, работа без метки без ошибки, ссылка из общего файла промпта с указанием файла и позиции

Дополнительно (решение задачи 6 плана предыдущей работы): то же правило применено пятым местом к выражению `if` — оно разбирается структурно тем же `references(expression)`, что и существующая проверка «работа выше по графу», и файлы петли несут такие условия (`self-improve.yml:167`, `:265`). Отражено в `design.md` и `specs/pipeline-definition/spec.md`, покрыто тестом в `test/lint.test.ts`.

## 4. Перечень дорожек сведения

- [x] 4.1 Принимать `--lanes all` в `stepcast merge-lanes` (`src/cli/commands/merge-lanes.ts`): множество берётся из `knownLanes(status.jobs)`, порядок — первого появления дорожки в записях работ прогона
- [x] 4.2 Отказ на `--lanes all` в прогоне без единой метки `lane`, называющий это, вместо пустого обхода с видом успеха
- [x] 4.3 Отказ на явном перечне, не назвавшем известную прогону дорожку (`src/core/lanes/merge.ts`, рядом с существующей проверкой неизвестной дорожки): сообщение называет недостающие дорожки и оба способа выразить намерение — `--lanes all` либо снятие метки `lane`
- [x] 4.4 Убедиться, что отказ наступает до наложения первой дорожки и дерево остаётся нетронутым
- [x] 4.5 Тесты (`test/merge-lanes.test.ts`): обход по `all` на прогоне с двумя дорожками, отказ на `all` без дорожек, отказ на укороченном перечне с нетронутым деревом, полный явный перечень работает как прежде, цепочка без метки `lane` упоминания не требует

## 5. Петля переходит на объявленную дорожку

- [x] 5.1 Снять параметр `lane` из `.stepcast/jobs/propose.yml`, `.stepcast/jobs/propose-express.yml` и `.stepcast/jobs/implement.yml`; заменить `${params.lane}` на `${job.lane}` в путях и в `context_upstream`
- [x] 5.2 Снять передачу `with: { lane: … }` во всех работах обоих пайплайнов петли — `.stepcast/pipelines/self-improve.yml` и `.stepcast/pipelines/self-improve-memory.yml`; ключи обвязки `lane` оставить нетронутыми
- [x] 5.3 Перевести `.stepcast/jobs/merge.yml` на `--lanes all`
- [x] 5.4 Прогнать `stepcast lint` по обоим пайплайнам петли и починить всякое обращение к чужой дорожке, если проверка задачи 3 его вскроет; правки документа перечислить в отчёте работы — ручной обход обоих файлов не нашёл ни одного обращения к чужой дорожке (все `${jobs.slots.output.lanes.<буква>...}` совпадают с меткой своей работы); проверено автоматически `test/loop-portability.test.ts` в рамках задачи 6
- [x] 5.5 Убедиться, что оба пайплайна петли остались копиями друг друга во всём, кроме трёх объявленных отличий: расхождение обесценивает замер, ради которого второй заведён

## 6. Проверка по действующим файлам петли

- [x] 6.1 Добавить в `test/loop-portability.test.ts` разбор обоих пайплайнов петли и требование, чтобы правила задачи 3 на них не срабатывали
- [x] 6.2 Добавить туда же проверку, что параметра `lane` в файлах работ петли не осталось и что перечень дорожек в `.stepcast/jobs/**` не набран именами; обе проверки называют файл и строку при отказе
- [x] 6.3 Оставить фикстуры `test/lanes.test.ts` как есть: модуль `src/core/lanes/lanes.ts` решает годность по меткам и обязан доказывать это именами работ, которых в петле нет

## 7. Документация и сборка

- [x] 7.1 Описать пространство `job.*` в разделе подстановок `docs/pipeline-format.md`, вместе с оговоркой про соседство `job` и `jobs` и запретом пустого значения
- [x] 7.2 Добавить в раздел `stepcast lint` того же документа новые пункты «Отклоняет»: обращение работы дорожки к чужой дорожке (все четыре места) и метку `all`; назвать риск ложного срабатывания на сегменте пути и способ починки — переименование дорожки
- [x] 7.3 Описать `--lanes all` и отказ на укороченном перечне в разделе сведения дорожек
- [x] 7.4 Пересобрать `schema/pipeline.schema.json` и `schema/job.schema.json` — не потребовалось: ни одна zod-модель (`src/core/pipeline/schema.ts`) в этом изменении не менялась, `test/schema-generated.test.ts` подтверждает отсутствие расхождения
- [x] 7.5 Прогнать объявленную команду проверки репозитория до зелёного
