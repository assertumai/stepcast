## 1. Падающие проверки

- [x] 1.1 Тест в `test/journal.test.ts`: записанный движком манифест несёт
  версию формата, равную `JOURNAL_FORMAT`, — и начальная запись, и финальная,
  дописывающая `finished_at` и `status`. Проверка обязана падать: поля ещё
  нет.
- [x] 1.2 Тест: манифест прежней формы (без поля `format`) читается как
  прежде, расхождением версий не считается и никакого предупреждения не даёт.
- [x] 1.3 Тест: `run.json`, дописанный сырым JSON полем, которого схема не
  знает, даёт диагноз «расхождение версий» с именем этого поля, файлом
  `run.json` и обеими версиями формата. Ключевой сценарий пункта очереди:
  ровно так `pid` и обрушил чтение шести прогонов.
- [x] 1.4 Тест: журнал, объявивший `format` больше `JOURNAL_FORMAT`, но
  разобравшийся целиком, отдаёт разобранные данные и предупреждение о
  расхождении версий — сверка не ждёт незнакомого ключа.
- [x] 1.5 Тест: оборванный `run.json` (нечитаемый JSON) и `status.json` с
  полем неверного типа дают диагноз порчи, а не расхождения версий; версия
  формата в диагнозе `status.json` взята из соседнего `run.json`.
- [x] 1.6 Тест: отпечаток схем журнала совпадает с пришпиленным рядом с
  `JOURNAL_FORMAT`; текст падения называет оба шага — поднять версию и
  обновить отпечаток.
- [x] 1.7 Тест в `test/ui-overview.test.ts`: прогон с полем, которого читатель
  не знает, приходит в обзоре с `problem` — вид `version-skew`, файл, поле,
  обе версии, — и по-прежнему остаётся в обзоре, а не пропадает.
- [x] 1.8 Тест: прогон с пустым каталогом (ни манифеста, ни состояния) даёт
  `problem` вида `missing`, а прежний булев `unreadable` остаётся истинным —
  экран уборки и `src/ui/usage.ts` читают его и должны продолжать работать.
- [x] 1.9 Тест в `test/ui-snapshot.test.ts`: снимок прогона с нечитаемым
  состоянием несёт тот же `problem`, вместо того чтобы отдавать пустое имя
  пайплайна и пустой список работ молча.
- [x] 1.10 Тест в `test/ui-watcher.test.ts`: наблюдатель, собравший обзор с
  нечитаемым прогоном, печатает одну строку с прогоном, файлом, полем и
  обеими версиями; при нескольких пересборках подряд строка остаётся одной;
  расхождение версий добавляет к строке команду перезапуска.

## 2. Версия формата журнала

- [x] 2.1 `src/core/journal/format.ts`: `JOURNAL_FORMAT = 1` с объяснением,
  когда его поднимают; пришпиленный отпечаток схем журнала и функция его
  вычисления через `z.toJSONSchema` — тем же приёмом, что
  `scripts/generate-schema.ts`.
- [x] 2.2 `src/core/journal/schema.ts`: необязательное `format` в
  `RunManifestSchema` с комментарием о смысле его отсутствия — журнал прежней
  формы, а не нулевая версия.
- [x] 2.3 `src/core/journal/writer.ts`: `writeManifest` проставляет
  `format: JOURNAL_FORMAT` сам, не полагаясь на вызывающего (design.md,
  Решение 1).
- [x] 2.4 Тест отпечатка из 1.6 в `test/journal.test.ts`; порядок схем в
  отпечатке объявлен явно, чтобы результат не зависел от порядка импортов.

## 3. Диагноз чтения

- [x] 3.1 `src/core/schema-failure.ts`: переезд `describeSchemaFailure` и её
  помощников из `src/core/pipeline/load.ts`; `load.ts` реэкспортирует имя,
  его вызывающие не меняются (design.md, Решение 5).
- [x] 3.2 `src/core/journal/format.ts`: дешёвая проба версии — сырой
  `JSON.parse` манифеста и числовое поле `format`, без схемы, чтобы версия
  была доступна и при отказе разбора.
- [x] 3.3 `src/core/journal/reader.ts`: тип `JournalProblem` и классификация
  по трём правилам (design.md, Решение 4) — незнакомый ключ даёт
  `version-skew`, отсутствие файла и нечитаемый JSON дают `missing`
  и `malformed`, версия журнала выше версии читателя даёт `version-skew` и
  при удавшемся разборе.
- [x] 3.4 `readManifestSoft` и `readStatusSoft` по образцу `readUsageSoft`:
  `{ manifest?, problem? }` и `{ status?, problem? }`, обе половины
  независимы. Бросающие `readManifest`/`readStatus` остаются для команд CLI.
- [x] 3.5 Текст отказа `readManifest` перестаёт утверждать порчу там, где её
  нет: «Манифест прогона повреждён» уступает место причине из диагноза.

## 4. Витрина

- [x] 4.1 `src/ui/overview.ts`: `readRun` строится на мягком чтении, пустых
  `catch` не остаётся; `RunOverview.problem` рядом с прежним `unreadable`,
  который сохраняет своё значение.
- [x] 4.2 `src/ui/snapshot.ts`: тот же `problem` в `RunSnapshot`, вместо
  молчаливого `undefined` при нечитаемом состоянии.
- [x] 4.3 `src/ui/watcher.ts`: печать беды через параметр `log` с умолчанием;
  множество уже сказанного с ключом «прогон + файл + причина» — одна строка
  на беду за жизнь демона (design.md, Решение 7).
- [x] 4.4 `src/ui/server.ts`: передача `log` наблюдателю; умолчание пишет в
  `process.stderr`, отведённый `startDetached` в `~/.stepcast/ui.log`.
- [x] 4.5 `ui/src/api.ts`: тип `JournalProblem`, построчно сверенный с
  `src/ui/overview.ts`.
- [x] 4.6 `ui/src/pages/Runs.tsx`: причина текстом в строке прогона — файл и
  поле; пометка «читатель устарел» вместо «не читается» при расхождении
  версий.
- [x] 4.7 `ui/src/pages/Runs.tsx`: одна полоса вверху экрана, когда
  расхождение задело хоть один прогон, — число задетых прогонов, обе версии и
  команда `stepcast down && stepcast up` (design.md, Решение 6).
- [x] 4.8 `ui/src/pages/RunDetail.tsx`: объяснение причины вместо пустого
  имени пайплайна и пустого списка работ.
- [x] 4.9 Стили в `ui/src/styles.css` в словаре существующих классов; полоса
  вверху экрана — той же вёрсткой, что прочие сообщения витрины.

## 5. Проверка

- [x] 5.1 `npm run check` зелёный целиком.

## 6. Документация

- [x] 6.1 `docs/run-layout.md`, раздел о манифесте: поле `format`, его смысл,
  правило подъёма и то, что отсутствие поля означает журнал прежней формы.
- [x] 6.2 `docs/run-layout.md`, раздел о витрине: что видно при расхождении
  версий — причина в строке прогона, полоса с командой перезапуска, строка в
  логе демона; и почему строгие схемы остаются строгими.
- [x] 6.3 Признак выполненности пункта очереди называет `docs/status.md` —
  документа больше нет (`drop-status-doc`), и возвращать его нельзя:
  `test/no-status-doc.test.ts` стережёт и файл, и упоминание. Вместо него
  согласуется `docs/run-layout.md`.

## 7. По итогам ревью

- [x] 7.1 Отпечаток схем связан с версией: версия подмешана в хеш, отпечатки
  лежат таблицей «версия → отпечаток». Тест сверяет строку действующей версии
  и полноту таблицы — правку схемы больше не замять заменой одной строки.
- [x] 7.2 `unknownKeyKind`: незнакомый ключ в журнале старше читателя даёт
  `legacy-journal`, а не `version-skew`; перезапуск демона такому прогону
  лекарством не называется — ни в логе, ни на странице прогона, ни пометкой в
  строке обзора.
- [x] 7.3 `readUsageSoft` ставит диагноз наравне с манифестом и состоянием;
  прежнее перечисление `unavailable` остаётся ради `stepcast usage`.
- [x] 7.4 `readEventsSoft`: строка событий, не прошедшая схему, называется
  файлом, номером строки и причиной; `readResetHint` по усечённому списку
  момент сброса окна не считает.
- [x] 7.5 Лог демона печатает отказ разбора, а не отсутствие ещё не
  записанного файла; витрина об отсутствии `status.json`/`usage.json` при
  читаемом манифесте молчит.
- [x] 7.6 `buildSnapshot` читает манифест и сводку расхода: беда `run.json`
  объясняется и на странице прогона, а не только в строке обзора.
- [x] 7.7 Тексты согласованы: `readStatus` говорит «Состояние прогона
  повреждено», полоса витрины склоняет число прогонов («2 прогона записаны»).
