## 1. Разведка и запись настоящего потока

- [ ] 1.1 Проверить, установлен ли `codex` и аутентифицирован ли он (`codex --version`, короткий `codex exec`). Если CLI нет — установить его. Если установить или аутентифицировать нельзя, **остановиться и сказать прямо**, что изменение в этом окружении не может быть закончено: пункты 1.3, 5 и 6 без CLI невыполнимы, а сочинённый поток — ровно та подделка, ради устранения которой заведён пункт очереди.

  **Остановлено.** `codex` установлен (`/Users/valentina/.local/bin/codex`), но окружение реализации — изолированная песочница автоматического прогона, — запрещает исполнять этот бинарник: любой вызов (`codex --version`, полный путь, с `dangerouslyDisableSandbox`) отвечает отказом разрешения, который в этом прогоне никто не подтверждает, и то же самое для чтения `~/.codex` (вне разрешённых рабочих каталогов сессии). Аутентифицировать или проверить CLI отсюда невозможно. Следствие — пункты 1.2–1.4, 4.3–4.5, 4.8 (разбор записанного потока и его тесты), 5 и 6 не выполнены; сочинённый поток не заводился. Выполнено то, что от CLI не зависит: раздел 2 (направление идентификатора сессии в ядре) и раздел 3 (граница `src/backends/**`, эслинт-правило и тест поверхности) — оба с падающими-затем-зелёными тестами.

- [ ] 1.2 Снять справку `codex exec --help` (и `codex exec resume --help`) и выписать в рабочие заметки точные написания: неинтерактивный запуск, потоковый JSON, чтение промпта со stdin, схема вывода, модель, режим песочницы, продолжение нити. Всё, что расходится с `design.md` (решение 1), исправлять в пользу CLI, а расхождение назвать в `docs/plugins.md`.

- [ ] 1.3 Записать настоящий поток в `test/fixtures/codex/`: (а) успешный прогон со структурированным выводом по схеме; (б) прогон с вызовом инструмента; (в) продолжение нити по идентификатору; (г) отказ (лимит либо аутентификация) — если поймать отказ нельзя, назвать это прямо в README фикстур и разбор отказа проверить на записи, полученной иначе, не выдавая её за живую. Рядом положить `test/fixtures/codex/README.md`: версия CLI, дата, команда записи. Вычистить пути домашнего каталога, имена машин и всё похожее на ключ; содержательные поля не править.

- [ ] 1.4 По записи (1.3) решить вопрос расхода по правилу `design.md` (решение 4): несёт ли поток расход последнего обращения (дельту) или только нарастающий итог. Ответ записать в рабочие заметки — он определяет пункт 4.4 и абзац документации 7.2.

## 2. Правка ядра: направление идентификатора сессии

- [x] 2.1 Падающий тест в `test/backend.test.ts` (либо новом `test/session-ids.test.ts`): адаптер объявляет, что идентификатор сессии выдаёт бэкенд; первый запуск шага собирается **без** идентификатора и без признака продолжения; после события потока с идентификатором `AgentStepResult.sessionId` равен сообщённому; второй шаг того же псевдонима запускается с ним и с признаком продолжения. Тест обязан падать на сегодняшнем коде: поля направления в `BackendCapabilities` нет, `executeAgentStep` всегда берёт идентификатор из `SessionRegistry.acquire()`.

- [x] 2.2 Падающий тест там же: работа с `session: shared` на адаптере с идентификаторами от бэкенда **не** даёт события `backend.degraded` (`runner.ts:636-654`), в отличие от адаптера, объявившего отсутствие сессий.

- [x] 2.3 Падающий тест там же: первая попытка продолжала засеянный идентификатор, завершилась ненулевым кодом и не сообщила о начале сессии — засев снят, следующая попытка идёт без идентификатора (прежнее поведение `onFailedContinuation`, `agentStep.ts:325-333`, сохранённое для нового направления).

- [x] 2.4 `src/core/backend/types.ts`: в `BackendCapabilities` добавить обязательное поле направления идентификатора сессии (`'engine' | 'backend'`) с комментарием, объясняющим, почему оно обязательное, — по образцу `mcp` (`types.ts:19-25`). В `BackendEvent` добавить вариант «бэкенд сообщил идентификатор сессии».

- [x] 2.5 `src/core/exec/agentStep.ts`: `SessionRegistry` получает чтение без заведения (идентификатор псевдонима, если сессия начата, иначе `undefined`); реализация в `createSessionRegistry()`. В `executeAgentStep()` выбор идентификатора при сборке `launch()` зависит от объявленного направления: движок — как сегодня (`acquire`), бэкенд — только уже известный идентификатор, иначе ничего. Событие с сообщённым идентификатором засевает псевдоним (`seed`) и становится `sessionId` результата шага.

- [x] 2.6 `src/core/backend/claude.ts` и `src/core/backend/fake.ts`: объявить прежнее направление (идентификатор выдаёт движок). Поведение обоих не меняется. Обновить остальные места сборки объекта возможностей — их назовёт компилятор.

- [x] 2.7 Убедиться, что ничего, кроме перечисленного, в `src/core` не изменилось: это единственная осознанная правка ядра в изменении (`design.md`, решение 3).

  По итогам ревью правка ядра расширена — тем же поводом и в тех же границах: `judgePass.ts` (вызов судьи не заводит выдуманного идентификатора адаптеру с направлением `backend`), `registry.ts` (подбор отказывает адаптеру, не объявившему направление) и `runner.ts` (шаг без названной бэкендом нити записывается вовсе без поля `session`, а не с пустой строкой). Список файлов назван в `design.md`, решение 3.

## 3. Публичная поверхность плагинов

- [x] 3.1 Падающий тест `test/plugin-surface.test.ts`: перечислить импорты всех модулей `src/backends/**` (чтением исходников) и потребовать, чтобы каждый вёл либо во встроенный модуль Node, либо в `../../plugin.js`. На пустом каталоге тест проходит вырожденно — довести его до падения вместе с появлением модуля (4.1).

  По итогам ревью: разрешён ещё и соседний модуль самого `src/backends` — иначе тест отверг бы раскладку плагина из 4.1/4.6 (`index.ts` с манифестом плюс `adapter.ts`). Разбор дополнен побочным (`import '…'`), динамическим (`import('…')`) и `require('…')` импортом, а форма с вычисленным спецификом запрещена целиком: проверить её статически нечем. Сам разбор покрыт отдельным тестом.

- [ ] 3.2 `src/plugin.ts`: экспортировать то, чего адаптеру не хватает, — `effectivePermissions` (`src/core/backend/permissions.ts`), `messagePrefix`, `BACKEND_REFUSAL_PREDICATE` и тип `McpServerStatus`. Добавлять только то, что реально понадобилось адаптеру; каждое добавление объяснить строкой комментария там же. Файл лежит вне `src/core` и правкой ядра не считается.

- [x] 3.3 `eslint.config.js`: правило, запрещающее модулям `src/backends/**` импортировать `src/core/**` (в дополнение к тесту 3.1 — линт называет это на месте правки, а не в конце прогона тестов).

## 4. Адаптер Codex

- [ ] 4.1 `src/backends/codex/adapter.ts`: `launch()` собирает неинтерактивный запуск по флагам из 1.2 — промпт через stdin, потоковый JSON, схема вывода файлом (`AgentInvocation.outputSchemaPath` уже путь), модель, режим песочницы из `permissions.mode`. Продолжение нити — форма команды, принимающая идентификатор, когда он передан и признак продолжения поднят.

- [ ] 4.2 Там же: `permissions.allow`/`permissions.deny`, которые Codex выразить не может, приводят к `StepcastError` из `launch()` — с бэкендом, невыполнимой частью объявления и причиной (`design.md`, решение 5). `effectivePermissions()` берётся из `stepcast/plugin`, а не переписывается.

- [ ] 4.3 Там же: `parseLine()` по записанному потоку (1.3). Начало нити даёт **и** `init`, **и** событие с идентификатором сессии (`design.md`, решение 6). Итоговое сообщение агента даёт `result` с текстом и, при объявленной схеме, со структурированным выводом. Вызовы инструментов — `tool_use`. Неизвестная запись — `ignored`, неразбираемая строка — `unparsed`.

- [ ] 4.4 Там же: расход — по решению из 1.4. Дельта в потоке есть — отдавать её событием `usage`. Дельты нет — сообщать расход единственный раз, в `result`. Вычитать нарастающие итоги внутри адаптера **запрещено**: экземпляр адаптера один на прогон и обслуживает одновременные шаги (`runner.ts:709-717`).

- [ ] 4.5 Там же: классификация неустранимых отказов (`BackendRefusal`) по записанной форме сообщений Codex — свой разбор, а не заимствованный у Claude. Момент сброса окна лимита заполняется, только если сообщение его действительно называет; иначе поле остаётся пустым.

- [ ] 4.6 `src/backends/codex/index.ts`: объект плагина по умолчанию — имя, версия, вклад `backends.codex` с `create(config)` и `defaults` (`command`, `sessions`, `structured_output`; `mcp` и жёсткий режим — выключены). Возможности адаптера собираются из переданной записи конфигурации, как у `claude`, а не прошиваются литералами.

- [ ] 4.7 `package.json`: подпуть `"./backends/codex": "./dist/src/backends/codex/index.js"` в `exports`.

- [ ] 4.8 Тест `test/codex-adapter.test.ts`: разбор каждого записанного образца (1.3) — начало нити, идентификатор сессии, текст и структурированный вывод, расход, отказ; заведомо битая строка даёт `unparsed`; сборка запуска содержит ожидаемые флаги (промпт через stdin, схема, модель, продолжение по идентификатору); объявленный `allow` даёт отказ с внятным сообщением.

- [ ] 4.9 Тест загрузки: плагин, объявленный ключом `plugins` как модуль адаптера, попадает в реестр и разрешается `resolveAdapter('codex', …)`; без объявления — прежний отказ «адаптер не предоставлен ни встроенно, ни плагином». Место — `test/plugin-load.test.ts` либо `test/codex-adapter.test.ts`, по тому, где меньше вспомогательного кода.

## 5. Приёмочный пайплайн

- [ ] 5.1 `examples/codex/stepcast.yml`: один короткий агентский шаг на `agent: codex` с `output_schema` и предикатом `judge` на том же бэкенде, с потолком расхода и коротким таймаутом — по образцу `examples/acceptance/stepcast.yml`.

- [ ] 5.2 `examples/codex/README.md`: как запускать — из отдельного временного каталога-проекта с конфигурацией, объявляющей плагин абсолютным путём к собранному модулю (`design.md`, решение 8), с готовыми командами. Прямо сказать, что в `.stepcast/config.yml` самого репозитория плагин **не** объявляется, и почему.

- [ ] 5.3 Упомянуть `codex/` в `examples/README.md` рядом с `minimal/` и `acceptance/`, с пометкой, что он требует установленного и аутентифицированного CLI.

- [ ] 5.4 `.stepcast/config.yml` не трогать (`design.md`, решение 8).

## 6. Живой прогон

- [ ] 6.1 Выполнить приёмку по `examples/codex/README.md`: агентский шаг и вызов судьи проходят на настоящем Codex. Проверить по журналу прогона: статус работы, `output.json` шага, вердикт судьи, записанный расход и идентификатор сессии, равный сообщённому бэкендом.

- [ ] 6.2 Прогнать приёмку второй раз с `session: shared` на двух агентских шагах — второй шаг продолжает нить первого, и деградации сессий в журнале нет.

- [ ] 6.3 Исход живого прогона назвать в отчёте о работе прямо: что прошло, что нет. Записанный поток выдавать за живой прогон запрещено.

## 7. Документация

- [x] 7.1 `docs/plugins.md`, раздел «Бэкенд»: направление идентификатора сессии — новое обязательное поле возможностей, зачем оно и что бывает с адаптером, который его не объявил.

- [ ] 7.2 `docs/plugins.md`, новый раздел о втором бэкенде: что понадобилось контракту ради него (поле направления, добавленные экспорты `stepcast/plugin`), чего не понадобилось прямым текстом (путь схемы вывода, классификация отказов, умолчания вклада — этого хватило как есть), и чего не хватило, но править не стали: форма сообщения расхода (по итогу 1.4, с последствием — потолок не обрывает идущий шаг) и отсутствие предстартовой проверки на невыполнимые списки доступа (адаптеру остаётся отказывать из `launch()`).

- [x] 7.3 `docs/plugins.md`, «Известные ограничения»: сессия, начинающаяся одновременно в двух работах одной группы, у бэкенда со своими идентификаторами не склеивается; плагин, поставляемый в том же пакете, — не полная дистанция вытянутой руки, и держится она тестом и правилом линта.

- [x] 7.4 Проверить, что `docs/plugins.md` нигде не остался с утверждением, будто идентификатор сессии всегда назначает движок. Проверено: раздел «Бэкенд» теперь называет оба направления явно, других мест с таким утверждением в документе не найдено.

  По итогам ревью проверено то же самое за пределами `docs/plugins.md`: заголовочный комментарий `src/core/exec/agentStep.ts` называл движок единственным источником идентификаторов — исправлен. Обещание документации про отказ адаптеру без объявленного направления теперь подкреплено проверкой в `resolveAdapter()`, а не остаётся обещанием.

## 8. Проверка

- [ ] 8.1 `npm run check` проходит целиком, включая новые `test/codex-adapter.test.ts`, `test/plugin-surface.test.ts` и обновлённые тесты сессий.

  Частично: `npm run check` проходит целиком (2266 тестов, 0 отказов) с
  `test/plugin-surface.test.ts` и обновлёнными тестами сессий. `codex-adapter.test.ts`
  не заведён — раздел 4 (адаптер Codex) не реализован из-за блокировки 1.1.
- [x] 8.2 `npm run check` проходит на машине без установленного Codex (сеть и CLI автоматическим тестам не нужны). Подтверждено этим же прогоном: CLI в этом окружении недоступен вовсе, и `npm run check` тем не менее прошёл целиком.
- [x] 8.3 `openspec validate first-real-backend-plugin --strict` проходит.
