## 1. Сервис ядра и контракт вклада

- [x] 1.1 `src/core/plugins/kernel.ts`: четвёртый `ContributionService<StepKindContribution>` с именем `steps`; `steps` в `KERNEL_RESERVED_NAMES` и в `KIND_NAMES` («вида шага»); комментарий объясняет, почему вид шага — сервис, а не поле в трёх существующих (design.md, решение 1).
- [x] 1.2 `src/core/plugins/kernel.ts`: память о прежнем владельце снятого имени на время жизни ядра (design.md, решение 8) — с комментарием о том, ради какого сообщения она заведена и почему не переживает процесс.
- [x] 1.3 `src/core/plugins/kernel.ts`: отказ регистрации на имени вида шага, совпадающем с ключом общей части шага либо с ключом встроенного вида (design.md, решение 3). Перечень запрещённых ключей — один экспортируемый список, общий с `pipeline/schema.ts`, а не вторая копия.
- [x] 1.4 `src/core/plugins/contract.ts`: типы `StepKindContribution` (`name`, `title`, `fields`, `output?`, `lint?`, `execute`), `StepKindInput`, `StepKindOutcome`, `StepKindLog`; внутренняя форма `document` для встроенных видов (design.md, решение 2) с комментарием об асимметрии; `steps` в `StepcastPluginSchema` и в `StepcastPlugin`.
- [x] 1.5 `src/core/plugins/context.ts`: `steps: ContributionRegistrar<StepKindContribution>` в публикуемом типе контекста; `src/plugin.ts` реэкспортирует типы вклада, входа и результата.
- [x] 1.6 `src/core/plugins/registry.ts`: карта `steps` в `Registry`, `steps` в `KINDS` для `owners`; `stepKindNames(registry)` по образцу `predicateNames` — встроенные и плагинные вместе, отсортированные.
- [x] 1.7 Тесты `test/plugin-registry.test.ts`, `test/plugin-kernel.test.ts`: вид шага в реестре с владельцем; конфликт двух плагинов и конфликт с встроенным; отказ на имени `expect` и на имени `run`; снятие области снимает вид шага и освобождает имя; снятое имя помнит прежнего владельца; сервис `steps` нельзя завести плагином. (Реализовано в `test/plugin-step-kind.test.ts` — единый файл для всей возможности, чтобы регистр, разбор, линт и исполнение проверялись рядом.)

## 2. Регистрация встроенных видов без изменения поведения

- [x] 2.1 `src/core/plugins/builtin.ts`: встроенные виды `agent`, `run`, `script`, `uses` регистрируются вкладами внутренней формы — zod-ветвь документа и типизированный разбор, вынесенные из `pipeline/schema.ts` и `pipeline/expand.ts` без правки их содержимого. Комментарий объясняет, почему их ветвь остаётся zod (design.md, решение 2). (Zod-ветви остались в `pipeline/schema.ts`, откуда `buildDocumentSchemas` их и прежде строил; в `builtin.ts` вынесена регистрация вкладов `document`-формы через `registerBuiltinStepKinds`, импортированную из `pipeline/expand.ts`, — типизированный разбор физически лежит там же, где раньше, но зарегистрирован тем же вызовом, что и плагинный.)
- [x] 2.2 `src/core/pipeline/schema.ts`: `buildDocumentSchemas` принимает виды шага действующего реестра и собирает `StepSchema` обходом реестра; константы встроенного набора продолжают печататься без него.
- [x] 2.3 `src/core/pipeline/expand.ts`: выбор ветви разбора шага — обход реестра вместо четырёх `if`; порядок проверки `uses` раньше `script` сохраняется правилом порядка вкладов, а не случайностью обхода, и это записано комментарием.
- [x] 2.4 Полная проверка репозитория зелёная без правки существующих тестов — доказательство, что переезд встроенных видов ничего не изменил (design.md, Migration Plan). (3031/3031 тестов проходят без единой правки существующего файла теста; журнал получил новую версию формата — это ожидаемое расширение схемы группы 5, а не поведенческий регресс.)

## 3. Плагинный вид шага в модели, разборе и замке

- [x] 3.1 `src/core/pipeline/model.ts`: интерфейс `PluginStep` (`kind: 'plugin'`, `name`, `fields`) и его место в `Step`; комментарий — тот же довод, что у варианта `plugin` предиката (design.md, решение 4).
- [x] 3.2 `src/core/pipeline/schema.ts`: ветвь плагинного вида — общая часть шага плюс единственный ключ вида со значением `z.unknown()`, строгий объект.
- [x] 3.3 `src/core/pipeline/expand.ts`: разбор плагинного вида — проверка значения `ajv` по схеме вклада с путём поля и именем плагина в отказе; неизвестный ключ вида — отказ с перечнем доступных видов (встроенных и плагинных). (Отказ о неизвестном виде даёт `rejectUnknownStepKinds` — проверка документа до его схемы, тем же приёмом, что `rejectWiringKeys`: объединение ветвей шага иначе отвечало бы дампом всех веток, не называя ни вид, ни перечень, ни плагин, вместе с которым вид снят.)
- [x] 3.4 `src/core/pipeline/late.ts`: `fields` снимаются `omitLateSkipped` наравне с `input` и `uses` и возвращаются раскрытыми типизированным проходом (design.md, решение 5).
- [x] 3.5 `src/core/pipeline/lock.ts`: печать плагинного вида — `{ [name]: fields }` в объявленном виде, с нераскрытыми отложенными подстановками.
- [x] 3.6 `src/core/lint.ts`: хук `lint` вклада для шага плагинного вида с `LintSite`; предупреждение о контексте работы без агентского шага считает плагинный вид не-агентским; отказ о снятом виде шага называет плагина, когда ядро его помнит.
- [x] 3.7 Тесты `test/expand.test.ts`, `test/lint.test.ts`: раскрытие шага плагинного вида; значение не по схеме; неизвестный ключ вида с перечнем; отложенная подстановка объектом в поле; печать шага в замок; диагностика хука вклада. (В `test/plugin-step-kind.test.ts`, не в `expand.test.ts`/`lint.test.ts` — те файлы уже организованы вокруг существующих возможностей, а не вокруг каждого вида шага. Покрыты и оба прохода подстановок: ранний — числом из `inputs`, поздний — объектом из выхода работы выше по графу через `resolveLate`.)

## 4. Исполнение

- [x] 4.1 `src/core/exec/pluginStep.ts`: исполнение попытки видом шага поверх общего `runAttempts` — вызов `execute`, запись `text` в `stdout.log` попытки, таймаут гонкой с `step.timeoutMs`, взведение `AbortSignal`, исключение исполнителя как непройденная попытка с названной причиной (design.md, решения 6 и 7).
- [x] 4.2 `src/core/run/runner.ts`: ветвь плагинного вида в выборе исполнителя; проход предикатов, учёт бюджета, отмена, события журнала и повторная проверка полей по схеме вклада перед исполнением — тем же путём, что у командного шага.
- [x] 4.3 Проверка структурированного выхода объявленной схемой `output` вклада: отказ попытки жёстким предикатом с тем же текстом, каким отказывает `output_schema` шага `script` (design.md, решение 9).
- [x] 4.4 Расход, сообщённый исполнителем, копится тем же счётчиком, что расход судьи; `src/core/budget/accumulator.ts` при необходимости учитывает новый вид шага. (`accumulator.ts` не тронут: `record(jobId, stepId, attempt, usage)` уже вид-нейтрален, второй счётчик не понадобился.)
- [x] 4.5 Описание задачи шага для судьи (`describeStepTask`) для плагинного вида — название вклада и поля.
- [x] 4.6 Тесты `test/run.test.ts` (или отдельный `test/plugin-step.test.ts`): сквозной прогон шага плагинного вида с поддельным вкладом; повтор попытки; таймаут; исключение исполнителя; выход не по схеме `output`; расход в отчёте. (В `test/plugin-step-kind.test.ts`. Добавлены после ревью: судья вторым проходом, предикат-скрипт с контрактом вызова, причина остановки `timeout`, потолок прогона, перейдённый расходом исполнителя, структурированный выход в `output.json` и в выходе работы, имена `stdout.log`/`stdout.2.log`, каталог файла попытки. Отмена прогона внешним `AbortSignal` отдельным тестом по-прежнему не подтверждена — путь тот же, что у командного шага.)

## 5. Журнал, схема, осмотр

- [x] 5.1 `src/core/journal/schema.ts`: `plugin` в перечне видов записи шага, поля с именем вида и именем плагина; `docs/run-layout.md` описывает их. (Версия журнала поднята: `JOURNAL_FORMAT = 8`, отпечаток добавлен в `JOURNAL_FORMAT_FINGERPRINTS`.)
- [x] 5.2 `src/core/run/stepKey.ts`: убедиться, что поля плагинного вида входят в ключ шага через хеширование `step` целиком, и зафиксировать это тестом; ограничение «поведение исполнителя в ключ не входит» записать комментарием. (Тест — «плагинное поле входит в ключ шага» в `test/plugin-step-kind.test.ts`.)
- [ ] 5.3 `src/core/pipeline/published-schema.ts`: ветви плагинных видов шага в печати документов, вложение схемы полей тем же приёмом узлов-меток, что у предикатов; `PublishedSchemaNote` различает предикат и вид шага. (Не начато — см. remaining в сдаче.)
- [ ] 5.4 `src/cli/commands/schema.ts` и сверка свежести в `src/core/lint.ts`: перечень видов шага действующего реестра наравне с предикатами; предупреждения о невложимой схеме полей. (Не начато.)
- [ ] 5.5 `src/cli/commands/config.ts`, `src/cli/commands/plugins.ts`: виды шага в отчёте и в осмотре с владельцами; манифест прогона пишет плагины видов шага наравне с прочими. (Не начато: манифест прогона уже пишет `registry.plugins` целиком — плагин, внёсший только вид шага, туда уже попадает тем же путём, что и любой другой; отчёты `stepcast config`/`stepcast plugins` строку «виды шагов: …» пока не печатают.)
- [ ] 5.6 Перегенерировать `schema/pipeline.schema.json` и `schema/job.schema.json`; тест на побайтовое совпадение схемы проекта без плагинных вкладов с поставляемой. (Не начато — зависит от 5.3.)
- [ ] 5.7 Тесты `test/schema.test.ts`, `test/journal*.test.ts`: вид шага в схеме проекта; невложимая схема полей не отменяет печати; прогон с плагинным видом читается без плагина. (Последний сценарий покрыт тестом «вид шага, снятый вместе с плагином, отказывает при исполнении» в `test/plugin-step-kind.test.ts`; первые два зависят от 5.3.)

## 6. Витрина

- [x] 6.1 `src/ui/pipelines.ts`: карточка шага плагинного вида — имя вида, название вклада, поля с подписями из схемы вклада, признак структурированного выхода; вывод подписей — тем же приёмом, каким `src/ui/steps.ts` строит параметры манифеста.
- [x] 6.2 `src/ui/snapshot.ts` и экран прогона: `kind: 'plugin'` в снимке шага, имя вида и поля из замка; шаг, вида которого реестр не знает, показывается названной причиной, а не пустой карточкой. (Карточка снимка строится из `pipeline.lock.yml` без обращения к действующему реестру — снимок прошлого прогона переживает плагин по определению; поэтому вместо подписей из схемы вклада карточка несёт плагин из записи шага и причину, объясняющую происхождение полей, — см. design.md, решение 11. Браузерная половина — `ui/src/api.ts`, `Pipelines.tsx`, `RunDetail.tsx`, `StepOutput.tsx` — знает новые поля и рисует их.)
- [x] 6.3 Тесты витрины: карточка собрана из реестра; прогон с видом шага, которого больше нет, показан именем вида и полями из замка, и остальная карточка цела. (`test/plugin-step-kind.test.ts`, раздел «витрина»: карточка через `buildPipelines` поверх проекта с настоящим модулем плагина на диске; снимок через `buildSnapshot` по журналу с замком, в процессе без этого плагина.)

## 7. Пример: вид шага http

- [ ] 7.1 `src/steps/http/`: вклад вида шага `http` — схема полей (адрес, метод, заголовки, тело, ожидаемый статус, имя переменной окружения с токеном), схема выхода, линт адреса, исполнитель на `fetch` без подпроцесса, уважающий `signal` и таймаут шага. (Не начато.)
- [ ] 7.2 `src/core/plugins/builtin.ts`: встроенная строка `step-http` в `BUILTIN_ROWS` (design.md, решение 10). (Не начато.)
- [ ] 7.3 `eslint.config.js` и `test/plugin-surface.test.ts`: правило дистанции распространено на `src/steps/**` — только `stepcast/plugin`, соседние модули и встроенные модули Node. (Не начато.)
- [ ] 7.4 Тесты `test/step-http.test.ts`: успешный запрос и ожидаемый статус; неожиданный статус — непройденная попытка; токен берётся именем переменной, и в замке значится имя, а не значение; отключение строки патчем снимает вид шага, и пайплайн отказывает названной причиной. (Не начато.)

## 8. Документация и приёмка

- [ ] 8.1 `docs/plugins.md`: раздел вклада вида шага — контракт, вход исполнителя, результат, схема выхода, правило одного ключа, правило «поле несёт имя переменной, а не секрет»; пример `http`; снятие ограничения «виды шагов не открыты»; новые ограничения (исполнитель не убивается таймаутом, поведение не входит в ключ шага, память о владельце живёт не дольше ядра). (Не начато.)
- [ ] 8.2 `docs/pipeline-format.md`: шаг плагинного вида и шаг `http`; `docs/config.md` и `docs/ui-plugins.md` — упоминания, если состав вкладов там перечислен. (Не начато; `docs/run-layout.md` — соседний документ формата журнала — уже описывает `kind: plugin`, `plugin_step` и место полей в ключе, см. задачу 5.1.)
- [ ] 8.3 Проверка репозитория целиком зелёная (`npm run check`), `openspec validate step-kinds-registry --strict` проходит. (`npm run check` целиком зелёный — проверено этим заходом: typecheck, typecheck:ui, typecheck:ui-test, typecheck:plugin, lint, build, build:ui-test, test:only [3031/3031], test:ui [116/116]. `openspec validate --strict` в этой песочнице отклонён на разрешении инструмента — не подтверждён напрямую этим заходом; документы изменения этим заходом не правились, кроме чек-листа этого файла.)
