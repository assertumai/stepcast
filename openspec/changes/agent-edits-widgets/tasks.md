## 1. Модель записи и проверка цели

- [x] 1.1 Завести `src/core/proposals/entry.ts` — чистый модуль без `node:*`, общий команде, демону и браузеру: модель записи (цель, действие, содержимое, причина, происхождение, отпечаток, состояние с моментом решения), сборка `<id>` из момента и свёрнутой цели, тексты отказов (Решение 1)
- [x] 1.2 Разбор и проверка цели там же: три вида кабинета, безопасный сегмент каждым сегментом, расширение и глубина по правилам читателя (виджет и дашборд — непосредственно в каталоге, файл плагина — на любой глубине), предел содержимого 256 КиБ (Решение 3)
- [x] 1.3 Приведение пути к реальному и проверка вложенности в каталог кабинета — вынести в `store.ts` (нужен `node:fs`), отказ по шагу вверх и по выходу символической ссылкой (Решение 3)
- [x] 1.4 `src/core/proposals/store.ts`: чтение каталога очереди (отсутствие — обычное состояние, негодная запись — названная, не стёртая), атомарная запись записи, решение записи, атомарная запись цели с созданием недостающих каталогов внутри кабинета, сверка отпечатка (Решения 1, 5, 6)
- [x] 1.5 Правило повтора: постановка, совпавшая с отклонённой записью по цели и хешу содержимого, — отказ с названной причиной (Решение 6)
- [x] 1.6 `test/proposals-entry.test.ts` и `test/proposals-store.test.ts`: законные и незаконные цели, симлинк наружу, предел размера, запись и перечитывание, негодная запись, решение, расхождение отпечатка, повтор отклонённого

## 2. Команда `stepcast propose` и режим доставки

- [x] 2.1 Ключ `project.proposals` (`queue` | `direct`) в модели конфигурации: только `.stepcast/config.yml`, отказ в машинном слое и в документе пайплайна, перечень допустимых значений в отказе (Решение 7)
- [x] 2.2 `src/cli/commands/propose.ts`: `--from`, стандартный ввод, `--reason`; происхождение из `STEPCAST_*`; проект из каталога прогона и указателя проектов, вне прогона — из каталога запуска (Решение 4)
- [x] 2.3 Ветка `direct`: немедленная запись цели теми же проверками, сообщение о записанном файле, очередь не трогается (Решение 7)
- [x] 2.4 Внести команду в разбор аргументов и в точку входа (`src/cli/args.ts`, `src/cli/main.ts`), в перечень CLI `docs/run-layout.md` (код внесён; `docs/run-layout.md` — в разделе 6)
- [x] 2.5 `test/cli-propose.test.ts`: постановка из шага (в том числе с `workspace: worktree` — запись в проекте, а не во временном дереве), вызов руками, содержимое вводом, отсутствие содержимого, чужая цель, оба режима доставки
- [x] 2.6 `stepcast init` вносит каталог очереди в `.gitignore`; тест на заведённый проект и на чистоту дерева после постановки (Решение 14)

## 3. Демон: чтение очереди, маршруты и экран

- [x] 3.1 Часть отпечатка наблюдателя по каталогу очереди и событие `proposals` в потоке — тем же правилом отличия, что `widgets` и `dashboards` (`src/ui/watcher.ts`, `src/ui/screens/rows.ts`)
- [x] 3.2 Строка состава `src/ui/screens/proposals/{declaration.ts,server.ts}`: `GET /api/proposals` (запись вместе с текущим содержимым цели), `POST /api/proposals` (принять либо отклонить одну названную запись), пробуждение наблюдателя после записи (Решения 2, 15)
- [x] 3.3 Отказ принятия по разошедшемуся отпечатку — код и текст на месте записи; отклонение не трогает цель (Решение 5)
- [x] 3.4 `src/core/textDiff.ts` — построчный LCS без `node:*`, годный браузеру (Решение 8)
- [x] 3.5 Браузерная половина: `ui/src/screens/proposals.tsx` и `ui/src/pages/Proposals.tsx` — диф, причина, прогон-источник, группировка по прогону, свёрнутые решённые записи, видимый режим доставки, локальный отказ; типы события в `ui/src/api.ts` и `ui/src/services/live.ts`
- [x] 3.6 `test/ui-proposals.test.ts` (маршруты, принятие с записью и пробуждением, создание недостающего каталога, отказ по отпечатку, отклонение), `test/ui-watcher.test.ts` (часть отпечатка и событие), `ui/test/proposals.test.tsx` (диф, создание файла целиком, кнопки, локальный отказ)

## 4. Таблица общих модулей: версия, заметки, устаревание

- [x] 4.1 Объявить в `src/ui/sharedModules.ts` версию таблицы и записи о смене имени (ушло, переименовано, заменено) с текстом для агента (Решение 10)
- [x] 4.2 Дополнить `test/ui-shared-modules.test.ts` отпечатком перечня имён для объявленной версии: правка перечня без поднятой версии и без записи о смене — красная проверка
- [x] 4.3 `src/ui/widgetImports.ts` — разбор голых спецификаторов файла виджета и сверка с действующей таблицей; консервативный разбор, ложный плюс дороже ложного минуса не считается (Решение 9)
- [x] 4.4 Признак устаревания и причина в составе виджетов (`src/ui/widgets.ts`) — тем же тактом и тем же правилом отличия, что версия виджета; отдача виджета признаком не отменяется
- [x] 4.5 Экран «Виджеты» (`ui/src/pages/Widgets.tsx`): причина устаревания и действие «мигрировать», скрытое, когда устаревших нет
- [x] 4.6 `src/cli/commands/widgets.ts` — `stepcast widgets [--json]`: состав, импорты, неразрешимые имена, версия таблицы; пустой состав — успех
- [x] 4.7 `test/widget-imports.test.ts` и тест команды: устаревший и обычный виджет, машинный вид, проект без виджетов, отсутствие записи в кабинет при вычислении признака

## 5. Пайплайн поставки и миграция виджетов

- [x] 5.1 Разрешение `stepcast:<имя>` для пайплайна: каталог `src/builtin/pipelines/`, поиск от расположения движка рядом с `packagedSchemaPath`, проверка имени (kebab-case, без `/`, `.`, `..`), отказ с перечнем поставляемых имён; позиционный аргумент без префикса остаётся путём (Решение 11)
- [x] 5.2 `POST /api/run` принимает ту же форму наравне с файлом пайплайна проекта, проверяя имя по закрытому перечню (`src/ui/runLaunch.ts`, `src/ui/runLaunch/row.ts`)
- [x] 5.3 `src/builtin/pipelines/migrate-widgets.yml` и файлы работ рядом: работа обзора (`"$STEPCAST_BIN" widgets --json`) и работа миграции; вход, называющий виджеты, по умолчанию — только устаревшие (Решения 12, 13)
- [x] 5.4 Промпт миграции рядом с пайплайном: обзор, исходники, заметки о смене; черновик в `$STEPCAST_SCRATCH`, постановка каждой новой версии командой `propose`
- [x] 5.5 Права агентского шага миграции: `Read` и вызов `propose`, без `Edit` и `Write`, `enforce: strict`, объявленный доступ к каталогу черновиков (Решение 12)
- [x] 5.6 `test/builtin-pipelines.test.ts`: `stepcast:migrate-widgets` раскрывается и проходит `stepcast lint`, неизвестное имя и имя с разделителем отказывают, файл пакета публикуется записью `files`
- [x] 5.7 Прогон миграции на проекте с устаревшим виджетом (тест с поддельным бэкендом): файл виджета не изменился, в очереди — открытая запись; проект без устаревших виджетов — успех без записей

## 6. Документация и зелёная проверка

- [x] 6.1 `docs/proposals.md` — очередь, формат записи, команда, режим доставки, экран, правила цели, строка `.gitignore` для заведённого раньше проекта, прямо названная граница «очередь — дверь, а не забор»
- [x] 6.2 `docs/widgets.md` — версия таблицы, заметки о смене, устаревание и миграция; `docs/config.md` — ключ `project.proposals` в формате и в разделе о принадлежности ключей репозитория
- [x] 6.3 `docs/pipeline-format.md` — форма `stepcast:<имя>` для пайплайна поставки в разделе разрешения путей; `docs/run-layout.md` — `propose` и `widgets` в перечне CLI
- [x] 6.4 `npm run check` зелёная целиком
