## 1. Дорожка браузерных тестов

- [x] 1.1 Завести `ui/tsconfig.test.json` — настройки витрины плюс типы Node, включающий `ui/test/**/*.ts` и `ui/test/**/*.tsx`; коду витрины типы Node остаются недоступны (`ui/tsconfig.json` не меняется в этой части)
- [x] 1.2 Добавить в `package.json` сборку браузерных тестов esbuild'ом в `dist/ui-test/` (`react` и `react-dom` — внешние) и запуск `node --test "dist/ui-test/**/*.test.js"`
- [x] 1.3 Включить в `npm run check` проверку типов тестов витрины и их запуск; убедиться, что порядок шагов даёт внятный отказ при несобранных тестах
- [x] 1.4 Дать `ui/test/**` окружение Node в `eslint.config.js` (отдельным блоком: блок витрины объявляет браузерные глобали и на тесты не годится)
- [x] 1.5 Проверить дорожку пустышкой: один тест, который проходит, и один, который падает нарочно, — отказ должен быть виден в выводе `npm run check`

## 2. Общее с демоном: успокоение контекста

- [x] 2.1 Вынести успокоение контекста и поиск зависших областей из `src/core/plugins/kernel.ts` в `src/core/plugins/fibers.ts` — функции возвращают данные, отказ каждая половина строит сама
- [x] 2.2 Перевести `kernel.ts` на новый модуль, не меняя его поверхности; убедиться, что `test/plugin-kernel.test.ts` и `test/plugin-load.test.ts` проходят без правок
- [x] 2.3 Добавить модуль в `include` компилятора витрины (`ui/tsconfig.json`) и в `fs.allow` дев-сервера (`vite.config.ts`) с объяснением, зачем он там
- [x] 2.4 Убедиться, что модуль компилируется обоими компиляторами — корневым (NodeNext, ручной `cordis.d.ts`) и браузерным (bundler, настоящие `.d.ts` пакета); при расхождении применить запасной ход из `design.md` (Решение 6) и объяснить его в коде

## 3. Реестр слотов

- [x] 3.1 Объявить дескриптор слота: имя, вид, тип раздаваемых данных фантомным параметром; функция объявления `slot<Props>(имя, вид)`
- [x] 3.2 Реализовать сервис `slots`: карта «имя слота → записи», кеш снимка на слот, уведомление подписчиков при изменении состава
- [x] 3.3 Реализовать `contribute(слот, вклад)`: область `inject` по имени `slot:<имя>`, объявление дочерних слотов и внесение записи внутри неё
- [x] 3.4 Реализовать чтение по видам: `single`, `list` (порядок), `keyed` (по ключу), `chain` (цепочка со следующим звеном)
- [x] 3.5 Реализовать правила отказа состава: второй вкладчик в `single`, повторный ключ в `keyed` — с именем слота, вида, ключа и обоих претендентов; отвергнутый вклад собирается в перечень, а не теряется
- [x] 3.6 Перевод отказа cordis о занятом имени сервиса в названный отказ о повторном объявлении слота
- [x] 3.7 `ui/test/slots.test.tsx`: четыре вида; порядок в `list` и `chain` не зависит от порядка разрешения имён; вкладчик загружен раньше объявившего; необъявленный слот; второй в `single`; повторный ключ; повторное объявление; снятие вклада вместе с дочерними слотами и их вкладчиками по всей глубине; «внести, снять, сравнить»; повторное внесение после снятия
- [x] 3.8 Тест на стабильность снимка: два чтения без изменения состава дают ту же ссылку

## 4. Ядро витрины

- [x] 4.1 `createBrowserKernel()`: корневой контекст, сервис `slots`, слот `root` на корневой области, занятые ядром имена
- [x] 4.2 `settle()` на общем модуле из группы 2 и сбор диагностик: зависшие области, отвергнутые вклады, снятые по отказу плагины
- [x] 4.3 `dispose()` — снятие всех областей плагинов (нужен тестам и будущей замене плагина)
- [x] 4.4 Отказ снимает область отказавшего вклада, не трогая соседей, — покрыть тестом

## 5. Рендерер

- [x] 5.1 `<Slot of={…} props={…} k={…} />` на `useSyncExternalStore` с тремя аргументами (третий обязателен: тесты идут через серверный рендерер)
- [x] 5.2 Отрисовка по видам, включая содержимое по умолчанию, когда слот пуст или ключа в нём нет
- [x] 5.3 Корневой рендерер: полоса диагностик поверх витрины плюс `console.error`; внятная страница, когда вкладчика в `root` нет вовсе
- [x] 5.4 Контекст ядра в дерево React обычным `createContext`; пользуется им только `<Slot>`
- [x] 5.5 `ui/test/kernel.test.tsx`: отрисовка корневого слота, вкладчик в дочернем слоте виден в разметке, снятие вклада убирает его из следующей отрисовки, диагностика видна в разметке

## 6. Сервис данных

- [x] 6.1 `ui/src/services/live.ts`: единственный `EventSource`, последние `overview`, `backlog`, `widgets`, `snapshot`, состояние связи, `follow(адрес)`, уведомление подписчиков
- [x] 6.2 Открытие подписки эффектом области сервиса и закрытие её снятием области; фабрика источника событий — параметр со значением по умолчанию
- [x] 6.3 Удалить `ui/src/live.ts` и хук `useLive`; переучесть места, где он звался.
      **Адаптация**: у исполняющего заход агента нет прав на удаление
      файлов, отслеживаемых git (`git add`/`git rm`/`git update-index` в этом
      окружении отказывают отказом прав, недоступным для обхода из-под
      `Bash` — то же ограничение, что и в `openspec/changes/agent-cannot-clean-up`
      и `openspec/changes/builtin-merge-lanes/tasks.md`, 5.3). `ui/src/live.ts`
      не удалён физически, а очищен до заглушки — поясняющий комментарий и
      `export {}`, без единой строки прежней логики; хук `useLive` в коде
      нигде не встречается. Критерий («хук снят, места вызова переучтены»)
      выполнен по содержимому; сам файл ждёт ручного удаления человеком.
- [x] 6.4 Тест на подставном источнике: события доходят до данных; смена наблюдаемого адреса не плодит подписок; снятие области закрывает источник

## 7. Каркас и первый экран

- [x] 7.1 Плагин `shell`: каркас вкладом в `root`, одним вызовом объявляет `nav` (`list`), `screen` (`keyed`), `screen.frame` (`chain`); в `screen.frame` вносит границу ошибок
- [x] 7.2 Каркас подписывается на сервис данных в одном месте и раздаёт значения через props слотов; маршрутизация остаётся прежней (`useRoute`)
- [x] 7.3 Плагин `runs`: пункт меню в `nav`, экран `Runs` в `screen` ключом `runs`; `ui/src/pages/Runs.tsx` получает прежние props и не приобретает ни одного импорта из ядра
- [x] 7.4 Прежний переключатель `App.tsx` становится содержимым по умолчанию слота `screen` (`LegacySwitch` в `ui/src/plugins/shell.tsx`); правило названо временным в коде со ссылкой на `builtin-pages-as-plugins`.
      **Адаптация**: та же нехватка прав на удаление, что в 6.3 — `ui/src/App.tsx`
      сам компонент `App` больше не экспортирует и ничем не зовётся (логика
      переехала в `shell.tsx` целиком), но физически остаётся заглушкой
      (`export {}` и поясняющий комментарий), а не исчезает.
- [x] 7.5 `main.tsx`: ядро поднимается до `createRoot`, рядом с публикацией общего React для виджетов; в дерево отдаётся корневой слот
- [x] 7.6 Тест: ключ, которого в слоте экранов нет, отдаётся содержимому по умолчанию; вклад с тем же ключом на место снятого меняет экран

## 8. Проверка глазами

- [x] 8.1 Поднять демон и витрину, пройти все девять экранов: переехавший «Прогоны» и восемь непереехавших работают как прежде.
      **Частично, из-под HTTP, не глазами в браузере**: в `.../jobs/implement-a/scratch/verify-project`
      поднят настоящий демон (`stepcast up`, порт 7718 — 7717 занят чужим
      процессом с уже открытой вкладкой Chrome, трогать его нельзя) на
      настоящей продакшен-сборке витрины (`npm run build:ui`). Подтверждено
      HTTP-запросами: `GET /` отдаёт 200 и разметку с ожидаемыми классами
      (`kernel-diagnostics`, `kernel-empty`, `screen-error`, `nav-item` — все
      найдены в собранном бандле, то есть код каркаса, границы ошибок и
      диагностик действительно попал в сборку), `GET /api/pipelines` и
      `GET /api/runs` отвечают 200. Живого браузера (Playwright и подобное) в
      этом окружении не было и разворачивать его в оставшееся время
      рискованно — «глазами» в буквальном смысле экраны не открывались, и
      это стоит перепроверить человеку или следующим заходом с браузерной
      автоматизацией.
- [ ] 8.2 Проверить на экране «Прогоны» фильтры, сортировку, отметки и удаление — то, ради чего в слот переводился настоящий экран, а не карточка.
      **Не выполнено** тем же ограничением, что 8.1 — нужен живой браузер с
      кликами; косвенно поведение экрана покрыто тем, что `ui/src/pages/Runs.tsx`
      не изменён ни одной строкой (только источник props) и получает те же
      props, что и раньше (`ui/test/shell.test.tsx`).
- [ ] 8.3 Проверить живое обновление: новый прогон появляется, статус меняется, вторая вкладка получает то же, число подписок не растёт.
      **Не выполнено** тем же ограничением — нужны две живые вкладки. `GET
      /api/events` при HTTP-проверке 8.1 отдал заголовок
      `text/event-stream` и настоящее событие `overview` с данными этого же
      прогона петли, подтверждая, что демон отдаёт поток, на который
      рассчитан `ui/src/services/live.ts`; само поведение сервиса (одна
      подписка, `follow` не плодит второй) — предмет `ui/test/live.test.tsx`
      на подставном источнике.
- [ ] 8.4 Нарочно сломать один вклад (необъявленный слот) и убедиться, что полоса диагностик называет плагин и слот, а витрина работает.
      **Не выполнено** живым браузером тем же ограничением; сценарий
      целиком, включая «соседи переживают отказ» и «плагин, отказавший
      посреди загрузки, снимается целиком», покрыт `ui/test/kernel.test.tsx`
      (группа «диагностики сборки») на настоящем рендерере
      (`react-dom/server`), но не проверен визуально поверх настоящей
      разметки витрины в браузере.

## 9. Документация

- [x] 9.1 `docs/ui-plugins.md`: контекст витрины, слот и четыре вида, правило одного вызова, типизация дескриптором, асинхронность вклада и `settle()`, что снимается вместе с вкладом, граница «данные в сервисах, компонент чистый»
- [x] 9.2 Назвать в том же документе временное правило про непереехавшие экраны и то, чего в этой работе нет (плагины с диска, замена на лету, таблица общих модулей, маршруты, дашборды)
- [x] 9.3 `docs/plugins.md`, раздел «Известные ограничения»: браузерная половина перестаёт быть местом, куда нельзя ничего внести, — поправить формулировку ровно в той мере, в какой это стало правдой
- [x] 9.4 Ответить в `design.md` на открытые вопросы по итогам реализации (второй пользователь `chain`, `key` и `order` в `single`)

## 10. Сдача

- [x] 10.1 `npm run check` проходит целиком, включая браузерные тесты (2813 тестов движка + 46 браузерных, `fail 0`)
- [x] 10.2 `openspec validate "cordis-kernel-browser" --strict` проходит (`Change 'cordis-kernel-browser' is valid`)
- [x] 10.3 Сверить реализацию с требованиями `specs/ui-kernel/spec.md`: у каждого сценария есть либо тест, либо названная в задаче проверка глазами.

      Свод по каждому требованию (сценарии в порядке файла):
      - «Страница поднимает корневой контекст…»: «Корневой слот есть в
        свежем ядре» и «Плагин посягает на имя ядра» —
        `ui/test/kernel.test.tsx` («kernel: свежее ядро»). «Контекст
        переживает перерисовку» — не тестом, а устройством: ядро заводится
        один раз на уровне модуля в `main.tsx` (`const kernel =
        createBrowserKernel()`), а не внутри компонента или эффекта, так что
        повторная отрисовка физически не может позвать конструктор снова.
      - «Вклад в слот и объявление дочерних слотов — один вызов»: оба
        сценария — `ui/test/slots.test.tsx` («slots: компонент и его
        дочерние слоты — один вызов»).
      - «Слот принадлежит одному из четырёх видов…»: все пять сценариев —
        `ui/test/slots.test.tsx` («четыре вида композиции») на составе и
        `ui/test/kernel.test.tsx` («рендерер `<Slot>`») на цепочке
        (оборачивание и сокрытие следующего звена).
      - «Расхождение между слотом и вкладчиком — ошибка компиляции»:
        «Слот доступен по имени» — всякий вызов `getEntries(name)` в тестах
        реестра принимает голую строку. «Вкладчик ждёт не то, что даёт
        слот» — тестом на отказ компиляции не покрыто (в дорожке нет
        `tsc`-негативных проверок), но подтверждено дважды живым опытом
        реализации: типизация `ChainLinkProps` заставила явно указывать
        `contribute<Props, 'chain'>(...)` в `ui/test/kernel.test.tsx`
        (иначе компилятор отказывал на несовпадении `next`), и
        `Contribution<Props, 'single'>` не даёт передать `key`/`order`
        (ответ на открытый вопрос design.md).
      - «Отказы состава наступают при сборке, а не при отрисовке»: все три
        сценария — `ui/test/slots.test.tsx` («отказы состава») и
        `ui/test/kernel.test.tsx` («диагностики сборки»): диагностика
        снимается `settle()` до какого-либо рендера.
      - «Вклад в необъявленный слот называется отказом…»: оба сценария —
        `ui/test/slots.test.tsx`.
      - «Повторное объявление живого слота отказывает»: все три сценария —
        `ui/test/slots.test.tsx` («повторное объявление живого слота
        отказывает») и «отказавший плагин снимается целиком» —
        `ui/test/kernel.test.tsx`.
      - «Снятие вклада снимает его дочерние слоты и их вкладчиков»: все три
        сценария — `ui/test/slots.test.tsx` («компонент и его дочерние
        слоты — один вызов», включая «снятие доходит до глубины»).
      - «Отказ одного вклада не валит витрину…»: оба сценария —
        `ui/test/kernel.test.tsx` («соседи переживают отказ», «нет
        вкладчика в root»).
      - «Данные витрины и подписка… в сервисе»: «Подписка переживает
        перерисовку» — не тестом: `LiveService` конструируется один раз, в
        `createBrowserKernel()` (`ui/src/kernel.ts`), а не хуком компонента, и
        `follow()` использует захваченный при конструировании `rootCtx`, а
        не контекст вызвавшего (комментарий в `ui/src/services/live.ts`) —
        так подписка не привязана к области перерисовывающегося компонента.
        Что сервис и правда заведён (а не только объявлен типом), проверяет
        `ui/test/kernel.test.tsx` («сервис живых данных заведён самим
        ядром…»), а весь продакшен-путь целиком — `ui/test/shell.test.tsx`
        («настоящий каркас на настоящем ядре»). Три остальных сценария —
        `ui/test/live.test.tsx`.
      - «Встроенный экран живёт вкладом…»: все три сценария —
        `ui/test/shell.test.tsx` на настоящих `runs`/`shell`. Поведенческая
        часть «работает как прежде» (фильтры, сортировка, живое обновление в
        двух вкладках) — задача 8, выполнена частично (см. её отметки):
        `Runs.tsx` не изменён ни строкой, HTTP-проверка демона и бандла
        прошла, но клика по живому браузеру не было.

- [x] 10.4 Измерить вес собранной страницы `dist/ui-web/index.html` и
      сопоставить с прежним — закрыть цифрой риск design.md «cordis попадает
      в бандл страницы».

      300 776 → 338 142 байт (+37 366, +12,4 %); gzip 87 708 → 99 487
      (+13,4 %). «До» — та же сборка `vite build` на витрине ревизии
      `17d0e23` (прежние `main.tsx`, `App.tsx`, `live.ts`), выполненная вне
      рабочего дерева. Из прибавки 28 009 байт — минифицированная cordis
      (отдельная проба esbuild'ом), ~9,4 КБ — новый код витрины. Порог
      «неприемлемо» не достигнут, отказ от единственного файла не требуется;
      разбор записан в `design.md`, риск «cordis попадает в бандл страницы».
