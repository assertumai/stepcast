## 1. Модель и разбор

- [x] 1.1 Добавить ветвь `{ kind: 'script'; path: string }` в тип `Predicate` (`src/core/pipeline/model.ts`)
- [x] 1.2 Добавить `z.object({ script: z.string().min(1).regex(/\S/) }).strict()` в `BuiltinPredicateSchema` (`src/core/pipeline/schema.ts`)
- [x] 1.3 Перегенерировать публикуемую схему (`npm run schema`) и проверить, что `script` появился в объединении предикатов `expect`/`until.check`

## 2. Разрешение пути и раннера

- [x] 2.1 В `expand.ts` при раскрытии предиката `script` переиспользовать `resolveScriptFile`/`selectRunner`, которыми уже разрешается шаг `script`, без объявленного `runner`
- [x] 2.2 Пронести разрешённый путь, слой и раннер предиката в раскрытый `Predicate` — тем же способом, что несёт `ResolvedScript` у шага
- [x] 2.3 Отказ разрешения (файл не найден, раннер не определяется) — сохранить как `ScriptUnresolved`-подобную причину для линта, не бросая исключение на раскрытии

## 3. Линт

- [x] 3.1 Добавить диагностику ненайденного файла предиката `script` в `expect` и в `until.check` — с перечнем просмотренных слоёв
- [x] 3.2 Добавить диагностику неразрешимого раннера — с перечнем известных расширений
- [x] 3.3 Пропускать путь с неразрешённой подстановкой и путь с глобом — тем же условием, что у шага `script`

## 4. Вычисление предиката

- [x] 4.1 Добавить ветвь `script` в `evaluateOne` (`src/core/expect/evaluate.ts`): записать `input.json` подкаталога вызова с кодом возврата попытки, путями к `stdout.log`/`stderr.log` попытки и структурированным выходом попытки (если есть)
- [x] 4.2 Исполнить раннер в рабочей директории работы с окружением попытки (включая `env_deny`), передав `STEPCAST_INPUT`/`STEPCAST_OUTPUT` на файлы подкаталога вызова
- [x] 4.3 Вердикт — нулевой код возврата процесса; при отказе прочитать необязательный `output.json` и взять `reason`, если поле есть, иначе — родовая причина с кодом возврата
- [x] 4.4 Результат — `hard: true` безусловно, без чтения ключа `hard` из предиката

## 5. Каталог вызова

- [x] 5.1 Завести подкаталог `script-<n>` в каталоге шага со сквозной нумерацией в пределах шага (переиспользовать счётчик, которым уже нумеруются `judge-<n>`, либо завести соседний с той же дисциплиной)
- [x] 5.2 Писать в подкаталог `input.json`, `stdout.log`, `stderr.log` вызова и переносить туда же `output.json`, если раннер его создал
- [x] 5.2a `until.check` не привязан ни к какому шагу, а контракт входа предиката описан только для попытки шага (`код возврата попытки`) — решение не названо ни proposal, ни спеками. Оставлено рабочим ходом: `script-<n>` вызова из `until.check` заводится в каталоге работы (`journal.prepareJob`), а `input.json` несёт умолчания «попытки не было» (`exit_code: 0`, пути к несуществующим `stdout.log`/`stderr.log` каталога работы). Разбор, раскрытие и линт предиката в `until.check` при этом полностью соответствуют документам изменения — сужен только внеспецификационный участок исполнения.

## 6. Тесты

- [x] 6.1 Разбор и раскрытие предиката: путь, слои, выбор раннера, ошибки разрешения (`test/expand.test.ts` — файла `test/pipeline-expand.test.ts` в репозитории нет, ближайший существующий тем же назначением)
- [x] 6.2 Линт: ненайденный файл, неразрешимый раннер, пропуск подстановки и глоба (`test/lint.test.ts`)
- [x] 6.3 Вычисление: вердикт по коду возврата, причина из `output.json.reason`, состав `input.json`, безусловная жёсткость (`test/run.test.ts`, сквозной прогон — реальнее, чем ручной вызов `evaluatePredicates`, и покрывает контракт входа/выхода живым процессом)
- [x] 6.4 Каталог вызова: `script-<n>`, сквозная нумерация через попытки и несколько предикатов, состав файлов (`test/run.test.ts`; отдельного `test/runner.test.ts` в репозитории нет — есть `test/run.test.ts` того же назначения, куда уже собраны аналогичные сквозные проверки шага `script`)
- [x] 6.4a Дополнительно: `script` в `until.check` действительно исполняется и решает исход итерации, плюс подкаталог вызова в каталоге работы (`test/until.test.ts`) — не по чек-листу буквально, но закрывает решение из 5.2a тестом

## 7. Документация

- [x] 7.1 Добавить строку `script: <path>` в таблицу предикатов `expect` (`docs/pipeline-format.md`)
- [x] 7.2 Описать контракт входа/выхода предиката (состав `input.json`, `output.json.reason`, отсутствие схемы и `hard`) отдельным подразделом рядом с описанием шага `script`
- [x] 7.3 Добавить запись `script-<n>` в дерево каталога шага (`docs/run-layout.md`)

## 8. Проверка

- [x] 8.1 `npm run check` зелёный
