## 1. Ограничение ошибки отказом работы

- [x] 1.1 Добавить `catch` циклу итераций в `executeJob` в
      `src/core/run/runner.ts`: исключение приводится к отказу работы с
      причиной `spawn_failed`
- [x] 1.2 Записывать причину событием в журнал с указанием работы

## 2. Тесты

- [x] 2.1 Нечитаемый файл контекста агентского шага роняет работу, а не прогон
- [x] 2.2 Прогон получает ненулевой код возврата. Предикат `schema` триггером
      не годится: на командном шаге он отсекается раньше чтения файла
      отсутствием структурированного вывода, а на агентском требует бэкенда
- [x] 2.3 `status.json` получает конечный статус, а не остаётся в `running`
- [x] 2.4 Работа с `needs: all` и `on: always` отрабатывает после такой ошибки

## 3. Проверка на петле

- [ ] 3.1 Прогон петли на заведомо нечитаемом пути завершается конечным
      статусом, `finalize` отрабатывает, очередь не остаётся в `in_progress`
