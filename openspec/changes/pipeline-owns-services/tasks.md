## 1. Закрепить нынешнее поведение до единой правки

- [x] 1.1 Тест порядка узнавания при отложенном разрешении (design.md, Решение 11): запись с ключами `uses` и `script` узнаётся видом `uses` — сегодня это проверяется на составе, собранном по порядку перечня; тест заводится так, чтобы после задачи 4 он проверял тот же ответ на дереве, где строка-поставщик стоит последней.
- [x] 1.2 Тест владельца встроенного вклада: у каждого вклада дефолтного состава владелец — «встроенный»; проверить, что это уже закреплено (`test/plugin-registry.test.ts`), и дополнить, если проверяется не по всем видам.
- [x] 1.3 Тест, закрепляющий подпись журнала и признак печати схемы проекта: запись шага плагинного вида `decision` называет владельцем «встроенный», а печать схемы проекта не считает `decision` отличием от схемы пакета (`test/published-schema.test.ts`, `test/run-decision.test.ts` — дополнить, если не закреплено).
- [x] 1.4 `npm run test:only` зелёный на этих тестах до правок кода.

## 2. Помощник области строки в ядре

- [x] 2.1 `src/core/plugins/load.ts`: помощник `rowScope(kernel, id, inject, apply)` — завести область `kernel.ctx.plugin`, дождаться её, снять при отказе, вернуть фибер; доменно пуст, комментарий называет оба правила приписывания (design.md, Решение 2).
- [x] 2.2 `src/ui/screens/registry.ts`: `screenRow` переписан поверх помощника, пометка `builtinFibers` остаётся его собственной частью; поведение витрины не меняется — проверить `test/ui-server.test.ts` без правки ожиданий.

## 3. Сервис вклада становится механизмом без доменных имён

- [x] 3.1 `src/core/plugins/kernel.ts`: `ContributionService` принимает имя сервиса, слово для текстов отказа и признак встроенной области (`(fiber) => boolean`) вместо `kind`/`builtinFiber`; `KIND_NAMES` и объединение `ContributionKind` сняты.
- [x] 3.2 Там же: проверка имени вклада (`ContributionNameGuard`) перестаёт быть опцией сборки ядра и становится параметром конструктора сервиса; `KernelOptions` снят целиком, `createKernel()` без опций.
- [x] 3.3 `src/parts/builtin.ts`: `createKernelShell` собирает ядро без `nameGuards` и вносит команды, как и прежде; импорт `assertStepKindNameAvailable` отсюда уходит.
- [x] 3.4 Ядро продолжает заводить четыре сервиса прежним вызовом — состав и все тесты зелёные на этом шаге. (Реализовано вместе с задачей 4: промежуточное состояние «ядро временно строит четыре сервиса» не задерживалось отдельным коммитом — конечное состояние, где ядро строит только `commands`, проверено тестами целиком.)

## 4. Строка `pipeline` и строки-потребители

- [x] 4.1 `src/parts/pipeline/services.ts`: три сервиса (`backends`, `predicates`, `steps`) со своими словами диагностик и проверкой имени вида шага; `declare module 'cordis'` с их типами переезжает сюда из ядра; множество помеченных областей строк встроенного слоя (`WeakSet<Fiber>`).
- [x] 4.2 Там же: помощник `partRow(id, inject, register)` поверх `rowScope` — помечает область до первой регистрации и возвращает объект, расширяющий `BuiltinRow` полями `inject` и `register(ctx)` (design.md, Решение 4).
- [x] 4.3 `src/parts/pipeline/row.ts`: `export const row` с `id: 'pipeline'`, тело заводит три сервиса на своей области и не вносит ни одного вклада.
- [x] 4.4 Строки-потребители переведены на `partRow` с объявленным `inject`: `src/parts/backends/claude/row.ts` (`backends`), `src/parts/expect/row.ts` (`predicates`), `src/parts/steps/{run,uses,script,agent,decision}/row.ts` (`steps`); тела регистрации не меняются, обращение к формам разбора остаётся внутри тела (цикл модулей).
- [x] 4.5 `src/parts/rows.ts`: `pipeline` первой строкой перечня; докстринг называет роли поставщика и потребителя и то, что порядок потребителей между собой по-прежнему значим для узнавания шага.
- [x] 4.6 `src/core/plugins/kernel.ts`: `createKernel` заводит только сервис `commands`; три вызова и объявление трёх сервисов в контексте cordis сняты.
- [x] 4.7 Проверить `docs/plugins.md`-примеры плагина контекста: вклад через сервис требует `inject` — правка примера идёт в задаче 9.1, здесь только убедиться, что декларативная форма объявляет `inject` сама (`toContextPlugin`).

## 5. Синхронное умолчание библиотеки

- [x] 5.1 `src/parts/builtin.ts`: `createBuiltinKernel` применяет тела строк (`register`) на корневом контексте в порядке перечня; комментарий называет, почему сборка остаётся синхронной (design.md, Решение 4).
- [x] 5.2 Там же: перед вызовом тела проверяется объявленный строкой `inject` по корневому контексту — именованный отказ, называющий строку и недостающее имя, вместо `TypeError`.
- [x] 5.3 Тест: состав, собранный `builtinRegistry()`, и состав, собранный обходом дерева дефолта, дают одни и те же вклады с одними и теми же владельцами.
- [x] 5.4 Тест: перестановка `pipeline` в конец перечня даёт именованный отказ синхронной сборки (и ничего не ломает в сборке обходом — там порядок не решает).

## 6. Ядро теряет доменные имена

- [x] 6.1 `src/core/plugins/kernel.ts`: `KERNEL_RESERVED_NAMES` снят; `translateReservedNameConflict` принимает контекст и называет владельца имени по объявленным сервисам (`declaredServices`) — строку либо ядро (design.md, Решение 7).
- [x] 6.2 `src/core/plugins/load.ts`: окно корневых регистраций (`snapshotRoot`/`diffRoot`) обходит объявленные сервисы, умеющие перечислить вклады по областям, вместо перечня `CONTRIB_KINDS`; `ContribKind` становится именем сервиса.
- [x] 6.3 `src/core/plugins/contract.ts`: таблица «ключ декларативной формы → имя сервиса → как достать имя вклада» рядом со `StepcastPluginSchema`; `toContextPlugin` идёт по ней и по ней же объявляет `inject` (design.md, Решение 10).
- [x] 6.4 `src/core/plugins/introspect.ts`: `IntrospectionContributions` — карта «имя сервиса → имена вкладов» без пустых сервисов; `CONTRIB_KINDS` снят; `isIntrospection` проверяет карту; номер формы `2`.
- [x] 6.5 Тест границы: ядро, собранное без строк, объявляет ровно один сервис (`commands`), и ни одного доменного имени в модулях ядра не остаётся — проверка на `createKernelShell()` через `declaredServices`.

## 7. Реестр, отказ команд и отчёты

- [x] 7.1 `src/core/plugins/registry.ts`: сервисы читаются по имени; отсутствующий даёт пустую карту; добавлен перечень недостающих имён (`missingServices`); `formerStepKindOwner` безопасен на отсутствующем сервисе.
- [x] 7.2 `src/core/plugins/contract.ts`: `CommandContribution.inject?: readonly string[]` с докстрингом о том, когда его объявляют.
- [x] 7.3 `src/cli/main.ts`: диспетчер проверяет `inject` команды по контексту до вызова и отказывает `StepcastError` с кодом ошибки конфигурации, называя команду, недостающее имя и подсказку о составе; `inject` объявлен у `run`, `resume`, `decide`, `lint`, `context`, `schema`.
- [x] 7.4 `src/cli/commands/plugins.ts`: печать вкладов по имени сервиса, таблица подписей снята; раздел встроенного вне строк печатается тем же способом.
- [x] 7.5 `src/cli/commands/config.ts`: строка о составе без служебных сервисов пайплайна — названная причина вместо молчаливо пустых перечней вкладов.
- [x] 7.6 Тесты: команда прогона в составе без поставщика отказывает названно и кодом конфигурации; `stepcast plugins` и `stepcast config` в том же составе работают и печатают дерево.

## 8. Тесты состава и осмотра

- [x] 8.1 `test/plugin-tree.test.ts`, `test/plugins-load.test.ts`: дерево дефолта на одну строку длиннее, `pipeline` первой; отключение `pipeline` при включённых потребителях — отказ загрузки, называющий ждущую строку и недостающее имя.
- [x] 8.2 Тест порядка: патч ставит строку-потребителя (и пользовательский плагин, ждущий `steps`) впереди `pipeline` — состав, владельцы и порядок узнавания те же (задача 1.1).
- [x] 8.3 `test/plugins-introspect.test.ts`, `test/plugins-command.test.ts`: вклады по имени сервиса; строка-поставщик с объявленными сервисами; потребители с запрошенными и разрешёнными; служебных сервисов пайплайна нет во встроенном вне строк; номер формы `2`.
- [x] 8.4 `test/plugin-kernel.test.ts`, `test/plugin-registry.test.ts`: отказ о занятом имени сервиса называет строку-поставщика; имя свободно при отключённой строке; владелец вклада строки поставки — «встроенный».
- [x] 8.5 Тест каскада: снятие области строки-поставщика снимает потребителей вместе с их вкладами, снятие потребителя поставщика не задевает.
- [x] 8.6 Прогнать тесты поведения пайплайна (`test/expand.test.ts`, `test/lint.test.ts`, `test/run-*.test.ts`, `test/published-schema.test.ts`) без правки ожиданий — это и есть проверка, что переезд ничего не изменил; всякую вынужденную правку ожиданий объяснить в тексте теста. (Правка ожиданий потребовалась там, где тесты собирали кастомный кернел синхронным циклом `row.apply(kernel)` без ожидания: `rowScope`/`ctx.plugin` всегда проходит через микрозадачу, и без `await Promise.all(...)` реестр читался раньше, чем успевала случиться хоть одна регистрация — объяснено в тексте каждого правленого теста.)

## 9. Документация и план

- [x] 9.1 `docs/plugins.md`, раздел «Контекст, область и сервис»: модель поставщика и потребителя вместо «трёх имён, занятых ядром»; кто заводит служебные сервисы; почему плагин контекста, пользующийся сервисом, объявляет `inject`; что делает отключение строки-поставщика.
- [x] 9.2 `docs/plugins.md`, раздел «Конфликты имён»: отказ о занятом имени сервиса называет того, кто его объявил.
- [x] 9.3 `docs/microkernel-target.md`: шаг 7 отмечен выполненным с уточнениями — две формы применения строки поставки (синхронное умолчание), номер формы осмотра, остаток для шага 8 (публикуемая поверхность по-прежнему доменна).

## 10. Находки ревью

- [x] 10.1 `src/cli/main.ts`: `inject` команды проверяется по действующему контексту (`ctx.get`), а не по `Registry.missingServices` — команда плагина, ждущая сервис вне четырёх доменных имён, получает названный отказ до вызова тела; сценарий дельты и тест в `test/plugins-command.test.ts`, `design.md` Решение 8 уточнено.
- [x] 10.2 `src/plugin.ts`, `src/core/plugins/context.ts`: докстринг публикуемой поверхности больше не приписывает служебные сервисы ядру и называет, чем держится объявление полей обязательными (`inject`).
- [x] 10.3 `src/core/pipeline/schema.ts`: комментарий `assertStepKindNameAvailable` называет нынешнего вызывающего — конструктор сервиса `steps` строки `pipeline`, — а не снятый `KernelOptions.nameGuards`.
- [x] 10.4 `docs/plugins.md`: два исхода отключения строки `pipeline` разведены — отключена одна строка (отказ загрузки, переживает только `stepcast plugins`) и отключены вместе с потребителями (загрузка проходит, имена свободны, команды отказывают названно); раздел «Команда» описывает поле `inject`.
- [x] 10.5 `test/plugin-kernel.test.ts`: имя свободно, когда строки-поставщика в составе нет, — сервис заводится без отказа, владельцем имени числится область плагина.
- [x] 10.6 `test/plugin-tree.test.ts`: пользовательская строка на месте `pipeline` заводит только свои сервисы, ни одно из трёх имён заменённой не появляется само собой.
- [x] 10.7 `test/plugins-load.test.ts`: ядро без строк объявляет ровно один сервис `commands`; ключи схемы декларативной формы и таблица `DECLARATIVE_CONTRIBUTION_FIELDS` сверяются одним тестом.

## 11. Проверка

- [x] 11.1 `npm run check` зелёный целиком.
- [x] 11.2 Ручная сверка: `stepcast plugins` на дефолтном составе печатает `pipeline` с объявленными сервисами и потребителей с разрешёнными; `stepcast config` называет тот же состав; прогон примера из `examples/` не изменился.
