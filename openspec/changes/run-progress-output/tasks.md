## 1. Наблюдение за потоком событий

- [x] 1.1 Добавить в `RunJournal.create` необязательный `onEvent` и вызывать его в `event()` после успешной дописи строки в `events.ndjson` (`src/core/journal/writer.ts`)
- [x] 1.2 Гасить исключение наблюдателя, записывая его событием `bookkeeping.failed`, чтобы вывод не мог остановить прогон
- [x] 1.3 Ввести тип снимка накопленного расхода прогона — токены, цена, прошедшее время, число попыток с несообщённой ценой; цена отсутствует, если её не сообщила ни одна попытка
- [x] 1.4 Добавить `onEvent` в `RunOptions`, собирать снимок из накопителя (`src/core/budget/accumulator.ts`) теми же вызовами, что и блок `budget` в `writeStatus`, и передавать его наблюдателю рядом с событием (`src/core/run/runner.ts`)
- [x] 1.5 Тесты: состав и порядок доставленных событий совпадают с файлом; событие доставлено после записи; снимок растёт по ходу прогона; отсутствующая цена не превращается в ноль; исключение наблюдателя не меняет исход прогона; прогон без наблюдателя ведёт журнал как прежде

## 2. Рендеринг строк хода

- [x] 2.1 Завести модуль рендеринга рядом с `src/cli/output.ts`: событие плюс снимок расхода — ноль или одна строка текста
- [x] 2.2 Реализовать закрытый перечень печатаемых видов: `run.started`, `run.finished`, `job.started`, `job.finished`, `job.errored`, `step.started`, `step.finished`, `step.stalled`, `iteration.started`, `iteration.finished`, `expect.failed`, `budget.warning`, `budget.exceeded`, `budget.waiting`, `budget.resumed`, `backend.refused`; остальные виды дают ноль строк
- [x] 2.3 Собирать строку как «время от старта прогона — адрес (`работа` или `работа/шаг`) — существо события», без отступов и вложенности
- [x] 2.4 Дописывать к строке исхода шага накопленный расход прогона, форматируя токены, цену и время функциями `src/core/units.ts` и ставя прочерк на месте несообщённой цены
- [x] 2.5 Не выпускать в строку управляющих последовательностей ни при каком выводе: ни цвета, ни возврата каретки, ни перерисовки
- [x] 2.6 Тесты: строка на каждый печатаемый вид, молчание на непечатаемых, адрес в каждой строке работы и шага, расход в строке исхода шага, прочерк вместо несообщённой цены, отсутствие ANSI в выводе на всём перечне событий

## 3. Лента в команде run

- [x] 3.1 Объявить флаг `--quiet` у команды `run` в `src/cli/main.ts` с описанием «не печатать ход прогона»
- [x] 3.2 Подключать наблюдателя в `runRunCommand` и печатать строки рендерера через тот же `write`, что и итог (`src/cli/commands/run.ts`)
- [x] 3.3 Печатать идентификатор прогона и путь к каталогу журнала по событию `run.started`, первыми строками ленты
- [x] 3.4 Не подключать наблюдателя вовсе при `--quiet`; итог, предупреждение о неприменённом денежном потолке, список рабочих деревьев и диагностику отказов оставить печатающимися в обоих режимах
- [x] 3.5 Сохранить неизменным поведение `--dry-run` и код возврата команды в обоих режимах
- [x] 3.6 Тесты: ход виден до завершения прогона, ранний идентификатор адресует прогон в `logs`, перемешанный поток двух параллельных работ читается по адресам, `--quiet` подавляет ход и сохраняет итог, код возврата одинаков с ключом и без него

## 4. По итогам ревью

- [x] 4.1 Отключать устойчиво падающего наблюдателя после трёх неудач подряд, называя отключение в `bookkeeping.failed`; удачная доставка счёт обнуляет (`src/core/journal/writer.ts`)
- [x] 4.2 Сводить свободный текст события (деталь предиката, сообщение отказа бэкенда, причину исхода) к одной строке без управляющих последовательностей и обрезать по длине (`src/cli/progress.ts`)
- [x] 4.3 Добавить `dimension` событиям `budget.warning`/`budget.exceeded` и печатать величины бюджета в единицах измерения через общий с `describeExceeded` форматтер (`src/core/journal/schema.ts`, `src/core/budget/accumulator.ts`, `src/core/run/runner.ts`)
- [x] 4.4 Довести тесты до содержания строк: что именно говорит строка каждого печатаемого вида, свёртка многострочной раскрашенной детали, рост токенов и цены в снимке от шага к шагу
- [x] 4.5 Оговорить в спеке `run-journal` и фрагменте status.md недоставку `bookkeeping.failed` собственного отказа и отключение наблюдателя

## 5. Документация

- [x] 5.1 Описать вывод команды `run` — состав ленты, формат строки, `--quiet` — в `docs/run-layout.md`
- [x] 5.2 Написать фрагмент `openspec/changes/run-progress-output/status.md` и пересобрать `docs/status.md` через `npm run status:build`
- [x] 5.3 Прогнать `npm run check` целиком
