## 1. Отбор прогонов по признаку в ядре

- [x] 1.1 Выделить в `src/core/run/cleanup.ts` чтение одного прогона как кандидата (`RunPaths` → момент возраста `finished_at ?? started_at`, возраст, размер через `dirSize`) и перевести `listCandidates` на неё, чтобы у `gc` и витрины возраст и размер считались одним кодом
- [x] 1.2 Добавить отбор по корню прогонов: перебор `listProjects`/`listRunsByKey`, необязательное сужение до одного проекта, адрес отобранного вида `<ключ>/<идентификатор>`
- [x] 1.3 Реализовать три признака — оборванный (состояние `running` и ложный `isRunAlive`), отказавший (через `isFailure` из `src/core/journal/schema.ts`, а не через свой список статусов), старше срока (порог в мс) — с объединением по «или» и без повторов в отборе
- [x] 1.4 Реализовать групповое снятие: по списку адресов, с перепроверкой живости перед каждым `removeRun`, исходом на адрес (`removed`, `skipped_alive`, `skipped_missing`, `failed` с причиной) и суммой освобождённого по размерам, снятым на отборе
- [x] 1.5 Тесты `test/cleanup.test.ts`: отбор оборванных не берёт живой и завершённый; отбор отказавших берёт `failed`, `budget_exceeded`, `canceled` и не берёт `success`; отбор по сроку от `finished_at` и от `started_at`; прогон под двумя признаками назван один раз; сужение до проекта; отбор не трогает диск; групповое снятие с живым прогоном в списке удаляет остальных; отсутствующий адрес даёт `skipped_missing`, а не отказ

## 2. Полное снятие прогона под требованием спеки

- [x] 2.1 Проверить `removeRun` против требования `run-cleanup`: каталог целиком, перестановка `latest` на новейший оставшийся, снятие ярлыка при отсутствии оставшихся, удаление записи проекта из `projects.json` и каталога проекта у проекта без прогонов
- [x] 2.2 Тесты `test/cleanup.test.ts` на непокрытые из перечисленного случаи — в частности, снятие ярлыка `latest` у проекта без оставшихся прогонов и отсутствие отказа на платформе без символических ссылок

## 3. Отказ по живости вместо статуса

- [x] 3.1 Заменить в `handleDelete` (`src/ui/server.ts`) проверку `readStatus(paths).status === 'running'` на `isRunAlive(paths)`, сохранив код `409` и формулировку причины
- [x] 3.2 Тесты `test/ui-server.test.ts`: прогон с живым процессом (`pid` текущего процесса в манифесте) даёт `409` и каталог не трогает; прогон со статусом `running` и заведомо мёртвым `pid` удаляется; прогон со статусом `running` без `pid` в манифесте удаляется; прогон с `wake_at` и живым процессом даёт `409`

## 4. Маршрут отбора у демона

- [x] 4.1 Добавить `GET`-маршрут отбора: признаки и необязательный проект в строке запроса, срок в формате `--older-than`; ответ — список адресов с размерами, число и суммарный размер
- [x] 4.2 Отклонять ошибкой клиента неизвестный признак и неразбираемый срок, называя ожидаемый формат
- [x] 4.3 Тесты `test/ui-server.test.ts`: отбор по каждому признаку и по их набору; пустой отбор — нулевые число и сумма, код `200`; неизвестный признак и битый срок дают `400`; отбор не удалил ничего

## 5. Групповое удаление у демона

- [x] 5.1 Добавить `DELETE /api/runs`: тело со списком адресов, разбор и проверка формы каждого адреса, предел на число адресов и на размер тела с кодом `413` за пределом
- [x] 5.2 Отвечать `200` с перечнем исходов по адресам и суммой освобождённого; живой прогон в списке не отменяет удаления остальных
- [x] 5.3 Звать `watcher.poll()` один раз после всей группы, а не на каждый прогон
- [x] 5.4 Убедиться, что новый маршрут подпадает под существующую проверку `sameOrigin` для не-`GET` запросов, и закрыть это тестом
- [x] 5.5 Тесты `test/ui-server.test.ts`: группа из нескольких прогонов снята одним запросом; живой пропущен, остальные удалены; отсутствующий адрес — `skipped_missing`; адрес неверной формы — `400` без удалений; список сверх предела — отказ и ни одного удаления; запрос со стороннего `Origin` — `403`

## 6. Оборванный прогон в обзоре

- [x] 6.1 Добавить в `RunOverview` (`src/ui/overview.ts`) признак оборванности, вычисляемый через `isRunAlive` только при статусе `running`
- [x] 6.2 Тесты `test/ui-overview.test.ts`: оборванный и идущий прогоны рядом различимы; у завершённого признак ложен; `isRunAlive` не зовётся для прогонов вне статуса `running`

## 7. Документация и проверка

- [x] 7.1 Написать фрагмент `openspec/changes/ui-run-deletion/status.md` и пересобрать `docs/status.md` через `npm run status:build`
- [x] 7.2 Отметить в `docs/status.md` смену поведения: витрина больше не отказывает удалению прогона, застрявшего в `running` без живого процесса
- [x] 7.3 Прогнать `npm run check` целиком

## 8. Правки по ревью

- [x] 8.1 Проверять ключ проекта в `GET /api/runs` так же строго, как ключ в адресе прогона: общая проверка сегмента раскладки, `400` на ключ со стороны корня
- [x] 8.2 Не терять прогон с испорченным журналом: статус берётся из манифеста при нечитаемом состоянии, возраст — по времени каталога при нечитаемом манифесте, признак `unreadable` виден в ответе отбора
- [x] 8.3 Разрешить снятию брать размер, пришедший вместе с адресом, и описать в дизайне, почему демон меряет каталог перед `rmSync`
- [x] 8.4 Тесты `test/cleanup.test.ts`: отбор по всем проектам без сужения; размер отобранного прогона; нечитаемый манифест и нечитаемое состояние; снятие ярлыка `latest` у проекта без оставшихся прогонов; снятие прогона там, где ярлык поставить не удаётся; размер с адреса в сумме освобождённого
- [x] 8.5 Тесты `test/ui-server.test.ts`: размер каждого отобранного и суммарный объём; отбор по всем проектам и сужение до одного; `400` на ключ проекта за пределами корня
- [x] 8.6 Тест `test/ui-overview.test.ts`: обзор не несёт размеров и не знает `dirSize`
