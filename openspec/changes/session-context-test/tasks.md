## 1. Каркас проверки на настоящем пути сборки

- [x] 1.1 Завести `test/session-context.test.ts`: прогон через `runPipeline` с проектом из `makeProject`, поддельным бэкендом `createFakeBackend({ lines: [initLine(), resultLine({ text: 'ок' })] })` и корнем прогонов во временном каталоге — по образцу `run()` из `test/iteration-context.test.ts`; своего `buildPrompt` и своей отметки об отправленном контексте в файле нет
- [x] 1.2 Написать помощник чтения состава: `context.json` каталога шага, найденного `findStepDir(result.journal.paths, job, step, iteration)`, разобранный в перечень `entries[].origin`
- [x] 1.3 Написать помощник `inheritedOrigins`: уровни `upstream`, `pipeline`, `job` из состава шага — то, чего у шага-продолжателя сессии быть не должно
- [x] 1.4 Объявить в пайплайнах-образцах контекст записями `text` с узнаваемым текстом на каждом уровне («контекст пайплайна», «контекст работы», «контекст шага <id>»), как в `test/context.test.ts`: текстовые записи не склеиваются между собой, и уровень виден в составе ровно там, где объявлен

## 2. Однократность в общей сессии

- [x] 2.1 Пайплайн-образец: работа с `session: shared` и двумя агентскими шагами, у каждого свой файл контекста и предикат `exit_code: 0`
- [x] 2.2 Проверка: у первого шага состав содержит записи с `origin: pipeline` и `origin: job`, у второго — ни одной унаследованной, но собственная запись `origin: step` есть (иначе зелёным окажется и шаг, вовсе не собравший контекста)
- [x] 2.3 Проверка вторым пробником: `prompt.txt` первого шага содержит текст записи уровня пайплайна, `prompt.txt` второго — не содержит
- [x] 2.4 Проверка блока выходов предшественников: работа, зависящая по `needs` от работы с опубликованным выходом, отдаёт `origin: upstream` первому агентскому шагу и не отдаёт второму шагу той же сессии; у второго шага, как и в 2.2, объявлен собственный контекст и проверено наличие `origin: step` — иначе пустой перечень унаследованных уровней был бы зелёным и у шага, вовсе не собравшего состава

## 3. Границы отметки

- [x] 3.1 `session: per_step`: работа с двумя агентскими шагами — состав каждого содержит `origin: pipeline` и `origin: job`
- [x] 3.2 Одноимённая сессия соседней работы: две работы, обе с `session: shared`, — первый агентский шаг каждой получает унаследованные записи; отметка живёт в работе, а не в прогоне
- [x] 3.3 Новая итерация `until`: работа с `until.max_iterations: 2`, чья проверка проходит только со второй итерации, — составы первого шага обеих итераций (`findStepDir` с номером итерации) содержат унаследованные записи
- [x] 3.4 Повторная попытка внутри шага: шаг с `attempts: { max: 2 }` и предикатом `matches`, где поддельный бэкенд отдаёт непроходящий ответ на первой попытке и проходящий на второй, — `prompt.txt` содержит текст записи уровня пайплайна, `prompt.2.txt` не содержит, а `context.json` (состав последней попытки) унаследованных записей не содержит
- [x] 3.5 Пояснить комментарием в 3.4, почему эта единственная проверка сравнивает промпты, а не составы: `context.json` у шага один и перезаписывается каждой попыткой

## 4. Снятие теста-двойника

- [x] 4.1 Удалить из `test/backend.test.ts` `describe('step-context: контекст и общая сессия')` целиком вместе с его локальными `contextSent` и `buildPrompt`
- [x] 4.2 Убедиться, что удалённый блок не был единственным потребителем импортов файла, и снять осиротевшие импорты, если такие остались

## 5. Признак годности: проверка мутацией

- [x] 5.1 Временно убрать `contextSent` из `src/core/run/runner.ts` (`first` всегда `true`), прогнать `npm run test:only` и записать в этой задаче, какие проверки упали; вернуть код

  Упавшие проверки (мутация a, `first` всегда `true`):
  - `step-context: однократность унаследованного контекста в общей сессии` › `первый шаг получает пайплайн и работу, второй — только собственную запись`
  - `step-context: однократность унаследованного контекста в общей сессии` › `промпт второго шага не содержит текст контекста пайплайна`
  - `step-context: блок выходов предшественников в общей сессии` › `первый шаг consumer получает блок выходов, второй — нет`
  - `step-context: границы отметки об отправленном контексте` › `повторная попытка внутри шага унаследованный контекст заново не получает`

- [x] 5.2 Повторить мутацию переносом: завести `contextSent` внутри `runAgentStep`, то есть на шаг, — тот самый случившийся регресс; убедиться, что проверки общей сессии и блока выходов предшественников падают; вернуть код

  Упавшие проверки (мутация b, `contextSent` заведён на шаг):
  - `step-context: однократность унаследованного контекста в общей сессии` › `первый шаг получает пайплайн и работу, второй — только собственную запись`
  - `step-context: однократность унаследованного контекста в общей сессии` › `промпт второго шага не содержит текст контекста пайплайна`
  - `step-context: блок выходов предшественников в общей сессии` › `первый шаг consumer получает блок выходов, второй — нет`

  Проверка повторной попытки (3.4) при этой мутации осталась зелёной: попытки
  одного шага уже исполняются в пределах одного вызова `runAgentStep`, так что
  перенос отметки на шаг границу между попытками не задевает — задевает
  только границу между шагами общей сессии.

- [x] 5.3 Повторить мутацию расширением: вынести `contextSent` из `runJobSteps` так, чтобы отметка была общей у всех работ и итераций прогона; убедиться, что падают проверки соседней работы и новой итерации `until` — границы, которых мутации (а) и (б) не задевают; вернуть код

  Упавшие проверки (мутация в, `contextSent` общий на прогон):
  - `step-context: границы отметки об отправленном контексте` › `одноимённая сессия соседней работы получает унаследованный контекст самостоятельно`
  - `step-context: границы отметки об отправленном контексте` › `новая итерация until начинает сессию заново и получает контекст снова`
  - `step-context: блок выходов предшественников в общей сессии` › `первый шаг consumer получает блок выходов, второй — нет`
  - `step-context: блок предшественников берётся по графу` › `не пускает в блок выход одновременной работы, не входящей в предшественники` (существовавшая проверка `test/backend.test.ts`)

  Проверки общей сессии (2.2, 2.3) при этой мутации остались зелёными: работа
  в том пайплайне одна, и общая на прогон отметка ведёт себя в ней так же, как
  общая на работу. Границу «отметка живёт в работе» стережёт проверка соседней
  работы, границу итерации — проверка `until`; ни ту, ни другую мутации (а) и
  (б) уронить не могут, поэтому мутация (в) обязательна.

- [x] 5.4 Убедиться, что после возврата кода `git status` по `src/` чист: изменение не меняет движок ни строкой

## 6. Документация и проверка

- [x] 6.1 Написать фрагмент `openspec/changes/session-context-test/status.md` с разделом «Известные ограничения»: унаследованный контекст не переотправляется на бэкенде без поддержки сессий, где `session: shared` деградирует до `per_step` одним предупреждением; `context.json` шага с несколькими попытками показывает состав только последней
- [x] 6.2 Пересобрать `docs/status.md` через `npm run status:build`
- [x] 6.3 Прогнать `npm run check` целиком
