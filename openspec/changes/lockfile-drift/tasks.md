## 1. Проверка синхронности

- [x] 1.1 Добавить `test/lockfile-sync.test.ts`: прочитать `package.json` и
      `package-lock.json`, сравнить `dependencies`, `devDependencies` и
      `optionalDependencies` манифеста с одноимёнными полями корневой записи
      `packages[""]` лока — тест красный при расхождении диапазона хотя бы
      одной зависимости, сообщение называет её имя и оба диапазона.
- [x] 1.2 Убедиться, что новый тест собирается в `dist/test` и подхватывается
      существующим `npm run test:only` без правок `package.json` или состава
      `npm run check`.

## 2. Синхронизация лока

- [x] 2.1 В чистом рабочем дереве (свежий checkout HEAD) выполнить `npm ci`.
      На момент написания этого изменения корневая запись
      `package-lock.json` уже посимвольно совпадает с `package.json`, поэтому
      отказа не ожидается; если он всё же случится — переиграть лок
      (`npm install`) и закоммитить получившийся `package-lock.json`.

## 3. Проверка

- [x] 3.1 `npm run check` проходит, включая новый тест.
