## 1. Таблица общих модулей и её применения

- [x] 1.1 `src/ui/sharedModules.ts` на месте `src/ui/widgetRuntime.ts`: запись на имя — сегмент адреса, перечень реэкспортируемых имён, признак экспорта по умолчанию; константы протокола модуля (`WIDGET_ERROR_EXPORT`, `WIDGET_STYLE_EXPORT`) переезжают вместе (Решение 1)
- [x] 1.2 Записи `cordis`, `@stepcast/slots`, `@stepcast/ui` в таблице с сегментами `cordis`, `stepcast-slots`, `stepcast-ui`; сегмент проверяется `isSafeSegment` на выводе применений таблицы (`checkedRouteSegment`, `src/ui/routes.ts`, Решение 3)
- [x] 1.3 Форма адреса `/shared/<имя>.js` в `src/ui/routes.ts` (`isSharedPath`, `sharedModuleHref`) и в диспетчере `src/ui/server.ts` вместо `/widgets/runtime/`; путь под `/shared/` сверх таблицы — 404
- [x] 1.4 Карта имён `ui/index.html` и публикация экземпляров `ui/src/main.tsx` — по шести именам таблицы; прокси `/shared` в `vite.config.ts` рядом с `/widgets`
- [x] 1.5 Внешние имена сборки браузерной половины берутся из таблицы (`src/ui/widgets.ts:364`), второго перечня не остаётся
- [x] 1.6 Общий модуль таблицы внесён в `ui/tsconfig.json` и `vite.config.ts` (`fs.allow`) под новым именем; имена `@stepcast/slots` и `@stepcast/ui` отображены на свои модули во всех четырёх местах — `vite.config.ts`, `ui/tsconfig.json`, `ui/tsconfig.test.json`, `scripts/build-ui-tests.mjs`, — и встроенная половина пишет их так же, как плагин
- [x] 1.7 `test/ui-shared-modules.test.ts` (на месте проверок карты имён в `test/ui-widgets.test.ts`): разметка сверена с таблицей, каждая форма адреса покрыта записью прокси, переходник каждого имени отдаётся и отказывает названной ошибкой без опубликованного экземпляра, `/shared/` сверх таблицы — 404, перечень внешних имён сборки равен таблице

## 2. Поверхность `@stepcast/slots`

- [x] 2.1 `ui/src/sharedSlots.ts`: `slot()`, типы (`SlotDescriptor`, `SlotKind`, `Contribution`, `ChainLinkProps`), дескрипторы `ROOT`, `NAV`, `SCREEN`, `SCREEN_FRAME`, дополнение типов контекста (Решение 5)
- [x] 2.2 Дескрипторы переезжают из `ui/src/plugins/shell.tsx:29` и `ui/src/kernel.ts:42` в модуль поверхности; каркас и ядро берут их оттуда — копии дескриптора не остаётся
- [x] 2.3 Экземпляр `@stepcast/slots` публикуется страницей (`ui/src/main.tsx`) наравне с React и cordis
- [x] 2.4 Адаптер `elementSlotComponent(tag)` — вкладчик, отрисовывающий пользовательский элемент с props слота; приведение типов вместо своего цикла присваивания свойств (Решение 10)
- [x] 2.5 `ui/test/sharedSlots.test.tsx`: перечень имён записи сверен с реальным экспортом модуля (Решение 4); дескриптор из поверхности — то же значение, которым пользуется каркас
- [x] 2.6 `ui/test/webComponent.test.tsx`: адаптер даёт разметку с объявленным элементом и переданными props

## 3. Библиотека `@stepcast/ui`

- [x] 3.1 Зависимости Radix под Dialog, Tabs и Select в `package.json` (Решение 6)
- [x] 3.2 `ui/src/ui/`: Button, Card, Table, Dialog, Tabs, Select, Input — исходники shadcn с вёрсткой на обычном CSS и токенах, рядом их стили; индекс с объявленным составом
- [x] 3.3 Экземпляр `@stepcast/ui` публикуется страницей; перечень реэкспортируемых имён записи сверен браузерным тестом с составом библиотеки (Решение 4)
- [x] 3.4 `ui/test/components.test.tsx`: состав библиотеки, отрисовка каждого компонента, ни одного цвета литералом мимо токенов

## 4. Токены и разделение стилей

- [x] 4.1 Набор токенов shadcn в `ui/src/styles.css` со значениями нынешней палитры, тёмная тема тем же `prefers-color-scheme`; три токена шкалы состояний (`--status-running`, `--status-success`, `--status-failed`) вместо `--run`/`--accent`/`--fail` (Решение 7)
- [ ] 4.2 Стили компонентов уходят из `ui/src/styles.css` в `@stepcast/ui`; стили экранов — к браузерным половинам (`ui/src/screens/*`), импортом рядом с половиной (Решение 8)
- [ ] 4.3 `ui/src/styles.css` сведён к токенам, сбросу и раскладке каркаса; проверка состава селекторов файла по объявленному перечню
- [x] 4.4 Каркас (`ui/src/plugins/shell.tsx`) и общие компоненты (`ui/src/components/*`) переведены на `@stepcast/ui` (у каркаса кнопок/карточек/таблиц нет вовсе — только раскладка, остающаяся его собственной по Решению 8; `FileView.tsx` и `StepOutput.tsx` переведены на `Button`, `JobGraph.tsx` — чистый SVG без соответствия в библиотеке)

## 5. Перевод встроенных экранов

- [ ] 5.1 Прогоны и прогон (`ui/src/pages/Runs.tsx`, `RunDetail.tsx`) — кнопки, поля, выбор, таблица и бейджи из библиотеки, свой CSS при экране (Решение 12)
- [ ] 5.2 Очередь, пайплайны, шаги (`Backlog.tsx`, `Pipelines.tsx`, `Steps.tsx`)
- [ ] 5.3 Расход и уборка (`Usage.tsx`, `Cleanup.tsx`); подтверждение удаления — на Dialog, переключатель периода — на Tabs
- [ ] 5.4 Агенты, настройки, виджеты (`Agents.tsx`, `Settings.tsx`, `Widgets.tsx`)
- [ ] 5.5 Вид витрины не изменился: разметочные тесты экранов (`ui/test/*.test.tsx`) зелёные без правки ожиданий там, где менялась только вёрстка

## 6. Отказ своему экземпляру общего модуля

- [x] 6.1 Сборка половины включает `metafile` и разбирает входы: файл, принадлежащий пакету с именем из таблицы, — отказ с именем файла, именем таблицы и способом исправления (Решение 9)
- [x] 6.2 Отказ доставляется исполняемым модулем тем же путём, что ошибка сборки (`errorModuleText`, `src/ui/widgets.ts:230`), и не мешает соседям
- [x] 6.3 `test/ui-plugins.test.ts`: копия React относительным путём отклонена; голое имя остаётся внешним и проходит; сосед собирается по-прежнему
- [x] 6.4 Подпуть пакета таблицы (`react-dom/client`) — которого карта имён не несёт и который остался бы нерезолвимым именем в браузере — отклонён сборкой, называя сам подпуть; подпуть пакета вне таблицы собирается по-прежнему; сборка без `metafile` отклонена названной причиной, а не пропущена молча (правило — в `docs/plugins.md`)

## 7. Образцы плагинов

- [x] 7.1 `examples/plugins/board/` — плагин на React: экран из `@stepcast/ui` с Dialog, Tabs и Select, вклад дескрипторами `@stepcast/slots`
- [x] 7.2 `examples/plugins/element/` — плагин на другом фреймворке: свой фреймворк в бандле, вклад через адаптер веб-компонента, проверка `customElements.get` перед `define`
- [x] 7.3 `test/ui-plugins.test.ts`: образец на чужом фреймворке собирается, фреймворк лежит внутри бандла, из имён таблицы в нём остаётся только `@stepcast/slots`, которым внесён вклад

## 9. Имена таблицы пишутся одинаково внутри поставки и у плагина (Решения 5, 6)

- [x] 9.1 Оба имени отображены на свои модули во всех четырёх местах — `vite.config.ts`, `ui/tsconfig.json`, `ui/tsconfig.test.json`, `scripts/build-ui-tests.mjs`; встроенные экраны, каркас, ядро, общие компоненты и браузерные тесты пишут голое имя, а не путь внутрь дерева
- [x] 9.2 Образцы типизируются теми же именами: `examples/plugins/board/tsconfig.json` и `examples/plugins/element/tsconfig.json` (`npm run typecheck:plugin` в `npm run check`) — до этого образцы не входили ни в один проект TypeScript; `npm run lint` охватывает `examples/plugins`
- [x] 9.3 `docs/ui-plugins.md`: откуда берутся типы обоих имён и чего у автора плагина вне дерева пока нет
- [ ] 9.4 Поставка типов автору плагина (Решение 11): собранные объявления обоих имён и `tsconfig.plugin.json` в корне пакета, запись в `files` и `exports`. **Вне поверхности этого изменения** — требует корневых файлов сборки, которых поверхность не объявляла; заводится отдельной работой, причина названа в `design.md`, Решение 11

## 8. Документация

- [x] 8.1 `docs/plugins.md`: раздел о таблице общих модулей — шесть имён, что даёт каждое, критерий членства и правило пополнения (все применения сразу), отказ плагину со своим экземпляром и его дыра (переименованная копия)
- [x] 8.2 `docs/ui-plugins.md`: `@stepcast/slots` вместо описания слота строкой, `@stepcast/ui` и токены, вклад веб-компонентом с его ограничением (имя элемента переживает снятие строки), принадлежность стилей по трём уровням
- [x] 8.3 `docs/widgets.md` и оговорка «Таблица общих модулей — `shared-module-table`» (`docs/ui-plugins.md:311`) заменены описанным поведением; новый адрес переходников назван
- [ ] 8.4 `npm run check` зелёный целиком
