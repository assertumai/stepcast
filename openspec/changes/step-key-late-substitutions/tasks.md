## 1. Падающий тест до правки

- [x] 1.1 В `test/resume.test.ts` завести случай: работа подключает контекстом
      `${run.dir}/item.json`, файл кладётся шагом этой же работы; прогон
      доводится до конца, затем возобновляется без единой правки дерева и
      определения — ожидание: шаги работы переиспользованы. До правки тест
      падает с причиной «изменилось определение шага, промпт или бэкенд»
- [x] 1.2 Добавить случай прямее: два прогона одной неизменной работы с
      `${run.dir}` дают совпадающие ключи шагов в `step.json` — сравнение
      ключей, а не решения планировщика

## 2. Ключ от нераскрытого определения

- [x] 2.1 В `src/core/run/runner.ts` протащить нераскрытое определение работы
      (`declared`) до места записи шага, не подменяя им раскрытое `job`, по
      которому шаг исполняется
- [x] 2.2 Сопоставлять шагу раскрытой работы шаг нераскрытой позиционно —
      идентификатор шага сам может содержать подстановку; отсутствие
      соответствия — внутренняя ошибка, а не тихий возврат к раскрытому шагу
- [x] 2.3 Подавать в `computeStepKey` нераскрытый шаг и `jobLockHash` от
      нераскрытой работы; `backendCommand` искать по нераскрытому `step.agent`
- [x] 2.4 Состав ключа в `src/core/run/stepKey.ts` не меняется: те же
      составляющие и тот же алгоритм; добавлено только приведение
      составляющей `upstream` к одному порядку — см. 7.2
- [x] 2.5 `src/core/run/resumePlan.ts` уже считает от нераскрытого определения;
      сбор входа сведён к общему — см. 7.2

## 3. Чувствительность ключа сохранена

- [x] 3.1 Тест: смена выхода работы выше по графу меняет ключ шага, который
      использует `${jobs.<id>.output.*}` — составляющая `upstream` работает
      вместо раскрытого текста
- [x] 3.2 Тест: правка файла работы, использующей `${run.dir}`, меняет ключи
      её шагов и не задевает шаги других работ
- [x] 3.3 Тест: правка значения в `env` работы меняет ключ шага, который его
      подставляет через `${env.<name>}`
- [x] 3.4 Тест: `stepcast resume --set` по-прежнему обесценивает затронутые
      шаги

## 4. Сравнение прогонов

- [x] 4.1 В `test/diff.test.ts` завести случай: два прогона одной неизменной
      работы с `${run.dir}` — `stepcast diff` относит её шаги к совпавшим и
      завершается кодом 0, а не показывает сплошное расхождение
- [x] 4.2 Проверить, что `src/cli/commands/diff.ts` правки не требует:
      сравнение идёт по записанным ключам

## 5. Документация

- [x] 5.1 `docs/run-layout.md`, раздел «Ключ шага и инвалидация»: назвать
      точку, в которой берётся определение (до раскрытия отложенных
      подстановок), и почему величины пространства `run` в ключ не входят;
      отметить, что выходы работ выше по графу входят отдельной составляющей
- [x] 5.2 `docs/status.md`: расширить известное ограничение о прогонах,
      записанных до сужения ключа, на прогоны, записанные до этого изменения —
      их первое возобновление переисполнит шаги работ с отложенными
      подстановками
- [x] 5.3 Сверить оба текста с реализацией построчно: расхождение здесь и было
      исходным дефектом

## 6. Проверка

- [x] 6.1 `npm run check` — типизация, линт, тесты
- [ ] 6.2 Прогнать петлю саморазвития до работы `plan` и возобновить с
      `--from plan`: работа `propose` переиспользуется, а не переисполняется

## 7. Устранение находок ревью

- [x] 7.1 Перенос состояния каталога прогона при переиспользовании: шаг,
      писавший в `${run.dir}`, переиспользуется, а его файл оказывается в
      каталоге нового прогона — иначе переисполняемый сосед той же работы
      падает на отсутствующем файле. Тест на `--from работа/думает`, тест на
      сценарий «Каскад не гасит нижележащие работы без причины», спека
      run-resume, docs/status.md
- [x] 7.2 Составляющая `upstream` собирается одинаково обеими сторонами:
      сортировка по идентификатору работы в `stepKey.ts` и обход разбора
      прогона в порядке исполнения (`executionOrder`) вместо порядка
      объявления. Тем же порядком исправлены приписывание произведённых путей
      и поиск конечного состояния дерева. Тест: работа, объявленная раньше
      своей зависимости, переиспользуется
- [x] 7.3 Соответствие раскрытого шага нераскрытому — позиционное:
      подстановка в `id` шага больше не даёт отказа со ссылкой на внутреннюю
      ошибку, совпадающие идентификаторы не дают молча посчитанного не от того
      шага ключа. Тест в `test/late.test.ts`
- [x] 7.4 Заголовочный комментарий `src/core/pipeline/lock.ts` приведён в
      согласие с локом: отложенные подстановки в нём остаются текстом, и
      именно поэтому `jobLockHash` считается от нераскрытого определения
