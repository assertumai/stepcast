## 1. Учёт наибольшего префикса

- [x] 1.1 Добавить необязательное поле пикового префикса в `UsageSchema` (`src/core/journal/schema.ts`) и в отчётные схемы попытки и шага `usage.json`; убедиться, что старые файлы без поля проходят схему
- [x] 1.2 Снимать пик по каждому сообщению потока в `recordUsageDelta` (`src/core/exec/agentStep.ts`) как `tokens_in + cache_read + cache_write` этого сообщения и держать максимум по попытке
- [x] 1.3 Сделать то же в разборе судейского потока (`src/core/exec/judgePass.ts`)
- [x] 1.4 Задать в `mergeUsage`/`sumUsage` (`src/core/backend/types.ts`) правило «пик берётся максимумом, а не суммой» — расход шага и вызванного им судьи не складывает префиксов
- [x] 1.5 Накапливать пик в `UsageAccumulator` (`src/core/budget/accumulator.ts`) по попытке и шагу (максимум по попыткам, включая запечатанные `sealStep`) и отдавать его в `report()`
- [x] 1.6 Отсутствие пика при наличии расхода пополняет `unreported` тем же правилом, что и прочие измерения; ноль не подставлять
- [x] 1.7 Тесты: пик попытки — максимум по сообщениям, а не сумма; пик шага — максимум по попыткам; слияние с судьёй берёт максимум; бэкенд без пооткликового расхода оставляет поле отсутствующим

## 2. Показ разницы в `stepcast usage`

- [x] 2.1 Добавить в `src/cli/commands/usage.ts` столбец наибольшего префикса рядом со «списано», заполняемый на уровнях попытки и шага и пустой на уровнях работы и прогона
- [x] 2.2 Печатать прочерк там, где пик неизвестен (старая сводка, бэкенд без пооткликового расхода), и упоминать его в блоке о неполноте учёта наравне с прочими несообщёнными измерениями
- [x] 2.3 Тесты команды: разбивка со столбцом пика, прочерк на сводке прежней формы, неизменное число ячеек в строках без расхода

## 3. Формулировка превышения

- [x] 3.1 Переписать ветку `tokens` в `describeExceeded` (`src/core/budget/accumulator.ts`) так, чтобы она говорила о переданном за область трафике при названном потолке, не употребляя «размер» и «объём»; прочие измерения не трогать
- [x] 3.2 Тест: причина остановки по токенному потолку говорит о трафике и не ссылается на контекст

## 4. Документация

- [x] 4.1 В разделе «Бюджет» `docs/pipeline-format.md` развести три предела — трафик прогона (`budget.tokens`), собранный контекст шага (`context.max_tokens`) и окно модели — с указанием, кто их задаёт и что происходит при упоре; явно сказать, что исчерпание потолка не означает переполнения контекста и не вызывает сжатия
- [x] 4.2 Дописать в `docs/pipeline-format.md`, что `cache_read` — сумма префиксов по всем обращениям, и потому трафик прогона на порядок превышает наибольший префикс
- [x] 4.3 Обновить `docs/status.md`: строка «Бюджет» и «Показ расхода» называют измеряемую величину трафиком прогона и упоминают показ пика; отметить, что сводки прежней формы пика не несут
- [x] 4.4 В `docs/run-layout.md` показать расхождение на примере: пример `usage.json` и вывода `stepcast usage` с пиком рядом с трафиком и величинами, различающимися на порядок; описать новое поле и правило «максимум, а не сумма»
- [x] 4.5 В `docs/config.md` рядом с `context.max_tokens` дать перекрёстную ссылку: это предел на контекст одного шага, а не на прогон, и с `budget.tokens` он не связан

## 5. Проверка

- [x] 5.1 `npm run check` проходит
- [x] 5.2 Пройти документацию перекрёстно: нигде не осталось формулировки, позволяющей прочесть `budget.tokens` как объём контекста
