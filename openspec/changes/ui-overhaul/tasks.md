## 1. Снятие `/steps`

- [x] 1.1 Экран, маршрут, строка, `/api/steps`, страница и обёртка удалены; тесты каталога переведены на прямой `buildSteps`.

## 2. Проекты-призраки

- [x] 2.1 `pruneOrphanProjects` в `cleanup.ts` с тестом; вызов при старте демона; пропуск исчезнувших путей в `proposals.ts`.

## 3. `nav.group`

- [x] 3.1 Схема, слияние по слоям, источники, `routes.yml` поставки, JSON Schema, документация, тест.

## 4. `@stepcast/ui`

- [x] 4.1 Badge, Label, Separator, Alert, Switch, Combobox, PageHeader, EmptyState; индекс, таблица общих модулей (версия 2), отпечаток в тесте, тесты компонентов, документация.

## 5. Каркас

- [x] 5.1 Группы меню, английские строки, `Alert` для отказов сборки; стили меню.

## 6. Строки демона

- [x] 6.1 Заголовки экранов, ошибки и подсказки API, источники настроек — по-английски; тесты обновлены.

## 7. Страницы

- [x] 7.1 Runs, RunDetail, Pipelines, компоненты — перевод и `@stepcast/ui`.
- [x] 7.2 Backlog, Board, Usage, Decisions.
- [x] 7.3 Routes (+ `bundled`, поле Group), Settings, Cleanup (+ заметка о призраках).
- [x] 7.4 Общие модули витрины (ядро, слоты, сервисы, хост виджета).

## 8. Каталог виджетов

- [x] 8.1 `src/builtin/widgets/*`, `listBuiltinWidgets`, `installBuiltinWidget`, ключ `builtin` в сервере, маршруты каталога и установки, экран, тесты, `docs/widgets.md`.

## 9. Agents

- [x] 9.1 Combobox со свободным вводом, варианты из CLI плюс конфигурация, английские причины; тест.

## 10. Proposals

- [x] 10.1 Шапка с объяснением, только проекты с записями, имя из пути, вкладки Pending / Resolved; `projectPath` в ответе; тест; `docs/proposals.md`.

## 11. Сверка

- [x] 11.1 `rm -rf dist && npm run check` зелёный.
- [x] 11.2 Экраны просмотрены в браузере на живом демоне.
