## 1. Место подстановки и её происхождение

- [x] 1.1 Добавить в `Substitution` (`src/core/pipeline/model.ts`) необязательные `origin` — путь к файлу, из которого пришёл текст, — и `line`/`column`, позицию начала выражения в этом тексте, считая с единицы
- [x] 1.2 Считать позицию в `interpolate` (`src/core/pipeline/interpolate.ts`) по смещению, которое `String.prototype.replace` передаёт колбэку, и записывать её в каждую подстановку; для полей документа `origin` не заполнять
- [x] 1.3 Дать `interpolate`/`interpolateTree` способ объявить `origin` для всего разбираемого текста (поле области видимости либо параметр), не меняя вызовов, где происхождение — сам документ
- [x] 1.4 Тесты: позиция выражения в многострочном тексте (строка и столбец первой и последующих подстановок), позиция считается по исходному шаблону, а не по результату; экранированное `$${...}` позиции не порождает

## 2. Подстановки файлов промптов попадают в карту

- [x] 2.1 Вернуть из `readPrompt` (`src/core/pipeline/expand.ts`) список подстановок наравне с текстом, проставив `origin` для формы `file:`
- [x] 2.2 Записывать этот список в карту подстановок под ключами `jobs.<id>.steps.<n>.prompt` и `jobs.<id>.steps.<n>.on_fail.prompt` — теми же, что и у промпта, объявленного в документе
- [x] 2.3 Тесты: подстановка из внешнего промпта видна в карте раскрытого пайплайна под путём шага, несёт путь к файлу и позицию; внутренний промпт учитывается как прежде и `origin` не несёт; `on_fail.prompt` из файла учтён наравне с `prompt`

## 3. Ссылка на несуществующую работу — ошибка

- [x] 3.1 Разделить `checkJobSubstitutions` (`src/core/lint.ts`) на проверку существования работы и проверку предшественников; первую выполнять при любом `concurrency` и при `needs: all`
- [x] 3.2 Отклонять ошибкой подстановку `${jobs.X...}` с идентификатором `X`, отсутствующим в графе: сообщение называет выражение, имя и место, подсказка перечисляет похожие идентификаторы работ, если они есть
- [x] 3.3 Указывать в диагностике подстановки из файла промпта путь к этому файлу и позицию выражения (`строка:столбец`), а для полей документа — файл и точечный путь, как раньше
- [x] 3.4 Отделить в `checkCondition` несуществующее имя от существующего соседа: `jobs.X` с отсутствующим `X` в `if` даёт ошибку независимо от `concurrency`
- [x] 3.5 Тесты: опечатка в `with`; имя без суффикса дорожки в общем файле промпта на пайплайне с дорожками (воспроизведение прогона `e1867e`); несуществующее имя в `if` при `concurrency: 2`; `$${jobs...}` в промпте и в поле документа ошибки не даёт; существующая работа ошибки не даёт

## 4. Ссылка на работу вне предшественников — ошибка

- [x] 4.1 Отклонять ошибкой подстановку `${jobs.X...}` на существующую работу, не входящую в предшественницы ссылающейся (прямые или транзитивные), независимо от `concurrency`; пропускать `needs: all` и ссылку работы на собственный идентификатор
- [x] 4.2 Оставить предупреждение о ссылке вне зависимостей только для условия `if` на существующую работу при `concurrency` больше единицы
- [x] 4.3 Тесты: ссылка на соседа при `concurrency: 2` и при `concurrency: 1` даёт ошибку; транзитивная предшественница и `needs: all` ошибки не дают; предупреждение по `if` сохраняется и код возврата от него по-прежнему не зависит

## 5. Проверка на существующих определениях

- [x] 5.1 Прогнать `stepcast lint` по `.stepcast/pipelines/self-improve.yml` и по пайплайнам `examples/`, убедиться, что новые ошибки не возникают; при возникновении — починить определение (`needs`), а не проверку
- [x] 5.2 Прогнать `npm run check` целиком

## 6. Документация

- [x] 6.1 Дописать в раздел `stepcast lint` (`docs/pipeline-format.md`) пункты «Отклоняет»: подстановка на несуществующую работу и подстановка выхода работы вне предшественников; сузить пункт предупреждения до условия `if`
- [x] 6.2 Написать фрагмент `openspec/changes/lint-unknown-job-reference/status.md` и пересобрать `docs/status.md` через `npm run status:build`
