## 1. Проверки, падающие до правки

- [x] 1.1 `test/engine-snapshot.test.ts` (новый): прогон, чей корень пакета движка лежит внутри дерева проекта, снимает снимок — каталог `engine/` в директории прогона содержит `package.json`, объявленные `files` и ссылку `node_modules`; шаг видит `STEPCAST_BIN`, ведущий внутрь каталога прогона. Падает до правки: снимка нет, переменная ведёт в дерево
- [x] 1.2 Там же: **сквозной сценарий пункта очереди** — пайплайн из двух шагов, первый переписывает точку входа движка в дереве, второй читает файл по `$STEPCAST_BIN`; после прогона содержимое, исполняемое вторым шагом, совпадает с тем, что было на старте. Падает до правки: второй шаг видит переписанное
- [x] 1.3 Там же: движок в `<дерево>/node_modules/<пакет>` правимым не считается — снимка нет, `STEPCAST_BIN` прежний; движок вне дерева — то же
- [x] 1.4 Там же: цепочка символических ссылок (каталог установки → дерево) распознаётся правимой, а буквальное сравнение путей — нет
- [x] 1.5 Там же: отказ копирования (недоступный каталог назначения) останавливает прогон ошибкой конфигурации до первой работы, называя причину
- [x] 1.6 `test/engine-snapshot.test.ts`: обход собранного `dist/src/**` не находит динамического импорта вне `dist/src/core/plugins/load.js`. Проходит на нынешнем коде — стережёт свойство, а не чинит беду
- [x] 1.7 `test/loop-portability.test.ts`: абзац `context` документа `.stepcast/pipelines/self-improve.yml` не запрещает `npm run build` и `npm test` — рядом с уже стоящим утверждением «текст контекста пайплайна не называет `${project.check}` и `${project.spec.*}`». Падает до правки

## 2. Распознавание и снимок

- [x] 2.1 `src/core/run/engine.ts` (новый): `locateEngine()` — корень пакета исполняющего движка и его точка входа, оба через `realpath`; корень ищется тем же подъёмом до ближайшего `package.json`, что и в `src/core/package-schema.ts`
- [x] 2.2 Там же: `isEditableEngine({ engineRoot, projectRoot })` — корень внутри дерева и ни один сегмент относительного пути не равен `node_modules`; сравнение по разрешённым путям, как в `checkRunsRootOutsideTree` (`src/core/run/preflight.ts`)
- [x] 2.3 Там же: `pinEngine({ engineRoot, runDir })` — копирование `package.json` и объявленных `files` в `<run-dir>/engine/`, `cpSync` с сохранением прав; пакет без `files` копируется целиком, минус `node_modules` и `.git`
- [x] 2.4 Там же: ссылка `<run-dir>/engine/node_modules` на `node_modules` исходного пакета; тип ссылки выбирается по платформе (`'junction'` на Windows)
- [x] 2.5 Там же: отказ копирования или создания ссылки поднимается `StepcastError` с причиной, не проглатывается
- [x] 2.6 `src/core/run/runner.ts`: вызов после `journal.writeManifest` и события `run.started`, до первой работы; результат (корень, точка входа, признак снимка) складывается в состояние прогона

## 3. Точка входа прогона

- [x] 3.1 `src/core/run/runner.ts:963` и `:2434`: `binPath` берётся из зафиксированного движка прогона, а не из `process.argv[1]` напрямую
- [x] 3.2 Проверить, что оба места получают одно значение (шаг и проверка цикла `until`), — расширить `test/until-env.test.ts` («`STEPCAST_BIN` доходит до проверки цикла») сценарием со снимком
- [x] 3.3 `test/exec.test.ts`: инжекция `STEPCAST_BIN` остаётся неизменяемой шагом — проверки не трогать, значение приходит извне и прежним полем

## 4. Журнал

- [x] 4.1 `src/core/journal/schema.ts`: секция `engine` манифеста (`root`, `entry`, `pinned`), необязательная, `.strict()`
- [x] 4.2 Там же: вид события `engine.pinned` с корнем пакета и путём снимка
- [x] 4.3 `src/core/run/runner.ts`: `engine` пишется в манифест у всякого прогона, включая `pinned: false`
- [x] 4.4 `src/core/journal/format.ts`: `JOURNAL_FORMAT` 3 → 4, строка отпечатка версии 4 с комментарием об этом изменении; строки 1–3 не трогать. Ориентир — `test/journal.test.ts`, который упадёт до подъёма версии
- [x] 4.5 `schema/` — перегенерировать публикуемые схемы, если правка их задела (`npm run schema`); сверяет `test/schema-generated.test.ts` (правка журнала публикуемых схем не задела — перегенерация не требовалась, `test/schema-generated.test.ts` зелен)

## 5. Видимость

- [x] 5.1 `src/cli/progress.ts`: печать `engine.pinned` — закрытый перечень видов расширяется явно, как требует `docs/run-layout.md`
- [x] 5.2 `src/cli/commands/status.ts`: строка о движке прогона из манифеста — корень, точка входа, признак снимка
- [x] 5.3 Проверка ленты: событие печатается первыми строками, рядом с идентификатором прогона, и не печатается вовсе, когда движок правимым не распознан. Про `--quiet` утверждение сделано там, где оно проверяемо: `test/run-progress.test.ts`, «`--quiet` подавляет ход», — строки о движке в выводе нет наравне со строками работы и шага (`--quiet` не заводит наблюдателя, и до печати не доходит ни одно событие)

## 6. Текст контекста петли

- [x] 6.1 `.stepcast/pipelines/self-improve.yml`: из абзаца `context` снимается запрет на `npm run build` и `npm test`; вместо него — что движок прогона зафиксирован снимком в каталоге прогона и пересборка `dist/` его не подменяет
- [x] 6.2 Там же: совет гонять части объявленной проверки по отдельности остаётся, переформулированный как совет о скорости, а не запрет
- [x] 6.3 Сверить с `1.7`: проверка зелёная

## 7. Документация

- [x] 7.1 `docs/run-layout.md`, «Дерево прогона»: каталог `engine/` в раскладке, с пометкой «только когда движок установлен из правимого дерева»
- [x] 7.2 `docs/run-layout.md`: новый раздел «Движок, установленный из правимого дерева» — условие распознавания, состав снимка, ссылка на `node_modules`, что снимок принадлежит прогону и уходит с уборкой, что родительский процесс остаётся на дереве и почему это безопасно
- [x] 7.3 `docs/run-layout.md`, абзац «Сведение зависит от собранного `dist/`»: дополнить тем, что пересборка деревом больше не подменяет движок прогона
- [x] 7.4 `docs/pipeline-format.md`, «Инжектируемые»: `STEPCAST_BIN` — точка входа движка этого прогона, а не `process.argv[1]`; сказать про снимок
- [x] 7.5 `docs/run-layout.md`, «Расхождение версий журнала»: упомянуть подъём формата до 4 в перечне поводов, если такой перечень там ведётся (такого перечня в документе нет — раздел объясняет механизм расхождения в общем виде, без списка версий по поводам; править нечего)

## 8. Гейты

- [x] 8.1 Объявленная команда проверки репозитория зелёная
- [x] 8.2 `openspec validate "merge-check-rebuilds-engine" --strict` зелёная

## 9. Правки по ревью

- [x] 9.1 `src/core/run/runner.ts`, `carryOverRunDir`: каталог снимка входит в раскладку прогона и не переносится из прогона-источника; покрыто сценарием возобновления в `test/engine-snapshot.test.ts`
- [x] 9.2 `src/core/run/engine.ts`: корень пакета движка выводится от `import.meta.url`, точка входа — от объявленного `bin` пакета, а не от `process.argv[1]`; `test/run.test.ts` («`STEPCAST_BIN`») сверяется с точкой входа движка, а не с файлом тестового процесса
- [x] 9.3 Там же: разрешимость зависимостей из снимка проверяется до старта — висячая ссылка `node_modules` и поднятые в корень монорепозитория зависимости дают внятный отказ, а не `ERR_MODULE_NOT_FOUND` посреди прогона
- [x] 9.4 Там же: точка входа обязана оказаться в снимке (и лежать внутри корня пакета) — иначе отказ, а не `STEPCAST_BIN`, ведущий в никуда
- [x] 9.5 Там же: записи `files` с шаблоном или отрицанием переводят снимок на копию всего корня, отсутствующая запись пропускается — формат поля шире копирования путей
- [x] 9.6 `src/cli/commands/status.ts`: манифест читается мягко — прогон с манифестом новее читателя показывается как прежде, без строки о движке
