## 1. Падающие тесты

- [x] 1.1 В `test/plugin-predicate.test.ts` (переиспользуя существующий помощник `pluginRegistry()`) построить прогон с плагинным предикатом через `runPipeline`, получить его `SourceRun` через `readSourceRun`, затем вызвать `planResume({ cwd, config, source, registry: pluginRegistry() })` — плагин загружен. Тест проверяет, что вызов не бросает исключение и пайплайн раскрывается. Тест обязан падать на сегодняшнем коде: `ResumeRequest` поля `registry` не имеет, `planResume()` его игнорирует и раскрывает пайплайн одними встроенными вкладами, отклоняя `text_has` как неизвестный ключ.

- [x] 1.2 В том же файле — тот же исходный прогон (манифест называет плагин `example@1.0.0` с путём `/модуль/example.js`), но `planResume()` вызывается с реестром без этого плагина (`builtinRegistry()`). Тест проверяет: бросается `StepcastError`, чьё сообщение называет имя `example`, версию `1.0.0` и путь модуля `/модуль/example.js` из манифеста, и НЕ содержит ни «неизвестный ключ», ни подсказки про `docs/pipeline-format.md`. Тест обязан падать на сегодняшнем коде (сообщение — «неизвестный ключ text_has» с этой подсказкой).

- [x] 1.3 В том же файле — регресс на «опечатка, а не недостающий плагин»: исходный прогон с манифестом, называющим плагин `example`, `planResume()` вызывается с реестром, где `example` загружен, а документ пайплайна содержит другой, не связанный ни с одним плагином ключ предиката (например, `exit_cod: 0`). Тест проверяет, что сообщение — прежнее «неизвестный ключ exit_cod» с подсказкой про формат пайплайна, а не сообщение о недостающем плагине.

- [x] 1.4 В том же файле — регресс на «манифест плагинов не называет»: построить (или прочитать и очистить) `SourceRun`, чей `manifest.plugins` равен `undefined` (журнал прежней версии, до появления этого поля), с документом, содержащим неизвестный ключ. Тест проверяет, что сообщение — прежнее «неизвестный ключ …» с подсказкой про формат пайплайна, независимо от того, какой `registry` передан.

## 2. Реализация

- [x] 2.1 В `src/core/run/resumePlan.ts`: добавить необязательное поле `registry?: Registry` в `ResumeRequest` (импорт типа `Registry` из `../plugins/registry.js`); в `planResume()` передать его в `expandPipeline({ pipelinePath: source.manifest.pipeline_file, config, inputs, registry: request.registry })`.

- [x] 2.2 В `planResume()` перед вызовом `expandPipeline()` сравнить `source.manifest.plugins` (если поле присутствует) с именами плагинов, фактически загруженных в `request.registry` (реестр по умолчанию — пустой список плагинов, как и умолчание `expandPipeline`, если `registry` не передан). Собрать список плагинов манифеста, чьё имя не найдено среди загруженных.

- [x] 2.3 Если список недостающих плагинов не пуст, бросить `StepcastError` до разбора документа схемой, называющий для каждого — имя, версию (если есть) и путь модуля (`source`) из записи манифеста. Подсказку `docs/pipeline-format.md` этой ошибке не ставить: она не относится к делу.

- [x] 2.4 Путь `expandPipeline()`/`validateDocument()`/`describeSchemaFailure()` не менять: он по-прежнему срабатывает своим прежним сообщением и подсказкой, когда сравнение из 2.2 не нашло недостающих плагинов (манифест их не называет либо все названные загружены) — то есть для настоящей опечатки.

- [x] 2.5 В `src/cli/commands/resume.ts`: передать в вызов `planResume({...})` уже имеющийся в `runResumeCommand` параметр `registry` (тот же, что уходит в `runPipeline()` строками ниже).

- [x] 2.6 `src/cli/commands/status.ts` и диспетчеризацию команды `status` в `src/cli/main.ts` не трогать: `stepcast status --explain` продолжает вызывать `planResume()` без `registry`, как и сегодня — это осознанное сужение объёма, описанное в `proposal.md`.

## 3. Проверка

- [x] 3.1 `npm run check` проходит, включая новые и существующие тесты `test/plugin-predicate.test.ts` и `test/resume.test.ts`.
- [x] 3.2 `openspec validate resume-names-missing-plugin --strict` проходит.
