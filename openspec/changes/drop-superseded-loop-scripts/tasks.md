## 1. Заглушки вместо старой логики

- [x] 1.1 `scripts/merge-lanes.mjs`: убрать тело, реализующее сведение
      (наложение, проверку, откат, коммит), и заменить коротким скриптом,
      который сразу печатает в stderr, что команда заменена
      `stepcast merge-lanes`, и завершается ненулевым кодом, не трогая дерево,
      git и очередь.
- [x] 1.2 `scripts/finalize.mjs`: тем же образом заменить тело заглушкой,
      называющей `stepcast backlog settle`.

## 2. Актуализация ссылок

- [ ] 2.1 Пересобрать `docs/status.md` командой `npm run status:build`, чтобы
      строки «Петля с параллельными дорожками» и «Команда `stepcast backlog`»
      перестали называть скрипты действующей реализацией — вслед за
      фрагментами `status.md`, которые их уже не упоминают.

      Не выполнено: посылка задачи неверна. `npm run status:build` запущен —
      диффа нет, обе строки по-прежнему называют скрипты действующей
      реализацией. Источник — фрагменты `openspec/changes/pipeline-lanes/
      status.md` («Петля с параллельными дорожками») и
      `openspec/changes/portable-backlog-format/status.md` («Команда
      `stepcast backlog`»): оба каталога не архивированы и продолжают
      участвовать в сборке, а `status.mjs` не даёт разделу «Работает»
      механизма «снято» (он есть только у «Пока нет» и «Известные
      ограничения» — см. `FRAGMENT_HEADINGS`/`BASE_HEADINGS` в
      `scripts/status.mjs`). Исправить можно только правкой этих двух чужих
      фрагментов или архивацией их изменений — то и другое вне каталога
      `openspec/changes/drop-superseded-loop-scripts/` и вне границ этого
      шага. Оставлено человеку.
- [x] 2.2 Поправить комментарий в `test/finalize.test.ts`, ссылающийся на
      `scripts/finalize.mjs` как на нечто, несущее логику расчистки, — под
      его новое состояние заглушки.
- [x] 2.3 Поправить комментарий в `test/cli-backlog.test.ts` (строка со
      ссылкой на `scripts/finalize.mjs`), исходящий из той же посылки.

## 3. Проверка

- [x] 3.1 `npm run check` проходит.
