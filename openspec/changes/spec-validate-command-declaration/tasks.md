## 1. Ключ `project.spec.check` в конфигурации

- [x] 1.1 `src/core/config/schema.ts`: `RawSpecSchema` получает `check: CheckCommandSchema.optional()` — та же модель непустой строки, что у `project.check` и `spec.tool` (переиспользование, а не копия).
- [x] 1.2 Комментарий у ключа объясняет то, чего из модели не видно: значение — команда целиком, движок её не разбирает и передаёт оболочке; слаг изменения приходит переменной `SPEC_CHANGE`, читать которую объявление не обязано; ключ не заменяет `tool`, потому что `tool` — имя для записи права.
- [x] 1.3 `src/core/config/resolve.ts`: чтение листа `spec.check` рядом с `dir`, `rules`, `tool`.
- [x] 1.4 Убедиться, что `PROJECT_ONLY_KEYS` править не нужно: шаблон `project.**` уже покрывает новый ключ и запрещает его в `~/.stepcast/config.yml`. Ничего не менять — закрепить тестом 1.6.
- [x] 1.5 Тесты `test/config.test.ts`: объявление в проектном конфиге даёт строку целиком; необъявленный ключ — не ошибка и `undefined`; `check: "   "` отклоняется с ошибкой, называющей файл и ключ.
- [x] 1.6 Тест: `project: { spec: { check: "make spec-check" } }` в глобальном конфиге отклоняется той же проверкой и тем же сообщением, что `project.check` там же.
- [x] 1.7 Тест полистового слияния: три ключа группы из конфигурации и `check` из документа пайплайна действуют вместе, ни один не затирается (реализовано в `test/expand.test.ts`, describe «действующее значение project.spec.*» — там же живут остальные слои этой группы).
- [x] 1.8 `src/cli/commands/config.ts`: отчёт `stepcast config` печатает `spec.check` и его происхождение рядом с `spec.dir`, `spec.rules`, `spec.tool`; покрыть тестом отчёта.

## 2. Ключ в документе пайплайна и в публикуемых схемах

- [x] 2.1 `src/core/pipeline/schema.ts`: секция `project.spec` документа получает тот же ключ той же моделью — пайплайн перекрывает конфигурацию, а не заводит второй формат.
- [x] 2.2 Перегенерировать `schema/config.schema.json` и `schema/pipeline.schema.json` (`npm run schema`); убедиться, что в обеих `project.spec.check` описан строкой с `minLength: 1` и `pattern: "\\S"` — как `project.check`.
- [x] 2.3 Убедиться, что `test/schema-generated.test.ts` проходит и что расхождение он ловит.

## 3. Пространство подстановки `project` пополняется именем `spec.check`

- [x] 3.1 `src/core/pipeline/expand.ts`: `PROJECT_NAMES` пополняется именем `spec.check`; комментарий над списком, перечисляющий состав, обновляется вместе с ним.
- [x] 3.2 `resolveProjectValues`: группа `spec` получает четвёртый лист тем же полистовым порядком слоёв (`document.project?.spec?.check ?? config.project.spec.check`).
- [x] 3.3 Тесты `test/expand.test.ts`: `${project.spec.check}` раскрывается из конфигурации и из пайплайна (пайплайн перекрывает); ссылка на необъявленный ключ даёт отказ, называющий оба места объявления; `${project.spec.validate}` даёт отказ с перечнем имён, среди которых назван `spec.check`.
- [x] 3.4 Тест снимка: `pipeline.lock.yml` пайплайна со ссылкой на ключ несёт объявленную команду и ни одной подстановки.

## 4. Разрешение репозитория пункта очереди

- [x] 4.1 `src/core/project/repos.ts`: `ResolvedRepo['spec']` получает `check: string`; `SpecFields` — `check: string | undefined`.
- [x] 4.2 `requireComplete`: пятая величина в порядке отказа `check` → `spec.dir` → `spec.rules` → `spec.tool` → `spec.check`; комментарий над функцией («четыре величины») приводится в соответствие.
- [x] 4.3 `resolveItemRepo`: значение переносится в результат как объявлено, без склейки с каталогом вложенного репозитория (в отличие от `dir` и `rules`), с коротким объяснением почему — команда исполняется после `cd` внутрь репозитория.
- [x] 4.4 `src/core/backlog/schema.ts`: блок `repo` ответа `backlog pick --lanes` получает `spec.check: z.string()` в обязательных.
- [x] 4.5 Перегенерировать `schema/backlog-slots.schema.json`; убедиться, что `repo.spec.required` содержит `check`.
- [x] 4.6 Тесты разрешения репозитория: корень со всеми пятью величинами даёт полный блок; отсутствие `spec.check` при остальных четырёх даёт отказ, называющий слаг пункта, репозиторий и `spec.check`; вложенный репозиторий даёт склеенные `dir` и `rules` и несклеенные `tool` и `check`.

## 5. Работы петли перестают знать грамматику подкоманд

- [x] 5.1 `.stepcast/jobs/verify.yml`: параметр `spec_tool` заменяется на `spec_check`; блок `env` работы объявляет `SPEC_CHANGE: "${params.change}"`; шаг зовёт `cd "${params.repo_dir}" && ${params.spec_check}`. Идентификатор шага переименовать в `spec-check` — `validate-spec` пересказывает глагол одной практики.
- [x] 5.2 `.stepcast/jobs/implement.yml`: та же замена параметра, тот же блок `env`, предикат `until` зовёт объявленную команду; комментарий у предиката сохраняет объяснение, почему гейт документов идёт шагом движка, а не правом агента, и перестаёт называть подкоманду.
- [x] 5.3 `.stepcast/jobs/fix-review.yml`: то же самое, с тем же комментарием-ссылкой на `implement.yml`.
- [x] 5.4 `.stepcast/jobs/implement-express.yml`: то же самое.
- [x] 5.5 Убедиться, что `propose.yml` и `propose-express.yml` не трогаются: у них `spec_tool` — имя для записи права `Bash(${params.spec_tool} *)`, и это единственное законное его употребление.
- [x] 5.6 `.stepcast/pipelines/self-improve.yml`: обвязка `implement`, `fix-review`, `verify` и обеих работ облегчённой ветки обеих дорожек передаёт `spec_check: "${jobs.slots.output.lanes.<дорожка>.repo.spec.check}"` вместо `spec_tool`; комментарий над блоком работ, перечисляющий, что кому передаётся, обновляется.
- [x] 5.7 `.stepcast/pipelines/self-improve-memory.yml`: та же правка тех же работ.
- [x] 5.8 `.stepcast/config.yml`: секция `project.spec` объявляет `check: openspec validate "$SPEC_CHANGE" --strict`; комментарий объясняет, что переносящему петлю править надо эту строку, а не файлы работ, и что `tool` рядом остаётся именем для записи права.
- [x] 5.9 `stepcast lint .stepcast/pipelines/self-improve.yml` и `.stepcast/pipelines/self-improve-memory.yml` проходят без новых диагностик.

## 6. Сторож переносимости

- [x] 6.1 `test/loop-portability.test.ts`: новая проверка — подстановка имени инструмента (`${params.spec_tool}`, `${project.spec.tool}`) в файлах `.stepcast/jobs/**` допустима только внутри записи права `Bash(… *)`; всякое другое вхождение даёт отказ, называющий файл, строку и правило.
- [x] 6.2 Список `FORBIDDEN` пополняется образцом `--strict` — вторая сеть на случай команды, собранной в файле работы не из подстановки.
- [x] 6.3 Убедиться, что обе проверки падают до правки задач 5.1–5.4, назвав все четыре файла работ; зафиксировать это в комментарии к проверке (зелёный результат на уже вычищенном дереве сам по себе ничего не доказывает). Проверено вручную по содержимому файлов до правки (задача 5 в этой же сессии): все четыре файла собирали `${params.spec_tool} validate "${params.change}" --strict` вне записи права.
- [x] 6.4 Убедиться, что проверка не бьёт по невиновным: права в `propose.yml` и `propose-express.yml` остаются законными, а текст комментариев файлов работ под правило не подпадает.

## 7. Чужое объявление и сквозной прогон

- [x] 7.1 `foreignPipeline` в `test/loop-portability.test.ts`: чужое объявление получает `spec.check: make spec-check`, а работы `implement-a`, `fix-review-a` — параметр `spec_check`.
- [x] 7.2 Утверждения статического раскрытия: предикат `until` и шаг несут чужую команду целиком, и ни в одном из них нет ни слова `validate`, ни флага `--strict`, ни имени `openspec`.
- [x] 7.3 `RUN_PIPELINE` сквозного прогона: `spec.tool` возвращается к `make`, а `spec.check` объявляется исполнимой командой, проверяющей доставку слага (`test "$SPEC_CHANGE" = demo-change`); комментарий про обходной `tool: "true"` снимается вместе с обходом.
- [x] 7.4 Утверждение: прогон доходит до последней работы; отдельной проверкой — что при слаге, не доехавшем до команды, прогон падает (то есть проверка 7.3 действительно проверяет доставку, а не проходит всегда).
- [x] 7.5 Тест доставки переменной на уровне движка не заводить: свойство «переменная работы доходит до шага и до предиката `until`» уже закреплено `test/until-env.test.ts` — убедиться, что оно там есть, и сослаться на него комментарием.

## 8. Документы

- [x] 8.1 `docs/config.md`, раздел «Практика спецификации»: четвёртый ключ группы — что объявляется (команда целиком, а не имя), почему она не собирается из `tool`, как до неё доезжает слаг (`SPEC_CHANGE`, объявляет работа петли, читать не обязательно), почему `tool` остаётся и чем два ключа различаются. Пример секции `project` в начале файла дополняется ключом.
- [x] 8.2 `docs/config.md`, абзац о вложенных репозиториях: перечень того, что репозиторий объявляет «вместо корневых», пополняется новым ключом группы.
- [x] 8.3 `docs/config.md`, абзац «Репозиторий, переносящий к себе петлю, объявляет все три ключа»: три становятся четырьмя, с оговоркой, что репозиторий без инструмента объявляет тем, чем документы всё же проверяются.
- [x] 8.4 `docs/pipeline-format.md`, «Когда что раскрывается»: состав пространства `project` пополняется именем `spec.check`; пример секции `project` в начале файла — тоже.
- [x] 8.5 `docs/status.md` не заводить: документ упразднён изменением `drop-status-doc`, и `test/no-status-doc.test.ts` запрещает его возвращать. Упоминание в признаке выполненности пункта очереди — инерция формулировки.
- [x] 8.6 `.stepcast/prompts/spec-rules.md` не трогать: файл правил — то самое место, где практика названа своим именем, и сторож переносимости делает для него исключение намеренно.

## 9. Проверка целиком

- [x] 9.1 Объявленная команда проверки этого репозитория проходит целиком.
- [x] 9.2 `openspec validate "spec-validate-command-declaration" --strict` проходит.
