## 1. Дорожка в формате пайплайна

- [x] 1.1 Добавить необязательный ключ обвязки `lane` в схему и модель пайплайна (`src/core/pipeline/`): значение — слаг в kebab-case, объявляется только на месте подключения работы
- [x] 1.2 Отклонять `lane` внутри документа с `kind: job` наравне с `needs`, `on`, `if`, `with`, называя файл и ключ
- [x] 1.3 Добавить в `stepcast lint` диагностику значения `lane`, не проходящего kebab-case, с именем работы и встреченным значением
- [x] 1.4 Переносить объявленные `lane` в зафиксированный `pipeline.lock.yml`
- [x] 1.5 Пересобрать публикуемые `schema/pipeline.schema.json` и `schema/job.schema.json`
- [x] 1.6 Тесты: разбор `lane`, отказ на неслаге, отказ на `lane` внутри файла работы, `lane` в локе, неизменность порядка исполнения при объявленной дорожке

## 2. Структурированный выход командного шага

- [x] 2.1 Разбирать `stdout` шага `run` с объявленным `output_schema` как один JSON-документ и публиковать его структурированным выходом шага (`src/core/exec/runStep.ts`)
- [x] 2.2 Сделать разбор строгим: снимать только пробелы по краям, без поиска первого объекта и без склейки последней строки
- [x] 2.3 Отказ шага с причиной, называющей шаг и неразбираемый вывод; подчинить его обычным правилам повторных попыток
- [x] 2.4 Убедиться, что `output.from` и предикат `schema` берут этот выход, а командный шаг без `output_schema` структурированного выхода не имеет
- [x] 2.5 Тесты: публикация выхода, предикат `schema` на командном шаге, отказ на неразбираемом выводе, отсутствие выхода без `output_schema`

## 3. `STEPCAST_BIN`

- [x] 3.1 Добавить `STEPCAST_BIN` — путь к точке входа исполняющего процесса stepcast — к инжектируемым `STEPCAST_*` (`src/core/exec/env.ts`)
- [x] 3.2 Тесты: переменная присутствует в окружении шага, не переопределяется объявленным `env`, указывает на исполняющий движок

## 4. Дорожка в журнале и в отчёте о расходе

- [x] 4.1 Добавить необязательное поле `lane` в запись работы `status.json` (`src/core/journal/schema.ts`); записи прежней формы читать как работы без дорожки
- [x] 4.2 Группировать работы по дорожкам в `stepcast usage`: строка итога дорожки над её работами, те же измерения, та же пометка несообщённого вместо нуля (`src/cli/commands/usage.ts`)
- [x] 4.3 Оставить прогон без объявленных дорожек печатающимся как сегодня, без группировки
- [x] 4.4 Тесты: запись `lane`, чтение состояния прежней формы, итог дорожки, пометка несообщённой цены в итоге, отчёт без дорожек

## 5. `stepcast apply --lane`

- [x] 5.1 Добавить ключ `--lane <имя>` в `src/cli/commands/apply.ts` и отбор изолированных работ дорожки в порядке графа в `src/core/run/apply.ts`
- [x] 5.2 Накладывать дорожку **одним** диффом: от `tree_before` первой работы дорожки до последнего `tree_id` последней
- [x] 5.3 Дорожку без единой изолированной работы с якорями завершать сообщением «накладывать нечего» и нулевым кодом возврата
- [x] 5.4 Сохранить поведение при конфликте: ненулевой код, перечисление конфликтующих путей, дерево не тронуто
- [x] 5.5 Тесты: наложение цепочки одним диффом, изоляция от соседней дорожки, пропущенная дорожка целиком, конфликт с откатом

## 6. Выдача слотов по дорожкам

- [x] 6.1 Добавить в `scripts/backlog.mjs` ключ `--lanes a,b`: отбор по правилам выдачи по числу слотов, одна запись файла, общая метка `started_at`
- [x] 6.2 Проверять перечень дорожек: непустой, попарно различные имена в kebab-case; иначе отказ с ненулевым кодом и без изменения файла
- [x] 6.3 Печатать в stdout запись каждой запрошенной дорожки с признаком заполненности и пустыми значениями полей у пустой дорожки
- [x] 6.4 Писать пункт каждой занятой дорожки файлом `item-<дорожка>.json` в каталог прогона
- [x] 6.5 Не перезаписывать исход пункта, уже переведённого в `done` или `failed`, завершая такой вызов нулевым кодом
- [x] 6.6 Тесты: раздача по порядку дорожек, пустая дорожка, отсутствие подходящих пунктов, некорректный перечень, файлы пунктов, защита проставленного исхода

## 7. Сведение дорожек

- [x] 7.1 Написать `scripts/merge-lanes.mjs`: обход дорожек в объявленном порядке, пропуск дорожки с неуспешной `verify` с записью причины
- [x] 7.2 Наложение дорожки вызовом `$STEPCAST_BIN apply --lane <имя> <run-id>` в дереве проекта
- [x] 7.3 Прогон `npm run check` на объединённом дереве после каждого наложения
- [x] 7.4 Отметка пункта выполненным до коммита и один коммит `<слаг>: <заголовок>` на сведённую дорожку
- [x] 7.5 Останов сведения при конфликте наложения или непрошедшей проверке: откат к последнему коммиту, причина различает конфликт и красную проверку и называет дорожку и путь к её рабочему дереву
- [x] 7.6 Тесты: обе дорожки сходятся, красная проверка после наложения, конфликт наложения, дорожка с отказавшей `verify`

## 8. Завершение по всем взятым пунктам

- [x] 8.1 Переписать `scripts/finalize.mjs` на обход файлов `item-*.json` каталога прогона
- [x] 8.2 Проставлять `failed` с различающей причиной каждому взятому пункту, которому исход ещё не проставлен; за дорожку без пункта не проставлять ничего
- [x] 8.3 Убрать создание коммита из завершения — коммитит теперь сведение
- [x] 8.4 Тесты: отмена по сигналу, останов по бюджету, одна дорожка свелась и другая нет, пустая дорожка

## 9. Пайплайн петли

- [x] 9.1 Добавить работу `slots` (режим `cwd`) с командным шагом `backlog.mjs pick --lanes a,b` и `output_schema`
- [x] 9.2 Развести две дорожки: по цепочке `propose → plan → implement → review → fix-review → verify` на каждую, с `lane` и общими файлами работ
- [x] 9.3 Объявить условие головной работы дорожки по признаку заполненности слота так, чтобы неопределённое значение давало пропуск, а не запуск
- [x] 9.4 Перевести пайплайн на `workspace.mode: worktree`, поднять `concurrency` до числа дорожек, объявить `workspace.inherit: none` на головных работах дорожек
- [x] 9.5 Переопределить режим на `cwd` у работ `slots`, `merge` и `finalize`
- [x] 9.6 Поставить `fail_fast: false` и заменить токенный потолок захода на денежный при `wallclock: 4h` и `rate_limit_pct: 80`
- [x] 9.7 Добавить работу `merge` с зависимостью от `verify` всех дорожек и вызовом `merge-lanes.mjs`
- [x] 9.8 Проверить пайплайн `stepcast lint`

## 10. Документация

- [x] 10.1 Описать `lane`, выход командного шага по `output_schema` и `STEPCAST_BIN` в `docs/pipeline-format.md`
- [x] 10.2 Описать `apply --lane` и группировку расхода по дорожкам в `docs/run-layout.md`
- [x] 10.3 Описать третью дорожку как правку пайплайна плюс поднятые в глобальном конфиге `limits.cost` и `backends.claude.concurrency`
- [x] 10.4 Завести фрагмент `openspec/changes/pipeline-lanes/status.md`: строки о дорожках в таблице «Работает» и ограничения — смежность объявляет человек, красная проверка после сведения обесценивает зелёную у себя дорожку, журнал с `lane` прежней версией не читается
- [x] 10.5 Пересобрать `docs/status.md` через `npm run status:build`

## 11. Проверка

- [x] 11.1 Прогнать `npm run check` и убедиться, что типизация, линт, тесты и сверка `docs/status.md` проходят
- [x] 11.2 Прогнать `openspec validate pipeline-lanes --strict`
