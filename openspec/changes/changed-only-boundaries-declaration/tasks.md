## 1. Ключ `project.edit_paths` в конфигурации

- [x] 1.1 `src/core/config/schema.ts`: `RawProjectSchema` получает `edit_paths: z.array(RelativeRepoPathSchema).min(1).optional()` — элемент той же моделью относительного пути, что `spec.dir` и `spec.rules` (переиспользование, а не копия), список непустой.
- [x] 1.2 Комментарий у ключа объясняет то, чего из модели не видно: почему список заменяется целиком между слоями, почему пустой список отклоняется и почему шаблоны глоба здесь ожидаемы, а `..` и абсолютный путь — тихо бесполезная запись, а не расширенная граница.
- [x] 1.3 `src/core/config/resolve.ts`: `Config['project']` получает `editPaths: readonly string[] | undefined`, читается из `values.get('project.edit_paths')` с той же проверкой на массив строк, что у `tools`.
- [x] 1.4 Убедиться, что `PROJECT_ONLY_KEYS`, `UNION_LIST_KEYS` и `TIGHTEN_ONLY_KEYS` править не нужно: `project.**` уже покрывает новый ключ, а отсутствие в двух других списках и есть требуемая замена целиком. Ничего не менять — только проверить и закрепить тестами 1.6 и 1.7.
- [x] 1.5 Тесты `test/config.test.ts`: объявление в проектном конфиге даёт список в объявленном порядке; необъявленный ключ — не ошибка и `undefined`; `edit_paths: []`, `["  "]`, `["/etc/**"]` и `["../other/**"]` отклоняются с ошибкой, называющей файл и ключ; `["cmd/**", "go.mod"]` принимается как есть.
- [x] 1.6 Тест: `project: { edit_paths: ["src/**"] }` в глобальном конфиге отклоняется той же проверкой и тем же сообщением, что `project.check` там же.
- [x] 1.7 Тест слияния слоёв: верхний слой заменяет список целиком, а соседний ключ секции, объявленный другим слоем, не затирается.
- [x] 1.8 `stepcast config` печатает значение и его происхождение — проверить на списочном значении (прецедент: `env_deny`, `project.tools`) и покрыть тестом отчёта.

## 2. Ключ в документе пайплайна и в публикуемых схемах

- [x] 2.1 `src/core/pipeline/schema.ts`: `ProjectSchema` документа получает тот же ключ той же моделью — объявление в пайплайне перекрывает конфигурацию, а не заводит второй формат.
- [x] 2.2 Перегенерировать `schema/config.schema.json` и `schema/pipeline.schema.json` (`npm run schema`); убедиться, что в обеих `project.edit_paths` описан как массив с `minItems: 1`, элемент — строка с `minLength: 1` и `pattern: "\\S"` (проверки на относительность и `..` в публикуемую схему не переносятся — как у `spec.dir`).
- [x] 2.3 Убедиться, что `test/schema-generated.test.ts` проходит и что расхождение он ловит.

## 3. Пространство подстановки `project` пополняется именем `edit_paths`

- [x] 3.1 `src/core/pipeline/expand.ts`: `PROJECT_NAMES` пополняется именем `edit_paths` — перечень доступных имён в диагностике обязан его называть.
- [x] 3.2 `resolveProjectValues` получает строку `edit_paths: document.project?.edit_paths ?? config.project.editPaths` — тот же полистовой порядок слоёв, что у `check`, `tools` и группы `spec`.
- [x] 3.3 Убедиться, что `src/core/pipeline/interpolate.ts` править не требуется: правило раскрытия списочного значения в элемент списка уже общее и на конкретное имя не смотрит. Ничего не менять — закрепить тестами 3.4 и 3.5.
- [x] 3.4 Тесты `test/expand.test.ts`: `${project.edit_paths}` раскрывается из конфигурации и из пайплайна (пайплайн перекрывает); элемент `changed_only` со ссылкой даёт по шаблону на объявленный путь, в объявленном порядке, а соседние записи предиката остаются на своих местах; необъявленный ключ даёт отказ, называющий оба места объявления; `${project.edit_path}` даёт отказ с перечнем имён, среди которых назван `edit_paths`.
- [x] 3.5 Тест: `check: ${project.edit_paths}` (скалярное поле) роняет разбор прежней ошибкой о значении, непредставимом строкой, с подсказкой о раскрытии только в элементе списка.
- [x] 3.6 Тест снимка: `pipeline.lock.yml` пайплайна со ссылкой на `${project.edit_paths}` содержит объявленные пути и ни одной подстановки.

## 4. Петля объявляет свои границы

- [x] 4.1 `.stepcast/pipelines/self-improve.yml`: в секцию `project` добавляется `edit_paths` с сегодняшними двенадцатью путями репозитория (`src/**`, `test/**`, `docs/**`, `schema/**`, `scripts/**`, `ui/**`, `package.json`, `package-lock.json`, `vite.config.ts`, `eslint.config.js`, `README.md`, `.gitattributes`), с комментарием о том, почему объявление живёт в пайплайне, а не в `.stepcast/config.yml`, — та же причина, что у соседей.
- [x] 4.2 Комментарии, объясняющие отдельные шаблоны (замок ходит вместе с `package.json`; схемы формата — часть кода; скрипты и конфигурация сборки входят в реализацию наравне с `src/`), перенести к объявлению: они объясняют выбор путей, а не устройство работы.
- [x] 4.3 `.stepcast/jobs/implement.yml`: двенадцать литералов раскладки в `changed_only` заменяются одной записью `- ${project.edit_paths}`; записи `${project.spec.dir}/**`, `${project.spec.rules}` и пять записей `.stepcast/**` остаются, с коротким объяснением, почему они не переехали (объявлены другим ключом; заданы движком).
- [x] 4.4 `.stepcast/jobs/fix-review.yml`: та же замена, то же объяснение; порядок оставшихся записей привести к порядку `implement.yml`, чтобы два списка перестали различаться ничем.
- [x] 4.5 Убедиться, что прочие работы петли не трогаются: у `propose` граница — `${project.spec.dir}/**`, у `plan` и `review` — пустой список, раскладки не называет ни одна.

## 5. Промпты перестают пересказывать раскладку

- [x] 5.1 `.stepcast/prompts/implement.md`: «меняй только `src/`, `test/`, `docs/` и каталог изменения» заменяется указанием держаться объявленных границ правок шага и каталога документов изменения — без имён каталогов этого репозитория.
- [x] 5.2 `.stepcast/prompts/fix-review.md`: та же правка в строке «границы правок те же, что у реализации».
- [x] 5.3 `.stepcast/prompts/propose.md`: запрет реализации формулируется через код и тесты, а не через `src/` и `test/`; смысл («ничего не реализовывать») сохраняется дословно.
- [x] 5.4 Убедиться, что подставить список в текст промпта нельзя и не нужно: `${project.edit_paths}` в скалярном тексте — отказ разбора (см. 3.5), а состав границ агент узнаёт из отказа предиката, подмешиваемого следующей попытке.

## 6. Проверки переносимости

- [x] 6.1 `test/loop-portability.test.ts`: перечень запрещённых литералов пополняется раскладкой npm-репозитория — шаблонами границ в файлах работ (`src/**`, `test/**`, `docs/**`, `schema/**`, `scripts/**`, `ui/**`, `package.json`, `package-lock.json`, `vite.config`, `eslint.config`, `README.md`, `.gitattributes`) и упоминанием каталогов в промптах. Файл правил (`spec-rules.md`) остаётся исключением, как и сегодня.
- [x] 6.2 Убедиться, что проверка падает до правки — на сегодняшних `implement.yml`, `fix-review.yml`, `implement.md`, `fix-review.md` и `propose.md`, назвав каждый.
- [x] 6.3 Проверка на настоящем `.stepcast/pipelines/self-improve.yml`: раскрытые `implement-a`, `implement-b`, `fix-review-a` и `fix-review-b` несут ровно тот же набор шаблонов `changed_only`, что до изменения. Ожидаемый набор выписан целиком и сравнивается как множество — порядок предикату безразличен, лишний шаблон такой же промах, как недостающий.
- [x] 6.4 Чужое объявление в том же тесте (`foreignPipeline` и `RUN_PIPELINE`) пополняется `edit_paths: [cmd/**, internal/**, go.mod]`; утверждения: `implement-a` и `fix-review-a` несут эти три шаблона и ни одного из шаблонов раскладки этого репозитория, а `docs/changes/**` и `docs/spec-rules.md` среди границ остаются.
- [x] 6.5 Сквозной прогон против чужого объявления остаётся зелёным; отдельным утверждением проверить, что отсутствие `edit_paths` в пайплайне, чьи работы на него ссылаются, роняет разбор до первого агентского вызова.

## 7. Документы

- [x] 7.1 `docs/config.md`: раздел «Границы правок» рядом с «Инструменты репозитория» — что объявляется (пути, не предикат), почему список заменяется целиком, почему пустой список отклоняется, какие значения отклоняются разбором и почему, запрет глобального слоя. Пример секции `project` в начале файла дополняется ключом.
- [x] 7.2 `docs/pipeline-format.md`, «Когда что раскрывается»: состав пространства `project` пополняется именем `edit_paths`.
- [x] 7.3 `docs/pipeline-format.md`, «Раскрытие списочного значения в элемент списка»: оговорка «сегодня это только `${project.tools}`» заменяется на два имени, с примером записи `changed_only`.
- [x] 7.4 `openspec/changes/changed-only-boundaries-declaration/status.md`: раздел «Работает» о границах правок настройкой; пересобрать `docs/status.md` командой `npm run status:build` (собранный документ руками не править).
- [x] 7.5 `README.md` править не требуется — состава секции `project` он не называет; задача только в том, чтобы не завести туда литерал при правке соседних абзацев.

## 8. Проверка целиком

- [x] 8.1 `stepcast lint .stepcast/pipelines/self-improve.yml` проходит без новых диагностик.
- [x] 8.2 `npm run check` зелёный.
- [x] 8.3 `openspec validate "changed-only-boundaries-declaration" --strict` проходит.
- [x] 8.4 Границы правок соблюдены: изменение не трогает `backlog.md` и `dist/`.
