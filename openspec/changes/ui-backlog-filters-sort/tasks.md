## 1. Общее двум экранам вынести из `Runs.tsx`

- [x] 1.1 Завести `src/ui/filters.ts` — чистый модуль с типом `FilterOption` и
      правилом `withCurrentOption` («выбранное значение остаётся в списке, даже
      если данные его больше не содержат»), переехавшим из
      `ui/src/pages/Runs.tsx:264-271` (Решение 8). `src/ui/runsView.ts` берёт
      `FilterOption` оттуда и продолжает его реэкспортировать либо импортёры
      правятся — поведение экрана прогонов не меняется.
- [x] 1.2 Завести `ui/src/SortHeader.tsx` — заголовок сортируемой колонки,
      переехавший из `ui/src/pages/Runs.tsx:304-329` и параметризованный
      величиной как строкой; знак направления и `aria-sort` — прежние
      (Решение 9).
- [x] 1.3 Перевести `ui/src/pages/Runs.tsx` на переехавшие `SortHeader` и
      `withCurrentOption`; убедиться, что ни одна строка поведения экрана
      прогонов при этом не изменилась.
- [x] 1.4 Добавить `src/ui/filters.ts` в перечень разрешённых браузеру файлов
      `vite.config.ts:47-54` и в `include` файла `ui/tsconfig.json`.

## 2. Правило нумерации, отбора и порядка отдельным модулем

- [x] 2.1 Завести `src/ui/backlogView.ts` — чистый модуль (ни React, ни
      `window`, ни диска) со структурными типами раздела очереди и пункта, тем
      же приёмом, что `src/ui/runsView.ts`: заголовок называет, почему правило
      живёт здесь, а не в `.tsx` (Решение 8).
- [x] 2.2 Реализовать нумерацию: плановый номер присваивается по всем пунктам
      раздела, считая с единицы, до всякого отбора и порядка; нумерация каждого
      проекта начинается заново (Решение 1).
- [x] 2.3 Объявить состав фильтров (статус, проект) и направление планового
      порядка с умолчанием «порядок файла» (Решения 2, 6).
- [x] 2.4 Реализовать значения фильтра статуса из `BACKLOG_STATUSES`
      (`src/core/backlog/schema.ts:19`) — все четыре всегда, с числом пунктов у
      каждого; число считается по пунктам, прошедшим остальные действующие
      фильтры (Решение 4).
- [x] 2.5 Реализовать значения фильтра проектов из полученных разделов и
      применение фильтров по «и»; выбранное значение, которого в данных больше
      нет, оставляет список пустым, а не переключает линзу (`withCurrentOption`,
      Решение 6).
- [x] 2.6 Реализовать порядок: внутри раздела, в обе стороны, единственной
      величиной — плановым номером; поперёк проектов порядок не применяется
      (Решения 2, 3).
- [x] 2.7 Реализовать правила видимости раздела: раздел с отказом разбора
      переживает любой выбранный статус, раздел пустого файла виден только в
      умолчании статуса, оба скрываются фильтром по проекту (Решение 7).
- [x] 2.8 Добавить `src/ui/backlogView.ts` в `vite.config.ts:47-54` и в
      `include` файла `ui/tsconfig.json`.

## 3. Экран очереди

- [x] 3.1 Добавить в `ui/src/pages/Backlog.tsx` полосу фильтров (статус,
      проект) готовым оформлением `.filters` (`ui/src/styles.css:186-187`):
      статус — со всеми четырьмя значениями и числом у каждого, проект — со
      значениями из `backlogView`.
- [x] 3.2 Добавить колонку планового номера первой в таблице; подпись колонки
      называет план, а не строку (Решение 1).
- [x] 3.3 Сделать заголовок колонки номера нажимаемым через общий
      `SortHeader`: нажатие меняет направление планового порядка.
- [x] 3.4 Показывать возврат к умолчанию, пока вид отличается от умолчания (все
      статусы, все проекты, порядок файла), а «показано N из M» — пока список и
      вправду сужен; возврат снимает фильтры и направление разом (Решение 6).
- [x] 3.5 Пустой результат назвать словами: «под фильтры не подошёл ни один
      пункт» с возвратом к умолчанию — отдельно от прежнего «очередей не
      найдено», которое остаётся ответом на пустой кадр демона.
- [x] 3.6 Хранить выбранное состоянием экрана: смена одного фильтра не
      сбрасывает другой, новый кадр живого потока не сбрасывает ничего, в адрес
      страницы выбор не пишется (Решение 6).
- [x] 3.7 Дописать `ui/src/styles.css` колонкой номера (узкая, числовая);
      полоса фильтров и сортируемая шапка берутся готовыми, новых сущностей
      оформления не заводить.

## 4. Тесты

- [x] 4.1 Завести `test/ui-backlog-view.test.ts`: нумерация по всем пунктам
      файла, включая `done`; пропуски номеров при отборе сохраняются; обратный
      порядок номеров не меняет; нумерация второго проекта начинается заново.
- [x] 4.2 Там же — фильтры: статус и проект объединяются по «и»; все четыре
      статуса присутствуют всегда, и статус без пунктов назван нулём; число у
      статуса считается по выбранному проекту; выбранный проект, ушедший из
      данных, даёт пустой список, а не другую линзу.
- [x] 4.3 Там же — порядок: умолчание равно порядку файла; обратное
      направление переворачивает раздел и не меняет состава; пункты двух
      проектов не перемешиваются.
- [x] 4.4 Там же — видимость разделов: раздел с отказом разбора виден при
      выбранном статусе и скрыт фильтром по чужому проекту; раздел пустого
      файла виден в умолчании статуса и скрыт при суженном.
- [x] 4.5 Убедиться, что `test/ui-backlog.test.ts`, `test/ui-runs-view.test.ts`
      и `test/ui-server.test.ts` остаются зелёными без правок: сборка вида
      очереди на стороне демона, отбор прогонов и маршруты не менялись.
- [x] 4.6 Там же — сторожевые проверки, которых не хватало: перечень статусов
      витрины сверяется со схемой очереди (`BACKLOG_STATUSES`,
      `src/core/backlog/schema.ts:19`), раздел без единого прошедшего пункта
      скрыт, а прошедшие разделы остаются; знаменатель «показано N из M»
      считает и пункты очередей, отсечённых фильтром по проекту (Решение 4).

## 5. Ручная проверка сценариев спеки

- [ ] 5.1 Собрать витрину, поднять демон на корне прогонов с двумя проектами и
      пройти: номера пунктов, отбор по статусу с числами, отбор по проекту,
      «показано N из M», возврат к умолчанию.
- [ ] 5.2 Проверить, что при выбранном статусе и обратном порядке смена проекта
      сохраняет и то и другое, а перезагрузка возвращает умолчание.
- [ ] 5.3 Сломать файл очереди одного проекта, выбрать статус `pending` и
      убедиться, что раздел с отказом остался на экране.
- [ ] 5.4 Пометить пункт `in_progress` при открытой вкладке и убедиться, что
      выбранные фильтр и порядок пережили кадр живого потока.

## 6. Сведение спек и проверка

- [x] 6.1 Записать обязанность архиватора: требование `ui-backlog-view`
      «Витрина показывает очередь улучшений отдельным экраном»
      (`openspec/changes/ui-backlog-view/specs/ui-dashboard/spec.md:5`) говорит
      «Пункты проекта SHALL идти в порядке файла очереди» без оговорки об
      обратном направлении; каталог того изменения лежит вне границ правки
      этого, и обе редакции сводятся при сведении изменений в
      `openspec/specs` — «в порядке файла, пока пользователь не выбрал
      обратного». Та же схема, что у `ui-runs-list-controls`, задача 6.1.
- [x] 6.2 Не заводить `docs/status.md` и не ссылаться на него: документ
      упразднён изменением `drop-status-doc`, его возврат стережёт
      `test/no-status-doc.test.ts`; согласование спеки `ui-dashboard` с
      реализацией несёт дельта-спека этого изменения и задача 6.1.
- [ ] 6.3 `openspec validate "ui-backlog-filters-sort" --strict` проходит.
- [x] 6.4 `npm run check` зелёная.
</content>
