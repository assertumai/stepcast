## 1. Группа в разборе очереди

- [x] 1.1 Принять необязательное поле `group` в разборе пункта
      (`scripts/backlog.mjs`): значение проверяется тем же `SLUG`, что и слаг
      пункта, пустое значение недопустимо. Отказ называет слаг пункта и
      встреченное значение и завершается ненулевым кодом возврата
- [x] 1.2 Ввести вычисление действующей группы: объявленное значение `group`,
      а при его отсутствии — слаг пункта. Держать одной функцией, чтобы отбор
      нигде не различал объявленную и подразумеваемую группу
- [x] 1.3 Добавить действующую группу в запись `record()`, чтобы она попадала
      и в `list`, и в выдачу
- [x] 1.4 Проверить тестами разбор: пункт с объявленной группой, пункт без неё
      (группа равна слагу), пункт с группой не в kebab-case и с пустым
      значением — отказ с называнием пункта и значения

## 2. Занятость группы в отборе

- [x] 2.1 Ввести понятие занятой группы: группа занята, если в очереди есть её
      пункт, не свободный по действующему `isFree` (то есть `in_progress`
      моложе порога протухания). Завершённые и протухшие пункты группу не
      держат
- [x] 2.2 Выделить отбор в отдельную функцию: собрать занятые группы по всей
      очереди, пройти пункты сверху вниз, брать свободный пункт с незанятой
      группой, добавляя его группу к занятым, до исчерпания слотов или очереди
- [x] 2.3 Провести отбор в `commandPick` при выдаче одного пункта: занятая
      группа теперь пропускается и здесь, форма вывода — прежний JSON-объект
- [x] 2.4 Проверить тестами отбор одного пункта: пункт занятой группы
      пропускается в пользу пункта ниже; при свободных пунктах только занятой
      группы — отказ и неизменный файл; пропуск не меняет приоритета — тот же
      пункт выбирается первым, когда его группа освободилась

## 3. Выдача нескольких слотов

- [x] 3.1 Добавить ключ `--slots N` в `pick`: целое положительное, иначе отказ
      с ненулевым кодом возврата и без правки файла
- [x] 3.2 Применить отбор с N слотами и пометить все выбранные пункты одной
      записью файла: правки применяются к тексту последовательно, `writeFileSync`
      вызывается один раз, всем выданным пунктам проставляется одна метка
      `started_at`
- [x] 3.3 Сделать форму вывода зависящей от ключа, а не от числа найденных:
      с `--slots` — JSON-массив записей в порядке очереди (в том числе при
      одном пункте), без ключа — прежний объект. `--out` пишет тот же текст,
      что и stdout
- [x] 3.4 Оставить отказ только для пустой выдачи: ноль найденных — ненулевой
      код возврата и неизменный файл; от одного до N — успех
- [x] 3.5 Проверить тестами выдачу: три слота на четырёх пунктах разных групп
      дают первые три; три слота на трёх смежных пунктах и одном чужой группы
      дают два; частичная выдача успешна; пустая — отказ с неизменным файлом;
      `--slots 1` даёт массив из одной записи; некорректное число слотов —
      отказ; общая метка `started_at` у всех выданных
- [x] 3.6 Проверить тестом совместимость: вызов `pick` без ключей печатает
      объект и берёт один пункт, как прежде

## 4. Документация

- [ ] 4.1 Описать в шапке `backlog.md` поле `group`: строка в таблице полей,
      правило умолчания (слаг пункта) и то, что смежность объявляет человек, а
      петля её не выводит. Сами пункты очереди не трогать — их ведёт движок.
      Не выполнено: `backlog.md` в этом прогоне вне границ правки — ведёт
      движок петли
- [x] 4.2 Назвать групповую выдачу в строке о петле саморазвития в
      `docs/status.md`
- [x] 4.3 Прогнать `npm run check`
