## 1. Контракт ответа судьи

- [ ] 1.1 Добавить схему ответа судьи в `schema/` — объект с обязательными
      `pass` (boolean) и `reason` (string), без дополнительных свойств
- [ ] 1.2 Добавить разрешение пути к этой схеме от корня установленного пакета,
      чтобы вызов работал и из `dist/`, и из исходников

## 2. Разделение фаз вычисления предикатов

- [ ] 2.1 В `src/core/expect/evaluate.ts` убрать бросок ошибки для `judge` и
      пропускать такие предикаты, сохраняя их позицию в исходном порядке
      `expect`
- [ ] 2.2 Ввести признак «структурный жёсткий предикат не прошёл» как вход
      второй фазы
- [ ] 2.3 Сделать хук `evaluate` у `executeAgentStep`
      (`src/core/exec/agentStep.ts`) асинхронным и дождаться его результата в
      цикле попыток
- [ ] 2.4 Сделать хук `evaluate` у `executeRunStep` (`src/core/exec/runStep.ts`)
      асинхронным тем же образом

## 3. Вызов судьи

- [ ] 3.1 Создать `src/core/expect/judge.ts` с функцией вычисления судей:
      на вход — предикаты, структурные результаты, адаптер, каталог шага,
      номер попытки, рабочая директория, таймаут, приёмник расхода
- [ ] 3.2 Собрать промпт судьи: утверждение, задание шага, текстовый и
      структурированный результат шага, сводка результатов остальных
      предикатов; контекст шага не включать
- [ ] 3.3 Выдавать вызову одноразовый идентификатор сессии, минуя
      `SessionRegistry`
- [ ] 3.4 Передавать вызову политику доступа, запрещающую запись и исполнение
      команд, независимо от `permissions` шага
- [ ] 3.5 Передавать адаптеру путь к схеме ответа судьи как `outputSchemaPath`
- [ ] 3.6 Разбирать ответ: структурированный вывод, а при отсутствии поддержки
      у бэкенда — последний JSON-объект из текста ответа
- [ ] 3.7 Трактовать ненулевой код возврата, таймаут и неразбираемый ответ как
      непрохождение предиката с причиной, без исключения
- [ ] 3.8 Пропускать вызов, если хотя бы один жёсткий структурный предикат не
      прошёл, и записывать результат невычисленным с причиной пропуска
- [ ] 3.9 Формировать `PredicateResult` судьи: признак прохождения,
      жёсткость из предиката, утверждение, `reason` и имя каталога вызова

## 4. Журнал и расход

- [ ] 4.1 Создавать каталог `judge-<n>` в каталоге шага со сквозной нумерацией
      вызовов, начиная с 1
- [ ] 4.2 Писать в каталог `prompt.txt`, `stdout.log`, `stderr.log` и
      `judge.json` с утверждением, номером попытки, позицией в `expect`,
      разобранными `pass` и `reason` и расходом вызова
- [ ] 4.3 Докладывать расход вызова в `UsageAccumulator` под номером текущей
      попытки шага
- [ ] 4.4 Выполнять проверку бюджетов в `src/core/run/runner.ts` после фазы
      судей и останавливать прогон при превышении по общему правилу

## 5. Связывание в runner

- [ ] 5.1 Передавать в вычисление предикатов агентского шага адаптер, каталог
      шага, номер попытки и приёмник расхода
- [ ] 5.2 Сделать то же для командного шага: судья у шага `run` вызывается
      через адаптер бэкенда по умолчанию
- [ ] 5.3 Оставить предикаты `until.check` на синхронной фазе без судей

## 6. Статическая валидация

- [ ] 6.1 Убрать из `src/core/lint.ts` отказ «Предикат judge ещё не реализован»
- [ ] 6.2 Добавить ошибку на пустое утверждение `judge`
- [ ] 6.3 Добавить ошибку на неизвестный бэкенд в `agent` предиката
- [ ] 6.4 Добавить ошибку на `judge` внутри `until.check` с объяснением
- [ ] 6.5 Добавить предупреждение о жёстком `judge` как единственном предикате
      шага

## 7. Тесты

- [ ] 7.1 Тест: судья подтверждает утверждение — предикат пройден, `reason`
      сохранён
- [ ] 7.2 Тест: судья отклоняет утверждение при `hard: true` — попытка
      непройденная; при `hard: false` — статус не меняется
- [ ] 7.3 Тест: судья пропущен после провала жёсткого структурного предиката —
      бэкенд не вызывался, результат записан невычисленным
- [ ] 7.4 Тест: ответ вне схемы и ненулевой код возврата бэкенда судьи дают
      непройденный предикат, а не исключение
- [ ] 7.5 Тест: разбор ответа из текста при бэкенде без структурированного
      вывода
- [ ] 7.6 Тест: сессия судьи не совпадает с сессией шага и не наследуется
      следующим шагом того же псевдонима
- [ ] 7.7 Тест: политика доступа вызова судьи запрещает запись независимо от
      политики шага
- [ ] 7.8 Тест раскладки: `judge-1` и `judge-2` на двух попытках, состав файлов
      каталога
- [ ] 7.9 Тест учёта: расход судьи попадает в расход попытки и в `usage.json`;
      превышение бюджета вызовом судьи останавливает прогон
- [ ] 7.10 Тесты линта: пустое утверждение, неизвестный бэкенд, `judge` в
      `until.check`, предупреждение о единственном жёстком судье, корректный
      пайплайн с `judge` проходит линт

## 8. Документация

- [ ] 8.1 `docs/status.md`: убрать `judge` из таблицы «Пока нет», добавить в
      «Работает», отметить ограничение — судья в `until.check` не поддержан
- [ ] 8.2 `docs/pipeline-format.md`: уточнить раздел `judge` — режим только для
      чтения, пропуск после провала структурных предикатов, таймаут шага,
      оговорку про `lint --budget` привести в соответствие с реальностью
- [ ] 8.3 `docs/run-layout.md`: описать состав каталога `judge-<n>`
- [ ] 8.4 Прогнать `npm run check` и убедиться, что типизация, линт и тесты
      проходят
