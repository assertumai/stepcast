## 1. Вычислитель судьи

- [ ] 1.1 Схема вердикта `{ pass: boolean, reason: string }` поставляется со stepcast и передаётся бэкенду как схема структурированного вывода
- [ ] 1.2 Вызов судьи: свежая сессия, бэкенд и модель из предиката либо из умолчаний, политика доступа шага не передаётся
- [ ] 1.3 Промпт судьи собирается из утверждения, задания шага, текстового и структурированного вывода и результатов остальных предикатов; контекст шага в него не входит
- [ ] 1.4 Разбор ответа: только структура по схеме; отсутствие вердикта, неразобранный ответ и прерывание по времени дают непройденный предикат с называющей причину подробностью
- [ ] 1.5 Вызов ограничен `timeout` шага и наблюдается общим детектором тишины

## 2. Встраивание в попытку

- [ ] 2.1 Синхронный проход предикатов помечает `judge` невычисленным вместо отказа «не реализовано»
- [ ] 2.2 Асинхронный второй проход вызывает судей по очереди и подставляет результаты на объявленные места в `expect.json`
- [ ] 2.3 Второй проход общий для агентского и командного шага; на командном шаге место структурированного вывода занимает stdout
- [ ] 2.4 Судья не вызывается, если в попытке уже не прошёл жёсткий предикат, вычисляемый без обращения к агенту; результат записывается невычисленным
- [ ] 2.5 Совещательный вердикт статус попытки не меняет; жёсткий отклоняет попытку
- [ ] 2.6 Причина непройденного вердикта входит в разбор прошлой неудачи при `include_failure`

## 3. Расход, бюджет и журнал

- [ ] 3.1 Расход судьи сливается с расходом попытки и учитывается областями шага, работы и прогона
- [ ] 3.2 Исчерпанный бюджет судью не начинает: результат невычислен; превышение после вызова останавливает шаг со статусом `budget_exceeded`
- [ ] 3.3 Каталог `judge-<n>` со сквозной нумерацией внутри шага: промпт, поток, вердикт, расход, номер попытки и проверявшееся утверждение

## 4. Линт

- [ ] 4.1 Отказ «Предикат judge ещё не реализован» снят
- [ ] 4.2 Ошибка на неизвестный бэкенд судьи
- [ ] 4.3 Ошибка на бэкенд судьи без поддержки структурированного вывода
- [ ] 4.4 Предупреждение о шаге без структурных предикатов продолжает срабатывать на шаге с одним совещательным судьёй

## 5. Тесты

- [ ] 5.1 Жёсткий вердикт `pass: false` отклоняет попытку, `pass: true` — проходит
- [ ] 5.2 Совещательный вердикт `pass: false` попытку не отклоняет и записан с признаком совещательного
- [ ] 5.3 Ответ без структуры и ответ не по схеме дают непройденный предикат с внятной причиной
- [ ] 5.4 Судья не вызывается после непройденного жёсткого структурного предиката; после непройденного совещательного — вызывается
- [ ] 5.5 Сессия судьи не связана с сессией шага
- [ ] 5.6 Промпт судьи не содержит контекста шага и содержит результаты остальных предикатов
- [ ] 5.7 Каталог `judge-<n>` создан, пронумерован сквозным образом и содержит обещанное
- [ ] 5.8 Расход судьи виден в расходе попытки и приводит к `budget_exceeded` при превышении потолка шага
- [ ] 5.9 Судья на шаге командной строки получает stdout
- [ ] 5.10 Линт: `judge` не отклоняется; неизвестный бэкенд судьи и бэкенд без структурированного вывода отклоняются
- [ ] 5.11 `npm run check` проходит

## 6. Документация

- [ ] 6.1 `docs/status.md`: `judge` переходит из «Пока нет» в «Работает»; в известных ограничениях зафиксировано, что судья судит по выводу шага, а не по его контексту, и не имеет прав шага
- [ ] 6.2 `docs/pipeline-format.md` и `docs/run-layout.md` приведены в соответствие с реализованным поведением (нумерация `judge-<n>`, пропуск вызова, требование к бэкенду)
