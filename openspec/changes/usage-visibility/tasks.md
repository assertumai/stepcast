## 1. Разбивка по попыткам в сводке расхода

- [x] 1.1 В `src/core/journal/schema.ts` заменить в `UsageReportSchema` поле `attempts: number` записи шага на перечень записей попыток: `attempt` (целое, положительное), `backend`, `model` (необязательная), `billable_tokens`, `wallclock_ms`
- [x] 1.2 В `UsageAccumulator` сохранять бэкенд и модель попытки вместе со счётчиками, чтобы `report()` мог их назвать
- [x] 1.3 В `report()` собирать перечень попыток вместо счётчика, схлопывая переисполнения одной и той же попытки (ключи с разным `sealCounter`) в одну запись с суммарным расходом
- [x] 1.4 Обновить фикстуры сводки в `test/helpers.ts`, `test/journal.test.ts`, `test/cleanup.test.ts`, `test/gc.test.ts` под новую форму
- [x] 1.5 Тест: шаг с двумя попытками даёт в сводке две записи с номерами `1` и `2`, бэкендом, моделью, токенами и временем каждой
- [x] 1.6 Тест: переисполнение одной попытки даёт одну запись с суммарным расходом, а не две записи с одним номером

## 2. Команда `stepcast usage`

- [x] 2.1 Завести `src/cli/commands/usage.ts`: разрешение прогона через `resolveRun` (сокращённый идентификатор и умолчание «последний прогон»), чтение `status.json` и, если он есть и читается, `usage.json`
- [x] 2.2 Собрать модель отчёта: итог по прогону, строки работ, строки шагов, строки попыток с бэкендом, моделью, входными и выходными токенами, чтением и записью кеша, временем и `reported_cost_usd`, когда он сообщён
- [x] 2.3 Печатать несообщённое измерение прочерком и перечислять внизу измерения из `unreported` как неполный учёт
- [x] 2.4 Отсутствующая или непрочитанная сводка: строить отчёт по `status.json`, помечать величины из сводки как не подведённые или сводку — как непрочитанную; ошибку не выбрасывать
- [x] 2.5 Оформить вывод через `formatColumns`, `formatTokens` и `formatDuration`, иерархия — отступом, как в `stepcast status`
- [x] 2.6 Зарегистрировать команду в `COMMANDS` и в `switch` в `src/cli/main.ts` с позиционным аргументом `run`, возврат `ExitCode.ok`
- [x] 2.7 Тест: отчёт по прогону с шагом, прошедшим со второй попытки, показывает обе попытки, их номера, модели и стоимость
- [x] 2.8 Тест: работы с разными моделями видны по строкам попыток
- [x] 2.9 Тест: несообщённое измерение печатается прочерком и попадает в перечень неполноты, а не нулём
- [x] 2.10 Тест: отчёт по прогону после `stepcast gc` строится и содержит разбивку по работам, шагам и попыткам
- [x] 2.11 Тест: отчёт по прогону без `usage.json` строится и помечает неподведённые величины
- [x] 2.12 Тест: `usage.json` формы, не проходящей схему, не роняет команду

## 3. Расход в витрине

- [x] 3.1 Добавить в `RunOverview` (`src/ui/overview.ts`) расход прогона: billable-токены и время, из состояния и сводки, с признаком неподведённости
- [x] 3.2 Добавить расход работы и шага (и число попыток шага) в `JobSnapshot` и `StepSnapshot` (`src/ui/snapshot.ts`), собирая его из записей `status.json`
- [x] 3.3 Отрисовать расход в обзоре и в раскрытом прогоне в `src/ui/assets.ts`, с прочерком на месте несообщённого
- [x] 3.4 Убедиться, что убранный прогон показывает расход и не ломается отсутствием `pipeline.lock.yml`, `jobs/` и `artifacts/`
- [x] 3.5 Тест: обзор отдаёт расход прогона; снимок отдаёт расход работы и шага
- [x] 3.6 Тест: снимок убранного прогона содержит расход; снимок идущего прогона без сводки не падает

## 4. Документация и проверка

- [x] 4.1 `docs/status.md`: перенести строку про показ расхода из «Пока нет» в «Работает», описав команду и расход в витрине
- [x] 4.2 `docs/run-layout.md`: описать фактический вывод `stepcast usage` и новую форму `attempts` в `usage.json`
- [x] 4.3 Прогнать `npm run check` — типизация, линт и тесты зелёные
