## 1. Разведка перед реализацией

- [x] 1.1 Проверить на живом прогоне, накопительна ли `total_cost_usd` внутри
      одной сессии (`session: shared`, два последовательных вызова): сложить
      сообщённые цены и сверить с суммой по токенам. От ответа зависит,
      складывает ли учёт цену как слагаемое или берёт как накопительный итог;
      результат зафиксировать комментарием в коде рядом с местом учёта
- [x] 1.2 Проверить, сообщает ли цену бэкенд судьи тем же полем, что и бэкенд
      шага

## 2. Единица и разбор

- [x] 2.1 `parseMoney(input, at)` в `src/core/units.ts`: число долларов и
      строка с необязательным `$`, результат — целые микродоллары; отказ на
      отрицательном, нечисловом, пустом и на любом обозначении валюты
- [x] 2.2 `formatMoney(microUsd)` там же: два знака для величин от доллара,
      четыре для меньших, чтобы дешёвый шаг не печатался как `$0.00`
- [x] 2.3 Тесты разбора и печати, включая отказы и мелкие величины

## 3. Объявление потолка

- [x] 3.1 `costMicroUsd?: number` в `Budget` (`src/core/pipeline/model.ts`) и
      разбор `cost` в `toBudget` (`src/core/pipeline/expand.ts`)
- [x] 3.2 `budget.cost` во всех местах объявления бюджета в
      `schema/pipeline.schema.json` и `schema/job.schema.json`
- [x] 3.3 `limits.cost` в конфигурации: умолчание `$50`
      (`src/core/config/defaults.ts`), разбор (`resolve.ts`), слияние
      (`merge.ts`), схема (`schema.ts`, `schema/config.schema.json`), вывод
      `stepcast config` (`src/cli/commands/config.ts`)
- [x] 3.4 Ошибка `stepcast lint` при `budget.cost` выше `limits.cost` на любом
      уровне объявления (`src/core/lint.ts`, рядом с проверкой `limits.tokens`)

## 4. Учёт цены

- [x] 4.1 `sumUsage` (`src/core/backend/types.ts`) переносит и складывает
      `reported_cost_usd`: цена вызова судьи перестаёт теряться, а цена
      попытки — затираться сложением с судьёй
- [x] 4.2 Денежные счётчики в `Counters` и `record` (`src/core/budget/
      accumulator.ts`): накопление в микродолларах по попытке, шагу, работе и
      прогону тем же разностным путём, что и токены
- [x] 4.3 Учёт попыток без сообщённой цены: их номера запоминаются, в денежные
      суммы они не входят, ноль вместо цены не подставляется
- [x] 4.4 `report()` пишет денежные величины (в долларах) в `total`, в работу,
      в шаг и в запись попытки, а `unreported` пополняет ценой, если хотя бы
      одна завершившаяся попытка её не сообщила
- [x] 4.5 Необязательные денежные поля в `UsageReportSchema` и
      `BudgetStateSchema` (`src/core/journal/schema.ts`); сводка и состояние
      прежней формы продолжают проходить схему

## 5. Применение потолка

- [x] 5.1 Измерение `cost` в `Exceeded` и в `check` (`src/core/budget/
      accumulator.ts`): проверка внутри области рядом с токенами, порядок
      областей — прежний, от ближней к дальней
- [x] 5.2 `describeExceeded` для измерения `cost`: причина называет область,
      потраченное и объявленное в долларах
- [x] 5.3 Денежное превышение всегда ведёт в остановку, независимо от
      `on_exceed`: ожидание сброса окна лимитов по нему не начинается
- [x] 5.4 Денежный расход и объявленный потолок в состоянии прогона
      (`src/core/run/runner.ts`, запись `status.json` по ходу прогона)
- [x] 5.5 Событие о несообщённой цене при объявленном денежном потолке —
      однократно за прогон: описание в `src/core/journal/schema.ts` и запись
      в прогоне
- [x] 5.6 Предупреждение по завершении прогона, у которого денежный потолок
      объявлен, а цена не сообщена ни одной попыткой: состояние и вывод
      команды прогона называют, что потолок не применялся; код возврата при
      этом не меняется

## 6. Показ

- [x] 6.1 Денежный столбец в `stepcast usage` на всех уровнях разбивки
      (`src/cli/commands/usage.ts`): прочерк вместо нуля, отдельная строка о
      числе попыток без цены и о неполноте суммы
- [x] 6.2 Денежный расход прогона и объявленный потолок в `stepcast status`
      (`src/cli/commands/status.ts`)
- [x] 6.3 Денежные величины в снимке витрины (`src/ui/snapshot.ts`,
      `src/ui/overview.ts`) и их показ в обзоре и в раскрытом прогоне
      (`src/ui/assets.ts`), с признаком «не сообщено» вместо нуля

## 7. Тесты

- [x] 7.1 Превышение денежного потолка шага, работы и пайплайна: шаг
      прерывается, область получает `budget_exceeded`, причина называет
      потраченное и объявленное
- [x] 7.2 Сосуществование потолков: денежный исчерпан раньше токенного —
      останавливает денежный, и наоборот
- [x] 7.3 `on_exceed: wait` с денежным превышением даёт остановку без ожидания
- [x] 7.4 Цена вызова судьи входит в цену попытки и в денежные потолки шага,
      работы и прогона
- [x] 7.5 Попытка без сообщённой цены: сумма её не включает, сводка называет
      цену несообщённой, отчёт печатает прочерк
- [x] 7.6 Денежный потолок при полностью несообщённой цене: событие,
      предупреждение по завершении, прогон доведён до конца
- [x] 7.7 Часть попыток без цены: потолок применяется по учтённой части, число
      неучтённых названо
- [x] 7.8 Остановка происходит после попытки, а не внутри неё: перерасход не
      больше цены одной попытки
- [x] 7.9 Сводка и состояние прежней формы читаются; денежные ячейки в них —
      прочерк
- [x] 7.10 `stepcast lint`: ошибка при `budget.cost` выше `limits.cost`;
      отрицательный и валютный `cost` отклоняются раскрытием
- [x] 7.11 Вывод `usage`, `status` и снимка витрины с денежными величинами

## 8. Документация

- [x] 8.1 `docs/pipeline-format.md`: раздел «Бюджет» — `cost` на трёх уровнях,
      единица и форма записи, граница проверки и допустимый перерасход на одну
      попытку, невлияние `on_exceed: wait`, поведение при несообщённой цене;
      утверждение «Денег в бюджете нет» заменяется описанием того, чем
      сообщённая цена является и чем не является (не счёт провайдера, у
      подписки — оценка «сколько стоило бы по API»)
- [x] 8.2 `docs/config.md`: `limits.cost` и его умолчание
- [x] 8.3 `docs/run-layout.md`: денежные поля `usage.json` и `status.json`,
      событие о несообщённой цене
- [x] 8.4 `docs/status.md`: денежный потолок в работающем; в ограничениях —
      проверка на границе попытки, зависимость от того, сообщает ли бэкенд
      цену, и отсутствие денег в сводках прогонов, записанных раньше

## 9. Проверка

- [x] 9.1 `npm run check` проходит
- [x] 9.2 `openspec validate "budget-cost-ceiling" --strict` проходит
