## 1. Зависимость и её проверка до перестройки

- [x] 1.1 Внести `cordis` в `dependencies` с точной версией и записать в
  `design.md` (Решение 1) то, что проверено на месте: пакет импортируется в ESM
  под Node 22, его собственные зависимости — чистый JS без платформенных
  бинарников, вес установки, поведение `npm run build:node` на наших файлах при
  `skipLibCheck`.
- [x] 1.2 Закрыть первый открытый вопрос `design.md`: какой формой в
  установленной версии дожидаются, что контекст перестал применять области, —
  область от `ctx.plugin()` или весь корень. Ответ записать в `design.md`, а не
  держать в коде без объяснения.
- [x] 1.3 Закрыть второй открытый вопрос: что именно реэкспортируется из cordis
  подпутём `stepcast/plugin` — только тип контекста или ещё и базовый класс
  сервиса. Решение проверяется образцом из задачи 2.6.

## 2. Падающие проверки

- [x] 2.1 Новый `test/plugin-kernel.test.ts`: свежее ядро поднимает контекст, в
  нём есть три служебных сервиса, встроенный бэкенд зарегистрирован, а имена
  девяти встроенных предикатов зарезервированы. Обязан падать: ядра как
  контекста сегодня нет вовсе.
- [x] 2.2 Тест обратимости: `register` возвращает disposer; вызов disposer
  снимает один вклад, не трогая соседей; снятие области плагина снимает все три
  вида его вкладов разом.
- [x] 2.3 Тест «загрузить, выгрузить, следов нет»: состав вкладов по видам,
  перечень загруженных плагинов, авторство имён и разрешимость сервисов после
  снятия совпадают с состоянием до загрузки; освобождённое имя достаётся
  следующему плагину; снятый плагин загружается заново.
- [x] 2.4 Тест занятых имён ядра: плагин, объявляющий сервис `backends`,
  `predicates` или `commands`, отказывает сообщением, называющим имя и его
  принадлежность ядру.
- [x] 2.5 Тест сервиса с новым именем: плагин регистрирует сервис, которого ядро
  не знает; сервис разрешается в контексте и исчезает вместе с областью плагина.
- [x] 2.6 Тест внедрения: плагин `b` объявляет зависимость от сервиса плагина
  `a`, получает **тот же объект**; снятие `a` снимает область `b` вместе с её
  вкладами; повторное появление сервиса исполняет тело `b` заново.
- [x] 2.7 Тест неудовлетворённой зависимости: плагин ждёт имя, которого не
  регистрирует никто из объявленных, — отказ называет плагин и имя; команда не
  доходит до диспетчеризации и возвращает код ошибки конфигурации.
- [x] 2.8 Тест отложенной регистрации: бэкенд, зарегистрированный после
  ожидания чужого сервиса, к моменту завершения сборки в реестре есть, и его
  умолчания участвуют во втором проходе разрешения конфигурации.
- [x] 2.9 Тест наполовину загруженного плагина: отказ на втором вкладе снимает
  область целиком — первого вклада в реестре нет, в перечне плагинов записи нет,
  имя свободно для другого плагина; состав полей отказа (`file`, `at`, `hint`)
  прежний.
- [x] 2.10 Тест двух форм объявления: декларативный объект и плагин контекста
  дают одинаково доступные вклады; экспорт, не опознанный ни одной формой,
  отказывает, называя обе.
- [x] 2.11 Тест окружения команды: команда плагина достаёт через контекст сервис
  своего плагина; команда `down` на обращении к контексту отказывает тем же
  способом, каким уже отказывает на `config` и `registry`.
- [x] 2.12 Тест в `test/ui-server.test.ts`: два проекта с одноимённым сервисом
  раскрываются оба и не спорят по конфликту имён; `close()` снимает поднятые
  сервером контексты, а контекст, полученный извне, остаётся действующим; после
  закрытия процесс `node --test` завершается сам.
- [x] 2.13 Убедиться, что `test/plugin-load.test.ts` и
  `test/plugin-surface.test.ts` в этой работе **не правятся**: они держат тексты
  отказов и границу поверхности, и их правка означала бы незамеченную смену
  поведения (`design.md`, Риск 6).

## 3. Ядро как контекст

- [x] 3.1 `createKernel()` в `src/core/plugins/` — корневой контекст, объявление
  трёх служебных сервисов и перечень занятых ядром имён одним модулем
  (Решение 3).
- [x] 3.2 Сервисы `backends`, `predicates`, `commands`: `register(имя, вклад)`
  как эффект вызывающей области, возвращающий disposer; `reserve(имя)` у
  предикатов для встроенных имён без вкладов (Решение 11); внутренняя карта,
  правимая только регистрацией и снятием.
- [x] 3.3 Конфликт имён на уровне сервиса: отказ с прежним текстом, включая
  различение «встроенный вклад» и «плагин <имя>»; признак «область самого ядра»
  несёт ядро, а не имя строки (Решение 5).
- [x] 3.4 `Registry` как вид над контекстом: три `ReadonlyMap`,
  `builtinPredicates`, `plugins` в порядке загрузки, `owners`, выведенный из
  областей. Интерфейс чтения не меняется; `availableNames` и `predicateNames`
  остаются на месте (Решение 4).
- [x] 3.5 `src/core/plugins/builtin.ts`: встроенные вклады вносятся тем же
  вызовом, что и плагинные; `builtinRegistry()` сохраняется как умолчание, но
  заводится один раз на вызов, а не на каждое обращение (Решение 13).
- [x] 3.6 Удалить приведение `target as unknown as MutableRegistry` и
  `addPlugin` вместе с параллельной картой владельцев — их работу выполняют
  области.

## 4. Две формы плагина и адаптер

- [x] 4.1 Распознавание формы в `src/core/plugins/load.ts`: функция или объект с
  `apply` — плагин контекста; всё прочее — прежняя проверка
  `StepcastPluginSchema`; неопознанное — отказ, называющий обе формы
  (Решение 7).
- [x] 4.2 Адаптер декларативного объекта в плагин контекста: один проход по
  трём видам вкладов, каждая регистрация — в области этого плагина.
- [x] 4.3 Снятие области при отказе посреди загрузки, до того как отказ уйдёт
  наружу; сохранить дописывание `file` и `at: 'plugins'` загрузчиком
  (Решение 10).
- [x] 4.4 Завершение сборки по успокоению контекста и отказ на незакрытом
  внедрении с именем плагина и недостающего сервиса (Решение 9); сбор слоя
  умолчаний плагинных бэкендов в `src/core/plugins/resolve.ts` — после этого
  момента.
- [x] 4.5 Убедиться, что `src/backends/codex/**` не правился ни строкой, и что
  правило границы `no-restricted-imports` для `src/backends/**` осталось
  прежним.

## 5. Поверхность плагина

- [x] 5.1 `src/plugin.ts`: тип `Context` с типизированными служебными сервисами,
  тип плагина контекста и реэкспорт из cordis по итогу задачи 1.3.
- [x] 5.2 `CommandEnv` получает `ctx`; `buildIndependentCommandEnv`
  (`src/cli/main.ts:380`) отказывает на нём тем же способом, что на `config` и
  `registry` (Решение 12).

## 6. Демон

- [x] 6.1 Кеш реестров по проекту (`createRegistryCache`,
  `src/ui/pipelines.ts:341`) становится кешем ядер: корень на проект, изоляция
  проектов друг от друга (Решение 6).
- [x] 6.2 Корневой контекст самого демона — для конфигурации, с которой подняли
  `stepcast up`; им пользуются `src/ui/settings.ts` и `src/ui/models.ts`.
- [x] 6.3 `close()` (`src/ui/server.ts:1106`) снимает поднятые сервером
  контексты рядом с `watcher.dispose()` и остановкой компилятора виджетов,
  правилом «останавливает тот, кто поднял».

## 7. Документация

- [x] 7.1 `docs/plugins.md`: раздел о модели — контекст, область, сервис,
  эффект; три служебных сервиса и перечень занятых ядром имён; обратимость
  регистрации и что именно снимается.
- [x] 7.2 `docs/plugins.md`: две формы плагина и правило выбора одной фразой —
  фиксированный вклад объектом, свой сервис или зависимость от чужого функцией;
  пример каждой формы.
- [x] 7.3 `docs/plugins.md`: граница единственного экземпляра cordis — почему
  плагину не следует объявлять свою зависимость от него и чем это не
  обеспечено механически (Решение 8).
- [x] 7.4 `docs/plugins.md`, раздел «Известные ограничения»: снять запись о
  необратимости ровно в той мере, в какой она снята, и оставить запись о том,
  что модуль Node импортируется один раз за жизнь процесса, назвав владельцем
  вопроса `user-plugins-from-files`. Добавить, что `Registry` перестал быть
  снимком (Риск 3).

## 8. Зелёная проверка

- [x] 8.1 Убедиться, что комментарии `src/core/plugins/registry.ts:10` и
  `src/core/plugins/builtin.ts:5`, описывающие прежнее устройство реестра,
  приведены в соответствие с контекстом, а доводы, которые остались верны
  (встроенное описано тем же контрактом; встроенные предикаты — имена без
  вкладов), сохранены, а не выброшены.
- [x] 8.2 `npm install --no-audit --no-fund && npm run check` — зелёный.
