## 1. Область видимости из одного источника

- [x] 1.1 Вынести построение области видимости из `conditionScope` в
      `src/core/run/scheduler.ts` так, чтобы её же получал исполнитель работы:
      исходы и выходы завершившихся работ плюс `scopeExtras`
- [x] 1.2 Добавить `run.workspace` в область видимости раскрытия. Не в
      `scopeExtras`: рабочая директория своя у каждой работы и известна только
      после её подготовки, поэтому значение собирается в `executeJob`

## 2. Раскрытие отложенных подстановок

- [x] 2.1 Написать `src/core/pipeline/late.ts`: обход определения работы с
      заменой строк через `interpolate` с областью видимости прогона; пустой
      набор `deferred`, чтобы нераскрытое имя давало ошибку, а не молчало
- [x] 2.2 Отказ при обращении к незавершённой работе: сообщение называет
      выражение и то, что работа ещё не завершилась
- [x] 2.3 Отказ при обращении к `output` упавшей работы: выход упавшей работы
      не публикуется, как это уже действует для `if:`
- [x] 2.4 Отказ при обращении к имени вне состава `run.*` с перечислением
      доступных имён
- [x] 2.5 Вызвать раскрытие в `src/core/run/runner.ts` перед исполнением первого
      шага работы; работа исполняется уже раскрытой

## 3. Журнал

- [x] 3.1 Писать раскрытое определение работы в её запись в директории прогона
- [x] 3.2 Проверить, что `pipeline.lock.yml` при этом не меняется: отложенные
      подстановки в нём остаются нераскрытыми

## 4. Тесты

- [x] 4.1 Раскрытие в пути контекста, в тексте промпта, в argv командного шага,
      в значении `env` и в пути схемы
- [x] 4.2 Отложенная подстановка, доехавшая через `with` и `${params.*}`
- [x] 4.3 Экранирование `$${` ведёт себя так же, как на этапе раскрытия
- [x] 4.4 Отказ по незавершённой работе, по выходу упавшей работы, по имени вне
      состава `run.*`
- [x] 4.5 Сквозной прогон из двух работ: вторая получает значение из выхода
      первой и доносит его до командного шага
- [x] 4.6 Работа с `until`: раскрытие происходит один раз и одинаково на всех
      итерациях

## 5. Документация

- [x] 5.1 `docs/pipeline-format.md`: состав `run.*` приводится к
      реализованному — `id`, `dir`, `workspace`; про величины уровня шага
      сказано, что они приходят переменными окружения
- [x] 5.2 `docs/pipeline-format.md`: сказано, когда раскрывается какое
      пространство — `inputs` и `params` при раскрытии, `jobs`, `run` и `env`
      перед исполнением работы
- [x] 5.3 `docs/run-layout.md`: раскрытое определение работы в записи работы;
      уточнение, что `pipeline.lock.yml` содержит нераскрытые отложенные

## 6. Проверка на петле

- [x] 6.1 `stepcast run --dry-run` на `.stepcast/pipelines/self-improve.yml`
- [x] 6.2 `stepcast context` на работах с отложенными путями по-прежнему не
      собирается — и это верно: вне прогона значений `jobs.*` не существует.
      Работы без таких путей (`review`) разрез показывают
