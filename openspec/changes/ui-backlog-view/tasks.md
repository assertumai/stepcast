## 1. Сборка вида очереди отдельным модулем

- [x] 1.1 Завести `src/ui/backlog.ts` — чистый серверный модуль (ни React, ни
      `window`), собирающий вид очередей по проектам готового обзора
      (Решения 2, 8). Модуль читает диск, поэтому браузеру он не отдаётся:
      ни в `server.fs.allow` файла `vite.config.ts`, ни в `include` файла
      `ui/tsconfig.json` он не попадает — в отличие от `routes.ts` и
      `runsView.ts`.
- [x] 1.2 Объявить в нём вид: раздел на проект (ключ, путь, пункты либо поля
      отказа `error`, `errorFile`, `errorAt` — теми же именами, что у
      `PipelineView`; подсказки в составе нет, ядро очереди её не даёт —
      Решение 6) и вид пункта: слаг, статус, заголовок, действующая группа,
      дорожка, `startedAt`, `reason` (Решение 3).
- [x] 1.3 Реализовать обход: проекты берутся из переданного обзора, проект без
      пути в указателе пропускается, файл ищется как `backlog.md` в корне
      проекта (Решения 1, 2).
- [x] 1.4 Разбирать файл через `readBacklogFile` и `parseBacklogFile` из
      `src/core/backlog` — те же функции, что зовёт `runList`
      (`src/cli/commands/backlog.ts:109-113`); действующую группу брать
      `effectiveGroup`. Публикуемую запись `BacklogRecordSchema` не трогать.
- [x] 1.5 Отсутствие файла (`ENOENT`) — не отказ, а отсутствие раздела; прочие
      беды чтения и отказ разбора — раздел с объяснением, обход соседних
      проектов продолжается (Решение 6).
- [x] 1.6 Порядок пунктов внутри раздела — порядок файла, без пересортировки;
      порядок разделов — тот же, в каком проекты идут в обзоре.

## 2. Наблюдатель: очередь рядом с обзором

- [x] 2.1 Включить файл очереди каждого проекта в отпечаток `fingerprint`
      (`src/ui/watcher.ts:77-102`) парой «mtime + размер», тем же приёмом,
      что и хранилище расхода; отсутствие файла — такое же состояние
      отпечатка, как и его наличие (Решение 4). Отпечаток разделить надвое —
      `runs` и `backlog`: слитый заставлял бы разбирать очереди всех проектов
      на всякую запись `status.json` идущего прогона.
- [x] 2.2 Собирать вид очереди в том же `poll()`, что и обзор, из того же
      значения обзора; хранить рядом и отдавать методом наблюдателя
      (`currentBacklog()`), подписчикам — вместе с обзором.
- [x] 2.3 Уточнить заголовочный комментарий модуля: наблюдатель следит не
      только за корнем прогонов, но и за файлом очереди в корне каждого
      проекта, названного указателем.
- [x] 2.4 Проверить, что разбор очереди не случается ни на такте, где
      отпечаток не изменился, ни на такте, где изменился только прогон
      (Решение 4, сценарии «Неизменный файл не перечитывается» и «Изменение
      прогона не перечитывает очередь»); проект же, впервые попавший в обзор,
      свою очередь получает тем же тактом.

## 3. Демон: маршрут и событие

- [x] 3.1 Завести `GET /api/backlog` в `src/ui/server.ts` — отдаёт значение
      наблюдателя, наравне с `/api/overview` (Решение 5).
- [x] 3.2 Научить `handleEvents` слать событие `backlog` рядом с `overview` —
      тем же `push()`, той же подпиской, без второго потока; повторять кадр
      только когда наблюдатель отдал другое значение очереди (Решение 5).
- [x] 3.3 Убедиться, что демон по-прежнему ничего не пишет: очередь только
      читается (требование `ui-daemon` «Наблюдение не изменяет журнал»).

## 4. Маршрут и меню витрины

- [x] 4.1 Добавить в `src/ui/routes.ts` вариант `Route` `{ page: 'backlog' }`,
      ветку `parseRoute` для `/backlog` и запись `MENU` — «Бэклог», третьим
      пунктом, между «Пайплайнами» и «Расходом» (Решение 7).
- [x] 4.2 Проверить, что неизвестный хвост адреса (`/backlog/что-то`) ведёт на
      первый экран, как и всё неизвестное.

## 5. Экран очереди

- [x] 5.1 Описать договор клиента в `ui/src/api.ts` — типы вида очереди
      заново, сверив поля с `src/ui/backlog.ts`, и `fetchBacklog()` для
      первого кадра.
- [x] 5.2 Принять событие `backlog` в `ui/src/live.ts` и отдать его состоянием
      наравне с обзором; второго `EventSource` на вкладке не заводить.
- [x] 5.3 Написать `ui/src/pages/Backlog.tsx`: раздел на проект (путь корня
      заголовком), таблица пунктов — слаг, статус, заголовок, группа,
      дорожка; момент взятия у `in_progress`, причина у `failed`; `why` и
      `done_when` раскрываются по требованию, а не занимают строку списка
      (Решение 3).
- [x] 5.4 Показать раздел проекта, чья очередь не разбирается, объяснением:
      текст, файл и место — отдельными полями, не склеенной строкой
      (Решение 6). Раздел проекта, чей файл очереди есть, но пуст, назвать
      пустым прямо, а не показывать таблицей из одних заголовков колонок.
- [x] 5.5 Пустой экран назвать пустым честно: «очередей не найдено» — только
      когда ответ демона получен, и с указанием, где демон ищет файл; до
      ответа — «Загрузка…», тем же приёмом, что на экране пайплайнов.
- [x] 5.6 Подключить экран в `ui/src/App.tsx` веткой маршрута.
- [x] 5.7 Стили — в `ui/src/styles.css`, повторно используя классы бейджей
      статуса и таблиц; новых сущностей оформления не заводить. Статусы
      пункта (`in_progress`, `done`) приписать к тем же трём цветам, которыми
      покрашены статусы прогона, — иначе бейдж пункта не окрашен вовсе и
      взятый в работу неотличим от завершённого.

## 6. Тесты

- [x] 6.1 `test/ui-backlog.test.ts`: два проекта с очередями; проект без
      `backlog.md`; проект без пути в `projects.json`; проект без прогонов;
      пункт без `group` и без `track`; `in_progress` с `started_at` и
      `failed` с `reason`; порядок пунктов равен порядку файла.
- [x] 6.2 `test/ui-backlog.test.ts`: неразбираемая очередь даёт раздел с
      текстом, файлом и местом и не отменяет показа очереди соседнего
      проекта; файл без единого пункта даёт раздел с пустым списком и без
      отказа; пункт с неизвестным полем показан.
- [x] 6.3 `test/ui-routes.test.ts`: `/backlog` разбирается в свой экран, пункт
      меню на месте и его `href` совпадает с разбираемым адресом,
      `/backlog/хвост` ведёт на первый экран.
- [x] 6.4 `test/ui-server.test.ts`: `GET /api/backlog` отдаёт очереди
      проектов; поток `GET /api/events` шлёт событие `backlog` первым кадром
      и после правки файла очереди, но не повторяет его, когда изменился
      только прогон; страница по адресу `/backlog` отдаётся кодом 200.
- [x] 6.5 `test/ui-watcher.test.ts`: правка файла очереди будит подписчика;
      такт без изменений — нет; такт, где изменился только прогон, оставляет
      прежнее значение очереди тем же объектом; проект, впервые появившийся в
      обзоре, свою очередь получает.

## 7. Ручная проверка сценариев спеки

- [x] 7.1 Собрать витрину, поднять демон на настоящем корне прогонов с двумя
      проектами и пройти: пункт меню, разделы обоих проектов, порядок
      пунктов, проект без очереди.
- [x] 7.2 Пометить пункт `in_progress`, затем `done` при открытой вкладке и
      убедиться, что статус меняется без перезагрузки.
- [x] 7.3 Сломать файл очереди одного проекта и убедиться, что его раздел
      объясняет отказ, а соседний проект показан как обычно.

## 8. Сведение и проверка

- [x] 8.1 `docs/status.md` не заводить и не ссылаться на него: документ
      упразднён изменением `drop-status-doc`, его возврат стережёт
      `test/no-status-doc.test.ts`.
- [x] 8.2 Сверить новый экран с требованием «Экраны витрины доступны из меню»
      изменения `ui-actions`
      (`openspec/changes/ui-actions/specs/ui-dashboard/spec.md:3-36`): оно не
      перечисляет пунктов поимённо, и новый экран обязан лишь удовлетворять
      ему — адрес переживает перезагрузку, пункт меню и разбор адреса не
      расходятся, хвост за именем экрана ведёт на первый экран. Дописывать в
      каталог того изменения ничего не нужно.
- [x] 8.3 `openspec validate "ui-backlog-view" --strict` проходит.
- [x] 8.4 `npm run check` зелёная.
