## 1. Печать схемы документов от реестра

- [x] 1.1 Падающий тест `test/published-schema.test.ts`: печать документов при пустом перечне плагинных предикатов даёт ровно то, что лежит в `schema/pipeline.schema.json` и `schema/job.schema.json` (сравнение разобранного JSON и заодно строки — побайтово, включая завершающий перевод строки). Тест обязан падать: модуля ещё нет.

- [x] 1.2 Падающий тест там же: печать с реестром, у которого плагин `example` вносит предикат `text_has` со схемой значения `{ "type": "string", "minLength": 1 }` (реестр собирается как в `test/plugin-predicate.test.ts:24-50` — `builtinRegistry()` плюс `addPlugin`), даёт схему, которая тем же `Ajv2020` (`ajv/dist/2020.js`, как в `src/core/expect/evaluate.ts:5-6`) **принимает** документ с `expect: [{ text_has: "ok" }]` в шаге и с `until.check: [{ text_has: "ok" }]` в работе, **отклоняет** `expect: [{ text_has: 42 }]` и по-прежнему отклоняет `expect: [{ exit_cod: 0 }]`.

- [x] 1.3 Падающий тест там же: схема значения с `$ref` на любом уровне и схема, которую `ajv` не компилирует, — ключ предиката признаётся, значение остаётся неограниченным (`{}`), а функция возвращает заметку с именем предиката, именем плагина и причиной. Отдельным случаем — `$id` и `$schema` внутри схемы значения.

- [x] 1.4 `src/core/pipeline/published-schema.ts`: функция, печатающая JSON Schema документа (`pipeline` и `job`) по перечню плагинных предикатов с их вкладами и владельцами. Внутри — `buildDocumentSchemas(имена)` (`src/core/pipeline/schema.ts:99`) и `z.toJSONSchema(…, { io: 'input', unrepresentable: 'any' })` теми же параметрами, что в `scripts/generate-schema.ts`, затем подстановка схем значений по метке (`design.md`, решение 2): узел с единственным свойством, чьё имя — плагинный предикат, единственным обязательным тем же именем и пустой схемой значения. Пустой перечень — ни одного обхода и ни одного отличия.

- [x] 1.5 Там же: пригодность схемы значения к вложению — рекурсивная проверка на `$ref`, `$id`, `$schema` и компиляция `Ajv2020`; непригодная даёт заметку и оставляет `{}` (`design.md`, решение 3). Функция возвращает документы вместе с заметками — печатать их дело команды, а не ядра.

- [x] 1.6 Там же: при непустом перечне в корень документа кладётся `description` с перечнем известных схеме плагинных предикатов и внёсших их плагинов (владелец берётся из `contributionOwner(registry, 'predicates', имя)`, `src/core/plugins/registry.ts:156-162`). При пустом перечне поле не появляется — иначе пункт 1.1 недостижим.

- [x] 1.7 `scripts/schema-targets.ts` и `scripts/generate-schema.ts`: цели `pipeline` и `job` печатаются той же функцией с пустым перечнем; `config`, `backlog` и `backlog-slots` от плагинов не зависят и печатаются как раньше. Перегенерировать `npm run schema` и убедиться, что **ни один** файл в `schema/` не изменился, а `test/schema-generated.test.ts` остался зелёным без правок.

## 2. Команда `stepcast schema`

- [x] 2.1 Падающий тест `test/cli-schema.test.ts`: во временном проекте (`makeProject`, `test/helpers.ts`) без плагинов команда пишет `.stepcast/schema/pipeline.schema.json` и `.stepcast/schema/job.schema.json`, побайтово равные поставляемым пакетом, и печатает оба пути.

- [x] 2.2 Падающий тест там же: проект, объявивший в `.stepcast/config.yml` плагин-файл с предикатом, получает схему, признающую его ключ; `--out schemas` кладёт файлы в названный каталог; плагин, который не импортируется, даёт код ошибки конфигурации и **ни одного** записанного файла (проверить, что каталога вывода не появилось).

- [x] 2.3 `src/cli/commands/schema.ts`: команда собирает перечень плагинных предикатов из `env.registry` (`[...registry.predicates.keys()]` — встроенные предикаты вкладов не имеют, `src/core/plugins/builtin.ts:22-36`), зовёт функцию из 1.4, создаёт каталог и пишет файлы с завершающим переводом строки — как `generate-schema.ts`. Заметки о невложимых схемах значений печатаются после путей. Проект без плагинных предикатов получает строку о том, что схема совпадает с поставляемой пакетом.

- [x] 2.4 `src/cli/main.ts`: описание команды в `COMMANDS` (ключ `--out`, описание для общей справки) и вклад в `BUILTIN_COMMANDS` рядом с `lint` и `config`. Загрузка плагинов и отказ при её неудаче достаются общим ходом — отдельного кода это не требует, убедиться проверкой из 2.2.

## 3. Устаревшую схему проекта называет линт

- [x] 3.1 Падающий тест в `test/lint.test.ts`: `.stepcast/schema/pipeline.schema.json`, записанный без плагина, в проекте, где плагин с предикатом уже объявлен, даёт **предупреждение** с путём файла и подсказкой перегенерировать, а `stepcast lint` завершается кодом `0`. Свежий файл предупреждения не даёт; отсутствующего файла линт не замечает вовсе.

- [x] 3.2 `src/core/lint.ts`: диагностика читает `.stepcast/schema/*.json` от `cwd` (`LintOptions.cwd`, строки 52-58) и сравнивает с тем, что даёт функция из 1.4 по `LintOptions.registry`. Файла нет — проверки нет. Нечитаемый или неразбираемый файл — то же предупреждение с причиной, а не крушение линта: подсказка редактора прогону не начальник.

- [x] 3.3 Убедиться, что проверка не удорожает линт проектов, которые командой не пользовались: печать схем происходит только после того, как файл найден.

## 4. Документация

- [x] 4.1 `docs/plugins.md`: снять из «Известных ограничений» пункт о статичной `schema/pipeline.schema.json` и завести раздел «Схема для редактора» — что печатает `stepcast schema`, куда, почему файл стоит коммитить (изменение плагинного состава видно в ревью там же, где правка `plugins`) и что о его устаревании говорит `stepcast lint`.

- [x] 4.2 `docs/plugins.md`, раздел «Предикат»: к описанию `schema` добавить требование самодостаточности — `$ref`, `$id` и `$schema` в схеме значения вкладывать в документ нельзя, и предикат с такой схемой попадёт в схему проекта ключом без проверки значения.

- [x] 4.3 `docs/pipeline-format.md`: как подключить схему к редактору — модельная строка `# yaml-language-server: $schema=./.stepcast/schema/pipeline.schema.json` в документе и настройка `yaml.schemas` для `stepcast.yml` и `.stepcast/jobs/*.yml`; чем схема проекта отличается от поставляемой пакетом и когда достаточно поставляемой.

- [x] 4.4 `README.md`: строка о схеме проекта рядом с абзацем «Ошибки ловятся до запуска» (`README.md:129`).

## 5. Проверка

- [x] 5.1 Прогнать `npm run check` целиком и убедиться, что зелено. Отдельно убедиться, что `git status` не показывает изменений в `schema/**`: файлы пакета этим изменением не двигаются.

- [x] 5.2 Живая проверка руками в подготовленном временном проекте: объявить плагин с предикатом, вызвать `stepcast schema`, открыть документ в редакторе с подключённой схемой и убедиться, что плагинный ключ не подчёркнут, а опечатка в соседнем ключе — подчёркнута. Если редактор в окружении реализации недоступен, сказать об этом прямо и подтвердить обе стороны валидацией `ajv` по записанному файлу, не выдавая её за проверку редактором.

  Редактор в этом окружении недоступен. Проверено вместо него: во временном
  проекте (`jobs/implement-a/scratch/live-schema-check`, вне рабочего дерева)
  объявлен плагин `stepcast-plugin-http-checks` с предикатом `http_ok`; вызов
  `stepcast schema` записал `.stepcast/schema/{pipeline,job}.schema.json` с
  `description`, называющим `http_ok (stepcast-plugin-http-checks)`. Тем же
  `Ajv2020`, каким проверяется значение при разборе, по записанному
  `pipeline.schema.json` подтверждены обе стороны: документ с
  `expect: [{ http_ok: "https://example.com" }]` принят, документ с
  `{ exit_cod: 0 }` — отклонён (`required: uses`/опечатка ключа). Это
  проверка валидатором, а не редактором.

## 6. По итогам ревью

- [x] 6.1 `src/core/pipeline/published-schema.ts`: перечень ключей, лишающих схему значения самодостаточности, дополнен целями и динамическими ссылками (`$anchor`, `$dynamicRef`, `$dynamicAnchor`, `$recursiveRef`, `$recursiveAnchor`) — автономно они компилируются, а в документе разрешались бы от чужого корня. Собранный документ проверяется компиляцией целиком, виновник ищется поимённо; схема, ломающая сборку, даёт заметку и оставляет ключ признанным. Тесты — на каждый новый ключ и на набор из нескольких непригодных схем сразу.

- [x] 6.2 Там же: сборка перечня плагинных предикатов из реестра вынесена в `pluginPredicateEntries(registry)` и зовётся и командой, и линтом — две копии разошлись бы, и сверка со «свежим» выводом стала бы ложной.

- [x] 6.3 `test/published-schema.test.ts`: вложение проверяется во всех трёх точках каждого документа (`expect` командного шага, `expect` агентского шага, `until.check`) и в обоих документах — работы и пайплайна.

- [x] 6.4 `test/cli-schema.test.ts`: покрыт сценарий «Плагин без предикатов» (загруженный плагин вносит только бэкенд — файлы побайтово равны поставляемым), а тест `--out` смотрит в вывод команды и проверяет, что названы именно новые пути.

- [x] 6.5 `test/lint.test.ts`: покрыта ветвь нечитаемого и неразбираемого файла схемы проекта — предупреждение с путём, а не отказ линта.
