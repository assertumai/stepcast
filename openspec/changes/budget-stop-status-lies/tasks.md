## 1. Честный исход шага при исчерпанном потолке

- [x] 1.1 В `runJobSteps` (`src/core/run/runner.ts`) превратить проверку `context.usage.check(budgetScopes())` перед шагом в ранний выход: шаг не исполняется, получает статус `budget_exceeded` с причиной `describeExceeded` и прекращает работу — запись шага, событие `step.finished` и возврат исхода работы остаются теми же, что у любого неуспешного шага
- [x] 1.2 Убрать затирание исхода: `status`/`reason` берутся из `outcome`, ветка `exceeded !== undefined ? 'budget_exceeded' : outcome.status` уходит
- [x] 1.3 Перенести назначение `budget_exceeded` в `runCommandStep` и `runAgentStep`: попытка, оборванная применением потолка (`abort.trigger`), отдаёт этот статус сама; попытка, дошедшая до собственного конца, отдаёт свой исход
- [x] 1.4 Проверить, нужна ли `UsageAccumulator.crossedBy` после 1.3 хоть одному вызывающему; если нет — удалить метод вместе с ветвью и её тестами

## 2. Защёлка перейдённого потолка

- [x] 2.1 Завести в контексте прогона запись первого превышения: область, измерение, израсходованное, потолок, момент, работа и шаг, на которых оно обнаружено; заполняется из того же `Exceeded`, что уходит в событие `budget.exceeded`
- [x] 2.2 Добавить поле в схему состояния прогона (`src/core/journal/schema.ts`) и писать его в `writeStatus` (`src/core/run/runner.ts`)
- [x] 2.3 Повышать исход прогона до `budget_exceeded` при заполненной защёлке поверх вычисленного по работам, сохраняя старшинство отмены; проверить код возврата 3
- [x] 2.4 Убедиться, что подсказка `resume` в состоянии (`blocked_by`) указывает на работу, которая действительно не доведена, а не на успешно отработавшую завершающую

## 3. Ключ обвязки `budget_exempt`

- [x] 3.1 Добавить `budget_exempt` в `WiringShape` (`src/core/pipeline/schema.ts`) и поле в `Job` (`src/core/pipeline/model.ts`)
- [x] 3.2 Перенести ключ в модель при раскрытии (`src/core/pipeline/expand.ts`) рядом с `lane` и `session_group`; убедиться, что внутри файла работы ключ по-прежнему отклоняется как обвязка
- [x] 3.3 Исключить область `run` из `budgetScopes()` у шагов освобождённой работы; проверить, что области работы и шага действуют по-прежнему, а расход по-прежнему копится в счётчиках прогона
- [x] 3.4 Проверить путь работы с циклом `until`: проверка бюджета работы между итерациями у освобождённой работы обязана считать только её собственные потолки

## 4. Правила линта

- [x] 4.1 Ошибка: `budget_exempt` у работы с `on: success`
- [x] 4.2 Ошибка: освобождённая работа с агентскими шагами без собственного `budget`
- [x] 4.3 Предупреждение: работа с `on: always`/`on: failure` в пайплайне с объявленным бюджетом без `budget_exempt`
- [x] 4.4 Проверить, что предупреждение 4.3 не влияет на код возврата

## 5. Запись исхода дорожки и отказ от повторного наложения

- [x] 5.1 Завести чтение и запись `merge-<дорожка>.json` рядом с `src/core/lanes/item.ts`: вид исхода, слаг, причина, затронутые репозитории, коммиты, момент
- [x] 5.2 Писать запись на каждую дорожку перечня в `mergeLanes` (`src/core/lanes/merge.ts`), включая несведённые и недостигнутые
- [x] 5.3 Отказывать в наложении дорожки с исходом `merged` в `applyRun` (`src/core/run/apply.ts`), называя коммиты сведения
- [x] 5.4 Добавить ключ `--force` команде `stepcast apply` (`src/cli/commands/apply.ts`) и описать его в справке команды
- [x] 5.5 Дать `mergeLanes` исход `already_merged`: дорожка пропускается, обход продолжается, код возврата команды не меняется

## 6. Коммит правки очереди в `backlog settle`

- [x] 6.1 Коммитить адресно файл очереди в репозитории, где он лежит (`nestedRepoOf`, `src/core/lanes/tree.ts`), сообщением `backlog: исходы дорожек прогона <короткий id>`
- [x] 6.2 Не делать коммита, когда правок нет, файл не отслеживается или лежит вне репозитория, и называть это в выводе
- [x] 6.3 Убедиться, что коммит не подметает прочие правки рабочего дерева

## 7. Видимость исхода сведения

- [x] 7.1 Печатать в `stepcast status` (`src/cli/commands/status.ts`) строку на каждую известную прогону дорожку: исход сведения, слаг, коммиты
- [x] 7.2 Дорожка без записи называется дорожкой, сведение которой не выполнялось

## 8. Пайплайны и документация

- [x] 8.1 Объявить `budget_exempt: true` у `merge` и `finalize` в `.stepcast/pipelines/self-improve.yml` и `.stepcast/pipelines/self-improve-memory.yml`, поправив комментарии, объясняющие исполнение после остановки
- [x] 8.2 Прогнать `stepcast lint` по обоим пайплайнам и по `examples/**` — снять новые предупреждения там, где они справедливы
- [x] 8.3 `docs/pipeline-format.md`: что делает исчерпанный потолок и чего он не делает; ключ обвязки `budget_exempt` и его ограничения
- [x] 8.4 `docs/run-layout.md`: файл `merge-<дорожка>.json`; `docs/backlog.md`: коммит `settle`

## 9. Тесты

- [x] 9.1 Пайплайн с исчерпанным потолком и освобождённой работой `on: always`: её шаги исполняются и отчитываются успехом — тест падает, если успешный шаг снова метят `budget_exceeded`
- [x] 9.2 Тот же пайплайн: исход прогона остаётся `budget_exceeded`, код возврата 3
- [x] 9.3 Шаг после исчерпанного потолка не запускается (процесс не стартовал), статус `budget_exceeded`
- [x] 9.4 Попытка, оборванная потолком на середине, даёт шагу `budget_exceeded`
- [x] 9.5 Линт: ошибка на `budget_exempt` при `on: success`; ошибка на освобождённую агентскую работу без бюджета; предупреждение на неосвобождённую `on: always`
- [x] 9.6 Повторное `apply --lane` сведённой дорожки отказывает, с `--force` накладывает; повторный `merge-lanes` даёт `already_merged` и сводит остальные
- [x] 9.7 `backlog settle` оставляет коммит ровно с файлом очереди; при отсутствии правок коммита нет
- [x] 9.8 `npm run check` зелёный

## 10. Разбор находок ревью

- [x] 10.1 Защёлка ставится только на превышение, остановившее дело: `Exceeded` несёт уровень области (`scopeKind`), в защёлку идут превышение, остановившее шаг, и любое превышение потолка прогона; перейдённый потолок шага или работы, никого не остановившего, исхода прогона не меняет
- [x] 10.2 Тесты на это: доигранный прогон с перейдённым потолком шага остаётся `success` с кодом 0 и без записи в состоянии; отменённый прогон с заполненной защёлкой остаётся `canceled` с кодом 130
- [x] 10.3 Правило линта о собственном бюджете освобождённой работы считает зовущим модель и командный шаг с судьёй в `expect`; тест на такую работу
- [x] 10.4 `rate_limit_pct` прогона остаётся действовать на освобождённую работу (`runScopeOf`); тест: освобождённая работа ждёт сброса окна и переисполняет шаг
- [x] 10.5 `readLaneMerge` проверяет состав записи и отказывает `StepcastError` на незнакомом или отсутствующем исходе вместо молчаливого «не сведена»; три теста
- [x] 10.6 Расход освобождённой работы и её собственный потолок покрыты тестами: траты целиком в отчёте и в блоке `budget`, собственный потолок обрывает попытку
- [x] 10.7 `budget.exceeded` описан в `docs/run-layout.md`, печатается `stepcast status` строкой «потолок перейдён» и проверен тестами состояния и команды
- [x] 10.8 `mergedLanes` вызывается из `stepcast status`: перечень дорожек — известные прогону вместе с теми, на которые есть запись исхода; тест на дорожку с записью без работ в состоянии
- [x] 10.9 Итоговая строка `merge-lanes` не числит уже сведённые дорожки несведёнными; тест на повторный обход
