## 1. Манифест и обход каталогов

- [x] 1.1 Завести модель манифеста `plugin.json` (`src/core/plugins/manifest.ts`): `version`, `description`, `server`, `browser`; поля имени нет; хотя бы одна половина обязательна (Решение 6)
- [x] 1.2 Чтение манифеста: разбор, отказ с файлом и местом, разрешение путей половин от каталога плагина и проверка реальным путём на выход за его пределы
- [x] 1.3 Добавить цель `schema/plugin-manifest.schema.json` в `scripts/schema-targets.ts` и сгенерировать схему (`npm run schema`)
- [x] 1.4 Обход каталога плагинов слоя (`src/core/plugins/discover.ts`): каталоги верхнего уровня, отсортированные; каталог без манифеста — операция строки, которой предстоит отказать; вложенность глубже уровня не считается
- [x] 1.5 Каталог, названный идентификатором встроенной строки (Решение 5), даёт строку с заведомым отказом — с текстом, называющим строку и патч как способ заменить её намеренно; встроенная строка остаётся в дереве, обход и сборка дерева не прекращаются
- [x] 1.6 Тесты обхода и манифеста: `test/plugin-manifest.test.ts`, `test/plugin-discovery.test.ts`

## 2. Строка каталожного плагина в дереве

- [x] 2.1 `TreeRowSource` получает вид «каталог» со слоем (`project` | `home`); `sourceEqual`/`treeEqual` учитывают его (`src/core/plugins/tree.ts`, `src/ui/pipelines.ts`)
- [x] 2.2 `resolveConfig` складывает операции слоя в порядке «обход, ключ `plugins`, патч» для домашнего и проектного слоёв (Решение 3)
- [x] 2.3 Опция разрешения «корень проекта для каталогов плагинов» — для демона, чей `projectPath` равен `null` (Решение 13)
- [x] 2.4 Загрузчик опознаёт каталожную строку по пути (Решение 2): каталог с манифестом — каталожный плагин, файл — прежний модуль; каталог без манифеста — названный отказ
- [x] 2.5 Применение каталожной строки: импорт серверной половины, прежнее опознание формы и контракт; сверка имени плагина с именем каталога (Решение 4)
- [ ] 2.6 Регистрация браузерной половины в сервисе состава плагинов демона — эффектом области строки, только если сервис есть (в дереве команд CLI его нет)
- [x] 2.7 Тесты дерева: строка от обхода, патч своего слоя над найденной строкой, проектный слой над домашним, строка, написанная руками с тем же каталогом

## 3. Мягкий отказ и видимость состояния

- [x] 3.1 `loadPlugins` возвращает итоги строк (`RowOutcome`) наравне с реестром; отказ каталожной строки не бросается — ни при применении, ни после него (незакрытое внедрение снимает область строки и оставляет ей состояние отказа), отказ явной — бросается, как прежде (Решение 10)
- [x] 3.2 Точки входа CLI и витрины передают итоги дальше: `stepcast plugins --dump` печатает состояние каталожных строк, `stepcast config` не врёт о составе, `DaemonKernel` несёт итоги своего состава
- [x] 3.3 Строка каталога в печати: слой — каталог со своим слоем, модуль — каталог, состояние — действует/отключена/отказ с причиной
- [x] 3.4 Тесты: `test/plugins-load.test.ts` — сломанный каталожный плагин среди исправных; команда, зависящая от конфигурации, исполняется; явная строка по-прежнему строгая

## 4. Отпечаток и перезагрузка одной строки

- [ ] 4.1 Отпечаток каталога плагина (`src/ui/plugins.ts`): рекурсивный обход с пропуском `node_modules`, `.git` и точечных каталогов; свёртка в строку версии (Решение 9)
- [ ] 4.2 Сверка отпечатков в единой точке ядра демона (`src/ui/kernel.ts`): разошедшаяся строка снимается своим disposer'ом и применяется заново с отпечатком в URL импорта (Решения 11, 12)
- [ ] 4.3 Сериализация перезагрузок на ядро; отпечаток перечитывается после применения; отказ disposer'а — строка в журнале демона, а не падение процесса
- [ ] 4.4 Отказ повторного применения оставляет строку в состоянии отказа с причиной; прежняя редакция не возвращается
- [ ] 4.5 Тесты: правка серверной половины перезагружает только её строку (соседи не пересоздавались, ядро не пересобиралось), удаление каталога снимает вклады, сломавшая правка даёт отказ с причиной

## 5. Сборка и отдача браузерной половины

- [ ] 5.1 Компилятор витрины учится собирать (`bundle: true`, `write: false`, `external` — три имени React) рядом с прежним преобразованием одного файла (`src/ui/widgets.ts`, Решение 8)
- [ ] 5.2 Кеш сборки по отпечатку каталога, тем же приёмом вытеснения, что и у виджетов
- [ ] 5.3 Маршрут `GET /plugins/<id>.js?v=<версия>` в диспетчере (`src/ui/server.ts`): безопасный сегмент, только плагин действующего состава, иначе 404 (Решение 14)
- [ ] 5.4 Ошибка сборки — 200, исполняемый модуль с разобранной ошибкой и признак в заголовке; имя экспорта общее с браузером
- [ ] 5.5 Тесты: отдача половины, локальный импорт внутри плагина, ошибка сборки модулем, 404 у отключённой строки и у небезопасного сегмента

## 6. Состав плагинов у демона и в потоке событий

- [ ] 6.1 Сборка состава плагинов по действующему ядру: `id`, слой, модуль, состояние, объявленные половины, версия, причина отказа
- [ ] 6.2 Часть отпечатка «плагины» у наблюдателя и событие `plugins` в `/api/events` по правилу «только при отличии от отправленного» (`src/ui/watcher.ts`, `src/ui/screens/rows.ts`)
- [ ] 6.3 `stepcast up` передаёт свой рабочий каталог серверу, сервер — единой точке ядра; ключ кеша ядра демона включает корень проекта (Решение 13)
- [ ] 6.4 Тесты: состав приходит первым обменом, неизменный состав не пересылается, правка меняет версию только своего плагина, настройки витрины не меняются от проектного конфига

## 7. Витрина: применение половин и экран «Плагины»

- [ ] 7.1 Плагин страницы `ui/src/plugins/userPlugins.tsx`: применение половин действующего состава областью на плагин, снятие и повторное применение по смене версии, снятие при исчезновении из состава (Решения 15, 16)
- [ ] 7.2 Отказ загрузки или применения половины показывается с причиной и не гасит страницу
- [ ] 7.3 `ui/src/plugins/screens.tsx`: половина, принесённая плагином с тем же `id`, вытесняет заглушку; заглушка остаётся там, где половины нет вовсе
- [ ] 7.4 Экран «Плагины»: `src/ui/screens/plugins/{declaration,server}.ts` (маршрут `GET /api/plugins`), строка в `src/ui/screens/rows.ts`, браузерная половина `ui/src/screens/plugins.tsx` — перечень со слоем, половинами, состоянием и причиной отказа
- [ ] 7.5 Тесты витрины: применение половины с диска, замена по версии, экран вместо заглушки, перечень на экране меняется без перезагрузки страницы

## 8. Образец и документация

- [ ] 8.1 `examples/plugins/<id>/` — плагин с обеими половинами: серверный сервис и вклад в слот витрины, с `plugin.json` и README-строкой о том, как его поставить
- [ ] 8.2 `docs/plugins.md`: каталоги и слои, формат манифеста, две половины, состояния строки, мягкий отказ и его следствие, перезагрузка по правке, снятое ограничение «изменённый код не подхватывается», названная утечка модулей
- [ ] 8.3 `docs/ui-plugins.md`: браузерная половина из файла, применение и замена по версии, дескриптор слота литералом и отложенная таблица общих модулей
- [ ] 8.4 `docs/widgets.md` и `docs/config.md`: чем виджет отличается от плагина; пример патча, отключающего каталожную строку
- [ ] 8.5 `npm run check` зелёный целиком
