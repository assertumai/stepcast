## 1. Правило отбора и порядка отдельным модулем

- [x] 1.1 Завести `src/ui/runsView.ts` — чистый модуль (ни React, ни `window`,
      ни диска) со структурными типами прогона обзора, тем же приёмом, что
      `src/ui/grouping.ts`: заголовок называет, почему правило живёт здесь, а
      не в `.tsx`.
- [x] 1.2 Объявить в нём состав фильтров (проект, пайплайн, статус) и
      сортируемые величины (начало, длительность, стоимость, токены) с
      направлением; умолчание — новейшими первыми (Решение 7).
- [x] 1.3 Реализовать сбор значений фильтров из прогонов обзора: пайплайн
      различается `pipelineFile` и подписывается именем, прогон без файла —
      именем, прогон без имени — значением «без имени» (Решение 5).
- [x] 1.4 Реализовать применение фильтров по «и» и порядок: неизвестная
      величина уходит в конец при обоих направлениях, равные величины идут
      новейшими первыми, длительность идущего считается от начала — тем же
      правилом, что `durationOf` в `ui/src/pages/Runs.tsx:20-24` (Решение 7).
- [x] 1.5 Перенести склонение «прогон / прогона / прогонов» из
      `ui/src/pages/Runs.tsx:60-67` в `src/ui/format.ts` и звать его из
      полосы расхождения версий (Решение 13).
- [x] 1.6 Добавить `src/ui/runsView.ts` в перечень разрешённых браузеру файлов
      `vite.config.ts` и в `include` файла `ui/tsconfig.json`.

## 2. Демон: отбор по явному списку адресов

- [x] 2.1 Добавить в `src/core/run/cleanup.ts` отбор кандидатов по явным
      адресам рядом с `selectCandidates` (`cleanup.ts:180-208`), тем же
      `candidateOf`; адрес без каталога в отбор не попадает.
- [x] 2.2 Научить `handleSelectRuns` (`src/ui/server.ts:356-410`) параметру
      `run=<адрес>`, повторяемому: разбирать адреса `parseRunAddress`,
      отвечать прежним составом (`runs[]`, `count`, `totalBytes`).
- [x] 2.3 Отклонять ошибкой клиента запрос, называющий и `run`, и любой из
      `trait` / `older-than` / `project` (Решение 9), и запрос с адресом
      неверной формы или уводящим за пределы корня.
- [x] 2.4 Применить к списку адресов тот же предел `MAX_RUN_ADDRESSES`
      (`src/ui/server.ts:51`), что и к групповому удалению, с тем же кодом
      отказа.
- [x] 2.5 Научить `selectRuns` в `ui/src/api.ts` списку адресов и назвать в
      комментарии взаимоисключение с признаками.

## 3. Экран: колонка проекта, фильтры, порядок

- [x] 3.1 Снять секции по проектам в `ui/src/pages/Runs.tsx:243-325` и собрать
      одну таблицу из прогонов всех проектов.
- [x] 3.2 Добавить колонку проекта: последний сегмент пути в ячейке, полный
      путь в `title`, ключ с пометкой «путь неизвестен» у проекта без записи в
      указателе (Решение 1).
- [x] 3.3 Добавить полосу фильтров (проект, пайплайн, статус) со значениями из
      `runsView`; выбранное значение остаётся выбранным, даже если ни один
      прогон ему больше не отвечает (Решение 6).
- [x] 3.4 Сделать заголовки сортируемых колонок нажимаемыми: первое нажатие —
      порядок по этой величине, повторное — обратный; текущая колонка и
      направление видны помимо цвета.
- [x] 3.5 Показывать «показано N из M» и кнопку сброса, пока фильтр отличается
      от умолчания; пустой результат называть словами (Решение 4).
- [x] 3.6 Оставить `VersionSkewBanner` над фильтрами и считающим по всему
      обзору, а не по показанному списку (Решение 12).

## 4. Экран: групповое удаление флажками

- [x] 4.1 Добавить колонку флажка; флажок получает только строка, у которой
      сегодня есть корзинка (`Runs.tsx:158-176`), — не идущая живым процессом
      и не потерявшая файлы (Решение 8).
- [x] 4.2 Хранить отметку множеством адресов; выбывший из обзора или из-под
      фильтров прогон выпадает из отметки (Решение 10).
- [x] 4.3 Общий флажок шапки отмечает и снимает ровно видимые под фильтрами
      строки (Решение 8).
- [x] 4.4 Полоса выбранного: число отмеченных, запрос объёма
      `GET /api/runs?run=…` по нажатию и подтверждение, называющее число
      прогонов и суммарный объём (Решение 9).
- [x] 4.5 Удаление одним `DELETE /api/runs` без `stats` (сводка расхода
      сохраняется, Решение 11); исход по каждому адресу показывается тем же
      составом, что на экране уборки, а отказ демона — его текстом.
- [x] 4.6 Оставить поштучную корзинку в строке доступной без отметки; полоса
      выбранного появляется только при непустой отметке.
- [x] 4.7 Дописать `ui/src/styles.css`: колонки флажка и проекта, полоса
      фильтров, полоса выбранного; колонки остаются друг под другом, страница
      не получает горизонтальной прокрутки.

## 5. Тесты

- [x] 5.1 Завести `test/ui-runs-view.test.ts`: фильтры объединяются по «и»;
      два файла пайплайна с одним именем не смешиваются; выбранное значение,
      исчезнувшее из обзора, даёт пустой список, а не другую линзу.
- [x] 5.2 Там же — порядок: по каждой из четырёх величин в обе стороны;
      прогон без величины уходит в конец при обоих направлениях; равные
      величины идут новейшими первыми; длительность идущего считается от
      начала.
- [x] 5.3 `test/ui-format.test.ts`: склонение «1 прогон / 2 прогона /
      5 прогонов» на переехавшем помощнике.
- [x] 5.4 `test/ui-server.test.ts`: отбор по трём адресам отвечает размерами и
      суммой; смешанный запрос с признаком отклонён; адрес неверной формы и
      адрес за пределами корня отклонены; адрес без каталога пропущен без
      ошибки; список сверх предела отклонён.
- [x] 5.5 `test/cleanup.test.ts`: отбор по явным адресам даёт тот же размер и
      возраст, что отбор по признаку на тех же прогонах.
- [x] 5.6 Убедиться, что обзор по-прежнему не считает размеров каталогов:
      существующая проверка `test/ui-overview.test.ts` / `ui-server` остаётся
      зелёной, новых обходов каталога при сборке обзора не появилось.

## 6. Сведение спек и проверка

- [x] 6.1 Записать обязанность архиватора: требование `ui-actions`
      «Первый экран показывает прогоны таблицей»
      (`openspec/changes/ui-actions/specs/ui-dashboard/spec.md:126-133`)
      перечисляет колонки без проекта; каталог того изменения вне границ
      правки этого, и колонка проекта дописывается при сведении обоих
      изменений в `openspec/specs`.
- [x] 6.2 Не заводить `docs/status.md` и не ссылаться на него: документ
      упразднён изменением `drop-status-doc`, его возврат стережёт
      `test/no-status-doc.test.ts`; поведение экрана описано дельта-спекой.
- [x] 6.3 `openspec validate "ui-runs-list-controls" --strict` проходит
      (`npx openspec validate` — «Change 'ui-runs-list-controls' is valid»).
- [x] 6.4 `npm run check` зелёная.

## 7. Правки по разбору реализации

- [x] 7.1 Сводка исходов группового удаления живёт состоянием экрана
      (`RemovalOutcomes`), а не полосы отметки: полоса уходит вместе со снятой
      отметкой, и сводка внутри неё не показалась бы никогда.
- [x] 7.2 Замер объёма снимается, как только изменился список отмеченного:
      подтверждение не переживает снятого флажка и не называет старого числа.
- [x] 7.3 Удаляется весь отмеченный список, а не измеренный демоном: прогон,
      исчезнувший между отметкой и замером, возвращается исходом «уже исчез»,
      а не выпадает молча.
- [x] 7.4 Строка исхода несёт бейдж судьбы статистики, как на экране уборки, и
      сводка называет словами, что записи расхода сохранены (Решение 11).
- [x] 7.5 Повторённый адрес в `GET /api/runs?run=…` считается одним прогоном —
      и в ответе, и при проверке предела; закреплено сценарием дельта-спеки и
      проверками `test/ui-server.test.ts`.
- [x] 7.6 Кнопка сброса и «показано N из M» смотрят и на порядок: неумолчальный
      порядок тоже возвращается к умолчанию одним действием.
- [x] 7.7 Сокращение пути проекта переехало на блок внутри ячейки: `max-width`
      на `td` таблица с авторазметкой не соблюдает, и длинная подпись
      растягивала колонку вместо многоточия.
