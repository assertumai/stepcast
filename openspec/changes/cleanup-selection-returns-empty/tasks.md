## 1. Воспроизведение: тесты, падающие до правки

- [x] 1.1 В `test/ui-server.test.ts` завести сценарий «прогон появился после
      старта демона и своей записи не дописал»: поднять демон на пустом корне,
      затем засеять прогон со сводкой расхода (`seedRun`) и запросить
      `GET /api/usage-records` по сроку, которому он отвечает. Сегодня ответ
      пуст — тест должен падать именно на этом.
- [x] 1.2 Там же — сценарий согласия с терминалом по сроку: на одном корне
      сначала запросить отбор витрины (`GET /api/runs?older-than=…&project=…`),
      затем выполнить `runGcCommand` с тем же сроком в том же проекте
      (`withHome` из `test/helpers.ts`, корень прогонов задаёт конфигурация
      каркаса) и сверить множества адресов (Решение 4). Прогоны засеваются
      после старта демона — как того требует спека. Красным до правки этот
      сценарий не бывает: отбор каталогов хранилища не касается, и обе
      реализации согласны и без догона; он сторожит согласие, а дефект
      воспроизводит сценарий 1.3.
- [x] 1.3 Там же — сценарий согласия по записям: `GET /api/usage-records` и
      `stepcast gc --stats` с тем же признаком и проектом, порядок тот же —
      сначала отбор витрины, потом команда. Прогоны засеваются после старта
      демона: только так витрина без догона отвечает пустым набором, и
      сценарий падает до правки.
- [x] 1.4 В `test/cleanup.test.ts` — сценарий «прогон с нечитаемым журналом»:
      каталог прогона, чьи `status.json` и `run.json` не проходят разбор, при
      отборе по признакам «оборванные»/«отказавшие» не отбирается и сегодня
      никак не назван; при отборе по сроку — отбирается.
- [x] 1.5 В `test/usage-store.test.ts` — сценарий «перенос уже был в этом
      процессе»: после `backfillUsageStore` появившийся на диске прогон в
      хранилище не оказывается.

## 2. Ядро: догон хранилища

- [x] 2.1 Завести в `src/core/journal/usageStore.ts` функцию
      `catchUpUsageStore(runsRoot, options?)` — перенос без множества
      `backfilled`, с необязательным `project`, сужающим обход до одного
      каталога проекта (Решение 1); заголовок называет, чем догон отличается от
      однократного переноса и почему обе функции нужны.
- [x] 2.2 Выразить `backfillUsageStore` через догон, сохранив за ней защёлку
      `backfilled` и её нынешний смысл — один перенос на путь хранилища за
      жизнь процесса.
- [x] 2.3 Убедиться, что догон дописывает только прогоны с подведённой сводкой
      (`appendIfSummarized`), не переписывает и не удаляет прежних строк и не
      трогает каталогов прогонов; повторный вызов при неизменном диске не
      дописывает ничего.
- [x] 2.4 Проверить тестами `test/usage-store.test.ts`: догон после переноса,
      сужение проектом, повторный вызов впустую, сохранность прежних строк.

## 3. Ядро: непроверенные прогоны в отборе

- [x] 3.1 Изменить `selectCandidates` (`src/core/run/cleanup.ts:180`) так,
      чтобы вместе с отобранными возвращалось число прогонов области, у
      которых `statusOf` не дал статуса (Решение 3); форма возврата — пара, а
      не массив, поэтому поправить всех вызывающих.
- [x] 3.2 Назвать в комментарии `statusOf` (`cleanup.ts:124`), что «статуса
      нет» — не то же, что «признак не подошёл», и что такой прогон остаётся
      кандидатом по сроку (`candidateOf`, `cleanup.ts:88-112`).
- [x] 3.3 Проверить тестами `test/cleanup.test.ts`: прогон с нечитаемым
      журналом не отобран по признаку, назван числом непроверенных, отобран по
      сроку; на читаемом корне число нулевое; число считается по области отбора
      (один проект против всех).

## 4. Демон: маршруты отбора

- [x] 4.1 Звать догон в `handleSelectUsageRecords` (`src/ui/server.ts:542`)
      перед отбором, сужая его тем же проектом, что назван в запросе.
- [x] 4.2 Звать догон в `handleSelectRuns` (`src/ui/server.ts:392`) перед
      `sendRunSelection` — ради `hasUsageRecord` и счётчика записей
      (Решение 2); отбор по явному списку адресов сужать проектом нечем, для
      него догон идёт по всему корню.
- [x] 4.3 Добавить в ответ `GET /api/runs` поле с числом непроверенных
      прогонов (`sendRunSelection`, `src/ui/server.ts:358`) и описать его рядом
      с прочими полями ответа.
- [x] 4.4 Оставить `backfillUsageStore` при старте демона
      (`createUiServer`, `src/ui/server.ts:775`) как есть: первый обзор
      по-прежнему собирается по перенесённому хранилищу.
- [x] 4.5 Проверить тестами `test/ui-server.test.ts` сценарии 1.1–1.3 и то,
      что отбор не удалил ни каталога, ни прежней записи хранилища.

## 5. Страница «Уборка»

- [x] 5.1 Дать `AgeAndProjectFields` (`ui/src/pages/Cleanup.tsx:94`)
      обязательный префикс раздела и строить `id` полей из него; поправить
      `htmlFor` обеих подписей (Решение 5).
- [x] 5.2 Передать префиксы из обоих вызовов (`Cleanup.tsx:223`,
      `Cleanup.tsx:418`) и убедиться, что в документе не остаётся двух
      элементов с одним `id`.
- [x] 5.3 Добавить в `RunSelection` (`ui/src/api.ts:444`) поле числа
      непроверенных прогонов, сверив состав с ответом демона.
- [x] 5.4 Показывать это число строкой рядом с итогом отбора в разделе файлов,
      когда оно не нулевое, — и когда отбор пуст, и когда не пуст; называть
      срок как способ отобрать такие прогоны. При нуле не показывать ничего.

## 6. Проверка

- [x] 6.1 Прогнать `npm run check` целиком и убедиться, что все сценарии
      изменения проходят, а прежние тесты уборки, витрины и хранилища не
      сломаны.
- [x] 6.2 Пройти глазами страницу «Уборка» на настоящем корне прогонов: отбор
      обоих разделов, подписи полей, строка о непроверенных прогонах — и
      сверить оба раздела с `stepcast gc --older-than` и
      `stepcast gc --stats --failed` того же проекта.

## 7. По итогам ревью

- [x] 7.1 Не считать непроверенным прогон, которого отбор назвал
      (`selectCandidates`): строка страницы иначе прибавляла бы к итогу уже
      посчитанное и советовала бы отобрать сроком отобранное сроком.
- [x] 7.2 Гасить отказ дозаписи в догоне (`catchUpRun` в `usageStore.ts`):
      маршруты витрины синхронны, и отказ записи на `GET` ронял бы демона
      вместо вырождения в «отбор без свежих записей».
- [x] 7.3 Сузить догон отбора по явным адресам самим списком
      (`catchUpUsageRecords`) и не звать догон там, где отбирать нечем — запрос
      без признака, срока и проекта.
- [x] 7.4 Засеять прогоны сценариев согласия после старта демона и покрыть
      тестом метку `hasUsageRecord` у прогона, появившегося после старта: без
      него догон в `handleSelectRuns` не сторожил ни один тест.
