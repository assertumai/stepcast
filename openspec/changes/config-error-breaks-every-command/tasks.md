## 1. Перечень и ранняя ветка точки входа

- [x] 1.1 Объявить в `src/cli/main.ts` рядом с `BUILTIN_COMMANDS` перечень
      независимых команд (`data`, `down`, `init`) с комментарием, называющим
      правило принадлежности: команда не читает ни одного значения действующей
      конфигурации и ни одного вклада реестра.
- [x] 1.2 Добавить в `run()` ветку до вызова `resolveWithPlugins`: если
      первый элемент `argv` назван перечнем, разобрать аргументы описанием
      этой одной команды и диспетчеризовать её вклад, не разрешая конфигурацию
      и не загружая плагины.
- [x] 1.3 Собрать для этой ветки `CommandEnv` с настоящим `cwd` и со
      свойствами `config` и `registry`, чтение которых выбрасывает
      `StepcastError` с именем команды и объяснением, что она объявлена
      независимой от конфигурации.
- [x] 1.4 Убедиться, что остальной путь `run()` не изменился: зависимые
      команды по-прежнему получают разрешённую конфигурацию и реестр, а отказ
      разрешения по-прежнему печатается `reportError` кодом `configError`.

## 2. Тесты

- [x] 2.1 Завести `test/cli-config-independence.test.ts` с помощником,
      заводящим проект с заведомо неразбираемым `.stepcast/config.yml`
      (и вариантом с неразбираемым глобальным конфигом через `withHome`).
- [x] 2.2 `stepcast data set` внутри `STEPCAST_JOB_DIR` при неразбираемом
      проектном конфиге: код `0`, данные работы записаны, диагностика
      конфигурации не напечатана.
- [x] 2.3 `stepcast down` при неразбираемом глобальном конфиге и
      `stepcast init` при недопустимом в проектном слое ключе: код `0`,
      ожидаемый вывод и созданные файлы.
- [x] 2.4 `stepcast data get` при валидной конфигурации, объявляющей
      несуществующий модуль плагина: команда исполняется — плагины не
      загружаются вовсе.
- [x] 2.5 `stepcast status`, `stepcast gc --older-than 30d` и `stepcast
      config` при неразбираемом конфиге: код `configError`, сообщение
      называет файл, `stdout` пуст, каталог прогонов не тронут.
- [x] 2.6 `stepcast --help` и несуществующая команда при неразбираемом
      конфиге: код `configError`, диагностика конфигурации, перечня команд в
      выводе нет.
- [x] 2.7 Команда плагина при неразбираемом конфиге: код `configError`
      (состав команд неизвестен).
- [x] 2.8 Сторожевой тест границы: пройти по всем именам `BUILTIN_COMMANDS`,
      не названным перечнем, вызвать каждое при неразбираемом конфиге и
      убедиться, что все отказывают кодом `configError` — новая команда не
      попадает в независимые молча.
- [x] 2.9 Команда перечня, обратившаяся к `env.config`, отказывает
      сообщением, называющим её имя и объявленную независимость.

## 3. Документация

- [x] 3.1 `docs/config.md`, раздел «Разрешение»: описать границу — какие
      команды исполняются при неразбираемой конфигурации, какие отказывают и
      почему подстановка умолчаний вместо непрочитанного объявления хуже
      отказа.
- [x] 3.2 `docs/plugins.md`, раздел «Объявление и разрешение»: назвать, что
      независимые команды плагинов не загружают, и что это сужает правило
      «отказ загрузки прекращает любую команду» до команд, зависящих от
      конфигурации.
- [x] 3.3 Свести тот же абсолют во всех местах, где он записан: раздел
      «Плагины» `docs/config.md` и раздел про витрину `docs/plugins.md`.
- [x] 3.4 Записать обязанность архиватора: требование
      `openspec/changes/plugin-contributions/specs/plugin-contributions/spec.md`
      («Команда до диспетчеризации в этом случае MUST NOT доходить, включая
      `stepcast config`») правится не этим изменением — его каталог вне границ
      правки, — а при сведении обоих изменений в `openspec/specs`: там оно
      обязано сузиться до команд, зависящих от конфигурации. Названо в
      `design.md`, раздел «Risks / Trade-offs».

## 4. Проверка

- [x] 4.1 `openspec validate "config-error-breaks-every-command" --strict`
      проходит.
- [x] 4.2 `npm run check` зелёная.
