## 1. Падающие проверки

- [x] 1.1 Новый `test/plugin-tree.test.ts`: свёртка трёх слоёв — встроенная
  строка, домашний патч, проектный патч — даёт дерево ожидаемого состава и
  порядка. Обязан падать: дерева сегодня нет вовсе.
- [x] 1.2 Тест неизменности: конфигурация без единого `plugins.patch.yml`
  сворачивается в тот же состав и тот же порядок, что загружались до изменения
  (Migration Plan).
- [x] 1.3 Тест замены по id: строка патча с известным `id` заменяет строку
  целиком, поле, которого патч не назвал, берёт умолчание формата, а место в
  порядке сохраняется (Решение 3).
- [x] 1.4 Тест вставки: `after`, `before` и умолчание «в конец»; отказы —
  ненайденный `id` позиции, оба поля сразу, позиция у замены, повтор `id` внутри
  файла. Каждый отказ называет файл и `id`.
- [x] 1.5 Тест отключения: `enabled: false` убирает вклады строки, не убирая её
  из дерева; освобождённое имя достаётся действующей строке (Решение 6).
- [x] 1.6 Тест замены встроенной строки: проектный патч ставит на место
  `backend-claude` модуль пользователя, объявляющий бэкенд `claude`; реестр
  содержит вклад пользователя, отказа по конфликту имён нет, встроенный адаптер
  не регистрируется. Тест-близнец: незаменённая встроенная строка оставляет
  владельцем «встроенный», и прежний текст отказа о конфликте сохраняется
  дословно (Решение 5).
- [x] 1.7 Тест сокращённой формы: строка ключа `plugins` заменяется и
  отключается патчем по `id`, равному спецификатору; повтор между слоями даёт
  одну строку и одну загрузку (Решение 4).
- [x] 1.8 Новый `test/plugins-command.test.ts`: `stepcast plugins --dump`
  печатает порядок, `id`, слой с путём файла, модуль и состояние каждой строки;
  отдельный случай — отказ загрузки: дерево напечатано, виновница названа
  причиной, код возврата — код ошибки конфигурации (Решение 8).
- [x] 1.9 `test/ui-server.test.ts`: правка патча на работающем демоне выгружает
  строку и загружает новую — вклады прежнего модуля недоступны, вклады нового
  действуют; правка, не меняющая ни одной строки, ядро не пересобирает
  (Решение 10).

## 2. Модель дерева и патч

- [x] 2.1 Новый `src/core/plugins/tree.ts`: тип строки (`id`, `use`, `enabled`,
  слой-источник, файл), тип операции слоя, свёртка слоёв в дерево с отказами из
  задачи 1.4.
- [x] 2.2 Схема документа патча в `src/core/config/schema.ts` (либо соседнем
  модуле): `version`, `kind: plugins-patch`, непустой список строк, строгий
  разбор ключей; текст отказа — тем же составом полей, что у прочих отказов
  конфигурации.
- [x] 2.3 Чтение патча слоя в `src/core/config/resolve.ts`: два документа на
  слой, отсутствующий файл — пустой вклад, неразбираемый — отказ с файлом и
  местом. Патч в общее слияние по точечным путям не попадает (Решение 9).
- [x] 2.4 Строки ключа `plugins` превращаются в операции «вставить в конец
  слоя» с `id`, равным спецификатору (Решение 4); вклад слоёв
  (`denyContributions`) остаётся тем, чем был, — им отчёт показывает
  объявленное.
- [x] 2.5 `Config.plugins` становится проекцией дерева — модулями действующих
  строк в порядке дерева (Решение 7); комментарий поля объясняет, почему второго
  представления состава нет.
- [x] 2.6 Цель `schema/plugins-patch.schema.json` в `scripts/schema-targets.ts`;
  сгенерировать схему и убедиться, что `test/schema-generated.test.ts` её
  подхватил.

## 3. Загрузка по дереву

- [x] 3.1 `src/core/plugins/load.ts`: `pluginDeclarations` уступает место
  разрешению дерева; загрузка идёт по действующим строкам в порядке дерева,
  отключённые пропускаются, `resolveModulePath` получает файл объявления строки
  (конфигурация или патч).
- [x] 3.2 Форма `use: stepcast:<имя>` разрешается по таблице встроенных строк, а
  не по диску; неизвестное имя — отказ, называющий строку и перечень
  поставляемых.
- [x] 3.3 `src/core/plugins/builtin.ts`: встроенные вклады описываются строками
  (сегодня — одна, `backend-claude`) и применяются на области ядра, сохраняя
  владельца «встроенный» (Решение 5). Резерв имён встроенных предикатов и
  внесение команд CLI остаются вне дерева.
- [x] 3.4 `builtinRegistry()` и прочие вызовы без конфигурации продолжают
  собирать полное встроенное дерево — библиотечный вызов не обязан читать файлы.

## 4. Команда осмотра

- [x] 4.1 Новый `src/cli/commands/plugins.ts`: печать дерева столбцами
  (`formatColumns`, `src/cli/output.ts`) — порядок, `id`, слой, модуль,
  состояние; флаг `--dump` объявлен и допустим, поведение без него то же.
- [x] 4.2 Объявление команды в `COMMANDS` и `BUILTIN_COMMANDS`
  (`src/cli/main.ts`).
- [x] 4.3 Отказ загрузки для этой и только этой команды не прекращает её до
  диспетчеризации: дерево печатается, строка-виновница несёт причину, код
  возврата — код ошибки конфигурации (Решение 8). Убедиться, что для прочих
  команд поведение отказа не изменилось ни в одном тесте.
- [x] 4.4 Раздел «Плагины» отчёта `stepcast config` отсылает к новой команде за
  слоями и порядком.

## 5. Демон

- [x] 5.1 `declarationsEqual` (`src/ui/pipelines.ts:318`) сравнивает строки
  дерева — `id`, модуль, включённость, порядок; тип записи кеша
  (`KernelCacheEntry`) несёт дерево вместо объявлений.
- [x] 5.2 Убедиться, что собственное ядро демона (ключ — домашний каталог,
  `projectPath: null`) собирает дерево без проектного слоя, но с домашним
  патчем.

## 6. Документация

- [x] 6.1 `docs/config.md`, раздел «Плагины»: три слоя, второй документ слоя,
  формат патча, замена и отключение строки, ключ `plugins` как сокращение.
- [x] 6.2 `docs/plugins.md`: дерево и `id` строки, замена встроенной строки,
  порядок загрузки и `before`/`after`, `stepcast plugins --dump` в разделе
  «Наблюдаемость».
- [x] 6.3 `docs/plugins.md`, «Известные ограничения»: правка патча
  подхватывается демоном, правка кода уже импортированного модуля — нет
  (владелец вопроса — `user-plugins-from-files`); замена встроенной строки не
  приносит её умолчаний (Риск 2).

## 7. Зелёная проверка

- [x] 7.1 `npm install --no-audit --no-fund && npm run check` — зелёный.
