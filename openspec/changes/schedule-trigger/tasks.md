## 1. Разбор cron-выражения

- [x] 1.1 Создать `src/core/trigger/cron.ts` — разбор пяти полей в маску:
      `*`, число, диапазон, список, шаг `*/n` и `a-b/n`; отказ с названной
      причиной на числе полей, значении вне диапазона поля, именах месяцев и
      дней, макросах `@…` и расширениях Quartz (`?`, `L`, `#`). Имя `scheduler`
      не занимать: так уже называется планировщик графа работ
- [x] 1.2 Добавить в `src/core/trigger/cron.ts` вычисление ближайшего момента
      после заданного: перебор минут в разложении по IANA-поясу через
      `Intl.DateTimeFormat`, горизонт четыре года, объединение ограниченных
      дня месяца и дня недели по «или», отсутствие момента в горизонте —
      отдельный различимый исход
- [x] 1.3 Написать `test/cron.test.ts` на разбор: каждая поддержанная форма
      поля принимается; макрос, имя дня, шестое поле, `?`, значение вне
      диапазона и четыре поля отклоняются, и причина названа
- [x] 1.4 Дополнить `test/cron.test.ts` вычислением моментов: ежедневный,
      ежечасный и шаговый случай; `0 3 13 * 5` даёт и 13-е число, и пятницу;
      29 февраля находится в горизонте; `0 0 30 2 *` даёт исход «моментов нет»
- [x] 1.5 Дополнить `test/cron.test.ts` часовыми поясами: один и тот же момент
      в двух поясах даёт разные мгновения; несуществующий локальный час в день
      перехода срабатывания не даёт; повторённый час даёт ровно одно

## 2. Объявление расписания в формате

- [x] 2.1 Добавить в разбор пайплайна (`src/core/pipeline/*`) верхний ключ
      `triggers` с единственным признанным видом `schedule` — списком записей
      `cron` и `timezone`; незнакомый вид отклонять с именем ключа
- [x] 2.2 Отклонять ключ `triggers` внутри файла работы наравне с `needs`,
      `on`, `if`, `with`
- [x] 2.3 Добавить в `src/core/lint.ts` проверки записи расписания: наличие и
      разбор `cron`, известность IANA-имени `timezone` (попытка построить
      `Intl.DateTimeFormat`), существование ближайшего момента; ошибка
      называет номер записи и причину
- [x] 2.4 Проверить, что расписание попадает в `pipeline.lock.yml` вместе с
      остальным раскрытым пайплайном
- [x] 2.5 Дополнить `test/lint.test.ts`: принятое расписание, отсутствующий
      `cron`, неизвестный пояс, невыполнимое выражение, `triggers.github`,
      `triggers` в файле работы, ошибочная вторая запись из двух
- [x] 2.6 Дополнить `test/expand.test.ts`: пайплайн без `triggers`
      раскрывается как прежде, с `triggers` — сохраняет их в локе

## 3. Признак живого прогона

- [x] 3.1 Добавить `pid` в `RunManifestSchema` (`src/core/journal/schema.ts`)
      и записывать его в `run.json` до запуска первой работы
- [x] 3.2 Добавить в `src/core/journal/reader.ts` поиск живого прогона
      пайплайна: состояние `running` и существующий процесс (`process.kill`
      сигналом 0); манифест без `pid` — неживой
- [x] 3.3 Добавить туда же чтение момента сброса окна лимита из событий
      последнего завершённого прогона пайплайна (`backend.refused`,
      `budget.waiting`, поле `resets_at`)
- [x] 3.4 Дополнить `test/journal.test.ts`: `pid` попадает в манифест; живой
      прогон отличим от брошенного; манифест прежней формы читается и даёт
      неживой прогон; спящий прогон с `wake_at` живой; прогон другого
      пайплайна живым для этого не считается

## 4. Команда планировщика

- [ ] 4.1 Создать `src/core/trigger/schedule.ts` — решение о срабатывании:
      собрать пайплайны проекта с расписанием, вычислить ближайшие моменты,
      вернуть по наступившему моменту либо запуск, либо пропуск с причиной
      (живой прогон, несброшенное окно); функция чистая относительно времени и
      журнала, чтобы проверяться без сна
- [ ] 4.2 Реализовать в ней правило пропуска: пропущенный момент не копится,
      следующий считается от текущего времени
- [ ] 4.3 Реализовать откладывание по несброшенному окну лимита и отсутствие
      откладывания по потолкам `tokens`, `cost`, `wallclock` и при
      несообщённом моменте сброса
- [ ] 4.4 Создать `src/cli/commands/schedule.ts`: ожидание ближайшего момента,
      запуск `stepcast run --pipeline <файл>` дочерним процессом, печать
      идентификатора прогона и причин пропуска; завершение по сигналу
      прерывания без убийства начатого прогона; флаги `--once` и `--list`;
      сообщение и выход, когда расписаний в проекте нет
- [ ] 4.5 Зарегистрировать команду в `src/cli/args.ts` и `src/cli/main.ts`,
      включая справку
- [ ] 4.6 Написать `test/schedule-trigger.test.ts` на решение о срабатывании:
      момент наступил и прогон запускается; живой прогон даёт пропуск с
      причиной; брошенный прогон запуску не мешает; прогон другого пайплайна
      не мешает; спящий прогон даёт пропуск
- [ ] 4.7 Дополнить `test/schedule-trigger.test.ts` откладыванием: окно не
      сброшено — пропуск с моментом сброса; окно сброшено — запуск; момент
      сброса не сообщён — запуск; остановка по `budget.tokens` — запуск
- [ ] 4.8 Дополнить `test/schedule-trigger.test.ts` пропусками: сутки простоя
      при ежечасном расписании дают не более одного прогона; пропущенный по
      занятости момент не отрабатывается после освобождения
- [ ] 4.9 Дополнить `test/schedule-trigger.test.ts` командой: `--list` ничего
      не запускает и печатает ближайшие моменты; `--once` отрабатывает
      наступившее и завершается; проект без расписаний даёт сообщение и код
      возврата 0

## 5. Документация и сдача

- [ ] 5.1 Добавить в `docs/pipeline-format.md` раздел «Триггеры»: ключ
      `triggers.schedule`, поддержанная грамматика cron и перечень
      неподдержанного, часовой пояс и умолчание, объединение дня месяца с днём
      недели по «или», единственность прогона, отсутствие догоняющих
      срабатываний, откладывание по несброшенному окну лимита
- [ ] 5.2 Описать в `docs/pipeline-format.md` команду `stepcast schedule` и её
      флаги, включая запуск под системным супервизором и роль `--once` для
      внешнего cron
- [ ] 5.3 Обновить `docs/status.md`: строку «Триггеры: расписание, GitHub»
      разделить — расписание переносится в «Работает», GitHub остаётся в
      «Пока нет»
- [ ] 5.4 Добавить в «Известные ограничения» `docs/status.md`: манифест с `pid`
      не читается прежней версией движка (та же несимметричность отката, что у
      сводок расхода); единственность прогона — проверка, а не блокировка, и
      два планировщика на одном проекте не предполагаются; пропущенные за
      простой моменты не отрабатываются
- [ ] 5.5 Прогнать `npm run check` и устранить замечания
