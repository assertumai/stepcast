## 1. Закрепить нынешнее поведение

- [x] 1.1 Убедиться, что снимок состава дефолтного дерева (`test/plugin-registry.test.ts`, задача 1.3 изменения `kernel-domain-free-imports`) покрывает строки `backend-claude` и `step-decision`, порядок регистрации и владельца встроенного вклада; дополнить его владельцем, если он не проверяется.
- [x] 1.2 `test/plugin-tree.test.ts`: тест «перечень движка сеет встроенный слой целиком и в своём порядке», а расхождение двух форм набора (id семени против фабрики обхода) проверяется там, где оно возможно, — на строках вызывающего.
- [x] 1.3 `npm run test:only` зелёный на этих тестах до единой правки кода.

## 2. Строки движка — модулями

- [x] 2.1 `src/parts/backends/claude/row.ts`: `export const row: BuiltinRow` с `id: 'backend-claude'` и прежним телом `apply` (регистрация `claude` через `createClaudeAdapter`/`claudeModelDiscovery`); комментарий называет шаг 5/10 плана как тот, что сведёт строку с реализацией.
- [x] 2.2 `src/parts/steps/decision/row.ts`: то же для `id: 'step-decision'` — прежнее тело, прежний комментарий о том, почему `decision` строка, а не вид ядра.
- [x] 2.3 Проверить, что оба модуля вносят вклады на корневой области ядра (`kernel.ctx.<сервис>.register`), а не через `kernel.ctx.plugin`: владелец вклада обязан остаться признаком области ядра.

## 3. Перечень движка — модулем данных

- [x] 3.1 `src/parts/rows.ts`: импорты обоих модулей строк, `BUILTIN_ROWS` списком в прежнем порядке (`backend-claude`, `step-decision`), `BUILTIN_ROW_IDS` выводом из него; в модуле нет ничего, кроме перечня.
- [x] 3.2 `src/parts/builtin.ts`: убрать `BUILTIN_ROWS` и `BUILTIN_ROW_IDS`, оставить сборку ядра (`BUILTIN_PREDICATE_NAMES`, `createKernelShell`, `createBuiltinKernel`, `builtinRegistry`); `createBuiltinKernel` берёт перечень из `./rows.js`.
- [x] 3.3 Перевести вызывающих на новый путь: `src/parts/load.ts` (`BUILTIN_ROWS`), `src/core/config/resolve.ts` (`BUILTIN_ROW_IDS`).
- [x] 3.4 Проверить, что комментарии `src/parts/builtin.ts` и `src/parts/load.ts`, называющие таблицу строк телом модуля, приведены в соответствие с перечнем.

## 4. Строка каркаса витрины и перечень витрины

- [x] 4.1 `src/ui/shell/row.ts`: тело строки `ui-shell` и три её обработчика (`/api/overview`, `/api/events`, `/api/screens`) вместе с `screensPayload` — переезд из `src/ui/screens/rows.ts` без правки содержания.
- [x] 4.2 `src/ui/shell/row.ts` экспортирует строку именем `row` — тем же соглашением, что строки движка и строки экранов; имя `UI_SHELL_ROW` живёт только в перечне, переименованием на импорте.
- [x] 4.3 `src/ui/rows.ts`: перечень витрины — `UI_SHELL_ROW` (из нового модуля), `SCREEN_ROWS`, `API_ROWS`, `UI_ROWS` — в прежнем порядке и без единого тела строки; докстринг перечня называет, кто эти строки подаёт.
- [x] 4.4 `src/ui/screens/rows.ts` — заглушка `export {}` с комментарием о переезде (как `src/core/plugins/builtin.ts` после шага 2).
- [x] 4.5 Перевести вызывающих: `src/ui/kernel.ts`, `test/ui-plugins.test.ts` и всё, что импортирует `UI_ROWS`/`UI_SHELL_ROW`.
- [x] 4.6 `src/parts/load.ts`: докстринг называет наборы входов — команды CLI идут с перечнем дефолта и состава у себя не называют, `stepcast up` подаёт `UI_ROWS` поверх него.

## 5. Пересечение перечней — именованный отказ

- [x] 5.1 `src/parts/load.ts`: в `defaultComposition` отказывать `StepcastError`, если `id` строки вызывающего есть в перечне движка; текст называет `id` и оба перечня, подсказка отправляет к патчу состава как к способу замены встроенной строки.
- [x] 5.2 Тест: вызывающий подаёт строку с `id: 'backend-claude'` — отказ с этим текстом, и ни одна строка не применена.
- [x] 5.3 Тест: замена `backend-claude` патчем по-прежнему проходит и этот отказ не задевает.

## 6. Документация

- [x] 6.1 `docs/plugins.md`, раздел «Объявление и разрешение»: соглашение «строка — модуль, экспортирующий `row`», перечни как единственное место состава, набор команд CLI и набор витрины; ссылка на `docs/microkernel-target.md`.
- [x] 6.2 `docs/plugins.md:87`: «сегодня одна, `backend-claude`» → две строки движка, `backend-claude` и `step-decision`.
- [x] 6.3 `docs/microkernel-target.md`: шаг 3 отмечен исполненным с названными адресами; строка структуры про `bin.ts` и открытый вопрос «состав дефолта — декларация или код» приведены в соответствие с решением (перечень — модуль данных, `bin.ts` — точка входа процесса).

## 7. Проверка

- [x] 7.1 `stepcast plugins` и `stepcast plugins --dump` печатают прежнее дерево (строки, порядок, происхождение вкладов).
- [x] 7.2 `npm run check` зелёный целиком.
