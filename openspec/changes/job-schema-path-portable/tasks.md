## 1. Схема пакета находится сама

- [x] 1.1 Завести в ядре общий модуль поиска схем пакета: `findPackageRoot` переезжает туда из `src/core/expect/verdict.ts` (частной там быть перестаёт), рядом появляется `packagedSchemaPath(<имя>)` — `<корень пакета>/schema/<имя>.schema.json`.
- [x] 1.2 `packagedSchemaPath` проверяет имя слагом в kebab-case до обращения к файловой системе и отказывает `StepcastError` на `/`, `.`, `..` и пустом имени, называя недопустимое имя.
- [x] 1.3 Ненайденная схема — отказ `StepcastError` с перечнем имён, прочитанных из `schema/` пакета (перечень берётся из каталога, константы имён в ядре нет).
- [x] 1.4 `judgeVerdictSchemaPath()` становится вызывающим общего модуля; второго обхода дерева вверх за корнем пакета в ядре не остаётся.
- [x] 1.5 Тест: разрешение идёт от расположения самого модуля — копия ядра в поддельной установке со своим `schema/` даёт схему своей установки, а не этого дерева; недопустимое имя и неизвестное имя дают отказ с ожидаемым текстом.
- [x] 1.6 Недоступный каталог `schema/` пакета — тот же отказ `StepcastError` с кодом `2` (перечень пуст, отказ называет каталог и неполноту установки), а не всплывшая `ENOENT`; покрыто тестом на поддельной установке без `schema/`.

## 2. Форма `stepcast:<имя>` в разрешении объявленных путей

- [x] 2.1 `resolveDeclaredPath` в `src/core/pipeline/expand.ts` получает ветку `stepcast:`: значение с этим префиксом идёт в `packagedSchemaPath`, прочие разрешаются как прежде относительно файла объявления.
- [x] 2.2 Ветка действует только в местах объявления схемы: `output_schema` шага (обе точки вызова в `expand.ts`), `output.schema` работы, предикат `schema` в `expect` и в `until.check`. Разрешение `uses` и `prompt: file:` остаётся прежним.
- [x] 2.3 Отказ разрешения называет место объявления (например `jobs.slots.steps.pick-lanes.output_schema`) и доходит до пользователя кодом `2` — тем же путём, что отказ на нечитаемом файле промпта.
- [x] 2.4 Тесты в `test/predicate-paths.test.ts`: форма принята во всех местах объявления схемы и всюду даёт один и тот же файл пакета; обычный путь схемы разрешается как прежде.
- [x] 2.5 Тесты в `test/lint-paths.test.ts`: линт не сообщает о ненайденном файле для `stepcast:`-ссылки; `uses: stepcast:implement`, `prompt: file:stepcast:implement` и запись `context: stepcast:backlog-slots` остаются обычными путями и дают ошибку ненайденного файла.
- [x] 2.6 Тест: неизвестное имя схемы роняет раскрытие пайплайна, а не превращается в диагностику линта.

## 3. Работа `slots` перестаёт смотреть за пределы `.stepcast/`

- [x] 3.1 Заменить в `.stepcast/jobs/slots.yml` три ссылки `../../schema/backlog-slots.schema.json` на `stepcast:backlog-slots` (`output.schema`, `output_schema` шага `pick-lanes`, предикат `schema` в его `expect`).
- [x] 3.2 Поправить комментарий-шапку работы, если он называет путь к схеме, — так, чтобы файл не обещал каталога `schema/` рядом с собой.
- [x] 3.3 Убедиться, что `stepcast lint .stepcast/pipelines/self-improve.yml` в этом репозитории проходит без новых диагностик.

## 4. Проверки переносимости петли

- [x] 4.1 Добавить в `test/loop-portability.test.ts` обход настоящих файлов `.stepcast/jobs/**`: каждая ссылка `uses`, `prompt: file:` (шага и его `on_fail`) и всякое объявление схемы (`output.schema`, `output_schema`, предикат `schema` в `expect` и в `until.check`) разрешается внутрь `.stepcast/`; `stepcast:`-ссылка засчитывается как не выходящая наружу.
- [x] 4.1.1 Обход берёт весь поддиректорий `.stepcast/jobs/**` — рекурсивно и в обоих написаниях расширения YAML, — чтобы файл в подкаталоге или `*.yaml` не выпадал из проверки молча.
- [x] 4.1.2 Тест самого обхода на синтетическом документе: ссылка наружу в `uses`, в `prompt`, в `on_fail.prompt` и в схеме ловится и называется ключом объявления; те же ссылки внутрь `.stepcast/` и форма `stepcast:` наружу не считаются.
- [x] 4.2 В обход не включать глобы `changed_only` и записи `context` — снабдить это объяснением в комментарии теста, чтобы следующий читающий не «дочинил» проверку до запрета петле знать о правимом репозитории.
- [x] 4.3 Убедиться, что обход падает на исходном `slots.yml`: временно вернуть одну из трёх строк на прежний путь и увидеть отказ теста, назвавший файл и ключ.
- [x] 4.4 Добавить тест раскрытия: настоящий `.stepcast/jobs/slots.yml` подключён пайплайном во временном проекте (`makeProject` — каталога `schema/` и `node_modules` в нём нет), раскрытие проходит, разрешённый путь схемы существует и читается как JSON.
- [x] 4.5 Убедиться, что и этот тест падает до правки движка — на ненайденном файле схемы.

## 5. Документы

- [x] 5.1 `docs/pipeline-format.md`, раздел «Разрешение путей»: описать вторую форму значения схемы (`stepcast:<имя>`), правило имени, каталог `schema/` пакета и то, что форма не действует в `uses`, `prompt: file:` и `context`; добавить её в перечень мест, где схема объявляется.
- [x] 5.2 `docs/backlog.md`, раздел «Схема формата в чужом репозитории»: переписать абзац — движок ссылку на свою схему теперь разрешает, и указывать `node_modules/stepcast/schema/` больше не нужно.
- [x] 5.3 `docs/run-layout.md`: снять абзац известного ограничения «Ссылка работы `slots` на схему годится только в этом репозитории».
- [x] 5.4 Завести `openspec/changes/job-schema-path-portable/status.md`: раздел «Известные ограничения: снято» с лидом снимаемого ограничения и раздел «Работает» с новой формой ссылки.
- [x] 5.5 Пересобрать `docs/status.md` командой `npm run status:build` (собранный документ руками не править).

## 6. Проверка

- [x] 6.1 `npm run check` — зелёная.
- [x] 6.2 Убедиться, что ни один файл `.stepcast/jobs/**` не содержит `../../` и что `stepcast:backlog-slots` встречается ровно трижды — в `slots.yml`.
