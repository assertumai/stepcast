## 1. Формат маршрута и его схема

- [x] 1.1 Объявить модель строки маршрута и документа `routes.yml` (Zod) в `src/ui/routesFile.ts`: `id`, `path`, цель (`screen` либо `widget`), `params`, `values`, `nav` (`title`, `order`, `active_for`), `enabled`; неизвестный ключ — отказ с файлом и местом (Решение 1, 5, 6)
- [x] 1.2 Внести цель генерации `schema/routes.schema.json` в `scripts/schema-targets.ts`, напечатать схему (`npm run schema`) и убедиться, что `test/schema-generated.test.ts` зелёный
- [x] 1.3 Разобрать шаблон пути в `src/ui/routes.ts`: сегменты, обязательный `:имя`, необязательный последний `:имя?`, нормализованный шаблон для сравнения путей (Решение 3)

## 2. Чтение слоёв и сборка таблицы

- [x] 2.1 Прочитать три файла слоёв в `src/ui/routesFile.ts`: встроенный (`findPackageRoot` + `src/builtin/routes.yml`, приёмом `src/ui/steps.ts:63`), домашний, проектный по корню демона; отсутствие пользовательского файла — обычное состояние
- [x] 2.2 Слить строки по `id` по листьям, сохранив источник каждого поля и место строки в порядке; `enabled: false` убирает маршрут из действующей таблицы (Решение 1, 2)
- [x] 2.3 Назвать отказы сборки: конфликт нормализованных шаблонов (оба `id`, оба файла), путь под `/api`, `/widgets/`, `/plugins/`, `/shared/`, подстановка на несуществующий параметр, неразбираемый файл слоя (Решение 3, 6)
- [x] 2.4 Отдать таблицу с диагностикой из `src/ui/kernel.ts` рядом с составом экранов: отказ сборки не меняет действующую таблицу и едет причиной, как `buildError` (Решение 3)
- [x] 2.5 `test/ui-routes-file.test.ts`: слои, слияние по `id` и по полям, отключение, конфликт пути, зарезервированный префикс, сломанный файл, источники полей

## 3. Разбор и сборка ссылок — общий модуль

- [x] 3.1 Переписать `parseRoute` в `src/ui/routes.ts` на таблицу маршрутов: выбор по конкретности шаблона, значения параметров, закрытый перечень `values` (Решение 4)
- [x] 3.2 Переписать `hrefFor` на поиск маршрута по цели с подстановкой параметров; вернуть `string | undefined` вместо нынешнего `/` и снять `defaultScreenId` (Решение 8)
- [x] 3.3 Реализовать подстановку `${params.<имя>}` в параметрах цели тем же языком, что и у пайплайнов (Решение 6)
- [x] 3.4 Оставить в модуле предикаты адресов демона (`isApiPath`, `isWidgetPath`, `isPluginPath`, `isSharedPath`) и проверить, что модуль не несёт ни одного импорта Node (Решение 9)
- [x] 3.5 `test/ui-routes.test.ts`: сопоставление, конкретность, необязательный сегмент, перечень значений, ссылка по цели, отсутствие ссылки, подстановка

## 4. Встроенный файл маршрутов и очистка объявлений

- [x] 4.1 Написать `src/builtin/routes.yml` с десятью нынешними адресами, их названиями, порядком пунктов, перечнем периодов расхода и подсветкой пункта прогонов на странице прогона; включить файл в `files` пакета
- [x] 4.2 Снять `path`, `nav` и `paramValues` с `ScreenDeclaration` (`src/ui/screens/declaration.ts`) и с десяти `src/ui/screens/*/declaration.ts`
- [x] 4.3 Убрать `path` и `nav` из ответа `GET /api/screens` (`src/ui/screens/rows.ts:124`)
- [x] 4.4 Перевести `ui/src/screens/run.tsx` (`runHref`), `ui/src/screens/runs.tsx` (свой пункт меню), `ui/src/screens/usagePeriods.ts` (ключи периодов из маршрута) и прочие браузерные половины на таблицу маршрутов
- [x] 4.5 Проверить, что `test/ui-server.test.ts` и `ui/test/*.test.tsx` остаются зелёными без правки ожидаемых адресов

## 5. Каркас, навигация и перечень маршрутов

- [x] 5.1 Завести сервис таблицы маршрутов страницы (`ui/src/services/routes.ts`) рядом со `screens` и переписать `ui/src/router.tsx` на него (Решение 9)
- [x] 5.2 Собрать навигацию из маршрутов с `nav`: общий вид пункта в каркасе, вклад по `id` маршрута — для экрана со своим видом, подсветка по `nav.active_for` (Решение 13)
- [x] 5.3 Показать перечень маршрутов на неразобранном адресе, на корне без стартового маршрута и на пустой таблице; убрать выбор экрана по умолчанию из `ui/src/plugins/shell.tsx`
- [x] 5.4 Завести слот видов цели (`route.target`): вид `screen` — ключ слота экранов, вид `widget` — хост виджета; неизвестный вид и отсутствующая цель — названная причина (Решение 5)
- [x] 5.5 Отклонить параметры цели у маршрута на виджет названной причиной, пока контракта props нет (`docs/widgets.md`, Non-Goals)
- [x] 5.6 `ui/test/routes.test.tsx`: навигация из маршрутов, порядок и названия, перечень на неизвестном адресе, подсветка дочернего маршрута, цель без вклада

## 6. Живое применение

- [x] 6.1 Добавить часть отпечатка по файлам маршрутов в `src/ui/watcher.ts` и пересобирать таблицу только по её сдвигу (Решение 10)
- [x] 6.2 Слать события `routes` и `screens` в `handleEvents` (`src/ui/screens/rows.ts`) по правилу «только при отличии от отправленного», состав — функцией на такт, приёмом `activePlugins`
- [x] 6.3 Принять оба события на странице (`ui/src/services/live.ts`, сервисы `routes` и `screens`) и пересчитать навигацию, адрес и ссылки без перезагрузки
- [x] 6.4 `test/ui-watcher.test.ts` и `test/ui-server.test.ts`: часть отпечатка, отправка при отличии, отсутствие пересылки на неизменном такте

## 7. Экран маршрутов

- [x] 7.1 Завести строку `screen-routes` с серверной половиной: `GET /api/routes` (таблица с источниками и диагностикой) и `POST /api/routes` (строка маршрута и слой)
- [x] 7.2 Писать файл слоя через `Document` библиотеки `yaml`: комментарии и порядок строк сохраняются, встроенный файл не пишется, неразбираемый файл не переписывается (Решение 11)
- [x] 7.3 Браузерная половина: перечень маршрутов с источником каждого поля и диагностикой, добавление и правка маршрута с названием файла до записи
- [x] 7.4 Внести маршрут экрана и его пункт меню во встроенный файл маршрутов (Решение 12)
- [x] 7.5 Вынести перечень маршрутов для неизвестного адреса вкладом этой же строки; каркас без неё показывает текстовый перечень адресов
- [x] 7.6 `test/ui-server.test.ts`: оба маршрута API, отказ на встроенном слое, отказ на сломанном файле, сохранение комментариев

## 8. Документация и проверка

- [x] 8.1 Написать `docs/routes.md`: формат, слои, цели, подстановки, перечень значений, навигация и стартовая страница, конфликты и зарезервированные префиксы, правка из витрины
- [x] 8.2 Поправить `docs/ui-plugins.md`: снять раздел про временный `path`, про место в навигации в объявлении и про перезагрузку ради нового состава; сослаться на `docs/routes.md`
- [x] 8.3 Назвать файл маршрутов среди файлов слоёв в `docs/config.md` и маршрут на виджет в `docs/widgets.md`
- [x] 8.4 Прогнать `npm run check` целиком и убедиться, что она зелёная

## 9. Правки по ревью

- [x] 9.1 Довести параметры цели до цели: `screen` получает их поверх параметров пути (Решение 6); подстановка проверена браузерным тестом
- [x] 9.2 Собрать ссылку пункта меню по пути своего маршрута (`hrefForRoute`), а не поиском первого маршрута на ту же цель (Решение 13)
- [x] 9.3 Назвать причину перечня в поставочной конфигурации: строка `screen-routes` вносит в ключ перечня экран под причиной, а не сам редактор (Решение 12)
- [x] 9.4 Отказать подъёму витрины на сломанном или отсутствующем встроенном файле маршрутов (Migration Plan), с тестами сборки и наблюдателя
- [x] 9.5 Пересобирать обзор только по своей части отпечатка: правка `routes.yml` не перечитывает прогоны (Решение 10)
- [x] 9.6 Свести три самодельные ветки «ссылки нет» к одному виду `TargetLink` с названной причиной (Решение 8)
- [x] 9.7 Показать источник каждого поля в экране «Маршруты», а не одного лишь пути (Решение 2)
- [x] 9.8 Показать отключённые маршруты отдельным перечнем и включать их обратно записью строки в слой, который их выключил
- [x] 9.9 Брать пресеты периода из маршрута, которым экран расхода открыт, а не из первого маршрута на эту цель (Решение 6)
- [x] 9.10 Поправить пример `docs/routes.md`: параметры цели объявляются у маршрута на экран, а маршрут на виджет с `params` отклоняется
- [x] 9.11 Дописать тесты: отказ таблицы при работающем демоне, молчание неизменного такта, живое применение правки, маршрут на виджет, цель вне состава
