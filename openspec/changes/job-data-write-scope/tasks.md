## 1. Объявление в формате работы

- [x] 1.1 `src/core/pipeline/schema.ts`: `data: z.array(z.string()).optional()` в `JobBodyShape` — рядом с `output` и `inputs`, то есть и в файле работы, и в описании работы на месте; в обвязку не попадает.
- [x] 1.2 Проверка имени при разборе: тот же набор символов, что у ключа данных (`assertDataKey`); недопустимое имя отклоняется с указанием работы и имени.
- [x] 1.3 `src/core/pipeline/model.ts`: `Job.data: readonly string[]` (пусто при отсутствии объявления) с пояснением, почему список именной, а не шаблонный.
- [x] 1.4 `src/core/pipeline/expand.ts`: перенос объявления в раскрытую модель; `src/core/pipeline/lock.ts`: поле в `pipeline.lock.yml`.
- [x] 1.5 Перегенерировать `schema/pipeline.schema.json` (`node dist/scripts/generate-schema.js` после сборки — сборку выполнять тем же способом, что и остальную проверку работы).
- [x] 1.6 Тесты: объявление разбирается и доезжает до лока и до `resolved.json`; имя с точкой отклоняется; работа без объявления имеет пустой состав.

## 2. Отказ на записи

- [x] 2.1 `src/core/journal/data.ts`: чтение объявления работы из `resolved.json` рядом с файлом данных; отсутствующее либо нечитаемое определение — пустой состав.
- [x] 2.2 `mergeJobData` отклоняет необъявленный ключ до касания файла; сообщение называет работу, ключ и объявленный состав.
- [x] 2.3 `src/cli/commands/data.ts`: `set` и `merge` доносят отказ ненулевым кодом; `get` объявлением не ограничивается.
- [x] 2.4 Тесты `test/job-data.test.ts`: необъявленный ключ отказывает и файл не меняет; объявленный публикуется как прежде; работа без объявления не публикует ничего; отказ при отсутствующем `resolved.json`.

## 3. Отказ на приёме

- [x] 3.1 `src/core/run/runner.ts`, `foldJobData`: в запись работы попадают только объявленные ключи; сверка идёт с определением работы в памяти, а не с файлом на диске.
- [x] 3.2 Необъявленный ключ в файле данных проваливает работу без повторных попыток — тем же путём, что и неразрешимая подстановка; причина называет работу и ключ.
- [x] 3.3 Файл данных не переписывается и не удаляется: отказ отменяет действие записи, а не её след.
- [x] 3.4 Проверка распространяется и на перенос данных при возобновлении (`transferJobData`): перенесённое подчиняется объявлению нового определения.
- [x] 3.5 Тесты: шаг пишет `data.json` в обход команды — работа отказывает, шаг не повторяется; объявленный сосед необъявленного сохраняется в записи работы; необъявленное значение не доезжает до подстановки в работе ниже по графу.

## 4. Публикация выбора в `backlog pick`

- [x] 4.1 `src/cli/commands/backlog.ts`: отказ публикации печатается на stderr с именем работы и ключа; выбор и код возврата не меняются.
- [x] 4.2 Вне шага прогона (`STEPCAST_JOB_DIR` пуст) поведение прежнее: ничего не публикуется и ни о чём не сообщается.
- [x] 4.3 Тесты: публикация внутри работы без объявления — пустые данные, ненулевой stderr, нулевой код; внутри работы с объявлением — прежняя публикация.

## 5. Линт

- [x] 5.1 `src/core/lint.ts`: `${jobs.<работа>.data.<ключ>}` в полях, потребляемых шагом, сверяется с объявлением названной работы; ошибка называет работу, ключ и объявленный состав.
- [x] 5.2 Та же сверка внутри `display` — рядом с уже действующими проверками имени работы и пространства.
- [x] 5.3 Тесты `test/job-data.test.ts` (раздел линта): необъявленный ключ у читателя и в подписи отклоняется; объявленный, но ещё не опубликованный — проходит.

## 6. Петля саморазвития

- [x] 6.1 `.stepcast/jobs/slots.yml`: `data: [title, title-a, slug-a]` с комментарием о связи списка с `--lanes` шага `pick-lanes`.
- [x] 6.2 Убедиться, что ни одна другая работа в `.stepcast/jobs/` объявления не получает, и что `stepcast lint` обоих пайплайнов (`self-improve.yml`, `self-improve-memory.yml`) зелёный.

## 7. По итогам ревью

- [x] 7.1 `src/core/run/runner.ts`: исход шага решает раньше сверки данных — упавший шаг сохраняет свою причину и `cause`, нарушение объявления дописывается к причине; причины остановки из закрытого перечня отказу по данным не назначается.
- [x] 7.2 `src/core/journal/data.ts`: отказ по ненайденному `resolved.json` объясняется отдельно от отказа по пустому объявлению и называет ожидавшийся путь.
- [x] 7.3 `src/core/journal/data.ts`: низкоуровневый писатель переименован в `writeJobDataUnchecked` — обход объявления называется в самом вызове; спека run-journal приведена к фактическому устройству.
- [x] 7.4 `src/core/lint.ts`: условие `if` сверяется с объявлением производителя наравне с полями шага и `display`.
- [x] 7.5 Тесты: упавший шаг с необъявленным ключом; перенос при возобновлении отбрасывает ключ, снятый из объявления; `if` с необъявленным и с объявленным ключом; фикстура `test/lint.test.ts` объявляет ключ подписи.

## 8. Документация и проверка

- [x] 8.1 `docs/pipeline-format.md`, раздел «Данные работы»: объявление `data`, буквальность имён, fail-closed умолчание, две точки отказа (запись и приём).
- [x] 8.2 `docs/pipeline-format.md`, перечень полей тела работы: `data` рядом с `output` и `inputs`.
- [x] 8.3 `docs/run-layout.md`: `resolved.json` как источник объявления для подпроцесса шага и судьба `data.json` при отказе.
- [x] 8.4 Обновить тесты движка, которые пишут `data.json` в обход команды (`test/job-data.test.ts`), объявлением ключей в их пайплайнах.
- [x] 8.5 Зелёный `npm run check` целиком (1949 тестов, 0 отказов), плюс `stepcast lint` обоих пайплайнов петли из собранного `dist/`.
