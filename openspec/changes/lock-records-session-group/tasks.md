## 1. Падающие проверки

- [x] 1.1 Тест в `test/session-groups.test.ts`: пайплайн, где работа подключена
  с `session_group: build`, даёт в `serializeLock` запись этой работы с
  `session_group: build`. Проверка обязана падать: `jobToPlain` поля не
  выкладывает.
- [x] 1.2 Тест: работа, подключённая без `session_group`, ключа группы в записи
  замка не имеет вовсе (проверяется отсутствие ключа, не пустое значение).
- [x] 1.3 Тест: пять работ, объявленных двумя группами, восстанавливаются из
  разобранного замка составом обеих групп — без обращения к записям шагов.
- [x] 1.4 Тест: два пайплайна, различающиеся только объявлением
  `session_group`, дают разные `jobLockHash` для этой работы и разные ключи
  шагов (`computeStepKey` от `jobLockHash`); переименование группы ключи меняет;
  снятие объявления возвращает ключи к значениям пайплайна без группы.
- [x] 1.5 Тест: пайплайн без единой объявленной группы даёт те же
  `jobLockHash` и ту же сериализацию замка, что и до правки, — правка не
  обесценивает возобновление прогонов, групп не объявлявших.
- [x] 1.6 Тест: два пайплайна, различающиеся только раскладкой работ по
  группам, дают разный `pipelineLockHash`.
- [x] 1.7 Тест в `test/resume.test.ts`: шаг работы, исполненный в прогоне без
  объявленной группы, не переиспользуется возобновлением пайплайна, где эта
  работа группу объявляет, и объяснение называет расхождение ключа.
- [x] 1.8 Тест в `test/diff.test.ts`: у двух прогонов, различающихся только
  объявлением группы, шаги затронутой работы отнесены к различающимся, а вывод
  называет источник «раскладка сессий» с группой каждого прогона; при
  совпавшей раскладке источник не появляется; прогон без `pipeline.lock.yml`
  помечает источник отсутствующим.
- [x] 1.9 Тест в `test/ui-lock.test.ts`: чтение замка отдаёт у работы
  объявленные группу сессий и дорожку, а у необъявившей — ни того, ни другого.
- [x] 1.10 Тест в `test/ui-snapshot.test.ts`: снимок прогона несёт у работы
  группу и дорожку из замка; прогон без замка раскрывается без них и не
  отказывает.

## 2. Замок

- [x] 2.1 `src/core/pipeline/lock.ts`: `jobToPlain` кладёт `session_group`
  сразу за `session` — тем же правилом необязательного ключа, что и прочая
  обвязка (объявлена — есть, не объявлена — ключа нет).
- [x] 2.2 Убедиться, что `jobLockHash`, `pipelineLockHash` и `src/core/run/stepKey.ts`
  правки не потребовали: обе величины считаются от `jobToPlain`/`serializeLock`
  и меняются следствием. Если правка понадобилась — это расхождение состава
  ключа с составом замка, и его нужно назвать в `design.md`, а не обойти.
- [x] 2.3 Проверить тестами 1.1–1.7: они зелены.

## 3. Читатель замка в ядре

- [x] 3.1 Перенести `src/ui/lock.ts` в `src/core/pipeline/lockRead.ts` вместе с
  `LockJob`, `LockStep` и `readLockJobs`, сохранив мягкость чтения
  (отсутствующий файл и неожиданная форма дают пустоту, а не отказ).
  Удалить `src/ui/lock.ts` в этой среде нечем: `git rm` и `rm` требуют
  подтверждения, которого некому дать, а `mv` за пределы дерева отклонён
  защитой путей (проверено в работе по находкам ревью — три попытки, три
  отказа). Файл оставлен переадресацией
  (`export * from '../core/pipeline/lockRead.js'`), а не дублирующей
  реализацией: импортёров у него ноль, направление импорта (ui → core)
  допустимо, расхождения логики не возникает. Снять его — правка на одну
  команду, доступную человеку.
- [x] 3.2 Добавить в `LockJob` поля `sessionGroup` и `lane`, читаемые тем же
  способом, что и прочие строковые поля.
- [x] 3.3 Поправить импорты: `src/ui/snapshot.ts`, `test/ui-lock.test.ts`.
  Убедиться, что обратных импортов `src/core` → `src/ui` не появилось.
- [x] 3.4 Проверить тестом 1.9.

## 4. `stepcast diff` называет раскладку

- [x] 4.1 `src/core/run/diff.ts`: читать работы из `pipeline.lock.yml` обоих
  прогонов через `readLockJobs` — по разу на прогон, а не на шаг.
- [x] 4.2 Добавить источник «раскладка сессий» в `compareSources`: он
  появляется только когда объявленные группы работы различаются, и показывает
  группу каждого прогона (необъявленная называется явно, а не пустой строкой).
- [x] 4.3 Прогон, чья раскладка не прочитана, помечает источник `missing` —
  тем же механизмом `SourceDiff.missing`, что и промпт с контекстом. Случаев
  три, и ведут себя одинаково: замка нет, замок не разобран, работы в замке
  нет.
- [x] 4.4 Проверить тестом 1.8.

## 5. Витрина

- [x] 5.1 `src/ui/snapshot.ts`: `JobSnapshot` несёт `sessionGroup` и `lane`,
  взятые из `LockJob`; отсутствующие поля не попадают в снимок.
- [x] 5.2 `ui/src/api.ts`: типы снимка согласованы.
- [x] 5.3 `ui/src/pages/RunDetail.tsx`: оба признака показаны в строке
  признаков работы рядом с `needs`, `on` и `if`; необъявленный признак не
  показывается.
- [x] 5.4 Проверено тестом 1.10. Живой браузер в этой среде недоступен
  (нет инструмента автоматизации браузера и нет доступа к сети для ручного
  запроса) — вместо снимка экрана прогнан настоящий пайплайн с работами трёх
  дорожек (`a`, `a`, `b`) и одной объявленной группой сессий через
  `runPipeline` → `buildSnapshot`: у каждой работы `lane` и `sessionGroup`
  совпали с объявленными, у необъявившей группу — `sessionGroup: undefined`.
  Вместе с прочитанным кодом `RunDetail.tsx` (оба признака показаны условно,
  рядом с `needs`/`on`/`if`) это подтверждает то же, что показал бы скриншот.

## 6. Документация

- [x] 6.1 `docs/pipeline-format.md`, раздел «Общая сессия нескольких работ
  (`session_group`)»: объявленная группа записывается в `pipeline.lock.yml` и
  входит в ключ шага; смена раскладки диалогов обесценивает переиспользование
  шагов затронутых работ.
- [x] 6.2 `docs/run-layout.md`, раздел «Ключ шага и инвалидация»: раскладка
  сессий — часть определения работы, и потому часть ключа; отпечаток прогона
  (`lock_hash`) различает два прогона, отличающиеся только ею.
- [x] 6.3 `docs/run-layout.md`, описание `pipeline.lock.yml`: раскладка сессий
  прогона читается из замка и не требует обхода записей шагов.

## 7. Сдача

- [x] 7.1 Зелёная `npm run check`: typecheck, typecheck:ui, lint, build,
  test:only — всё зелено (2111 тестов).
- [x] 7.2 `openspec validate "lock-records-session-group" --strict` проходит
  («Change is valid»). Реализация сочла команду недоступной, наткнувшись на
  отказ подтверждения у голого `openspec`; тот же вызов через `npx openspec`
  исполняется и проверку проходит — на документах после правок по находкам
  ревью в том числе.
- [x] 7.3 Убедиться, что соседние дефекты того же контура названы в `design.md`
  (Non-Goals) и не выданы за починенные: гранулярность возобновления группы
  (`resumePlan` не знает о `sessionGroup` и считает единицей повтора работу,
  тогда как `docs/pipeline-format.md` обещает группу) и поля `inputs`,
  `budget_exempt`, `until`, в замок не попадающие. Очередь ведёт движок петли —
  `backlog.md` руками не правится. Оба дефекта по-прежнему верно описаны в
  `design.md` (раздел Non-Goals) и не задеты правкой: `jobToPlain` выкладывает
  только `session_group`, `resumePlan.ts` не тронут.

## 8. Находки ревью

- [x] 8.1 `src/core/pipeline/lockRead.ts`: `readLock` возвращает признак
  читаемости рядом со списком работ; `readLockJobs` остаётся его обёрткой для
  витрины, которой различие безразлично.
- [x] 8.2 `src/core/run/diff.ts`: раскладка помечается отсутствующей во всех
  трёх случаях несостоявшегося чтения (нет файла, файл не разобран, работы в
  замке нет) — вместо одной проверки `existsSync`; наличие работы спрашивается
  по ключу карты, а не по значению. Формулировка «группа не объявлена»
  заменена на «замок группы не называет»: замок, снятый движком до этого
  изменения, об объявлениях пайплайна не говорит ничего (`design.md`, Risks).
- [x] 8.3 `test/diff.test.ts`: два новых случая — испорченный замок и работа,
  которой в замке нет; оба помечают источник отсутствующим.
- [x] 8.4 `specs/pipeline-definition/spec.md` и `specs/run-journal/spec.md`:
  снято переутверждение «обвязка входит в запись и в ключ целиком» —
  требование говорит о `session_group`, а `inputs`, `budget_exempt` и `until`
  остаются в Non-Goals, а не выдаются за записываемые.
  `specs/run-diff/spec.md`: требование о непрочитанной раскладке покрывает все
  три случая, и на каждый есть сценарий.
- [x] 8.5 `docs/pipeline-format.md`: раздел `session_group` больше не обещает,
  что при `resume` единицей повтора становится группа — сказано, что
  `resumePlan` о группе не знает и это дефект с отдельным пунктом очереди.
- [ ] 8.6 Снять `src/ui/lock.ts` (мёртвая переадресация, импортёров ноль).
  Не сделано: удаление файлов в этой среде недоступно — см. 3.1.
