## 1. Хранилище данных работы

- [x] 1.1 `src/core/journal/data.ts`: путь `jobs/<работа>/data.json`, чтение, запись целиком и дописывание поверх; запись через уже существующий `atomicWrite`.
- [x] 1.2 Проверка ключа: латинские буквы, цифры, подчёркивание и дефис; точка отклоняется — иначе `${jobs.x.data.a.b}` двусмысленно.
- [x] 1.3 Чтение мягкое: отсутствующий, повреждённый и не соответствующий форме файл дают пустую карту; значение не той формы отбрасывается поштучно, а не вместе со всей картой.
- [x] 1.4 Тесты `test/job-data.test.ts`: последняя запись побеждает, соседние ключи сохраняются, повреждённый файл читается пустотой, запись атомарна (читатель никогда не видит обрубка).

## 2. Команда `stepcast data`

- [x] 2.1 `src/cli/commands/data.ts`: действия `set`, `merge`, `get` первым позиционным аргументом — по образцу `backlog`.
- [x] 2.2 `set` берёт ключ и значение двумя позиционными аргументами; значения с двоеточиями проходят как есть.
- [x] 2.3 `merge --json` принимает объект; скаляр приводится к строке, составное значение отклоняется.
- [x] 2.4 Целевая работа — из `STEPCAST_JOB_DIR`; путей команда не принимает вовсе; вызов вне шага прогона отказывает с объяснением.
- [x] 2.5 `src/cli/main.ts`: описание команды и ветка диспетчера.
- [x] 2.6 Тесты: значение с двоеточием, дописывание, чтение по ключу и целиком, отказ вне шага, отклонение ключа с точкой и составного значения, отсутствие флага пути.

## 3. Движок складывает данные в состояние

- [x] 3.1 `src/core/journal/schema.ts`: `JobRecordSchema` получает необязательное `data: Record<string, string>`.
- [x] 3.2 `src/core/run/runner.ts`: `RunContext.refreshStatus` — переписать `status.json` по текущим записям; владелец файла остаётся один.
- [x] 3.3 Складывание после каждого шага — и исполненного, и переиспользованного.
- [x] 3.4 `src/core/run/scheduler.ts`: область видимости условий и подстановок пополняется данными завершившихся работ; данные переживают отказ работы, в отличие от выхода.
- [x] 3.5 `src/core/pipeline/late.ts`: `JobScopeEntry.data`; объяснение отказа называет состав опубликованных работой данных.
- [x] 3.6 Тесты: данные видны в состоянии после записавшего шага; работа ниже по графу читает их подстановкой; отсутствующий ключ — отказ с объяснением.

## 4. Блок `display`

- [x] 4.1 `src/core/pipeline/schema.ts`: `display` в `WiringShape` и в `WIRING_KEYS` — внутри файла работы отклоняется той же диагностикой.
- [x] 4.2 `src/core/pipeline/model.ts`: `Job.display`; `src/core/pipeline/expand.ts`: разбор в обвязке и отделение от тела работы.
- [x] 4.3 `src/core/pipeline/lock.ts`: перенос в `pipeline.lock.yml` нераскрытым.
- [x] 4.4 `src/core/pipeline/late.ts`: `display` отделяется до обхода и возвращается на место как был — через раскрытие не проходит.
- [x] 4.5 `src/core/pipeline/display.ts`: раскрытие подписи против данных работ; мягкое — неразрешённое поле опускается, подпись без единого раскрытого поля отсутствует целиком.
- [x] 4.6 Перегенерировать `schema/pipeline.schema.json` (`npm run schema`).
- [x] 4.7 Тесты: `resolveLate` подпись не трогает; лок хранит её нераскрытой; раскрытие против данных; отсутствующий ключ не роняет и не отменяет соседа.

## 5. Правила линта

- [x] 5.1 `src/core/lint.ts`: `${jobs.<сам>.data.*}` вне `display` — ошибка с объяснением, что к раскрытию данных ещё нет.
- [x] 5.2 Ключи подписи исключаются из общей проверки подстановок работы и проверяются отдельно: работа должна существовать, пространство — быть `data`, прочие отложенные имена отклоняются.
- [x] 5.3 Уже раскрытые разбором имена (`inputs`, `params`, `project`) внутри подписи остаются допустимыми.
- [x] 5.4 Тесты: самоссылка вне подписи отклоняется, внутри — разрешена; неизвестная работа и не то пространство в подписи отклоняются.

## 6. Возобновление

- [x] 6.1 `src/core/run/runner.ts`: перенос `data.json` из исходного прогона, когда хотя бы один шаг работы переиспользуется; уже записанное не затирается.
- [x] 6.2 Неудача переноса — запись внутреннего учёта (`bookkeep`), а не отказ работы.
- [x] 6.3 Тест: возобновление с работы ниже по графу сохраняет данные работы выше.

## 7. Права агентского шага

- [x] 7.1 `src/core/backend/claude.ts`: в жёстком режиме к списку разрешений неявно добавляется `Bash(stepcast data *)` — узко, не `stepcast *`.
- [x] 7.2 Вне жёсткого режима право не добавляется; объявленное явно — не удваивается.
- [x] 7.3 Тесты `test/backend.test.ts`, включая правку уже существующего, проверяющего точный состав списка.

## 8. Витрина

- [x] 8.1 `src/ui/lock.ts`: чтение подписи из лока (мягко, как остальные поля).
- [x] 8.2 `src/ui/snapshot.ts`: сбор данных всех работ прогона, раскрытие подписи, поля `display` и `data` в снимке работы.
- [x] 8.3 `src/ui/graph.ts`: `display` на входе раскладки и в узле.
- [x] 8.4 `ui/src/api.ts`: зеркало типов границы.
- [x] 8.5 `ui/src/components/JobGraph.tsx`: третья строка узла из `display.title`; высота узла общая на весь граф; подпись попадает и во всплывающую подсказку.
- [x] 8.6 `ui/src/pages/RunDetail.tsx`: подпись целиком и опубликованные данные в карточке работы.
- [x] 8.7 Тесты снимка: самоссылка раскрывается против собственных данных работы; работа без данных подписи не несёт.

## 9. Петля саморазвития

- [x] 9.1 `src/cli/commands/backlog.ts`: `pick --lanes` публикует `title`, `title-<дорожка>` и `slug-<дорожка>` данными работы, когда `STEPCAST_JOB_DIR` присутствует; вне шага ничего не публикует и не отказывает.
- [x] 9.2 Неудача публикации не отменяет выбор: пункты уже помечены в очереди.
- [x] 9.3 `.stepcast/pipelines/self-improve.yml`: `display: { title: ${jobs.slots.data.title} }` у работы `slots`.
- [x] 9.4 `.stepcast/jobs/slots.yml`: комментарий у шага `pick-lanes` о том, что заголовок публикуется самой командой.

## 10. Документация

- [x] 10.1 `docs/pipeline-format.md`: разделы «Подпись работы (`display`)» и «Данные работы (`jobs.<id>.data.*`)».
- [x] 10.2 `docs/pipeline-format.md`, «Когда что раскрывается»: третий случай — раскрытие при отрисовке.
- [x] 10.3 `docs/pipeline-format.md`, «Выражения»: `jobs.<id>.data.*` в перечне доступного.
- [x] 10.4 `docs/run-layout.md`: `data.json` в раскладке каталога прогона и абзац о владельце файла и переносе при возобновлении.
- [x] 10.5 Фрагмент `status.md` изменения и пересборка `docs/status.md` (`npm run status:build`).
