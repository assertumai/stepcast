## 1. Пункт очереди называет репозитории

- [x] 1.1 `test/backlog-file.test.ts`: тесты на поле `repos` — одно имя, перечень, `.`, отсутствие поля (пустой перечень в записи), недопустимое имя (абсолютное, с `..`, с `*`), пустое значение, повтор имени. Написать первыми и убедиться, что они падают до правки схемы. *(тесты добавлены в `test/backlog.test.ts` — рядом с остальными тестами разбора и `toRecord`, как диктует существующая структура тестов ядра очереди.)*
- [x] 1.2 `src/core/backlog/schema.ts`: поле `repos` пункта (строка перечня через запятую) и `repos: string[]` в публикуемой записи `BacklogRecordSchema`. Модель имени — та же, что у `RelativeRepoPathSchema` конфигурации по смыслу, но живёт в ядре очереди: очередь не должна зависеть от конфигурации. Комментарий у поля объясняет, почему разбор не проверяет объявленность имени.
- [x] 1.3 `src/core/backlog/parse.ts`: разбор перечня в `toRecord`, диагностика отказа по образцу `describeFailure` — слаг пункта, имя, номер строки.
- [x] 1.4 `src/core/backlog/schema.ts`: необязательный блок `repo` у дорожки в `BacklogSlotsResponseSchema` (каталог, команда проверки, практика спецификации); строгость объекта сохраняется.
- [x] 1.5 `npm run schema` и `test/schema-generated.test.ts`: пересобранные `schema/backlog.schema.json` и `schema/backlog-slots.schema.json` принимают ответ `pick` и дополненный ответ, отклоняют посторонний ключ дорожки.

## 2. Вложенный репозиторий объявляет проверку и практику

- [x] 2.1 `test/config.test.ts`: тесты на объектную форму элемента `nested_repos` — разбор, отказ без `dir`, отказ на пустой `check` и на абсолютный путь практики, повтор каталога в составе, `project.**` в глобальном конфиге, отсутствие подстановки `${project.nested_repos}`. Написать до правки схемы.
- [x] 2.2 `src/core/config/schema.ts`: объектная форма элемента (`dir` обязателен, `check` и `spec` — те же модели, что у одноимённых ключей секции `project`, `CheckCommandSchema` и `RelativeRepoPathSchema` переиспользуются, а не копируются); отказ символам шаблона глоба у `dir` сохраняется; отказ на повтор каталога.
- [x] 2.3 `src/core/config/resolve.ts`: приведение обеих форм к одному виду — прежний перечень каталогов (`nestedRepos`) плюс карта объявлений по каталогу. Один раз на движок: читатели состава (`anchor/composite.ts`, `lanes/tree.ts`, `run/workspace.ts`, `run/preflight.ts`, `cli/**`) правок не требуют — проверено прогоном полного набора тестов (1557 зелёных) без единой правки читателей.
- [x] 2.4 `src/core/config/merge.ts`: убедиться, что список `nested_repos` между слоями по-прежнему заменяется целиком, а не сливается поэлементно, и что объектная форма это не меняет. *(Правок не потребовалось: `flatten()` уже трактует массив как единый лист независимо от типа его элементов — весь список побеждающего слоя заменяет предыдущий целиком.)*
- [x] 2.5 `src/cli/commands/config.ts`: отчёт `stepcast config` печатает объявленные вложенные репозитории с их объявлениями — именами, а не счётчиком.

## 3. Разрешение репозитория и команда `stepcast project repos`

- [x] 3.1 Новый `src/core/project/repos.ts`: разрешение имени в объявление — каталог (`.` у корня), команда проверки, практика спецификации с путями, склеенными от корня дерева и без приставки `./` у корня. Единственное место склейки и единственное место отказов (неизвестное имя, несколько имён, недостающий ключ). Юнит-тесты на него — новым `test/project-repos.test.ts`, до реализации команды.
- [x] 3.2 Новый `src/cli/commands/project.ts`: подкоманда `repos` — чтение документа со стандартного ввода либо из `--file`, разрешение каждой заполненной дорожки, печать дополненного документа, коды возврата. Разбор подкоманды первым позиционным аргументом, как в `backlog.ts`.
- [x] 3.3 `src/cli/main.ts`: объявление команды в `COMMANDS` (описание, ключ `--file`) и ветка в `run`. Проверить, что `src/core/index.ts` экспортирует всё, что команде нужно. *(Команда импортирует зависимости напрямую из их модулей — тем же приёмом, что `assert-clean.ts`, — а не через барrel `core/index.ts`, который `project/pipelines.ts` тоже не использует; правка `core/index.ts` не потребовалась.)*
- [x] 3.4 Новый `test/cli-project-repos.test.ts` по образцу `test/cli-assert-clean.test.ts`: дорожка с вложенным репозиторием; дорожка без поля `repos` (корень, пути без приставки); явный `.`; незаполненная дорожка; `--file`; необъявленное имя; два имени; недостающий ключ объявления; объявление только в документе пайплайна; пустой и неразбираемый ввод; неизменность очереди и конфигурации после вызова.
- [x] 3.5 Тест на конвейер: вывод `backlog pick --lanes`, поданный на вход `project repos`, даёт документ, проходящий поставляемую схему; отказ `pick` доезжает ненулевым кодом второго звена.

## 4. Петля: объявления в конфигурации и головная работа

- [x] 4.1 Новый `.stepcast/config.yml`: секция `project` целиком переезжает из `.stepcast/pipelines/self-improve.yml` — `check`, `tools`, `spec`, `edit_paths` теми же значениями; комментарии о причинах объявления переносятся вместе с ключами.
- [x] 4.2 `.stepcast/pipelines/self-improve.yml`: секция `project` удаляется, а на её месте остаётся комментарий, называющий новый файл и причину (объявление, читаемое командой, живёт в конфигурации). Проверить, что все `${project.*}` в работах и промптах раскрываются в прежние значения.
- [x] 4.3 `.stepcast/jobs/slots.yml`: шаг `pick-lanes` становится конвейером `backlog pick … | project repos`; комментарий объясняет, почему разрешение — вторая команда (переносимость `backlog`), и почему отказ первого звена доезжает.
- [x] 4.4 `.stepcast/pipelines/self-improve.yml`: на местах подключения работ дорожек добавляются параметры из `${jobs.slots.output.lanes.<дорожка>.repo.*}` — по числу значений, которые работа потребляет. Обе дорожки правятся одинаково.

## 5. Петля: гейты и документы по выбранному репозиторию

- [x] 5.1 `.stepcast/jobs/verify.yml`: параметры каталога, команды и инструмента; шаг проверки — `cd "${params.repo_dir}" && ${params.check}`; шаг проверки документов — тем же приёмом с инструментом. Комментарий о том, почему каталог меняется командой, а не полем шага.
- [x] 5.2 `.stepcast/jobs/implement.yml` и `.stepcast/jobs/fix-review.yml`: предикаты `cmd` цикла `until` — тем же приёмом; пути документов изменения и файла правил — из параметров; границы `changed_only` для каталога документов и файла правил — из параметров, а `${project.edit_paths}` остаётся корневым (см. Non-Goals).
- [x] 5.3 `.stepcast/jobs/propose.yml`, `plan.yml`, `review.yml`: пути документов, файл правил и право `Bash(<инструмент> *)` — из параметров дорожки.
- [x] 5.4 `.stepcast/prompts/{propose,plan,implement,review,fix-review}.md`: ссылки `${project.spec.*}` и `${project.check}` заменяются параметрами тех же работ; текст остаётся о репозитории вообще, без литералов практики.
- [x] 5.5 `test/loop-portability.test.ts`: действующие проверки переносимости проходят; добавить проверку, что в файлах работ и промптов петли не осталось `${project.check}` и `${project.spec.*}` там, где значение обязано приходить дорожкой.
- [x] 5.6 Прогон петли на подставном бэкенде (тем же приёмом, что действующие тесты петли): пункт без `repos` даёт прежние команды; пункт с объявленным вложенным репозиторием даёт команды и каталог этого репозитория. *(У `verify.yml` нет агентских шагов — «подставной бэкенд» здесь не нужен вовсе, реальное исполнение шага оболочкой доказывает cd напрямую; третий тест — обратная сторона: команда корневого сценария в чужом repo_dir проваливается.)*

## 6. Возобновление, документация и статус

- [x] 6.1 `test/resume.test.ts`: смена выхода работы `slots` (другой пункт с другим репозиторием) делает невалидными шаги работ ниже по графу с причиной пересчёта выхода работы выше по графу.
- [x] 6.2 `docs/backlog.md`: поле `repos` в формате пункта, его значения и умолчание; команда `stepcast project repos` рядом с `backlog pick` — что читает, что печатает, чем отказывает.
- [x] 6.3 `docs/config.md`: объявление вложенного репозитория объектной формой; почему объявление, читаемое командой, живёт в конфигурации, а слой пайплайна остаётся для подстановки.
- [x] 6.4 `docs/run-layout.md`: ключ шага и выбранный репозиторий — правило (репозиторий входит в ключ выходом работы `slots`) и его граница (`resume --from` переиспользует всё выше точки независимо от него).
- [x] 6.5 `openspec/changes/backlog-item-names-repo/status.md`: фрагмент «Работает» о проверке выбранного репозитория и «Пока нет» о границах правок по репозиторию и о нескольких репозиториях у пункта; пересборка `npm run status:build` (собранный документ руками не править).
- [x] 6.6 `openspec validate backlog-item-names-repo --strict` и финальный `npm run check`. *(`npm run check` зелёный: 1587/1587 тестов, typecheck, typecheck:ui, lint, build, status:check. `openspec validate` в этой среде отказывал разрешением на вызов CLI при каждой попытке — все двенадцать требований изменения вручную сверены на правило валидатора «SHALL в первой физической строке»: `SHALL` в каждом стоит сразу после заголовка требования, без переноса.)*
