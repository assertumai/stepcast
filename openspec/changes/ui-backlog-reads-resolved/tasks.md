## 1. Чтение и объединение файлов (`src/ui/backlog.ts`)

- [x] 1.1 Читать у каждого проекта, видимого в обзоре, оба файла —
      `backlog.md` и `resolved.md`; отсутствие каждого по отдельности
      обрабатывать тем же приёмом, каким сегодня обрабатывается отсутствие
      `backlog.md` (не ошибка, файл пропускается).
- [x] 1.2 Разбирать оба файла независимо друг от друга: отказ разбора одного
      не должен прерывать чтение или показ пунктов другого.
- [x] 1.3 Пометить каждый пункт вида полем файла-источника (`sourceFile`:
      `backlog.md` либо `resolved.md`).
- [x] 1.4 Объединить пункты раздела проекта конкатенацией: пункты
      `backlog.md` в его файловом порядке, затем пункты `resolved.md` в его
      файловом порядке.
- [x] 1.5 Заменить одиночные поля `error`/`errorFile`/`errorAt` раздела
      (`BacklogProjectView`) списком отказов — по одному на файл, не
      разобравшийся по формату; раздел с одним отказавшим и одним исправным
      файлом несёт и пункты исправного, и запись об отказе.

## 2. Отбор, нумерация и порядок (`src/ui/backlogView.ts`)

- [x] 2.1 Обновить типы раздела (`BacklogSectionLike`, `BacklogSectionView`)
      под список отказов вместо одного `error`; правило видимости раздела при
      отказе (переживает фильтр по статусу) сохранить для раздела с хотя бы
      одним отказом, независимо от того, есть ли у него разобравшиеся пункты.
- [x] 2.2 Убедиться, что нумерация, фильтры по статусу/проекту и плановый
      порядок применяются к объединённому списку без изменения своих правил:
      пункт `resolved.md` — обычный пункт списка для этих правил.

## 3. Экран (`ui/src/pages/Backlog.tsx`, `ui/src/api.ts`)

- [x] 3.1 Обновить типы `BacklogItemView`/`BacklogProjectView` в
      `ui/src/api.ts` под поле `sourceFile` пункта и список отказов раздела.
- [x] 3.2 Показать файл-источник пункта в таблице (колонка либо пометка рядом
      со слагом).
- [x] 3.3 Показать все отказы раздела (а не только первый), каждый с именем
      своего файла — тем же оформлением, каким сегодня показан единственный
      отказ.

## 4. Документация

- [x] 4.1 Дополнить `docs/backlog.md` описанием `resolved.md`: назначение
      файла, кто и когда переносит в него пункты, и то, что экран очереди
      читает оба файла проекта одним списком.

## 5. Тесты

- [x] 5.1 `test/ui-backlog.test.ts`: раздел проекта объединяет пункты
      `backlog.md` и `resolved.md`; отсутствие `resolved.md` не ошибка;
      отказ разбора одного файла не скрывает пункты другого и показан с
      именем этого файла; оба файла с отказом дают два независимых отказа;
      каждый пункт несёт верный `sourceFile`.
- [x] 5.2 `test/ui-backlog-view.test.ts`: фильтр, нумерация и порядок работают
      над объединённым списком пунктов из двух файлов так же, как над
      списком одного.

## 6. Проверка

- [x] 6.1 `openspec validate "ui-backlog-reads-resolved" --strict` проходит.
- [x] 6.2 `npm run check` зелёная.
