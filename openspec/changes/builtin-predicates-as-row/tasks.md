## 1. Закрепить нынешнее поведение

- [x] 1.1 `test/plugin-registry.test.ts`: снимок дефолтного состава проверяет перечень предикатов и владельца каждого — «встроенный»; дополнить владельцем, если он не проверяется.
- [x] 1.2 Тест, закрепляющий дыру в резерве до её закрытия: плагин, объявивший предикат `script`, сегодня принимается загрузкой, а его вычислитель не зовётся ни разу (ключ забирает встроенная ветвь). Тест заводится красным на будущее поведение и правится в задаче 5.3 — либо заводится зелёным на нынешнее и переписывается там же; выбрать одно и назвать в тексте теста. (Реализация вышла одним проходом без промежуточного коммита на старом поведении: тест на закрытую дыру заведён сразу в задаче 6.2, текст называет это явно.)
- [x] 1.3 `npm run test:only` зелёный на этих тестах до единой правки кода.

## 2. Внутренняя форма предиката

- [x] 2.1 `src/core/plugins/contract.ts`: `NativePredicateForm` (`test`, `parse`) и `NativePredicate` по образцу `NativeStepKindForm`/`NativeStepKind`; тип вклада сервиса — объединение «плагинный вклад либо внутренняя форма».
- [x] 2.2 Там же: признаки `isNativePredicate` и `hasPredicateEvaluator` по образцу `isNativeStepKind`/`hasStepExecutor` — проверяется содержание поля, а не одно его имя.
- [x] 2.3 `src/core/plugins/context.ts`: регистратор предикатов, видимый плагину, сужается до `PredicateContribution` — тем же приёмом, каким сужен регистратор видов шага; проверить, что `PredicateContributionSchema` уже отклоняет внутреннюю форму (требует `schema` и `evaluate`), и добавить тест, если такого нет.
- [x] 2.4 `src/core/plugins/kernel.ts`: сервис `predicates` параметризуется новым объединением; ничего, кроме типа, здесь пока не меняется.

## 3. Формы встроенных предикатов и строка

- [x] 3.1 `src/core/pipeline/expand.ts`: цепочка ветвей `toPredicate` разложена в перечень внутренних форм — по форме на предикат, включая `script`; тела ветвей переносятся без изменения содержания.
- [x] 3.2 Там же: помощник сборки формы делает единственное приведение (`raw`/`ctx`), `test` пишется предикатом типа над `RawBuiltinPredicate` с сужением оператором `in`; в телах ветвей приведений типа нет ни одного.
- [x] 3.3 `toPredicate` выбирает форму по действующему реестру (`isNativePredicate` + `native.test`), а не по цепочке `in`; не узнанная ни одной формой запись уходит в прежний путь плагинного предиката.
- [x] 3.4 `src/parts/expect/row.ts`: `export const row: BuiltinRow` с `id: 'predicates'`, тело `apply` — регистрация всех форм на корневой области; обращение к импортированным формам внутри тела `apply`, не на верхнем уровне модуля (цикл модулей, как у строк видов шага).
- [x] 3.5 `src/parts/rows.ts`: строка добавлена сразу после `backend-claude`; докстринг называет, что у строки предикатов своего ограничения порядка нет.
- [x] 3.6 `src/parts/builtin.ts`: `createKernelShell` без резерва имён, `BUILTIN_PREDICATE_NAMES` снят; комментарий шапки о «встроенных предикатах — исключении» переписан по факту.
- [x] 3.7 `src/core/plugins/kernel.ts`: сняты `reserveName`, `Kernel.reservePredicate`, `ContributionService.reserved` и обе её ветви (`owner()`, сборка `owners` в `registry.ts`).
- [x] 3.8 `src/core/plugins/registry.ts`: `builtinPredicates` снято, `predicateNames` читает один реестр; добавлен перечень имён предикатов внутренней формы в порядке регистрации — для сборки схем по составу.
- [x] 3.9 `src/core/expect/evaluate.ts`: в `evaluatePlugin` вклад сужается признаком вычислителя; `switch` `evaluateOne` и модель `Predicate` не трогаются вовсе — проверить `git diff` файла.
- [x] 3.10 `src/core/lint.ts`: `lintPluginPredicate` сужает вклад признаком вычислителя; прочие ветви линта по `predicate.kind` не трогаются.

## 4. Схема документа по составу

- [x] 4.1 `src/core/pipeline/schema.ts`: `buildDocumentSchemas` принимает четвёртым параметром перечень имён встроенных предикатов; умолчание — канонический перечень, так что константы модуля и типы `RawPredicate`/`RawBuiltinPredicate` не меняются.
- [x] 4.2 Там же: таблица ветвей предикатов — местная для фабрики, объединение собирается по поданному перечню; имя без ветви — именованный отказ сборки, называющий предикат.
- [x] 4.3 Там же: помощник «состав встроенных предикатов совпал с дефолтным» по образцу `isDefaultNativeStepKinds` — по составу, а не по длине перечня.
- [x] 4.4 `src/core/pipeline/expand.ts`, `expandPipeline`: условие быстрого пути готовых схем — «плагинных вкладов нет (по признаку вычислителя) и встроенный набор совпал с дефолтным» вместо `registry.predicates.size === 0`.
- [x] 4.5 `src/core/pipeline/published-schema.ts`: отбор плагинных вкладов по признаку вычислителя; `buildPublishedSchemas` принимает и передаёт перечень встроенных предикатов; вызов без него даёт прежний файл схемы пакета байт в байт.
- [x] 4.6 `src/cli/commands/schema.ts` и `src/core/lint.ts` (сверка устаревания) подают перечень встроенных предикатов действующего реестра; `scripts/generate-schema.ts` не правится.
- [x] 4.7 `npm run schema` не меняет `schema/pipeline.schema.json` и `schema/job.schema.json` — проверить сравнением с `git diff`.

## 5. Отказ на предикате вне состава

- [x] 5.1 `src/core/pipeline/expand.ts`: предпроверка сырого документа обходит `steps[].expect` и `jobs.*.until.check` рядом с проверкой видов шага, на обоих её вызовах (файл пайплайна и подключённый файл работы), до `validateDocument`.
- [x] 5.2 Предикат спрашивается формой узнавания из локального перечня форм (по образцу `BUILTIN_STEP_KINDS_IN_ROW_ORDER`), а не перечнем имён: форма отвечает на вопрос «узнал бы ты этот ключ», на который реестр без снятой строки ответить не может.
- [x] 5.3 Отказ называет имя предиката и адрес записи; подсказка говорит, что предикат приносит строка дерева, а состав показывает `stepcast plugins`; имя строки в тексте не звучит.
- [x] 5.4 Прежний отказ о неизвестном предикате с перечнем доступного не изменён — проверить существующими тестами разбора.
- [x] 5.5 Тест: патч состава отключает строку предикатов, пайплайн с `exit_code:` отказывает новым текстом; тот же пайплайн при дефолтном составе разбирается как прежде. Случаи: предикат шага, предикат `until.check`, предикат в подключённом файле работы, многоключевая запись `judge`.
- [x] 5.6 Тест: при снятой строке предикатов документ с плагинным предикатом разбирается и вычисляется, а линт на нём не падает (ветвей `kind` встроенных предикатов в модели нет вовсе).

## 6. Имя `script` и освобождение имён

- [x] 6.1 Проверить, что перечень форм — единственный источник истины: второго перечня имён встроенных предикатов в репозитории не осталось (поиск по `exit_code`, `knowledge_valid` в перечнях).
- [x] 6.2 Тест: плагин, объявляющий предикат `script`, получает отказ загрузки о занятом имени; текст — тот же, что и для `exit_code`.
- [x] 6.3 Тест: перечень доступного в диагностике разбора называет `script` наравне с остальными.
- [x] 6.4 Тест: при отключённой строке предикатов плагин вправе внести предикат `exit_code` со своей схемой и своим вычислителем — документ разбирается его схемой, вычисление идёт его вычислителем.
- [x] 6.5 Тест: пользовательская строка, заменившая `predicates` по `id`, не наследует ни одного встроенного предиката.

## 7. Тесты состава и осмотра

- [x] 7.1 `test/plugin-kernel.test.ts`: ожидания резерва заменены ожиданиями вкладов; сценарий «имя занято посреди применения» повторён для предиката по образцу встроенного бэкенда.
- [x] 7.2 `test/plugin-registry.test.ts`: `predicateNames` дефолтного реестра, владельцы вкладов, `createKernelShell` без предикатов.
- [x] 7.3 `test/plugin-tree.test.ts` и `test/plugin-load.test.ts`: ожидания дефолтного дерева и семени — семь строк в порядке перечня.
- [x] 7.4 `test/plugins-introspect.test.ts`: предикаты числятся за своей строкой, во «встроенном вне строк» их нет.
- [x] 7.5 Тест порядка: перестановка строки предикатов в перечне не меняет ни разбора, ни вычисления, ни содержимого реестра.
- [x] 7.6 Тесты поведения предикатов и их диагностик (`test/expect.test.ts`, `test/lint.test.ts`, `test/judge.test.ts`, `test/changed-only.test.ts`, `test/predicate-paths.test.ts`) прошли без правки ожиданий — проверить `git diff` этих файлов пустым.

## 8. Документация

- [x] 8.1 `docs/plugins.md`: перечень строк встроенного слоя — семь; абзац «имена девяти встроенных предикатов строками дерева не становятся» заменён описанием строки, её отключения и замены; раздел «Имена встроенных предикатов заняты» говорит, что имя занято вкладом строки и освобождается вместе с ней; названа цель — `docs/microkernel-target.md`.
- [x] 8.2 `docs/pipeline-format.md`: предикаты приходят строкой поставки; описан отказ на предикате вне состава; названо, что неявная проверка кода возврата при пустом `expect` составом не снимается.
- [x] 8.3 `docs/microkernel-target.md`: шаг 6 отмечен выполненным с уточнением, что `unknown` остался в адаптере разбора, а вычислитель его не видит.

## 9. Проверка

- [x] 9.1 Пайплайны `examples/` и поставки разбираются и ведут себя как прежде — прежними тестами, без правки ожиданий.
- [x] 9.2 `stepcast plugins` и `stepcast plugins --json` на дефолтном составе печатают семь строк, и предикаты видны за своей строкой.
- [x] 9.3 `npm install --no-audit --no-fund && npm run check` зелёный.

## 10. Находки ревью

- [x] 10.1 Объединение ветвей предиката собирается фиксированными позициями с `z.never()` на месте снятого (образец `StepSchema`), а не длиной перечня: `PredicateSchema` снова размеченное объединение, `RawStep['expect']` и `UntilSchema.check` — снова `RawPredicate`, и оба приведения `entry as RawPredicate` в `expand.ts` сняты. Этим же снято расхождение с `isDefaultNativePredicates`, который сверяет состав без учёта порядка.
- [x] 10.2 `test/published-schema.test.ts`: печать схемы проекта при снятой строке предикатов — ни один из десяти ключей не признан, документ без предикатов принимается, а `isDefaultNativePredicates` на этом составе ложен; ключ плагинного предиката при той же снятой строке в схеме есть, и его значение проверяется. Там же — дефолтный перечень, поданный перемешанным, печатает то же побайтово, и имя без ветви отказывает сборке.
- [x] 10.3 `test/lint.test.ts`: прежде напечатанная схема проекта при снятой строке `predicates` становится устаревшей — тем же образцом, что и для снятого вида шага.
- [x] 10.4 `test/expand.test.ts`: тест «плагинный предикат при снятой строке» доведён до риска, ради которого заведён, — модель вычисляется `evaluatePredicates` и проверяется `lintPipeline`, а не только сверяется `kind`.
- [x] 10.5 `test/plugin-load.test.ts`: вклад предиката с полем `native` отказывает при загрузке, называя `predicates.0.native` (сценарий «Плагин пытается объявить внутреннюю форму»).
- [x] 10.6 `test/plugin-tree.test.ts`: отключение строки `predicates` проверено настоящим путём состава — `enabled: false` в проектном патче через `resolveConfig`/`loadPlugins`, — и освободившееся имя занимает плагин; заголовок и комментарий теста о занятом имени больше не называют причиной снятый резерв.
