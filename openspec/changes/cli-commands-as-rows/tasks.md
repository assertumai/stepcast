## 1. Закрепить нынешнее поведение до переезда

- [x] 1.1 Тест справки: печать общей справки дефолтного состава (`stepcast` без аргументов и `stepcast --help`) сверяется с эталоном дословно — порядок команд, описания, флаги. Эталон снимается с нынешнего вывода и после переезда не правится.
- [x] 1.2 Тест кодов возврата: неизвестная команда, неизвестный флаг, лишний позиционный аргумент, отказ команды — коды возврата пишутся в тест как есть, до правок.
- [x] 1.3 `npm run test:only` зелёный на этих проверках.

## 2. Признак встроенной области переезжает в ядро (Решение 7)

- [x] 2.1 `src/core/plugins/load.ts`: `rowScope` помечает заведённую область как область строки встроенного слоя; пометка и её чтение — модульное множество областей, наружу отдан только признак.
- [x] 2.2 `src/core/plugins/kernel.ts`: сервис `commands` признаёт встроенным вклад корневой области **или** помеченной области строки.
- [x] 2.3 `src/parts/pipeline/services.ts`: своё множество `builtinFibers` снято, `partRow` ставит общую пометку (синхронное умолчание на корне продолжает работать по идентичности корня).
- [x] 2.4 `src/ui/screens/registry.ts`: `screenRow` пользуется тем же признаком; собственного множества у витрины не остаётся, если оно дублирует общий.
- [x] 2.5 `npm run typecheck` и `npm run test:only` зелёные: владельцы вкладов прежние.

## 3. Строка команды и перечень (Решения 1, 2, 5)

- [x] 3.1 Завести помощник объявления строки команды (`src/cli/rows.ts`): `id` = `command-<имя вклада>`, объявленные `inject` и признак независимости; тело строки регистрирует ровно объявленный вклад через `rowScope`.
- [x] 3.2 Каждый модуль `src/cli/commands/*.ts` экспортирует `row`: описание аргументов (`CommandSpec`) переезжает сюда из литерала `COMMANDS`, рядом с телом и вкладом. Комментарии о встроенности отдельных команд (`decide`, `propose`) переезжают вместе с ними.
- [x] 3.3 `src/cli/rows.ts`: перечень строк команд тем же порядком, каким сегодня идёт `BUILTIN_COMMANDS` (Решение 8), плюс перечень их идентификаторов для семени дерева.
- [x] 3.4 Доменным строкам (18 команд, Решение 3) проставлен `inject` на сервисы движка пайплайнов; ядерным (7) — нет. Классификация перечислена комментарием у перечня, с критерием.
- [x] 3.5 Со вкладов встроенных команд снято поле `inject` (Решение 9); у команды плагина поле и его проверка диспетчером не тронуты.

## 4. Точка входа перестаёт быть источником команд

- [x] 4.1 `src/cli/main.ts`: литералы `COMMANDS` и `BUILTIN_COMMANDS` сняты; точка входа подаёт `resolveConfig` идентификаторы строк команд, а `loadPlugins` — сами строки (`builtinRows`), как это делает витрина.
- [x] 4.2 Ранний путь независимых команд берёт вклад из объявления строки (Решение 5); `buildIndependentCommandEnv` и его отказ не меняются.
- [x] 4.3 `src/cli/commands/plugins.ts`: `runPluginsCommandAfterLoadFailure` принимает строки команд вместо их вкладов; печать дерева не трогается.
- [x] 4.4 `src/parts/load.ts` и `src/parts/builtin.ts`: `DefaultLoadOptions.builtinCommands` снято, `createKernelShell`, `createBuiltinKernel` и `builtinRegistry` теряют параметр команд; вызывающие (`scripts/generate-schema.ts`, `core/lint.ts`, `core/run/runner.ts`, тесты) правятся компилятором.
- [x] 4.5 `test/job-data.test.ts` и прочие, импортировавшие `COMMANDS`, читают описание команды из её модуля.

## 5. Отказ об отсутствующей команде (Решение 10)

- [x] 5.1 `src/cli/main.ts`: до разбора аргументов имя команды сверяется с объявлениями строк перечня и итогами строк (`RowOutcome`); имя, объявленное строкой, которой в составе нет, даёт `StepcastError` с командой, строкой, её состоянием и подсказкой про `stepcast plugins`.
- [x] 5.2 Прочие неизвестные имена отказывают прежним «Неизвестная команда: …» со справкой; код возврата в обоих случаях прежний.
- [x] 5.3 Тесты: команда, отключённая патчем; команда, снятая вместе со строкой движка пайплайнов; имя, не объявленное никем, — три разных ответа, один код возврата.

## 6. Независимые команды и их неприкосновенность (Решения 4, 6)

- [x] 6.1 Строки `command-data`, `command-down`, `command-init` объявлены независимыми; перечень независимых команд выводится из объявлений строк, а `CONFIG_INDEPENDENT_COMMANDS` как отдельный список имён снят.
- [x] 6.2 Загрузка отказывает названно, если патч состава заменил или отключил строку независимой команды; отказ рождается там же, где точка входа подаёт свой перечень строк, — внутри загрузки, чтобы `stepcast plugins` печатал дерево с причиной.
- [x] 6.3 `test/cli-config-independence.test.ts`: граница «независимая либо зависимая» проверяется по объявлениям строк; прежние сценарии сломанной конфигурации не правятся.
- [x] 6.4 Тесты на 6.2: патч на независимую строку — отказ у зависимой команды, работа у самой независимой, печать дерева у `stepcast plugins`.

## 7. Таблица декларативной формы — параметр сборки (Решение 11)

- [x] 7.1 `src/core/plugins/load.ts`: таблица ключей декларативной формы приходит опцией обхода; импорта `DECLARATIVE_CONTRIBUTION_FIELDS` в ядре плагинов не остаётся.
- [x] 7.2 `src/parts/load.ts`: состав дефолта подаёт нынешнюю таблицу целиком.
- [x] 7.3 Ключ декларативной формы, которого таблица не называет, отказывает по имени, называя ключ и плагин; тест на это.
- [x] 7.4 `test/plugins-load.test.ts`: сверка таблицы со схемой разбора продолжает действовать по новому адресу таблицы.
- [ ] 7.5 ~~`eslint.config.js`: граница ядра плагинов подтянута под снятый импорт; `test/eslint-config.test.ts` под новый состав блоков.~~ Снято как ненужное (находка ревью): подтягивать нечего. Запреты блока `src/core/plugins/**` — группы чужих деревьев (`**/pipeline/**`, `**/parts/**`, `**/ui/**` и прочие), импорта соседа по собственному каталогу они не касались ни до правки, ни после; `src/core/plugins/load.ts` по-прежнему импортирует `StepcastPluginSchema` из `./pipeline-contract.js` значением, и снятие импорта таблицы состава границ конфига не сдвинуло. Оба файла не изменены.

## 8. Осмотр состава

- [x] 8.1 Проверить, что вклады команд приписываются своим строкам окном/фибером без правок `introspect.ts`; править только если правило приписывания их не покрывает.
- [x] 8.2 Тесты осмотра (`stepcast plugins`, текст и `--json`): строки команд на месте, раздел вкладов вне строк команд не содержит, номер формы прежний.
- [x] 8.3 Проверить дерево витрины: строк команд в нём нет, осмотр демона не изменился.

## 9. Документация

- [x] 9.1 `docs/plugins.md`: абзац «Встроенные команды CLI строками дерева не становятся» переписан — команда становится строкой `command-<имя>`, заменяется и отключается патчем, доменные команды снимаются вместе со строкой движка; названы перечень строк команд и правило одного вклада на строку.
- [x] 9.2 `docs/plugins.md`, раздел об исключении трёх команд: откуда берётся их вклад до разрешения конфигурации и почему составу они не принадлежат (Решение 6), с ответом «заведите свою команду под своим именем».
- [x] 9.3 `docs/plugins.md`: отказ об отсутствующей команде — что именно печатается вместо «неизвестной команды».
- [x] 9.4 `docs/microkernel-target.md`: шаг 9 отмечен выполненным с уточнениями (перечень команд — строки вызывающего, а не `src/parts/rows.ts`; закрыт открытый вопрос «Стоимость отказа»); названы остатки шага 10 — переезд `src/cli/commands/**` в `src/parts/pipeline/commands/` и переезд `pipeline-contract.ts`.

## 10. Проверка

- [x] 10.1 Справка и коды возврата совпадают с эталонами задач 1.1 и 1.2 без правки эталонов.
- [x] 10.2 `npm run check` зелёный целиком.
- [x] 10.3 `openspec validate cli-commands-as-rows --strict` проходит.

## 11. Правки по ревью

- [x] 11.1 `test/cli-commands-rows.test.ts`: сценарий дельты «Патч заменяет строку встроенной команды» — замена `command-widgets` модулем пользователя, вносящим одноимённую команду (исполняется тело пользователя, конфликта имён нет, справка называет его описание), и замена модулем без такой команды (названный отказ ветки «заменена патчем состава, не принёсшим этой команды»).
- [x] 11.2 Там же: сценарий «Патч отключает строку одной команды» — отключение ровно одной строки, ядерной и доменной; перечень команд отличается от дефолтного ровно на блок снятой команды, соседняя команда исполняется и доходит до собственного тела.
- [x] 11.3 Там же: сценарий «Строка плагина вставлена перед строкой команды» — патч с `before: command-run`, приносящий свою команду; она стоит в справке перед первой командой дефолта, прочий порядок не изменился, тело исполняется.
- [x] 11.4 `src/core/config/resolve.ts`: `protectIndependentRows` возвращает встроенную идентичность только тому месту, которое защищённая строка занимает (первому вхождению `id`). Каталожная строка, дописанная в дерево со своим `reservedNameFailure`, сохраняет свою причину, свой путь и свой `source` — поведение независимых команд совпало с поведением остальных двадцати двух. Тест: каталог `.stepcast/plugins/command-init/` и `.stepcast/plugins/command-run/` дают один и тот же текст отказа.
- [x] 11.5 `src/core/plugins/load.ts`: регистрация в `toContextPlugin` идёт по ключам поданной таблицы (`Object.values(fields)`), а перечень, выведенный от схемы, остаётся источником одной только проверки на неизвестный ключ; тест на то, что ключ таблицы, которого схема не знает, попадает и в `inject`, и в регистрацию.
- [x] 11.6 `docs/plugins.md`: каталог плагина, названный идентификатором строки независимой команды, отказывает общим правилом встроенных строк, а не защитой строки от состава.
- [x] 11.7 Задача 7.5 переписана: она не выполнена, а снята как ненужная, с причиной.
