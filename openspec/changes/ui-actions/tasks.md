## 1. Меню экранов

- [x] 1.1 Завести `MENU` в `src/ui/routes.ts` — общий с разбором адреса перечень экранов
- [x] 1.2 Научить `parseRoute` адресам `/settings` и `/cleanup`; хвост за именем экрана адресом экрана не признаётся
- [x] 1.3 Меню в шапке `ui/src/App.tsx`: текущий экран отмечен подчёркиванием и `aria-current`, Cmd/Ctrl-клик оставлен браузеру
- [x] 1.4 Страница прогона подсвечивает пункт «Пайплайны» и сама пунктом меню не является
- [x] 1.5 Тесты `test/ui-routes.test.ts`: адрес каждого пункта разбирается в его же экран, хвост за именем экрана даёт первый экран

## 2. Договор витрины с демоном

- [x] 2.1 Описать в `ui/src/api.ts` формы `Settings`, `SettingsPatch`, отбора (`RunSelection`, `RunCandidate`) и исхода удаления (`RemovalSummary`), сверив с `src/ui/settings.ts` и обработчиками `src/ui/server.ts`
- [x] 2.2 Вызовы `fetchSettings`, `saveSettings`, `selectRuns`, `deleteRun`, `deleteRuns`
- [x] 2.3 Снять из шапки `api.ts` оговорку, что настройки и удаление в витрину не входят

## 3. Экран настроек

- [x] 3.1 `ui/src/pages/Settings.tsx`: агент выбором из объявленных бэкендов, модель полем, путь правимого файла и происхождение каждого значения
- [x] 3.2 Пустая модель уходит как `null` — снятие значения, а не пустая строка
- [x] 3.3 Ответ `PUT` принимается целиком: экран показывает перечитанные настройки, а не эхо правки
- [x] 3.4 Отказ демона показан его же текстом, прежние значения остаются
- [x] 3.5 Список бэкендов с командой, моделью бэкенда и признаком выключенного

## 4. Экран уборки

- [x] 4.1 `ui/src/pages/Cleanup.tsx`: признаки, срок и проект; отбор без условий не запрашивается и объясняет почему
- [x] 4.2 Отбор показывает список, число и суммарный объём до всякого удаления
- [x] 4.3 Удаление идёт по списку адресов из показанного отбора, а не по признаку
- [x] 4.4 Исход по каждому адресу, включая отклонённые; освобождённый объём в шапке
- [x] 4.5 Всякая правка условий обесценивает показанный отбор

## 5. Удаление прогона из списка

- [x] 5.1 Кнопка в строке `ui/src/pages/Pipelines.tsx`, подтверждение в два нажатия, отмена возвращает строку к обычному виду
- [x] 5.2 Строка уходит с экрана живым потоком: демон пересобирает обзор сам, список на стороне браузера не правится
- [x] 5.3 Отказ демона — целой фразой во всю ширину строки, а не обрезком в колонке действий

## 6. Вёрстка

- [x] 6.1 Полоса содержимого по центру окна (`margin: 0 auto`)
- [x] 6.2 Строка прогона — сетка с общими треками; числовые колонки по правому краю
- [x] 6.3 Узкое окно: строка переносится потоком, страница не прокручивается вбок
- [x] 6.4 Тени у краёв прокрутки графа, обе темы
- [x] 6.5 Поля форм, флажки и опасные кнопки в общей палитре

## 7. Форматирование длительности

- [x] 7.1 Перенести правила форматирования в общий с демоном `src/ui/format.ts`; `ui/src/format.ts` остаётся путём импорта
- [x] 7.2 Открыть модуль дев-серверу (`vite.config.ts`) и типизации витрины (`ui/tsconfig.json`)
- [x] 7.3 Округление один раз, до секунд; разряды считаются из округлённого, переполнение переносится в старший
- [x] 7.4 Тест `test/ui-format.test.ts` на обе границы разрядов и на прочерк вместо нуля

## 8. Проверка

- [x] 8.1 `npm run check` зелёный целиком
- [ ] 8.2 Сценарии спеки пройдены человеком по экрану: настройки записываются, отбор показывает объём до удаления, группа удаляется одним подтверждением, живой прогон отклоняется с названной причиной
