## 1. Ключ `project.nested_repos` в конфигурации

- [x] 1.1 `src/core/config/schema.ts`: `RawProjectSchema` получает `nested_repos` — непустой список, элемент собран из `RelativeRepoPathSchema` с дополнительной проверкой на отсутствие символов шаблона глоба (`*`, `?`). Проверка пишется поверх переиспользуемой модели, а не копией её тела.
- [x] 1.2 Комментарий у ключа объясняет то, чего из модели не видно: почему шаблон здесь запрещён (по значению заводится отдельная часть якоря и им же префиксуются пути — открывать шаблон нечем), почему пустой список отклоняется и почему у ключа, в отличие от соседей по секции, нет пайплайнового слоя.
- [x] 1.3 Нормализация состава: отбрасывание завершающего разделителя, отбрасывание повторов, приведение к одному порядку сортировкой. Место — там же, где значение читается в модель конфигурации, чтобы дальше по коду состав был уже каноническим.
- [x] 1.4 `src/core/config/resolve.ts`: `Config['project']` получает `nestedRepos: readonly string[] | undefined`, читается из `values.get('project.nested_repos')` той же проверкой на массив строк, что `tools` и `editPaths`.
- [x] 1.5 Убедиться, что `PROJECT_ONLY_KEYS` править не нужно — шаблон `project.**` уже покрывает новый ключ; закрепить тестом 1.8.
- [x] 1.6 `src/cli/commands/config.ts`: список печатается именами через запятую — третьим к `project.tools` и `project.edit_paths`.
- [x] 1.7 Тесты `test/config.test.ts`: объявление даёт канонический состав; необъявленный ключ — не ошибка и `undefined`; `[]`, `["  "]`, `["/srv/site"]`, `["../site"]`, `["site/*"]` и `["site/**"]` отклоняются с ошибкой, называющей файл и ключ; `["public-site/", "public-site", "vendor/sdk"]` даёт `["public-site", "vendor/sdk"]`.
- [x] 1.8 Тест: `project: { nested_repos: [public-site] }` в `~/.stepcast/config.yml` отклоняется тем же правилом и тем же сообщением, что `project.check` там же.
- [x] 1.9 Тест отчёта `stepcast config`: объявленные каталоги напечатаны именами и с источником.

## 2. Ключ не имеет пайплайнового слоя и не публикуется подстановкой

- [x] 2.1 `src/core/pipeline/schema.ts`: `ProjectSchema` документа пайплайна ключ **не** получает — убедиться, что `.strict()` отклоняет его как незнакомый, и что сообщение называет ключ. Если сообщение ключа не называет, добавить его явным отказом рядом с разбором секции.
- [x] 2.2 Убедиться, что `PROJECT_NAMES` и `resolveProjectValues` в `src/core/pipeline/expand.ts` не меняются: обращение к `${project.nested_repos}` обязано отказывать действующим сообщением о составе пространства. Ничего не менять — закрепить тестом 2.4.
- [x] 2.3 Перегенерировать `schema/config.schema.json` (`npm run schema`); убедиться, что `project.nested_repos` описан массивом с `minItems: 1`, элемент — строка с `minLength: 1` и образцом, отклоняющим пустую строку (проверки на относительность, `..` и шаблон в публикуемую схему не переносятся — как у `spec.dir`). Убедиться, что `schema/pipeline.schema.json` не изменилась.
- [x] 2.4 Тесты: `project: { nested_repos: [...] }` в документе пайплайна роняет разбор с указанием ключа; `${project.nested_repos}` роняет разбор перечнем доступных имён пространства, среди которых этого имени нет.
- [x] 2.5 Убедиться, что `test/schema-generated.test.ts` проходит и ловит расхождение.

## 3. Составной способ фиксации

- [x] 3.1 `src/core/anchor/types.ts`: `AnchorKind` пополняется `'composite'`; `restorable()` остаётся истинным только для `'git'`, комментарий у неё дополняется вторым случаем — почему составное состояние чужой каталог не засевает.
- [x] 3.2 Новый файл `src/core/anchor/composite.ts`: `createCompositeAnchorer({ dir, stateDir, scope, nested })` держит `createGitAnchorer` на корень и по одному на каждый объявленный каталог; индексные файлы частей различаются в пределах работы (`<scope>.<n>.index`), `dispose()` освобождает все.
- [x] 3.3 `capture()`: снимает частные состояния корня и всех частей, склеивает идентификатор `<отпечаток состава>+<корень>+<часть>…` в порядке канонического состава. Отпечаток состава — первые 12 знаков `sha256` от состава, склеенного переводами строк.
- [x] 3.4 Разбор идентификатора обратно: функция, возвращающая отпечаток и части, — она нужна и `changedPaths`, и `diff`, и `restorePaths`. Идентификатор чужой формы (не составной, испорченный, с другим числом частей) — несравнимость с причиной, а не исключение.
- [x] 3.5 `changedPaths(from, to)`: разные `kind` — действующая формулировка; разные отпечатки состава — несравнимость с причиной, называющей действующий состав; иначе — `diff-tree` корня плюс `diff-tree` каждой части с приписанным префиксом её каталога, всё вместе отсортировано.
- [x] 3.6 `diff(from, to)`: патч корня плюс патчи частей с `--src-prefix=a/<каталог>/ --dst-prefix=b/<каталог>/`, в том же порядке; всё пустое — `undefined`.
- [x] 3.7 `restore(anchor)`: отказ `StepcastError` с причиной про собственные базы объектов вложенных репозиториев — по образцу отказа хеш-манифеста.
- [x] 3.8 `restorePaths(anchor, paths)`: маршрутизация по самому длинному совпавшему объявленному префиксу, префикс отрезается; путь без совпадения и путь, равный каталогу части ровно, идут в корень. Пустой перечень для части — не вызывать её вовсе.
- [x] 3.9 `src/core/anchor/index.ts`: `detectAnchorKind(dir, nested)` возвращает `'composite'` при непустом составе; `AnchorerOptions` получает `nested`; `createAnchorer` разводит три способа. На пустом составе поведение обеих функций прежнее.
- [x] 3.10 Тесты `test/anchor.test.ts` на стенде с вложенным репозиторием: правка внутри части меняет якорь; правка в игнорируемом *корнем* вложенном репозитории меняет якорь; правка в игнорируемом *самой частью* файле — не меняет; необъявленный вложенный репозиторий якорь не меняет; порядок объявления на идентификатор не влияет; `changedPaths` даёт путь с префиксом; составы разного размера и разного содержания несравнимы с причиной; составное и одиночное состояния несравнимы; `diff` содержит путь с префиксом и склеивает изменение корня с изменением части; `restore` отказывает; `restorePaths` восстанавливает файл внутри части; вложенные друг в друга объявленные каталоги (`a`, `a/b`) маршрутизируются по длинному префиксу; обе стороны чужого состава несравнимы (сверка ведётся с отпечатком самого якоря, а не между сторонами), восстановление по такому состоянию отказывает, а git-якорь в составном и составной в git-якоре несравнимы без исключения из git.

## 4. Предстартовые проверки и режимы рабочей директории

- [x] 4.1 `src/core/run/workspace.ts`: `checkWorkspaceAvailability` получает состав и отказывает при объявленном составе и работе в режиме `worktree` или `copy` — сообщение называет работу, режим и причину (изолированное дерево частей не содержит, и якорь там снова стал бы однорепозиторным).
- [x] 4.2 Там же — проверка самого состава: корень является рабочим деревом git; каждый объявленный каталог существует, является рабочим деревом git, является деревом собственного репозитория (`rev-parse --show-toplevel` равен ему самому) и имеет хотя бы один коммит, если корень его не игнорирует (иначе корневой `add -A` отказывает целиком, и якорь не снимается вовсе). Каждая причина — своё сообщение.
- [x] 4.3 `src/core/run/preflight.ts`: состав доезжает до проверок (`PreflightOptions` или через уже передаваемую конфигурацию — выбрать то, что не тянет конфигурацию туда, где её нет).
- [x] 4.4 `src/core/lint.ts`: та же диагностика статически — по образцу уже живущих там отказов пригодности режима, с файлом работы и путём `jobs.<id>.workspace.mode`.
- [x] 4.5 Тесты `test/workspace.test.ts` и `test/lint.test.ts`: каждый отказ срабатывает до первой работы и называет своё; часть без коммита, игнорируемая корнем, отказа не вызывает; пригодный состав в режиме каталога запуска прогону не мешает; без объявленного состава изолированные режимы доступны как прежде.

## 5. Прогон, возобновление и сравнение

- [x] 5.1 `src/core/run/runner.ts`: `detectAnchorKind` и создание якоря работы получают состав из конфигурации; `anchorKind` по-прежнему считается один раз на прогон.
- [x] 5.2 `src/core/journal/schema.ts`: `anchor_kind` в `StepRecordSchema` и в `RunManifestSchema` пополняется значением `composite`; манифест получает необязательное `nested_repos` — канонический состав прогона.
- [x] 5.3 `runner.ts`: состав пишется в манифест рядом с `anchor_kind`.
- [x] 5.4 `src/core/run/resumePlan.ts`: якорь плана создаётся с составом; несовпадение состава исходного прогона с сегодняшним даёт «сравнить нечем» с причиной, называющей оба состава (сегодняшний — из конфигурации, прошлый — из манифеста исходного прогона), и все шаги признаются невалидными по этой причине, а не по изменению дерева.
- [x] 5.5 Убедиться, что `src/core/anchor/fingerprint.ts` править не требуется: объявленные и наблюдённые входы читаются относительно корня дерева, и путь с префиксом разрешается сам собой. Ничего не менять — закрепить тестом 5.8.
- [x] 5.6 `src/cli/commands/diff.ts`: якорь сравнения создаётся с составом из конфигурации; несравнимость по составу доезжает до вывода `describeComparison` пометкой, а не молчанием.
- [x] 5.7 Убедиться, что `src/core/run/apply.ts` и `src/cli/commands/merge-lanes.ts` не трогаются: составной якорь допускает только режим каталога запуска, где накладывать нечего.
- [x] 5.8 Тесты `test/resume.test.ts`: правка во вложенном репозитории обесценивает шаг с указанием пути с префиксом; правка вне объявленных входов работы — не обесценивает; возобновление прогона другого состава исполняет пайплайн с начала с причиной про состав — в том числе когда его шаг дерево изменил и разбор произведённых им путей доходит до сравнения якорей; восстановление дерева заходит внутрь части; `--dry-run` называет путь целиком.
- [x] 5.9 Тест `test/diff.test.ts`: два прогона разного состава дают помеченную несравнимость деревьев, а сравнение промптов и контекста выполняется; составные прогоны, разбираемые якорем дерева без объявленного состава, помечены несравнимыми, а не роняют команду ошибкой git.

## 6. Сквозной сценарий вложенной правки

- [x] 6.1 Стенд: дерево с корневым репозиторием и вложенным `public-site`, игнорируемым корнем; работа в режиме каталога запуска; шаг с `changed_only: ["src/**"]`, который правит `public-site/src/api.ts`.
- [x] 6.2 Проверка: без объявления шаг проходит (сегодняшнее поведение), с объявлением — жёсткий предикат не проходит, а в объяснении назван `public-site/src/api.ts`. Убедиться, что первая половина падает до правки движка ровно в той части, где ожидается отказ.
- [x] 6.3 Проверка того же прогона: `diff.patch` шага содержит изменение по пути с префиксом, а `tree_id` на начало и конец шага различаются. Обратная половина сопоставления: `changed_only: ["public-site/**"]` — обычный шаблон совпадает с путём части, и предикат проходит.
- [x] 6.4 Проверка однорепозиторной регрессии: прогон в дереве без объявления даёт те же идентификаторы, тот же способ фиксации и ту же форму записей шага, что до изменения.

## 7. Документы

- [x] 7.1 `docs/config.md`: раздел «Вложенные репозитории» рядом с «Границы правок» — что объявляется, почему конкретный каталог, а не шаблон, почему только в `.stepcast/config.yml` и без пайплайнового слоя, почему не публикуется подстановкой, и что границы такого дерева объявляются `edit_paths` записями `public-site/**` (а при отслеживаемой корнем ссылке — и `public-site`).
- [x] 7.2 `docs/run-layout.md`: блок «Якорь снимается с одного репозитория» заменяется описанием составного якоря — форма идентификатора, префикс путей, склейка патча — и двумя объявленными ограничениями: составное состояние чужой каталог не засевает, изолированные режимы при объявленном составе отклоняются.
- [x] 7.3 `openspec/changes/nested-repo-anchor/status.md` заполняется **вместе с реализацией**, а не раньше: `npm run status:check` сравнивает `docs/status.md` со сборкой побайтно и входит в `npm run check`, а работа, заводящая описание изменения, `docs/**` не правит по объявленным границам — фрагмент без пересборки оставил бы дерево красным между заведением описания и реализацией. Поэтому файл сейчас пуст: пустой фрагмент сборку не меняет.
- [x] 7.4 Состав фрагмента: раздел «Известные ограничения: снято» — ровно один лид, `Якорь снимается с одного репозитория.` (сегодняшний блок `docs/status.base.md`); раздел «Работает» — три пункта с ключами «Составной якорь: вложенные репозитории объявляются и считаются» (объявление, третий способ фиксации, форма идентификатора), «Пути вложенных репозиториев приходят с префиксом и сопоставляются как обычно» (сложение путей, нетронутые `changed_only` и отпечаток входов, склейка `diff.patch`, возобновление) и «Непригодный состав и несравнимое состояние объявляются заранее» (предстартовые отказы состава, несравнимость по составу и по способу, состав в манифесте); раздел «Известные ограничения» — три блока с лидами «Многорепное дерево работает только в каталоге запуска.», «Составное состояние не засевает чужой каталог.» и «Смена состава обесценивает возобновление целиком.».
- [x] 7.5 Пересобрать `docs/status.md` командой `npm run status:build`; руками собранный документ не править.
- [x] 7.6 `npm run check` зелёный.
