## 1. Падающие тесты до правки

- [x] 1.1 В `test/ui-server.test.ts` завести проект с плагином на диске (по образцу `withPlugin` из `test/cli-plugins.test.ts`: `.stepcast/config.yml` с `plugins: ["./plugins/<имя>.mjs"]` и модуль рядом) и тест: пайплайн с `expect: [{ <предикат плагина>: … }]` раскрыт — `/api/pipelines` отдаёт работы и шаги, поле `error` отсутствует. Тест обязан падать до правки.
- [x] 1.2 Тот же предикат в `until.check` работы — второй тест того же вида.
- [x] 1.3 Тест на границу реестра: предикат объявлен плагином одного проекта, а тот же ключ стоит в пайплайне соседнего проекта без этого плагина — первый раскрыт, второй показан ошибкой о неизвестном ключе.
- [x] 1.4 Тест на модель: шаг без объявленной модели, агент — бэкенд плагина, модель даёт умолчание плагина; карточка шага показывает эту модель и слой `backend`.

## 2. Реестр на проект в раскрытии витрины

- [x] 2.1 В `src/ui/pipelines.ts` заменить в `projectSection` вызов `resolveConfig` на `resolveWithPlugins({ cwd: projectPath, home }, { projectRoot: projectPath })` и вернуть собранный `Registry` рядом с секциями `project`, `defaults`, `backends` и `modelConfigFile` (`ProjectOverrides`). `builtinCommands` не передавать: вклады команд витрине не нужны.
- [x] 2.2 Передать реестр в `expandPipeline` из `readPipeline` и сделать `buildPipelines` асинхронной; обход проектов оставить последовательным, порядок карточек прежним (design.md, Решение 5).
- [x] 2.3 В `src/ui/server.ts` сделать обработчик `/api/pipelines` асинхронным по образцу прочих `void handle…`, сохранив прежнюю форму ответа и ответ пустым списком при `config === undefined`.
- [x] 2.4 Поправить прямые вызовы `buildPipelines` в `test/schema-diagnostics.test.ts` на ожидание промиса.

## 3. Кеш реестров у демона

- [x] 3.1 Завести кеш «корень проекта → { объявления, реестр }» с ключом из `pluginDeclarations(resolved)` — спецификатор и файл объявления в порядке объявления (design.md, Решение 3). Кешировать только успешно собранный реестр.
- [x] 3.2 Создавать кеш в `createUiServer` и передавать в `buildPipelines` опцией; модульного синглтона не заводить — тесты поднимают несколько серверов в одном процессе.
- [x] 3.3 Тест: после добавления объявления плагина в конфигурацию проекта следующий запрос `/api/pipelines` раскрывает документ с его предикатом, демон не перезапускался.
- [x] 3.4 Тест: два проекта с разными плагинами раскрыты каждый своим реестром, и повторный запрос даёт тот же ответ.

## 4. Отказ загрузки как карточка проекта

- [x] 4.1 Свести отказ загрузки плагина к уже существующей ветви `failure` в `buildPipelines`: ошибка `loadPlugins`/`addPlugin` — это `StepcastError` с `file`, `at: 'plugins'` и подсказкой, и `errorView` показывает её тем же составом полей, что нечитаемую конфигурацию проекта.
- [x] 4.2 Убедиться, что обход продолжается: отказ одного проекта не прекращает цикл по `listProjects` и не роняет ответ демона.
- [x] 4.3 Тест: объявлен несуществующий модуль — пайплайны проекта показаны карточками с причиной, файлом объявления и строкой спецификатора.
- [x] 4.4 Тест: конфликт имён вкладов (плагин объявляет занятое имя предиката) — карточка с текстом отказа, файлом объявления и местом `plugins`.
- [x] 4.5 Тест: при сломанном плагине одного проекта пайплайны соседнего исправного проекта раскрыты, а витрина продолжает отвечать.
- [x] 4.6 Тест: исправленное объявление действует следующим запросом — отказ не закеширован.

## 5. Документация

- [x] 5.1 `docs/plugins.md`: витрина раскрывает пайплайны реестром вкладов проекта, которому файл принадлежит; отказ загрузки показывается карточкой проекта, а не гасит экран, — и почему это не совпадает с поведением CLI, где отказ прекращает команду.
- [x] 5.2 `docs/plugins.md`, «Известные ограничения»: изменённый код уже загруженного плагина действует только после `stepcast down && stepcast up`; модуль, упавший при исполнении, Node запоминает упавшим. Там же уточнить существующую строку «витрина не открыта»: витрина по-прежнему не принимает вкладов, но вкладами пользуется.
- [x] 5.3 `docs/plugins.md`, предупреждение «плагин — код с правами процесса движка»: назвать, что этот код исполняется и в фоновом демоне витрины, по запросу к ней, и в каких рамках (design.md, Решение 6).
- [x] 5.4 `docs/run-layout.md`, раздел «Витрина»: карточка проекта, чей плагин не загрузился, и то, что пайплайн с плагинным предикатом раскрывается наравне с `stepcast lint`.

## 6. Правки по ревью

- [x] 6.1 Отказ по конфликту имён вкладов доносит место объявления: `loadPlugins` дописывает `file` и `at: 'plugins'` ошибке реестра, который про конфигурацию не знает. Покрыто тестом витрины (карточка с `errorFile`, `errorAt`, `errorHint`) и тестом CLI (строка «где:»).
- [x] 6.2 Файл вне корня проекта (плагин объявлен глобальным `~/.stepcast/config.yml`) назван в карточке абсолютным путём, а не цепочкой `../../..`; решение записано в `design.md` (Решение 4), в дельта-спеке `ui-dashboard` и в `docs/run-layout.md`, поведение закреплено тестом.
- [x] 6.3 Тест на инвариант «кешируется реестр, но не конфигурация»: правка `defaults.model` при неизменном списке объявлений видна следующим запросом.
- [x] 6.4 Тесты на границу загрузки (дельта-спека `ui-daemon`): проект без файлов пайплайнов и проект без объявленных плагинов не приводят ни к одному импорту (плагин-отметка на диске), а обход и ответ не трогают файлов журнала.
- [x] 6.5 `resolveWithPlugins`: готовый реестр и опции загрузки разведены типом — пара `{ builtinCommands, registry }` больше не компилируется вместо того, чтобы молча вернуть реестр без команд.
- [x] 6.6 Сверка ключа кеша делается только при попадании: лишнего разрешения конфигурации на промахе нет, объявления для новой записи берутся из второго прохода.

## 7. Проверка

- [x] 7.1 `npm run check` зелёная целиком.
- [x] 7.2 `openspec validate ui-pipelines-registry --strict` проходит (`npx openspec validate …` — «Change 'ui-pipelines-registry' is valid»).
