## 1. Объявление практики памяти

- [x] 1.1 Добавить в `schema/config.schema.json` секцию `project.knowledge` с ключами `provider`, `command`, `dir`, `rules`, `index_max_tokens`, `stale_after`, `timeout`
- [x] 1.2 Разобрать секцию в `src/core/config`: модель пути для `dir` и `rules` (та же, что у `project.spec`), отказ от `provider: cmd` без `command`, отказ секции в `~/.stepcast/config.yml`
- [x] 1.3 Опубликовать подстановки `${project.knowledge.dir|rules|provider}` там же, где публикуются `${project.spec.*}`
- [x] 1.4 Принять секцию верхним ключом `project` документа пайплайна с полистовым слиянием
- [x] 1.5 Тесты разбора: область объявления, модель пути, слои, отказы — в `test/config.test.ts`

## 2. Контракт источника знания

- [x] 2.1 Завести `src/core/knowledge` с типами запроса и ответа четырёх глаголов и схемой ответа
- [x] 2.2 Реализовать вызов источника: запуск командой движком, JSON на stdin/stdout, таймаут `project.knowledge.timeout`, отказ шага при ненулевом коде, невалидном ответе или таймауте
- [x] 2.3 Реализовать `provider: cmd` — запуск объявленной команды с глаголом первым аргументом
- [x] 2.4 Тесты контракта на источнике-заглушке: успешный отбор, отказ по коду, отказ по схеме, отказ по таймауту

## 3. Запись контекста knowledge

- [x] 3.1 Расширить схемы `pipeline`, `job` и запись контекста пятой формой: `index`, `{scope}`, `{id}`, необязательный `budget`
- [x] 3.2 Разрешать записи знания в `src/core/context` до запуска шага, сохраняя место записи в порядке сборки по уровню и позиции объявления
- [x] 3.3 Применять к возвращённому `context.deny`, `inline_threshold` и `context.max_tokens`; исключение по `deny` отмечать, а не молчать
- [x] 3.4 Учесть наследование: `context_inherit: false` отменяет записи знания, `context_exclude` к ним не применяется
- [x] 3.5 Отразить записи знания в `context.json` идентификатором, происхождением и уровнем объявления; расширить схему отчёта совместимостью снизу вверх
- [x] 3.6 Отклонять в `stepcast lint` неизвестную форму селектора и запись `knowledge:` при необъявленном источнике
- [x] 3.7 Тесты в `test/context.test.ts` и `test/lint.test.ts`: три формы, порядок, бюджет, `deny`, наследование, отказы линта

## 4. Встроенный источник fs

- [x] 4.1 Разбор единицы знания: шапка YAML (`id`, `title`, `scope`, `status`, `anchors`), тело Markdown, отказ на неполной шапке
- [x] 4.2 Глагол `index`: сборка производного оглавления из шапок, включение документов `project.spec.dir` с областью по их каталогу, проверка против `index_max_tokens`
- [x] 4.3 Глагол `select`: пересечение областей теми же правилами глоба, что у путей контекста, детерминированный порядок, отбор по `id`, учёт `budget`, исключение `status: superseded` кроме поимённого запроса
- [x] 4.4 Глагол `check`: несуществующий якорь и превышение индекса — красным, изменённый после ревизии — жёлтым, просроченное жёлтое сверх `stale_after` — красным, дубль области с совпадающим заголовком — предупреждением
- [x] 4.5 Глагол `write`: сборка шапки, подстановка ревизий якорей из `git log`, отказ с неоставлением файла при непрохождении собственного `check`
- [x] 4.6 Тесты источника на временном репозитории: разбор, индекс и его предел, детерминизм отбора, три уровня дрейфа, инвалидация, запись и откат записи

## 5. Команда stepcast knowledge

- [x] 5.1 Завести `src/cli/commands/knowledge.ts` с подкомандами `index`, `select`, `check`, `write`; код возврата `check` отражает исход
- [x] 5.2 Печать оглавления человеку и `--json` для машинного чтения
- [x] 5.3 Тесты `test/cli-knowledge.test.ts`: работа в произвольном каталоге репозитория, коды возврата, отказ при необъявленном источнике

## 6. Предикат knowledge_valid

- [x] 6.1 Реализовать предикат в `src/core/expect`: вызов `check`, провал только на красных нарушениях, отчёт с идентификаторами
- [x] 6.2 Отклонять предикат в `stepcast lint` при необъявленном источнике
- [x] 6.3 Тесты в `test/expect.test.ts`: пройден, провален красным, пройден при жёлтом, отказ линта

## 7. Разворачивание практики

- [x] 7.1 Флаг `stepcast init --knowledge fs`: каталог знания, единица-образец, файл правил письма, секция конфигурации
- [x] 7.2 Отказ при занятом каталоге или объявленной секции, перезапись только по подтверждению
- [x] 7.3 Тест в `test/init.test.ts`: развёрнутое проходит `stepcast knowledge check` с нулевым кодом

## 8. Документация и петля

- [x] 8.1 Раздел «Практика памяти» в `docs/config.md` рядом с «Практикой спецификации»
- [x] 8.2 Запись контекста `knowledge` в `docs/pipeline-format.md`: три формы, бюджет, наследование, отражение в `context.json`
- [x] 8.3 Документ `docs/knowledge.md`: контракт четырёх глаголов, раскладка источника `fs`, работа вне пайплайна и её ограничения
- [x] 8.4 Проверить `.stepcast/pipelines/self-improve-memory.yml` командой `stepcast lint`; существующий `self-improve.yml` не трогать
- [x] 8.5 Развернуть каталог знания этого репозитория и завести в нём первые единицы из разобранных заходов петли

## 9. По итогам ревью

- [x] 9.1 Источник знания заводить по рабочей директории работы, а не по каталогу запуска: иначе `knowledge_valid` в режиме `worktree` проверяет не то дерево, которое правит шаг
- [x] 9.2 Провести запись петли через `stepcast knowledge write`: агент кладёт описание в каталог черновиков, пишет движок — иначе якоря пишутся без ревизий и устаревание не считается вовсе
- [x] 9.3 Не проверять якоря единиц со `status: superseded`: архив иначе делает гейт вечно красным и выталкивает к удалению, которое инвалидация заводилась заменить
- [x] 9.4 Проверять идентификатор записываемой единицы на контракте: `../../` записал бы за пределы каталога знания мимо всякой проверки
- [x] 9.5 `stepcast init --knowledge fs --force` не переписывает существующий `.stepcast/config.yml`: подтверждение относится к каталогу знания, а не к команде проверки и границам правок
- [x] 9.6 `write` принимает список описаний и читает ввод ключом `--stdin`, а не соглашением `--file -`, которое разбор аргументов не принимает
- [x] 9.7 Различать «истории у пути нет» и «прочитать историю не удалось»: сорвавшийся вызов git молча превращал нарушение в «память цела» — на этом падал через раз собственный тест дрейфа
- [x] 9.8 Тесты на всё перечисленное, включая прогон в режиме `worktree` со сломанной памятью дорожки
