Порядок групп: сперва общие утилиты (формат размера, удаление по списку путей
`RunPaths`), затем три команды по отдельности — каждая независима от других и
проверяется собственными тестами, — затем регистрация в CLI и документация.

## 1. Общие утилиты

- [x] 1.1 Добавить `formatBytes` в `src/core/units.ts` рядом с `formatTokens`/`formatDuration`: человекочитаемый вывод байт (Б/КБ/МБ/ГБ)
- [x] 1.2 Написать тесты `formatBytes` в `test/units.test.ts`: границы округления, нулевое значение
- [x] 1.3 Добавить в `src/core/journal/reader.ts` или новый `src/core/run/cleanup.ts` функцию подсчёта размера директории рекурсивным обходом
- [x] 1.4 Написать тест подсчёта размера на временном дереве с известным числом байт

## 2. `stepcast gc`

- [x] 2.1 Создать `src/core/run/cleanup.ts`: функция отбора прогонов-кандидатов из `listRuns` по возрасту (`finished_at` из `run.json`, откат на `started_at`), используя `parseDuration` для разбора `--older-than`
- [x] 2.2 Реализовать функцию удаления: для каждого выбранного прогона удалить содержимое `RunPaths`, кроме `manifest`, `status`, `usage` — перечисление полей `RunPaths` как источник истины, а не отдельный жёстко заданный список путей
- [x] 2.3 Реализовать режим отчёта (без `--older-than`): список всех прогонов проекта с датой завершения и размером, итоговая сумма, код возврата 0, ничего не удаляется
- [x] 2.4 Реализовать режим удаления (`--older-than <длительность>`): список удалённых прогонов и освобождённого места
- [x] 2.5 Обработать случай отсутствия прогонов: сообщение и код 0 в обоих режимах
- [x] 2.6 Создать `src/cli/commands/gc.ts`, вызывающий `cleanup.ts`, без чтения stdin
- [x] 2.7 Покрыть тестами сценарии спеки `run-cleanup`: отчёт без действия, удаление по порогу, прогоны младше порога не тронуты, возраст прерванного прогона по `started_at`, минимум (`run.json`/`status.json`/`usage.json`) переживает уборку, работа без интерактивного ввода
- [x] 2.8 Покрыть тестом сценарий run-journal «Минимум переживает уборку»

## 3. `stepcast init`

- [x] 3.1 Подготовить содержимое шаблона: `stepcast.yml` с работой `build` (шаг `run`, без агентского бэкенда) и отдельный файл работы `.stepcast/jobs/example.yml`, подключаемый через `uses`
- [x] 3.2 Создать `src/cli/commands/init.ts`: запись обоих файлов в текущий каталог
- [x] 3.3 Реализовать отказ при существующем `stepcast.yml` без `--force`, с сообщением об ошибке конфигурации
- [x] 3.4 Реализовать перезапись при `--force`
- [x] 3.5 Покрыть тестами сценарии спеки `project-init`: файлы созданы, отказ без флага, перезапись с флагом
- [x] 3.6 Написать интеграционный тест: `init` → `expandPipeline` на результат (эквивалент `lint`) не даёт ошибок → `runPipeline` завершается успехом без подмены бэкенда

## 4. `stepcast context`

- [x] 4.1 Создать `src/cli/commands/context.ts`: раскрыть пайплайн через `expandPipeline` с переданными `--input`, найти работу и шаг по `--job`/`--step`
- [x] 4.2 Отклонить вызов, если найденный шаг — `run`, а не `agent`, с объяснением, что командные шаги контекст не собирают
- [x] 4.3 Реализовать чтение выхода предшественника: `resolveRun(..., 'latest')` и `artifacts/<job>.json`, если прогон существует; иначе явная пометка «неизвестно», не ноль
- [x] 4.4 Вызвать `assembleContext` с собранными уровнями и вывести отчёт по четырём уровням и итог, тем же группировками, что в `context.json`
- [x] 4.5 Прокинуть ошибку `expandPipeline` при отсутствующем обязательном входе как есть — тем же сообщением, что у `lint`
- [x] 4.6 Покрыть тестами сценарии спеки `context-preview`: отчёт без прогона, разрез по уровням, ошибка на не переданном обязательном входе, отказ для командного шага, оценка по прошлому прогону, пометка «неизвестно» при отсутствии прогонов

## 5. Регистрация и документация

- [x] 5.1 Зарегистрировать `gc`, `init`, `context` в `COMMANDS` и в диспетчере `src/cli/main.ts` с описаниями и флагами (`--older-than`, `--force`, `--job`, `--step`, `--input`)
- [x] 5.2 Прогнать `npm run check`: typecheck, lint, полный набор тестов
- [x] 5.3 Прогнать вручную: `stepcast init` в пустом временном каталоге → `stepcast lint stepcast.yml` → `stepcast run stepcast.yml` → `stepcast gc` (отчёт) → `stepcast gc --older-than 0s` → `stepcast context --job build --step <id>` (или отказ для командного шага, если пример состоит из них)
- [x] 5.4 Обновить `docs/status.md`: перенести `stepcast gc`, `init`, `context` из «Пока нет» в «Работает», оставить `ui` в «Пока нет»
- [x] 5.5 Сверить `docs/run-layout.md` и `docs/pipeline-format.md` с фактическим поведением и поправить расхождения, если они появились в реализации
