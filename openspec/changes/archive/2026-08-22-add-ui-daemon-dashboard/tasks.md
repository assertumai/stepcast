Порядок групп: сперва чтение журнала (чистые функции, проверяются без сервера
и без демона), затем наблюдатель, затем HTTP/SSE поверх него, затем страница,
затем жизненный цикл демона и команда, затем проверка и документация.

## 1. Читатель указателя проектов

- [x] 1.1 Добавить `listProjects(runsRoot)` в `src/core/journal/reader.ts`: перечислить каталоги проектов в корне прогонов и сопоставить каждому путь из `projects.json`, отдавая `{ key, path?: string }` — путь отсутствует, если записи в указателе нет
- [x] 1.2 Не падать на повреждённом или отсутствующем `projects.json`: каталоги перечисляются, пути остаются неизвестными (та же терпимость, что у писателя указателя)
- [x] 1.3 Экспортировать `listProjects` из `src/core/index.ts` рядом с `listRuns`
- [x] 1.4 Написать тесты `listProjects` в `test/journal.test.ts`: два проекта под разными ключами, проект без записи в указателе, повреждённый указатель, пустой корень

## 2. Мягкий разбор `pipeline.lock.yml`

- [x] 2.1 Создать `src/ui/lock.ts`: `readLockJobs(path)` — `yaml.parse` в `unknown` и точечный доступ к `jobs[].id/needs/output/context/steps[].id/prompt/run/context`, без Zod-схемы
- [x] 2.2 Отсутствующий файл и неожиданная форма поля дают пустой результат, а не исключение
- [x] 2.3 Написать тесты `readLockJobs` на локе, полученном из `serializeLock`: работа с `output` и без, агентский и командный шаг, отсутствующий файл

## 3. Обзор всех проектов и прогонов

- [x] 3.1 Создать `src/ui/overview.ts`: типы `Overview`, `ProjectOverview`, `RunOverview` (проект, пайплайн, статус, старт, финиш, признак идущего)
- [x] 3.2 Реализовать `buildOverview(runsRoot)` поверх `listProjects` и `readManifest`/`readStatus`: прогоны проекта новейшими первыми, ярлык `latest` в перечисление не попадает
- [x] 3.3 Прогон с нечитаемым или отсутствующим `status.json` попадает в обзор с пометкой, а не выбрасывается из него молча
- [x] 3.4 Написать тесты `buildOverview`: два проекта, порядок прогонов, идущий против завершённого, проект без записи в указателе, пустой корень

## 4. Детальный снимок прогона

- [x] 4.1 Создать `src/ui/snapshot.ts`: типы `RunSnapshot`, `JobSnapshot`, `StepSnapshot` — вход (выходы предшественников по `needs`), выход (`artifacts/<job>.json`), список существующих файлов шага, разрез контекста
- [x] 4.2 Реализовать `buildSnapshot(paths)`: свести `readStatus`, определения из `readLockJobs` и фактические файлы; пути файлов — относительно каталога прогона, не абсолютные
- [x] 4.3 Подмешать разбор `context.json` агентского шага (четыре уровня и суммарная оценка), если файл существует
- [x] 4.4 Обработать убранный `stepcast gc` прогон: снимок строится по `status.json`, отсутствие `pipeline.lock.yml`, `jobs/` и `artifacts/` даёт признак «подробности убраны», а не исключение
- [x] 4.5 Написать тесты `buildSnapshot` на прогоне, собранном через `RunJournal` (по образцу `test/cleanup.test.ts`): выход предшественника как вход зависимой работы, работа без `output`, неисполнявшаяся работа, агентский шаг с `context.json`, прогон после `cleanupRun`

## 5. Чтение файла журнала

- [x] 5.1 Создать `src/ui/file.ts`: `readJournalFile(runDir, requestedPath)` — резолв через `path.resolve` и отказ, если результат вне `runDir`
- [x] 5.2 Потолок 1 МБ: файл крупнее отдаётся первым мегабайтом с признаком `truncated`
- [x] 5.3 Написать тесты: путь внутри читается, `../` и абсолютный путь наружу отклоняются, крупный файл усечён с признаком

## 6. Наблюдатель за корнем прогонов

- [x] 6.1 Создать `src/ui/watcher.ts`: `createWatcher({ runsRoot, intervalMs })` с подпиской слушателей — один наблюдатель на демон, слушателей много
- [x] 6.2 Опрос по таймеру: перечисление проектов и прогонов, обнаружение появившихся каталогов, перечитывание `status.json` только при изменившемся `mtimeMs`
- [x] 6.3 Уведомлять слушателей новым обзором при появлении прогона или изменении любого `status.json`; `dispose()` останавливает таймер
- [x] 6.4 Написать тесты: новый прогон, созданный после старта наблюдателя, доходит до слушателя; неизменившийся прогон не порождает лишних уведомлений; два слушателя получают одно и то же; `dispose` прекращает уведомления

## 7. HTTP-сервер и живое обновление

- [x] 7.1 Создать `src/ui/server.ts`: `createUiServer({ runsRoot, port })` на `node:http`, слушающий только `127.0.0.1`
- [x] 7.2 Маршрут `GET /` — страница дашборда из `src/ui/assets.ts`
- [x] 7.3 Маршрут `GET /api/overview` — текущий обзор JSON-ом
- [x] 7.4 Маршрут `GET /api/run?run=<projectKey>/<runId>` — детальный снимок прогона; неизвестный адрес даёт 404, неразобранный — 400 (querystring вместо сегментов пути: составной адрес содержит слэш)
- [x] 7.5 Маршрут `GET /api/file?run=<projectKey>/<runId>&path=<rel>` — `readJournalFile`; путь вне каталога прогона даёт 400, а не 500
- [x] 7.6 Маршрут `GET /api/events` — SSE: начальный обзор, затем обзор на каждое уведомление наблюдателя; при `?run=<...>` дополнительно шлётся снимок этого прогона; отключение клиента снимает подписку по `req.on('close')`
- [x] 7.7 Написать тесты сервера через `http.request` к серверу на случайном порту: обзор отдаётся, SSE присылает начальный обзор и обновление после появления нового прогона, файл вне каталога прогона отклоняется кодом 400, неизвестный прогон даёт 404

## 8. Страница дашборда

- [x] 8.1 Создать `src/ui/assets.ts`: HTML со встроенными CSS и JS строковой константой — список проектов, прогонов и их статусов
- [x] 8.2 Подключить `EventSource('/api/events')`, перерисовывающий обзор на каждое сообщение
- [x] 8.3 Раскрытие прогона: запрос снимка, показ работ, их входов и выходов, разреза контекста агентских шагов
- [x] 8.4 Раскрытие файла: `fetch('/api/file?...')` и показ содержимого в `<pre>`, JSON — форматированным, с явной пометкой при `truncated`
- [x] 8.5 Показ убранного прогона: объяснение «подробности убраны» вместо пустой карточки

## 9. Жизненный цикл демона

- [x] 9.1 Создать `src/ui/daemon.ts`: чтение и запись pid-файла `~/.stepcast/ui.pid` с полями `pid`, `port`, `started_at`
- [x] 9.2 Реализовать проверку живости через `process.kill(pid, 0)`; осиротевший pid-файл перезаписывается
- [x] 9.3 Реализовать `startDetached()`: `spawn` того же бинарника с `up --foreground`, `detached: true`, вывод в `~/.stepcast/ui.log`, `unref()`
- [x] 9.4 Реализовать `stopDaemon()`: сигнал процессу из pid-файла и удаление файла; отсутствие демона — сообщение и код 0
- [x] 9.5 Отличить занятый нашим демоном порт от занятого посторонним процессом: `EADDRINUSE` без живого pid-файла — внятный отказ с указанием порта и `ui.port`
- [x] 9.6 Написать тесты жизненного цикла: осиротевший pid-файл не мешает запуску, повторный запуск при живом демоне не поднимает второй сервер, остановка без запущенного демона завершается кодом 0

## 10. Команды и регистрация

- [x] 10.1 Создать `src/cli/commands/up.ts`: без флагов — отсоединённый запуск с печатью URL, `--foreground` — сервер в текущем процессе
- [x] 10.2 Повторный `stepcast up` при живом демоне печатает URL, pid и время старта из pid-файла и завершается кодом 0
- [x] 10.3 Создать `src/cli/commands/down.ts`: остановка демона; отсутствие демона — сообщение и код 0
- [x] 10.4 Зарегистрировать `up` (флаг `--foreground`) и `down` в `COMMANDS` и диспетчере `src/cli/main.ts`
- [x] 10.5 Убедиться, что регистрация `up`/`down` не задела `status`: команда по-прежнему про состояние прогона, её флаги и вывод не изменились
- [x] 10.6 Прогнать `npm run check`: typecheck, lint, полный набор тестов

## 11. Ручная проверка и документация

- [x] 11.1 Прогнать вручную: `stepcast up` → открыть URL → в другом терминале `stepcast run` минимального примера → убедиться, что прогон появился и статусы обновляются без перезагрузки страницы
- [x] 11.2 Прогнать вручную: `stepcast up` повторно (печатает URL, pid и время старта, второго демона нет), `stepcast down` (демон остановлен), `stepcast down` ещё раз (код 0)
- [x] 11.3 Прогнать вручную: раскрыть работу, посмотреть вход и выход, раскрыть `prompt.txt` и `artifacts/<job>.json`; затем `stepcast gc --older-than 0s` и убедиться, что убранный прогон остался виден с объяснением
- [x] 11.4 Обновить `docs/status.md`: заменить строку `stepcast ui` на `stepcast up` / `stepcast down`, перенести её из «Пока нет» в «Работает», отметив в известных ограничениях отсутствие аутентификации и привязку к `127.0.0.1`
- [x] 11.5 Заменить `stepcast ui` в CLI-разделе `docs/run-layout.md` на `stepcast up [--foreground]` и `stepcast down`, уточнив, что наблюдение охватывает все проекты сразу и работает без SQLite-индекса
- [x] 11.6 Проверить `README.md` и остальные документы на упоминания `stepcast ui` и привести их к новым именам
