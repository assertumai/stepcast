## 1. Вес пункта в формате очереди

- [x] 1.1 `BacklogItemSchema` принимает необязательное `track` — слаг в kebab-case; словаря допустимых слов в ядре нет.
- [x] 1.2 Публикуемая запись (`BacklogRecordSchema`, `toRecord`) несёт объявленную метку, а пункт без поля — пустую строку.
- [x] 1.3 Значение, не являющееся слагом, — отказ разбора, называющий пункт и значение (`describeFailure`).
- [x] 1.4 Дорожка ответа `pick --lanes` несёт плоское поле `track`; у незаполненной — пустая строка (`BacklogLaneSchema`, `runPickLanes`).
- [x] 1.5 Публикуемые JSON Schema перегенерированы из моделей.
- [x] 1.6 Тесты: пустая метка у пункта без поля, объявленная метка как есть, метка, движку не известная, отказ на значении не-слаге; схема ответа принимает метку заполненной и пустоту незаполненной дорожки.
- [x] 1.7 Тесты CLI: вес доезжает до дорожки, до записи `item` и до файла `item-<дорожка>.json`.

## 2. Происхождение пропуска в журнале

- [x] 2.1 `SKIP_KINDS` и поле `skip` у записи работы (`journal/schema.ts`).
- [x] 2.2 Планировщик проставляет `condition` пропускам по решению графа и `halted` — пропускам по остановке прогона.
- [x] 2.3 Значение доезжает до `status.json` (`runner.ts`, `onSettled`).

## 3. Годность дорожки

- [x] 3.1 `evaluateLane` считает по работам, кроме пропущенных с `condition`; дорожка, пропущенная целиком, остаётся `empty`.
- [x] 3.2 Тесты: пропуск условием даёт `ready`, пропуск остановкой — `unfit`, пропуск без происхождения — `unfit`.
- [x] 3.3 Тест сведения: работа, пропущенная условием, сведению не мешает — вместо прежнего, требовавшего обратного.

## 4. Облегчённый ход практики спецификации

- [x] 4.1 Проектная схема OpenSpec `express`: `proposal` → `specs` → `tasks`, без `design`; упоминания снятого документа из инструкций убраны.
- [x] 4.2 Правила спецификации репозитория описывают облегчённый ход и обязательность дельта-спеки отдельным разделом.

## 5. Ветка петли

- [x] 5.1 Работа описания изменения облегчённой ветки: младшая модель, свой промпт, потолок 20m.
- [x] 5.2 Работа реализации облегчённой ветки: без работы плана и без параметра дорожки, прочие гейты прежние.
- [x] 5.3 Промпты обеих работ требуют объявить ошибку разметки вслух и не называют практику спецификации литералом.
- [x] 5.4 Пайплайн: полная ветка получает условие по весу, облегчённая — свои три узла на каждой дорожке, `merge` ждёт проверку обеих ветвей.
- [x] 5.5 `stepcast lint` пайплайна проходит.

## 6. Документация

- [x] 6.1 `docs/backlog.md`: поле, его значения, критерий применимости и цена ошибки разметки.
- [x] 6.2 Преамбула `backlog.md`: поле в таблице и краткое правило разметки.
