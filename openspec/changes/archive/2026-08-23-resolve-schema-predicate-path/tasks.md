## 1. Разрешение пути

- [x] 1.1 Передать файл объявления в `toPredicate` в `src/core/pipeline/expand.ts`
- [x] 1.2 Разрешать путь предиката `schema` через `resolveDeclaredPath`
- [x] 1.3 Убедиться, что `file_exists` и `cmd` остались от корня рабочей директории

## 2. Тесты

- [x] 2.1 Предикат `schema` в файле работы вне корня читает схему рядом с ним
- [x] 2.2 Тот же предикат в `until.check` разрешается так же
- [x] 2.3 `file_exists` в файле работы вне корня по-прежнему от корня рабочей
      директории

## 3. Документация

- [x] 3.1 `docs/pipeline-format.md`: в правиле разрешения путей явно названы все
      три места, где встречается `schema`, и сказано про `file_exists`

## 4. Проверка на петле

- [x] 4.1 `stepcast run` на петле проходит работу `propose` дальше схемы
