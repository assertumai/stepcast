## 1. Общая проверка существования

- [x] 1.1 Вынести в `src/core/lint.ts` проверку существования пути с пропуском
      интерполированных и глобов
- [x] 1.2 Применить её к схеме предиката в `expect` шага и в `until.check`
- [x] 1.3 Применить её к путям `context` пайплайна, работы и шага. Основание —
      корень проекта, а не каталог файла пайплайна: пайплайн живёт и в
      `.stepcast/pipelines/`, а контекст всё равно считается от корня

## 2. Тесты

- [x] 2.1 Отсутствующая схема предиката в `expect` — ошибка
- [x] 2.2 Отсутствующая схема предиката в `until.check` — ошибка
- [x] 2.3 Отсутствующий литеральный путь контекста на трёх уровнях — ошибка
- [x] 2.4 Глоб и путь с подстановкой пропускаются
- [x] 2.5 `file_exists` по-прежнему не проверяется

## 3. Документация

- [x] 3.1 `docs/pipeline-format.md`: перечень отклоняемого линтом пополняется;
      сказано про пропуск глобов
