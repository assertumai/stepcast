## 1. Воспроизведение дефекта тестом

- [x] 1.1 Добавить в `test/knowledge.test.ts` тест «ревизия якоря из одних цифр проверяется на устаревание»: якорь с `rev: 9517869` без кавычек, файл изменён позже — ожидается нарушение об устаревании; тест обязан падать до правки
- [x] 1.2 Снять с тестов `жёлтым, когда файл изменён позже зафиксированной ревизии` и `красным, когда устаревание держится дольше объявленного срока` зависимость от случайного `stale.slice(0, 7)` — задать ревизию детерминированно, сохранив проверяемое поведение
- [x] 1.3 Убедиться, что новый тест падает, а два переписанных проходят на неизменённом коде

## 2. Приём ревизии независимо от типизации YAML

- [x] 2.1 В `toAnchors` (`src/core/knowledge/fs.ts`) принимать `rev`, прочитанный числом (`number`, `bigint`), приводя к строке; строковое значение принимать как прежде
- [x] 2.2 Отклонять `rev` непригодного типа (логическое, список, отображение) отказом `StepcastError` с путём файла и полем `rev`; отсутствие `rev` оставить законной формой без ревизии
- [x] 2.3 Тесты разбора: числовая ревизия принята строкой, `rev: true` / `rev: [a, b]` / `rev: {}` отклонены с названным полем, якорь строкой и якорь без `rev` по-прежнему дают ревизию `undefined`

## 3. Предупреждение о ревизии, не похожей на хеш

- [x] 3.1 Ввести в `check()` проверку формы ревизии `/^[0-9a-f]{4,40}$/i` до чтения истории пути; несовпадение — жёлтое нарушение рода `anchor-bad-rev` с путём якоря и значением ревизии
- [x] 3.2 Не проверять такой якорь на устаревание после выданного нарушения, чтобы не выдавать неверную причину
- [x] 3.3 Тесты: `rev: d5f15e2-fix` даёт жёлтое нарушение и нулевой код возврата; при изменённом файле нарушение о форме есть, а нарушения об устаревании по этому якорю нет

## 4. Формулировка отказа на непригодном скаляре

- [x] 4.1 Разделить в `requireField` причины «поля нет» и «поле есть, но не строка»; во втором случае называть фактический тип значения
- [x] 4.2 Уточнить формулировку `toStringList` для случая нестрокового элемента `scope`, не меняя поведения
- [x] 4.3 Тесты: `id: 1234567`, `title: 1234567`, `scope: [1234567]` отклоняются сообщением о типе значения, а не об отсутствии поля

## 5. Документация и очередь

- [x] 5.1 Описать в `docs/knowledge.md` форму `rev`: кавычки не требуются, числовая запись равнозначна строковой, непригодное значение отклоняется разбором, непохожая на хеш ревизия даёт жёлтое нарушение
- [x] 5.2 Переименовать пункт очереди `flaky-knowledge-drift-test` в `backlog.md` под настоящий диагноз и переписать `title`, `why`, `done_when`: нестабильный тест был симптомом дефекта движка
- [x] 5.3 Прогнать `npm run check` и десять полных прогонов `npm run test:only` подряд зелёными, подтверждая, что случайность хеша больше не влияет на исход
