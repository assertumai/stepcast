Порядок групп повторяет план сборки из дизайна: каждый этап заканчивается
чем-то запускаемым, а живая проверка двух допущений стоит в группе 6, а не в
конце. Приёмка задачи — сценарии соответствующей спеки.

## 1. Каркас проекта

- [x] 1.1 Инициализировать пакет: Node 22+, TypeScript, ESM, строгий режим компилятора, точка входа `scarp`
- [x] 1.2 Добавить зависимости `zod`, `yaml`, `execa`, `ajv` и разборщик аргументов; убедиться, что нативных зависимостей нет
- [x] 1.3 Настроить сборку, прогон тестов и линтер исходников; добавить команды в `package.json`
- [x] 1.4 Развести каркас модулей: `core/` (движок), `cli/` (поверхность), общие типы; зафиксировать правило, что `core` не импортирует `cli`
- [x] 1.5 Ввести общий тип ошибки с кодом возврата и единой печатью диагностики

## 2. Конфигурация

- [x] 2.1 Описать `zod`-схему конфигурации: `runs`, `defaults`, `limits`, `env_deny`, `context`, `backends`, `ui`
- [x] 2.2 Реализовать разбор единиц `k`/`M` для токенов и `s`/`m`/`h`/`d` для времени с регистрозависимостью
- [x] 2.3 Реализовать слияние источников с сохранением происхождения каждого значения
- [x] 2.4 Реализовать объединение списков `env_deny` и `context.deny` без возможности вычитания снизу
- [x] 2.5 Отклонять `runs.root` и `backends.*.command` в проектном конфиге
- [x] 2.6 Реализовать команду `scarp config` с выводом значения и источника, включая вклад источников в списки запретов
- [x] 2.7 Покрыть тестами девять сценариев спеки `scarp-configuration`

## 3. Разбор и раскрытие пайплайна

- [x] 3.1 Описать `zod`-схемы документов `kind: pipeline` и `kind: job`
- [x] 3.2 Реализовать интерполятор, возвращающий значение вместе со списком применённых подстановок
- [x] 3.3 Реализовать пространства подстановок `inputs`, `params`, `run`, `env`, `jobs.<id>.output` и экранирование `$${`
- [x] 3.4 Реализовать разрешение путей по двум правилам: от файла объявления для `uses`/`prompt`/`schema`, от корня рабочей директории для остальных
- [x] 3.5 Реализовать раскрытие `uses` и `with` с проверкой обязательных и лишних параметров и подстановкой умолчаний
- [x] 3.6 Отклонять `needs`, `on`, `if`, `with` внутри документа `kind: job`
- [x] 3.7 Применять `defaults` пайплайна и конфигурации к работам и шагам
- [x] 3.8 Записывать раскрытый пайплайн в `pipeline.lock.yml`
- [x] 3.9 Покрыть тестами сценарии спеки `pipeline-definition`, относящиеся к разбору и раскрытию

## 4. Статическая проверка

- [x] 4.1 Реализовать построение графа и обнаружение циклов, недостижимых работ и неизвестных идентификаторов
- [x] 4.2 Реализовать разбор выражений `if` рекурсивным спуском по закрытой грамматике
- [x] 4.3 Проверять обращения `if` к неопределённым `inputs` и к работам не выше по графу
- [x] 4.4 Отклонять подстановку `${jobs.*.output.*}` в строковую форму `run`
- [x] 4.5 Отклонять переменную в `env`, попадающую под действующий `env_deny`
- [x] 4.6 Проверять существование файлов промптов и схем только для путей без подстановок и не производимых работами выше по графу
- [x] 4.7 Проверять превышение `limits` из конфигурации
- [x] 4.8 Реализовать предупреждения, не влияющие на код возврата
- [x] 4.9 Реализовать команду `scarp lint` с кодом возврата 2 при ошибках и 0 при чистом результате
- [x] 4.10 Покрыть тестами сценарии спеки `pipeline-definition`, относящиеся к проверке

## 5. Журнал прогона

- [x] 5.1 Реализовать вычисление ключа проекта от реального пути и ведение указателя проектов
- [x] 5.2 Создавать директорию прогона с ограниченными правами и символической ссылкой на последний прогон
- [x] 5.3 Описать `zod`-схемы `run.json`, `status.json`, `step.json`, `usage.json`, `context.json`, `expect.json` и записей `events.ndjson`
- [x] 5.4 Реализовать дозапись событий построчно и атомарную замену состояния через временный файл с переименованием
- [x] 5.5 Реализовать раскладку каталогов работ и шагов с числовым префиксом и адресацией по идентификатору
- [x] 5.6 Реализовать команды `scarp status` и `scarp logs`, включая сопровождение идущего прогона
- [x] 5.7 Покрыть тестами сценарии спеки `run-journal`, относящиеся к раскладке, состоянию и событиям

## 6. Исполнение шага командной строки

- [x] 6.1 Реализовать сборку окружения шага по цепочке уровней с инжектируемыми `SCARP_*` и фильтром `env_deny` последним
- [x] 6.2 Реализовать запуск процесса в отдельной группе с формами argv и строки через оболочку платформы
- [x] 6.3 Реализовать ограничение времени: `SIGTERM`, отсрочка 30 секунд, `SIGKILL`, убийство всей группы процессов
- [x] 6.4 Реализовать определение тишины с событием `step.stalled` без прерывания шага
- [x] 6.5 Реализовать повторные попытки с отдельной записью на каждую и применение ступеней `attempts.escalation`
- [x] 6.6 Покрыть тестами шестнадцать сценариев спеки `step-execution`

## 7. Планировщик и исполнение графа

- [x] 7.1 Реализовать цикл над множеством готовых работ с выборкой по одной и детерминированным порядком по объявлению
- [x] 7.2 Реализовать вычисление `on` со значениями `success`, `failure`, `always` и трактовкой `budget_exceeded` как отказа
- [x] 7.3 Реализовать правило «пропущенная зависимость не блокирует» и пропуск работы при всех пропущенных зависимостях
- [x] 7.4 Реализовать вычисление `if` в момент готовности работы и совместное применение с `on`
- [x] 7.5 Реализовать вторую фазу для работ с `needs: all`
- [x] 7.6 Реализовать `fail_fast` с доведением идущей работы до конца и последующим запуском работ разбора
- [x] 7.7 Реализовать обработку `SIGINT` и `SIGTERM` с корректным завершением, статусом `canceled` и выполнением работ с `needs: all`
- [x] 7.8 Реализовать итоговые статусы и коды возврата 0, 1, 2, 3, 130
- [x] 7.9 Покрыть тестами восемнадцать сценариев спеки `pipeline-execution`

## 8. Адаптер агентского бэкенда

- [x] 8.1 Определить интерфейс адаптера: объявление возможностей, сборка запуска, разбор записи потока, приведение расхода, работа с идентификатором сессии
- [x] 8.2 Реализовать поддельный адаптер с заранее заданным потоком вывода для быстрых тестов
- [x] 8.3 Реализовать адаптер Claude Code: запуск через каналы, промпт через stdin, потоковый структурированный вывод
- [x] 8.4 Реализовать терпимый разбор потока: неизвестная запись игнорируется, неразбираемая строка уходит в журнал и не роняет шаг
- [x] 8.5 Реализовать передачу пользовательской схемы вывода и запись результата в `output.json` силами движка
- [x] 8.6 Реализовать трансляцию политики доступа агента в параметры запуска бэкенда
- [x] 8.7 Реализовать назначение идентификаторов сессий движком и режимы `shared`, `per_step`, именованные псевдонимы
- [x] 8.8 Реализовать деградацию `shared` до `per_step` с предупреждением, когда адаптер не объявляет поддержку сессий
- [x] 8.9 Реализовать приведение расхода с явной пометкой несообщаемых полей и сохранением всех окон лимитов
- [x] 8.10 Записывать сообщённые бэкендом сведения об инициализации, включая ошибки загрузки, в `step.json`
- [x] 8.11 **Живая проверка допущения о сессиях**: тест против настоящего Claude Code, где второй шаг ссылается на содержимое, переданное первому
- [x] 8.12 **Живой замер кеша**: зафиксировать чтение из кеша на втором шаге общей сессии и записать результат в дизайн
- [x] 8.13 Покрыть тестами четырнадцать сценариев спеки `agent-backend`

## 9. Сборка контекста

- [x] 9.1 Реализовать объединение записей трёх уровней в порядке «пайплайн, работа, шаг»
- [x] 9.2 Реализовать поддержку записей вида пути, глоба, отображения с `path` и `mode`, отображения с `text`
- [x] 9.3 Реализовать детерминированное раскрытие глобов с сортировкой по пути
- [x] 9.4 Реализовать автоматический блок выходов предшественников и ключ `context_upstream`
- [x] 9.5 Реализовать режимы `inline`, `reference`, `auto` с порогом `context.inline_threshold`
- [x] 9.6 Реализовать `context_inherit` и `context_exclude`
- [x] 9.7 Применять `context.deny` с записью каждого исключения в журнал событий
- [x] 9.8 Реализовать локальный оценщик размера контекста и использовать его же в отчётах
- [x] 9.9 Реализовать предел `context.max_tokens` с понижением записей `auto` от самой крупной и отказом шага до запуска бэкенда
- [x] 9.10 Реализовать отправку унаследованного контекста только в первом сообщении сессии
- [x] 9.11 Записывать `context.json` с источником, способом передачи и размером каждой записи; реализовать команду `scarp context`
- [x] 9.12 Покрыть тестами девятнадцать сценариев спеки `step-context`

## 10. Проверка результата и выход работы

- [x] 10.1 Реализовать вычисление всех объявленных предикатов независимо от того, что один уже не прошёл
- [x] 10.2 Реализовать предикаты `exit_code`, `file_exists`, `matches`, `not_matches`
- [x] 10.3 Реализовать предикат `schema` через `ajv` с перечислением нарушений и путей к полям
- [x] 10.4 Реализовать предикат `cmd` с сохранением вывода для эскалации следующей попытки
- [x] 10.5 Записывать `expect.json` с результатом каждого предиката, включая пройденные
- [x] 10.6 Реализовать публикацию выхода работы в `artifacts/<job>.json` с `output.from` и умолчанием по последнему агентскому шагу
- [x] 10.7 Отклонять на валидации работу с `output` без `from` и без агентских шагов
- [x] 10.8 Реализовать разрешение `${jobs.<id>.output.*}` и трактовку выхода упавшей работы как неопределённого
- [x] 10.9 Покрыть тестами девятнадцать сценариев спеки `result-contract`

## 11. Учёт расхода и бюджет

- [x] 11.1 Реализовать центральный накопитель расхода, обновляемый по каждой записи от адаптера
- [x] 11.2 Реализовать подсчёт токенов как суммы входных, выходных, записи в кеш и чтения из кеша с весом бэкенда
- [x] 11.3 Реализовать проверку потолков шага, работы и прогона после каждой записи расхода
- [x] 11.4 Реализовать прерывание шага при превышении тем же путём, что и таймаут, со статусом `budget_exceeded`
- [x] 11.5 Реализовать агрегацию в `usage.json` по попыткам, шагам, работам и прогону
- [x] 11.6 Реализовать команду `scarp usage` с распределением по номеру попытки и пометкой несообщаемых измерений
- [x] 11.7 Покрыть тестами сценарии спеки `run-journal`, относящиеся к расходу и бюджету

## 12. Завершение

- [x] 12.1 Добавить пример пайплайна без `until`, пригодный для запуска этим срезом, с промптами и схемами
- [x] 12.2 Прогнать пример на настоящем репозитории от начала до конца и приложить получившийся журнал прогона
- [x] 12.3 Сгенерировать JSON Schema для `scarp.yml` из `zod`-схем и подключить её для автодополнения в редакторе
- [x] 12.4 Отметить в `docs/pipeline-format.md`, `docs/run-layout.md` и `docs/config.md`, что реализовано этим срезом, а что ещё нет
- [x] 12.5 Определить умолчание `context.max_tokens` по данным живых прогонов и зафиксировать его в конфигурации
- [x] 12.6 Записать в дизайн результаты живой проверки сессий и замера кеша и пересмотреть решение о сессиях, если данные того требуют
