## 1. Падающий тест

- [x] 1.1 В `test/knowledge.test.ts`, блок `describe('knowledge-fs: отбор', …)`: тест-свойство «всякое имя из оглавления разрешается». Фикстура `repo({…})` с единицей знания и двумя каталогами практики спецификации (например `openspec/changes/one/proposal.md` и `openspec/changes/two/proposal.md`), источник — `box.source({ specDir: 'openspec/changes' })`. Для каждой записи `source.index()` вызвать `source.select({ kind: 'id', id: [entry.id] })` и проверить, что вызов не бросает и отдаёт хотя бы одну запись, а каждая запись несёт `path`, читаемый с диска. Тест обязан падать на сегодняшнем коде: `spec:one` даёт `StepcastError «Единица знания не найдена»`.
- [x] 1.2 Тест состава: каталог с `proposal.md`, `design.md` и вложенной `specs/some/spec.md` разрешается тремя записями, порядком по пути; у всех трёх `id === 'spec:<слаг>'`, `path` — исходные пути в дереве (`openspec/changes/<слаг>/…`), а `text` не заполнен ни у одной (решения 2 и 4 design.md).
- [x] 1.3 Тест границы: `select({ kind: 'scope', scope: ['openspec/changes/**'] })` при том же источнике не возвращает ни одной записи каталога (решение 7 design.md).
- [x] 1.4 Тест отказов: `select({ kind: 'id', id: ['spec:нет-такого'] })` бросает `StepcastError`, называющий идентификатор; каталог, лежащий в `project.spec.dir` без единого документа Markdown, не попадает ни в `index()`, ни в разрешение по имени — отказ тот же.
- [x] 1.5 Тест бюджета: каталог с двумя крупными документами и `budget`, в который помещается один, — отбор режет по границе документа и оставляет хотя бы одну запись (то же правило, что у единиц знания: `'предел записи режет по границе единицы, оставляя хотя бы одну'`).
- [x] 1.6 В `test/cli-knowledge.test.ts` — проверка человеческого пути: `stepcast knowledge select --id spec:<слаг>` завершается нулём и печатает содержимое документов каталога (тело читает CLI по `path`, `runSelect` в `src/cli/commands/knowledge.ts`).

## 2. Правка источника `fs`

- [x] 2.1 В `src/core/knowledge/fs.ts` завести приватный метод, разворачивающий запись каталога в записи ответа: по идентификатору `spec:<слаг>` найти запись среди `this.specEntries()`, собрать файлы `**/*.md` внутри `join(specDir, слаг)` (`globSync`, сортировка по пути) и отдать `KnowledgeEntry` на документ — `id` запрошенный, `title` называет документ (его путь относительно каталога изменения), `path` — путь относительно корня репозитория (`toPosix`), `tokens` — `estimateTokens` содержимого файла. Правило «документ каталога — файл Markdown» жить должно ровно здесь, одним местом.
- [x] 2.2 В ветке `selector.kind === 'id'` метода `select()` (сейчас `fs.ts:197-209`): искать имя сначала среди `units()`, и лишь не найдя — среди записей каталогов (решение 6 design.md, столкновение имён разрешается в пользу единицы знания). Ненайденное имя по-прежнему бросает `StepcastError` с тем же текстом и подсказкой.
- [x] 2.3 Сохранить действующие свойства ветки: порядок перечня — порядок запроса, повторно названный идентификатор записей не удваивает, бюджет режет по границе записи и пропускает первую всегда. Второго бюджетного цикла не заводить — записи каталога должны попадать в тот же цикл, что и тела единиц (решение 5 design.md).
- [x] 2.4 Ветку `selector.kind === 'scope'` не трогать: невозвращение записей каталогов остаётся сегодняшним поведением и становится объявленным требованием, а не правкой кода.
- [x] 2.5 Убедиться, что `check()`, `index()` и `write()` не задеты: новых нарушений изменение не заводит, `unit_max_tokens` меряет тело единицы знания и на документы каталогов не распространяется.

## 3. Документация

- [x] 3.1 `docs/knowledge.md`, раздел «Встроенный источник `fs`» (абзац «Оглавление производно»): дописать, что запись каталога разрешается отбором по имени и отдаётся записью на каждый документ Markdown каталога ссылкой на исходный путь, а отбором по области не возвращается — с причиной (число каталогов растёт от петли и памятью не управляется, тот же довод, что у `spec_index_max_tokens`).
- [x] 3.2 `docs/knowledge.md`, раздел «Работа вне пайплайна»: добавить строку примера `stepcast knowledge select --id spec:<слаг>` рядом с отбором по области.
- [x] 3.3 `docs/knowledge.md`, раздел «Контракт источника»: назвать согласованность оглавления и отбора по имени требованием контракта — наравне с уже записанными там требованиями к политике и детерминизму, — распространяющимся и на источник `cmd`.
- [x] 3.4 Сверить шапку-комментарий `src/core/knowledge/fs.ts` (три свойства раскладки) с новым поведением и поправить, если она стала неточной.

## 4. Находки ревью

- [x] 4.1 Оглавление, уезжающее в контекст шага, называет записи каталогов запрашиваемыми только по идентификатору (`renderContextIndex`, строка `SPEC_ID_ONLY`): область у них печатается наравне с областью единицы знания, а отбор по области их не возвращает, и без этой строки шаг получил бы на объявленную область пустой ответ вместо отказа. Тесты: оговорка есть там, где показана хотя бы одна запись каталога, и её нет там, где не показано ни одной.
- [x] 4.2 Чтение документа каталога обёрнуто `StepcastError` с путём и причиной (`readSpecDocument`): `project.spec.dir` держит что угодно, включая каталог с именем на `.md`, а отбор зовётся посреди прогона. Тест на нечитаемый документ; `describeReadFailure` названа без привязки к закреплению якоря.
- [x] 4.3 Перечень записей каталогов снимается не больше раза на вызов `select`, а не на каждое имя запроса: обход `project.spec.dir` с `describeSpecDir` стоит десятков `statSync` и чтений.

## 5. Проверка

- [x] 5.1 `npm run check` проходит целиком, включая новые тесты `test/knowledge.test.ts` и `test/cli-knowledge.test.ts`.
- [x] 5.2 `openspec validate knowledge-spec-entries-by-id --strict` проходит.
