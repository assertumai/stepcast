## 1. Проверки, падающие до правки

- [x] 1.1 `test/tmp.test.ts` (новый): дочерний процесс, заводящий каталог средствами песочницы и завершающийся успехом, не оставляет корня на диске. Падает до правки: песочницы нет вовсе
- [x] 1.2 Там же: то же для дочернего процесса, упавшего на утверждении, и для процесса, завершившегося необработанным исключением, — корня нет в обоих случаях
- [x] 1.3 Там же: дочерний процесс, прерванный `SIGINT` и `SIGTERM`, снимает корень и завершается так же, как завершился бы без песочницы
- [x] 1.4 Там же: `os.tmpdir()` внутри процесса ведёт в корень песочницы, а каталог, заведённый кодом движка через `mkdtempSync(tmpdir())`, оказывается внутри него
- [x] 1.5 Там же: дочерний процесс, поднятый с унаследованным окружением, заводит свой временный каталог внутри корня песочницы родителя
- [x] 1.6 Там же: с признаком `STEPCAST_TEST_KEEP=1` корень остаётся на диске, назван `stepcast-test-keep-…`, а его путь напечатан в поток ошибок; тест снимает его сам
- [x] 1.7 Там же: отказ уборки не меняет кода возврата — процесс с невыполнимым снятием (каталог заменён на путь, который снять не удаётся) возвращает код, определённый исходом тестов, и печатает путь остатка
- [x] 1.8 Там же: имена внутри песочницы не начинаются с `stepcast-` — единственная запись с этим префиксом в системном временном каталоге принадлежит корню
- [x] 1.9 `test/diff.test.ts`: после `stepcast diff` в системном временном каталоге не появилось записи `stepcast-diff-*`. Падает до правки
- [x] 1.10 `test/merge-lanes.test.ts` (или `test/lanes.test.ts` — где живут проверки наложения): после наложения, сошедшегося и не сошедшегося, записи `stepcast-apply-*` не остаётся. Падает до правки
- [x] 1.11 `test/resume.test.ts`: после построения плана возобновления записей `stepcast-plan-*` и `stepcast-adopt-*` не остаётся. Падает до правки

## 2. Песочница тестового процесса

- [x] 2.1 `test/tmp.ts` (новый): корень `stepcast-test-<случайное>` заводится один раз на процесс в системном временном каталоге при первом импорте модуля
- [x] 2.2 Там же: `TMPDIR`, `TMP` и `TEMP` процесса переводятся в корень сразу при его заведении — до того, как временный каталог успеет спросить кто-либо ещё
- [x] 2.3 Там же: `tempDir(prefix)` — каталог внутри корня; имена по назначению, без префикса `stepcast-`
- [x] 2.4 Там же: уборка на `process.on('exit')` — рекурсивное щадящее снятие корня; отказ ловится и печатается путём в поток ошибок, код возврата не трогается
- [x] 2.5 Там же: обработчики `SIGINT` и `SIGTERM` — уборка и переподнятие сигнала, чтобы код завершения остался сигнальным
- [x] 2.6 Там же: `STEPCAST_TEST_KEEP=1` — корень зовётся `stepcast-test-keep-<случайное>`, уборка не ставится, путь печатается по завершении процесса
- [x] 2.7 Сверить с `1.1`–`1.8`: проверки зелёные

## 3. Каркас тестов

- [x] 3.1 `test/helpers.ts:24`: `makeProject` заводит базу через `tempDir('project-')`
- [x] 3.2 `test/helpers.ts:113`: `makeJournalBed` — через `tempDir('bed-')`
- [x] 3.3 Снять локальные обёртки того же назначения в пользу общей: `test/engine-snapshot.test.ts:27` (`tempDir`), `test/exec.test.ts:19` (`workdir`) и прочие, найденные при замене

## 4. Замена в тестовых файлах

- [x] 4.1 Тяжёлые файлы: `test/merge-lanes.test.ts` (≈51 вызов), `test/lanes.test.ts` (≈17), `test/cli-backlog.test.ts` (≈16), `test/cleanup.test.ts` (≈11), `test/workspace.test.ts` (≈10)
- [x] 4.2 Средние: `test/journal.test.ts`, `test/scratch.test.ts`, `test/budget.test.ts`, `test/run.test.ts`, `test/backend.test.ts`, `test/loop-portability.test.ts`, `test/session-context.test.ts`, `test/job-data.test.ts`, `test/run-progress.test.ts`, `test/exec.test.ts`
- [x] 4.3 Остальные файлы с одним-тремя вызовами — по списку, который даст правило линта из раздела 5 после его включения
- [x] 4.4 Замена механическая: `mkdtempSync(join(tmpdir(), 'stepcast-<имя>-'))` → `tempDir('<имя>-')`; утверждения тестов не трогаются
- [x] 4.5 Разобрать по месту тесты, покрасневшие от замены (сравнение с `tmpdir()`, длина или вид пути): либо тест берёт путь у песочницы явно, либо утверждение переписывается на то, что оно на самом деле проверяет

## 5. Запрет создания мимо песочницы

- [x] 5.1 `eslint.config.js`: для `test/**/*.ts` — запрет импорта `mkdtempSync` из `node:fs` и `tmpdir` из `node:os` (`no-restricted-imports` с `importNames`), сообщение указывает на `test/tmp.ts`; исключение — сам `test/tmp.ts`
- [x] 5.2 Там же: то же правило для `src/**/*.ts` с исключением `src/core/fs/tempDir.ts`
- [x] 5.3 Прогнать `npm run lint` и добить оставшиеся места из `4.3`
- [x] 5.4 Сверить, что правило действительно роняет проверку: временно добавить прямой вызов в тестовый файл, убедиться в отказе с именем файла, снять

## 6. Временные каталоги движка

- [x] 6.1 `src/core/fs/tempDir.ts` (новый): `withTempDir(prefix, fn)` — заведение, передача телу, снятие в `finally` независимо от исхода, включая брошенное исключение
- [x] 6.2 `src/cli/commands/diff.ts:31`: каталог состояния якоря сравнения через `withTempDir`; снятие охватывает и `dispose()` якоря
- [x] 6.3 `src/core/run/apply.ts:329` и `:384`: то же для наложения дорожки и наложения работ; снятие не раньше, чем дочитаны файлы патчей, которые пишутся в тот же каталог (`applyDiffByRepo`, `:154`)
- [x] 6.4 `src/core/run/resumePlan.ts:843` и `:979`: то же для усыновления дерева и построения плана
- [x] 6.5 Убедиться, что состояние якоря в каталоге прогона (`journal.paths.anchors`, `src/core/run/runner.ts:1367`, `:2768`) правкой не задето
- [x] 6.6 Сверить с `1.9`–`1.11`: проверки зелёные

## 7. Документация

- [x] 7.1 `docs/testing.md` (новый): песочница — где живут временные файлы прогона, что уборка не зависит от исхода теста и от автора, почему прямое создание запрещено и чем заменяется
- [x] 7.2 Там же: признак `STEPCAST_TEST_KEEP=1`, имя сохранённого корня `stepcast-test-keep-…` и чем оно отличает разбор от утечки
- [x] 7.3 Там же: рецепт замера — счёт записей `stepcast-*` в системном временном каталоге до и после прогона на чистом `$TMPDIR`
- [x] 7.4 `README.md`, перечень документации: ссылка на `docs/testing.md`

## 8. Замер и гейты

- [x] 8.1 Замер по признаку выполненности: прогон `npm run check` с чистым, пустым `$TMPDIR` — записей `stepcast-*` до прогона 0, после прогона (2085/2085 тестов зелёные) — 0
- [x] 8.2 Тот же замер с намеренно упавшим тестом (временная `assert.fail` в `test/until.test.ts`, снята после замера) — `npm run check` завершился кодом 1, записей `stepcast-*` в `$TMPDIR` до и после — 0 в обоих случаях
- [x] 8.3 Объявленная команда проверки репозитория (`npm run check`) зелёная: 2085/2085 тестов, typecheck/typecheck:ui/lint/build без ошибок
- [x] 8.4 `openspec validate "test-temp-dirs-leak" --strict` зелёная

## 9. Правки по ревью

- [x] 9.1 `eslint.config.js`: запреты `no-restricted-imports` собраны в одну запись правила на файл — блоки плоского конфига опции одноимённого правила не сливают, и блок временного каталога для `src/**` молча снимал границу ядра с `src/core/**`
- [x] 9.2 `test/eslint-config.test.ts` (новый): запреты проверяются `ESLint.lintText` на подставном содержимом — граница ядра и запрет временного каталога срабатывают в одном файле ядра, помощник и модуль песочницы остаются исключениями. Заменяет ручную пробу `5.4`
- [x] 9.3 `test/tmp.test.ts`: напечатанный фикстурой корень проверяется на вид (`stepcast-test-…`) до утверждения об его отсутствии — пустой вывод несостоявшейся фикстуры зеленил проверку, ничего не проверив
- [x] 9.4 Там же: ожидание первой строки от дочернего процесса ограничено по времени и оканчивается отказом по `error`/`close` — у `node --test` таймаута теста нет, и молча умерший процесс вешал бы прогон
- [x] 9.5 `test/tmp.ts`: путь сохранённого корня печатается и при прерывании сигналом — от умирающего по сигналу процесса обработчики `exit` не исполняются; покрыто проверкой «keep + SIGINT»
- [x] 9.6 `src/core/fs/tempDir.ts`: отказ уборки не подменяет собой исход вызова — путь называется в потоке ошибок, а исключение тела уходит наружу; `test/temp-dir.test.ts` (новый) покрывает успех, исключение и оба случая с невыполнимой уборкой
- [x] 9.7 `test/workspace.test.ts`: ветка наложения работ (`applyRun` без дорожки) замеряется на остаток `stepcast-apply-*` — сошедшееся и не сошедшееся наложение; прежние замеры уходили только в ветку дорожки
- [x] 9.8 Перепроверка: `npm run check` зелёная (2097/2097), замер по `8.1` на чистом `$TMPDIR` повторён после правок — записей `stepcast-*` 0 до и 0 после, `openspec validate --strict` зелёная. Красноту новых проверок сверили снятием правки: без печати по сигналу «keep + SIGINT» красный, без обезвреженного отказа уборки красны обе проверки `test/temp-dir.test.ts` (вторая — ровно подменой исключения тела ошибкой файловой системы), а старый порядок блоков конфига оставляет файл ядра без границы ядра
- [ ] 9.9 Снять пустой `test/lint-probe.test.ts` — остаток ручной пробы `5.4`, заменённой проверкой `9.2`. Удалить файл из этой работы не удалось: удаление и перемещение файлов в её песочнице запрещены (`rm`, `mv`, `git clean` отклонены). Снимается командой `git clean -f -- test/lint-probe.test.ts`; файл не отслеживается git и пуст
