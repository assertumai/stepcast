## 1. Модель и конфигурация

- [x] 1.1 Снять отказ на `on_exceed: wait` в `toBudget`
      (`src/core/pipeline/expand.ts`), пропуская значение в модель бюджета
- [x] 1.2 Ввести предел ожидания умолчанием конфигурации
      (`src/core/config/defaults.ts`, `merge.ts`, `resolve.ts`, схема) с
      разбором как длительности
- [x] 1.3 Предупреждение `stepcast lint` о бюджете с `wait` без
      `rate_limit_pct` на всех трёх уровнях объявления

## 2. Учёт: режим области и момент сброса

- [x] 2.1 Дополнить `Exceeded` в `src/core/budget/accumulator.ts` режимом
      упёршейся области и, для измерения `rate_limit`, моментом сброса окна
- [x] 2.2 Брать момент сброса из превысившего окна; при нескольких превысивших
      окнах — более поздний из сообщённых
- [x] 2.3 Вести интервалы ожидания в аккумуляторе и вычитать из длительности
      каждой области ту их часть, что пришлась на её жизнь; `elapsedMs()`
      возвращает время за вычетом ожидания
- [x] 2.4 Ограничить суммарную длительность ожиданий пределом: аккумулятор
      сообщает исчерпание предела как основание остановки

## 3. Ожидание в прогоне

- [x] 3.1 Развязка исхода превышения в `src/core/run/runner.ts`: `stop` и
      неожидаемые случаи ведут в существующий путь `budget_exceeded`, `wait`
      по `rate_limit` с известным моментом сброса — в ожидание
- [x] 3.2 Прерывание текущего шага перед ожиданием существующим путём
      завершения дерева процессов
- [x] 3.3 Прерываемое ожидание: сигнал отмены будит его немедленно и ведёт в
      существующий путь отмены
- [x] 3.4 Переисполнение прерванного шага после пробуждения без увеличения
      счётчика попыток и без потери учтённого расхода оборванной попытки
- [x] 3.5 Вырождение в остановку: отсутствие момента сброса, момент дальше
      предела, исчерпанный суммарный предел, превышение по токенам или
      времени — с причиной, называющей основание

## 4. Журнал и состояние

- [x] 4.1 События `budget.waiting` (область, измерение, порог, момент сброса,
      рассчитанная длительность) и `budget.resumed` (фактическая длительность)
      в `src/core/journal/schema.ts` и их запись в прогоне
- [x] 4.2 Момент пробуждения в состоянии прогона: поле схемы, запись состояния
      на диск до начала ожидания, снятие поля после продолжения
- [x] 4.3 Проверить, что `stepcast status` и витрина показывают спящий прогон
      осмысленно; при необходимости — вывод момента пробуждения

## 5. Тесты

- [x] 5.1 Превышение `rate_limit_pct` с `wait` и известным `resets_at`:
      шаг прерывается, прогон ждёт, шаг исполняется заново, прогон завершается
      успехом
- [x] 5.2 Попытки не расходуются ожиданием, а расход оборванной попытки
      остаётся в потолках
- [x] 5.3 Отсутствие `resets_at` → `budget_exceeded` с причиной о неизвестном
      моменте сброса
- [x] 5.4 Момент сброса дальше предела → `budget_exceeded` с причиной,
      называющей предел и момент
- [x] 5.5 Превышение по токенам при `on_exceed: wait` → `budget_exceeded` без
      ожидания
- [x] 5.6 Ожидание не засчитывается в `wallclock` прогона, работы и шага;
      работа, начатая после ожидания, его не вычитает
- [x] 5.7 Отмена спящего прогона: работы `canceled`, состояние дописано,
      `budget.waiting` без парного `budget.resumed`
- [x] 5.8 Состояние с моментом пробуждения читается во время ожидания
- [x] 5.9 Предупреждение линта о `wait` без `rate_limit_pct`, код возврата 0

## 6. Документация

- [x] 6.1 `docs/status.md`: убрать `on_exceed: wait` из нереализованного,
      описать ограничения — ожидание только по окну лимита, предел ожидания,
      вырождение в `stop`
- [x] 6.2 `docs/pipeline-format.md`: предел ожидания, поведение при неизвестном
      моменте сброса, невлияние ожидания на `wallclock`
- [x] 6.3 Проверить, запускается ли теперь `examples/target-state/`, и
      отметить в `docs/status.md` оставшуюся причину, если нет

## 7. Проверка

- [x] 7.1 `npm run check` проходит
