## 1. Падающие проверки

- [x] 1.1 Тест в `test/knowledge.test.ts`: оглавление из двух единиц знания и
  множества каталогов практики спецификации, вместе перерастающих
  `index_max_tokens`, не даёт нарушения о пределе памяти, а `check` отдаёт
  `ok: true`. Проверка обязана падать: сегодня подсчёт общий и нарушение красное.
- [x] 1.2 Тест: единицы знания сами по себе перерастают `index_max_tokens` —
  красное нарушение `index-overflow`, чья деталь называет единицы знания, их
  размер и ключ `index_max_tokens`.
- [x] 1.3 Тест: производная часть перерастает `spec_index_max_tokens` — жёлтое
  нарушение `spec-index-overflow`, чья деталь называет число записей, их
  размер и ключ; `check` отдаёт `ok: true`.
- [x] 1.4 Тест: при переполнении производной части отбор
  `select({ kind: 'index' })` отдаёт текст, где записи единиц знания показаны
  все, записи каталогов уложены в предел, а секция закончена строкой о числе
  не показанных записей и о каталоге практики спецификации.
- [x] 1.5 Тест: глагол `index` при том же переполнении перечисляет все записи
  каталогов; два вызова отбора `index` на неизменном дереве дают посимвольно
  совпадающий текст.
- [x] 1.6 Тест: тело активной единицы сверх `unit_max_tokens` — красное
  нарушение `unit-too-large`, называющее единицу и её размер; тело единицы со
  `status: superseded` нарушения не даёт.
- [x] 1.7 Тест: `write` единицы с телом сверх `unit_max_tokens` отвечает
  `ok: false` с этим нарушением, а файла в дереве не остаётся (и прежнее
  содержимое возвращается, если файл существовал).
- [x] 1.8 Тест в `test/config.test.ts`: умолчания `index_max_tokens` `2k`,
  `spec_index_max_tokens` `2k`, `unit_max_tokens` `1k`; объявление
  `spec_index_max_tokens: 6k` не меняет `index_max_tokens`; величины
  разбираются записью токенов (`6k`) и попадают в разрешённую конфигурацию.
- [x] 1.9 Тест: документ пайплайна, объявивший `project.knowledge` с новыми
  ключами, перекрывает ими проектный конфиг полистовым слиянием — тем же
  порядком, каким перекрывает `index_max_tokens`. (Тест лежит в
  `test/expand.test.ts`, где уже живёт вся остальная проверка
  полистового слияния пайплайновых значений `project.*` — `test/config.test.ts`
  проверяет только слияние проектного и глобального конфига, не документа
  пайплайна.)

## 2. Величины конфигурации

- [x] 2.1 `src/core/config/schema.ts` — ключи `spec_index_max_tokens` и
  `unit_max_tokens` в `RawKnowledgeSchema` (её же читает `ProjectSchema`
  документа пайплайна), с объяснением у каждого.
- [x] 2.2 `src/core/config/defaults.ts` — умолчания `2k` и `1k` рядом с
  `index_max_tokens`, с причиной выбора каждой величины.
- [x] 2.3 `src/core/config/merge.ts` — оба пути в `TOKEN_VALUED`.
- [x] 2.4 `src/core/config/resolve.ts` — поля `specIndexMaxTokens` и
  `unitMaxTokens` в разобранной секции знания.
- [x] 2.5 `src/core/pipeline/model.ts` и `src/core/pipeline/expand.ts` — те же
  поля в `KnowledgeDeclaration` и их наследование от базового слоя.
- [x] 2.6 `src/core/knowledge/source.ts` и `src/cli/commands/knowledge.ts` —
  передача обеих величин встроенному источнику; `FsSourceOptions` их принимает.
- [x] 2.7 `npm run schema` — перегенерация `schema/config.schema.json` и
  `schema/pipeline.schema.json`.

## 3. Раздельный подсчёт и нарушения

- [x] 3.1 `src/core/knowledge/fs.ts` — сборка оглавления различает две части:
  записи единиц знания и записи каталогов практики спецификации.
- [x] 3.2 Нарушение `index-overflow` считается только по записям единиц знания,
  остаётся красным, и его деталь называет часть, размер и ключ.
- [x] 3.3 Нарушение `spec-index-overflow` — жёлтое, считается по производной
  части, деталь называет число записей, размер и ключ.
- [x] 3.4 Нарушение `unit-too-large` — красное, по телу каждой активной
  единицы, деталь называет единицу, её размер и ключ.

## 4. Усечение производной части в отборе

- [x] 4.1 `src/core/knowledge/fs.ts` — отбор `select({ kind: 'index' })` кладёт
  записи каталогов в `spec_index_max_tokens` в порядке по идентификатору и
  заканчивает секцию строкой о числе не показанных записей и о каталоге
  практики спецификации.
- [x] 4.2 Глагол `index` остаётся полным; расхождение объяснено комментарием у
  места, где оно заведено (решение 3 в `design.md`).
- [x] 4.3 Записи единиц знания не усекаются ни при каком пределе.

## 5. Документация

- [x] 5.1 `docs/knowledge.md` — раздел про три предела вместо абзаца «Предел
  кладётся на оглавление»: что каждый ограничивает, почему уровни разные и что
  происходит с производной частью в контексте.
- [x] 5.2 `docs/knowledge.md` — строки таблицы дрейфа: жёлтая про производную
  часть, красная про тело единицы; существующая строка про оглавление уточнена
  до единиц знания.
- [x] 5.3 `docs/config.md` — строки таблицы ключей секции памяти с умолчаниями.
- [x] 5.4 `src/cli/commands/init.ts` — шаблоны правил письма и единицы-образца,
  если они называют предел оглавления единственным дефицитом.

## 6. За человеком (вне `project.edit_paths`)

- [ ] 6.1 `.stepcast/config.yml` — снять `index_max_tokens: 3k` вместе с
  комментарием о числе открытых изменений: предел памяти возвращается к
  умолчанию `2k`.
- [ ] 6.2 `.stepcast/prompts/knowledge-rules.md` — привести раздел «Что
  удерживает память от разрастания» к трём пределам: место в оглавлении,
  объём тела единицы и отдельно ограниченная производная часть.
- [ ] 6.3 `.stepcast/pipelines/self-improve-memory.yml:81-86` — комментарий у
  записи `knowledge: index` считает стоимость оглавления в контексте «при
  пределе `index_max_tokens` меньше двух процентов `context.max_tokens`».
  После изменения этот предел покрывает только записи единиц знания: стоимость
  ограничена суммой `index_max_tokens` и `spec_index_max_tokens` (`2k` + `2k`
  умолчанием при `context.max_tokens` `120k`, то есть меньше четырёх
  процентов). Величина стала точной сверху — производная часть в отборе
  усекается, — но названа в комментарии неверно.

## 7. По находкам ревью

- [x] 7.1 `src/core/knowledge/fs.ts` — укладка производной части и нарушение
  `spec-index-overflow` меряют одну величину одинаково (`renderSpecSection`),
  а хвостовая строка входит в предел: полосы, где отбор уже усекает, а `check`
  молчит, больше нет. Тесты в `test/knowledge.test.ts`: усечение начинается
  ровно на пороге жёлтого; секция вместе с хвостом укладывается в предел;
  хвостовая строка выводится и при пределе, в который сама не помещается.
- [x] 7.2 Запирание записи красным о чужой единице объявлено спекой
  (`specs/knowledge-fs/spec.md`), названо в `docs/knowledge.md` и в Risks
  `design.md`, покрыто тестом: раздутая единица в дереве не даёт записать
  другую, а ответ называет виновную.
