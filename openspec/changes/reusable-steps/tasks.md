## 1. Падающие проверки

- [x] 1.1 Тест в `test/expand.test.ts`: шаг `uses: <имя>` с `with` раскрывается
  в шаг, несущий имя, слой, путь манифеста и сведённые параметры; имя
  разрешается проектным слоем поверх домашнего и домашним поверх встроенного
  (корни слоёв подставляются тестом, как это уже делает `scriptRoots`). Обязан
  падать: ключ `uses` внутри `steps` сегодня отклоняется как неизвестный.
- [x] 1.2 Тест разбора: `uses` вместе с `script`, `run` или `prompt`
  отклоняется; `runner`, `args`, `input`, `output_schema` рядом с `uses`
  отклоняются сообщением, называющим их объявляемыми манифестом; `with` не
  отображением отклоняется; общие ключи (`timeout`, `expect`, `attempts`,
  `env`, `on_fail`) принимаются.
- [x] 1.3 Тест формы имени: `alice/lint-fix` отклоняется сообщением о
  неподдерживаемой форме `автор/имя`; `./tools/step.yml` — сообщением,
  называющим шаг `script`; пустое имя и имя с пробелом отклоняются.
- [x] 1.4 Тест манифеста в новом `test/reusable-steps.test.ts`: каталог без
  `step.yml` пропускается слоем; несовпадение `name` с именем каталога —
  ошибка; незнакомое поле манифеста отклоняется; `file` за пределами каталога
  шага отклоняется; отсутствующий `file` — ошибка с ожидаемым путём.
- [x] 1.5 Тест схемы параметров: `params` не объектной схемой отклоняется;
  дефектная схема называется ошибкой с именем шага и файлом; шаг без `params` с
  непустым `with` — ошибка; умолчания подставляются при раскрытии и видны в
  замке.
- [x] 1.6 Тест сведения параметров: неизвестное имя — сообщение с перечнем
  объявленных; непереданный обязательный — сообщение с именем параметра;
  значение не по схеме — замечание валидатора. Все три называют работу, шаг и
  файл манифеста.
- [x] 1.7 Тест типизированного раскрытия `with` в `test/expand.test.ts` и
  `test/late.test.ts`: `"${params.retries}"` даёт число; `"${jobs.plan.output.item}"`
  даёт объект целиком; `"чинить ${jobs.plan.output.slug}"` даёт строку.
- [x] 1.8 Тест в `test/lint.test.ts`: неизвестное имя шага — ошибка с перечнем
  просмотренных каталогов; два шага с разными промахами дают два сообщения за
  один проход; значение с отложенной подстановкой схемой не проверяется, а
  состав параметров проверяется всегда.
- [x] 1.9 Тест в `test/run.test.ts`: прогон шага `uses` — параметры доезжают
  `input.json`, выход читается и проверяется схемой манифеста, публикуется
  работой через `output.from` и читается следующей работой как
  `${jobs.*.output.*}`.
- [x] 1.10 Тест терминального отказа: параметр, пришедший выходом работы выше по
  графу, не проходит схему — шаг отказывает одной попыткой при `attempts.max: 3`,
  процесс не запускается, причина называет шаг и параметр.
- [x] 1.11 Тест неразрешённого шага в прогоне: `uses` с неизвестным именем
  отказывает одной терминальной попыткой тем же текстом, что дал линт.
- [x] 1.12 Тест встроенного образца: прогон `uses: changed-files` в проекте без
  каталогов `.stepcast/steps/`; тот же прогон при раннере `node` со снятой
  обёрткой (`wrapper: none`); выход соответствует объявленной схеме.
- [x] 1.13 Тест в `test/expand.test.ts` и `test/resume.test.ts`: правка
  манифеста, правка `with` и смена слоя разрешения — каждая даёт другой ключ
  шага; неизменные — тот же, и шаг переиспользуется с переносом выхода.
- [x] 1.14 Тест витрины (`test/ui-*.test.ts`): каталог перечисляет шаги трёх
  слоёв с параметрами, отмечает перекрытые, называет причину дефектного
  манифеста и остаётся непустым в чистом проекте; карточка шага в пайплайне и в
  прогоне несёт имя, слой и параметры.
- [x] 1.15 Тест в `test/schema-generated.test.ts` и
  `test/published-schema.test.ts`: опубликованная схема документов несёт ветвь
  `uses`/`with`; схема манифеста опубликована и свежа.

## 2. Манифест: модель, схема, чтение

- [x] 2.1 `src/core/pipeline/schema.ts`: `StepManifestSchema` — `version`,
  `kind: step`, `name`, `description`, `file`, необязательные `params`,
  `output_schema`, `runner`; строгий объект.
- [x] 2.2 `src/core/pipeline/steps.ts` (новый): корни трёх слоёв каталога шагов,
  разрешение имени, чтение и проверка манифеста, разрешение `file` и
  `output_schema` от каталога шага с запретом выхода за его пределы, проверка
  совпадения `name` с именем каталога, компиляция схемы параметров.
- [x] 2.3 Проверка формы имени: один сегмент строчных букв, цифр и дефисов;
  отдельные сообщения для формы `автор/имя` и для пути.
- [x] 2.4 Сведение параметров: проверка состава (неизвестный, непереданный
  обязательный) отдельным правилом с сообщениями `resolveParams`, затем
  применение умолчаний, затем проверка схемой.
- [x] 2.5 `src/core/expect/evaluate.ts`: проверка значения схемой, данной
  значением, а не файлом, — тем же компилятором; `validateAgainstSchemaFile`
  становится тонкой обёрткой над ней.

## 3. Шаг uses: разбор, раскрытие, замок

- [x] 3.1 `src/core/pipeline/schema.ts`: `UsesStepSchema` в союзе шагов — `uses`,
  `with`, общие ключи; отсутствие ключей, объявляемых манифестом.
- [x] 3.2 `src/core/pipeline/model.ts`: поле `uses` у `ScriptStep` (имя, слой,
  путь манифеста, отпечаток манифеста, сведённые параметры); новые причины в
  `ScriptUnresolved` — ненайденное имя, дефектный манифест, несовпадение имени,
  отсутствующий файл шага, промах параметров.
- [x] 3.3 `src/core/pipeline/expand.ts`: сборка шага из манифеста — путь,
  раннер, argv, отпечаток файла, схема выхода; `with` раскрывается тем же
  типизированным проходом, каким раскрывается `input` (вынос из общего обхода
  теми же `omitStepInputs`/`restoreStepInputs`).
- [x] 3.4 `src/core/pipeline/late.ts`: `with` проходит отложенное раскрытие тем
  же проходом, что и `input`.
- [x] 3.5 `src/core/pipeline/expand.ts`: `describeScriptUnresolved` пополняется
  новыми причинами — один текст на линт, прогон и вердикт.
- [x] 3.6 `src/core/pipeline/lock.ts`: блок `uses` в записи шага рядом с
  разрешённым; `src/core/pipeline/lockRead.ts` — чтение тех же полей.

## 4. Линт и исполнение

- [x] 4.1 `src/core/lint.ts`: диагностика шага `uses` по причине —
  ненайденное имя с перечнем каталогов, дефектный манифест, несовпадение имени,
  отсутствующий файл, промахи параметров; каждое сообщение называет работу, шаг
  и файл манифеста.
- [x] 4.2 `src/core/lint.ts`: значение параметра с отложенной подстановкой
  пропускается проверкой схемы тем же правилом, что и путь с подстановкой
  (`pathCheckSkipped`); состав параметров проверяется всегда.
- [x] 4.3 `src/core/run/runner.ts`: проверка сведённых параметров схемой перед
  записью `input.json`; отказ — одна терминальная попытка с названной причиной,
  тем же приёмом, что `unresolvedScriptOutcome`.
- [x] 4.4 `src/core/run/runner.ts`: блок `uses` в записи шага рядом с блоком
  `script`.

## 5. Встроенный образец

- [x] 5.1 `src/builtin/steps/changed-files/`: `step.yml` (имя, описание,
  параметры `ref` и `include_untracked`, схема выхода, `file`), `main.mjs` на
  голом контракте и `output.schema.json`.
- [x] 5.2 `src/builtin/steps/README.md` — как встроенный слой находится от
  расположения движка и почему образец не зависит ни от SDK, ни от обёртки; по
  образцу `src/builtin/scripts/README.md`.
- [x] 5.3 `package.json`: убедиться, что запись `files` покрывает
  `src/builtin/steps` (сегодня объявлен `src/builtin` целиком — проверить, что
  каталог попадает в пакет).
- [x] 5.4 Убедиться, что встроенный слой находится и в снимке движка прогона.

## 6. Витрина

- [x] 6.1 `src/ui/steps.ts` (новый): сбор каталога по трём слоям — имя,
  описание, слой, параметры с типами и умолчаниями, схема выхода, перекрытые
  одноимённые, причина дефектного манифеста.
- [x] 6.2 `src/ui/server.ts` и `src/ui/routes.ts`: обработчик `/api/steps`,
  маршрут `/steps`, пункт меню «Шаги» после «Пайплайнов».
- [x] 6.3 `ui/src/pages/Steps.tsx`: экран каталога — карточка шага с описанием,
  слоем, перечнем параметров и схемой выхода.
- [x] 6.4 `src/ui/pipelines.ts` и карточка шага прогона: имя, слой и параметры
  переиспользуемого шага; путь и раннер остаются, но имя занимает первое место.

## 7. Публикуемые схемы и документация

- [x] 7.1 `scripts/schema-targets.ts`: цель `schema/step-manifest.schema.json`;
  перегенерировать схемы (`npm run schema`).
- [x] 7.2 `docs/pipeline-format.md`: раздел о переиспользуемом шаге — каталог и
  манифест, `uses` и `with`, типизированная подстановка, что объявляет манифест
  и чего не переопределяет место вызова, диагностика линта.
- [x] 7.3 `docs/pipeline-format.md`, «Разрешение путей»: четвёртое правило —
  имя шага слоями каталогов шагов, пути внутри манифеста от каталога шага.
- [x] 7.4 `docs/run-layout.md`: блок `uses` в записи шага и в замке.
- [x] 7.5 `README.md`: каталог шагов в обзоре возможностей — одной фразой,
  рядом со скриптовыми шагами.

## 8. Сдача

- [x] 8.1 Зелёная `npm run check`.
- [x] 8.2 `openspec validate "reusable-steps" --strict` проходит.
- [x] 8.3 Проверить на живом пайплайне: работа из встроенного шага
  `changed-files` и агентского шага, читающего `${jobs.*.output.files}`;
  убедиться, что параметры доезжают, выход публикуется, витрина показывает
  каталог и карточку, а повторный прогон переиспользует шаг — и перестаёт
  после правки манифеста.
- [x] 8.4 Убедиться, что названное в `design.md` как Non-Goals не выдано за
  сделанное: установки шагов извне, имён с пространством автора, агентского
  переиспользуемого шага, умолчаний `timeout`/`env`/`expect` в манифесте и
  команды `stepcast steps` в изменении нет. Очередь ведёт движок петли —
  `backlog.md` руками не правится.
