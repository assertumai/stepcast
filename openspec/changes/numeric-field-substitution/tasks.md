## 1. Разбор числовых величин

- [x] 1.1 Добавить в `src/core/units.ts` разбор счётчика (`parseCount`): принимает число и строку, требует целое положительное, суффиксы величин отклоняет отдельной подсказкой, сообщение называет ожидаемую форму
- [x] 1.2 Добавить туда же разбор процента (`0..100`, дробная часть допустима) и целого кода возврата (допускает ноль и отрицательные), в том же тоне сообщений
- [x] 1.3 Дать функциям разбора возможность дописать в подсказку выражение, из которого получено значение (необязательный аргумент), не меняя вызовы величин с суффиксами
- [x] 1.4 Тесты `test/units.test.ts`: строка и число дают одно значение; `много`, пустая строка, `2k`, `0`, дробное для счётчика отклоняются; сообщение называет ожидаемую форму

## 2. Схема принимает строку

- [x] 2.1 Ввести в `src/core/pipeline/schema.ts` союз `count` (`string | number`) рядом с существующим `amount`
- [x] 2.2 Объявить им `concurrency`, `until.max_iterations`, `attempts.max`, `budget.rate_limit_pct` и `expect[].exit_code`, сняв со схемы ограничения `int`/`positive`/диапазон — они переезжают в разбор
- [x] 2.3 Проверить, что типы модели (`src/core/pipeline/model.ts`) остались числовыми: строкой значение живёт только между разбором документа и сборкой модели

## 3. Приведение после раскрытия

- [x] 3.1 Привести `attempts.max` в `toAttempts` (`src/core/pipeline/expand.ts`) перед проверкой против `limits.attempts`, сохранив прежний текст сообщения о превышении потолка
- [x] 3.2 Привести `rate_limit_pct` в `toBudget` и `exit_code` в `toPredicate`
- [x] 3.3 Привести `until.max_iterations` при сборке работы; отсутствие поля по-прежнему даёт ошибку «Цикл until объявлен без max_iterations»
- [x] 3.4 Раскрывать подстановки в скалярных полях документа пайплайна в области `pipelineScope` и собирать их в общую карту `substitutions`; привести `concurrency` из раскрытого значения
- [x] 3.5 Отклонять значение, дошедшее до приведения с нераскрытой подстановкой: ошибка называет пространство (`jobs`, `run`, `env`) и говорит, что числовое поле раскрывается при разборе пайплайна
- [x] 3.6 Поднимать из карты `substitutions` по точечному пути поля выражение-источник и передавать его в разбор, чтобы сообщение об отказе называло `${params.x}`

## 4. Тесты движка

- [x] 4.1 `test/expand.test.ts`: `max_iterations`, `attempts.max` и `concurrency` из параметра и входа раскрываются в числа; литеральная запись разбирается как прежде
- [x] 4.2 `test/expand.test.ts`: нечисловое значение отклоняется с путём поля, значением и выражением-источником в сообщении
- [x] 4.3 `test/expand.test.ts`: `${jobs.*}`, `${run.*}` и `${env.*}` в числовом поле отклоняются с объяснением, а не с отказом схемы
- [x] 4.4 `test/expand.test.ts`: `attempts.max` из подстановки, превышающий `limits.attempts`, даёт ошибку о потолке
- [x] 4.5 `test/lint.test.ts`: пайплайн с параметризованными числовыми полями проходит `stepcast lint`, а предупреждение о худшем случае работы с циклом считается по раскрытому пределу

## 5. Пример целевого состояния

<!-- examples/ вне границ этого прогона (разрешены только src/, test/, docs/
и openspec/changes/numeric-field-substitution/) — задачи ниже не выполнялись
в этой сессии; правки examples/README.md и
examples/target-state/openspec-change.yml из предыдущей попытки отменены. -->

- [ ] 5.1 Прогнать `stepcast lint examples/target-state/openspec-change.yml --input change=<любой>` из корня репозитория и устранить оставшиеся препятствия примера, не вводя неподдерживаемых возможностей (в том числе литеральный путь контекста `AGENTS.md`, которого в этом репозитории нет)
- [ ] 5.2 Снять устаревшую шапку `examples/target-state/openspec-change.yml` о том, что пример не запускается из-за `until` и `on_exceed: wait`, и привести её к действительному положению дел
- [ ] 5.3 Обновить `examples/README.md`: подстановка в числовые поля, `on_exceed: wait` и предикат `judge` перестали быть препятствием; сказать, чем пример остаётся примером целевого состояния и что он проходит `lint`

## 6. Документация и схемы

- [x] 6.1 В `docs/pipeline-format.md` назвать поля, принимающие подстановку наравне с числом, и правило «раскрытие при разборе, приведение после него»; сказать, почему отложенные пространства в них недопустимы
- [x] 6.2 В `docs/pipeline-format.md` рядом с `attempts` и `until` показать параметризованную запись и оговорить, что суффиксов у счётчиков нет
- [x] 6.3 Обновить `docs/status.md`: строка формата упоминает подстановку в числовые поля; снять оговорку о непроверяемом `examples/target-state`, если она там есть
- [x] 6.4 Перегенерировать `schema/pipeline.schema.json` и `schema/job.schema.json` (`npm run schema`) и убедиться, что числовые поля в них стали союзом со строкой

## 7. Проверка

- [x] 7.1 `npm run check` проходит
- [x] 7.2 Проверить, что `pipeline.lock.yml` фиксирует приведённые числа: прогнать существующий тест фиксации и убедиться, что запись не изменилась
