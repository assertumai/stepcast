## 1. Ключ `project.tools` в конфигурации

- [x] 1.1 `src/core/config/schema.ts`: `RawProjectSchema` получает `tools: z.array(CheckCommandSchema).min(1).optional()` — элемент той же моделью непустой строки, что `check` и `spec.tool` (переиспользование, а не копия: копии расходятся), список непустой.
- [x] 1.2 Комментарий у ключа объясняет две вещи, которые из модели не видны: почему пустой список отклоняется (объявление, не объявляющее ничего, — забытая правка) и почему пробел внутри значения допущен намеренно (`./gradlew`, `npm run`).
- [x] 1.3 `src/core/config/resolve.ts`: `Config['project']` получает `tools: readonly string[] | undefined`, читается из `values.get('project.tools')` с проверкой на массив строк — рядом с `check` и группой `spec`.
- [x] 1.4 Убедиться, что `PROJECT_ONLY_KEYS`, `UNION_LIST_KEYS` и `TIGHTEN_ONLY_KEYS` править не нужно: `project.**` уже покрывает новый ключ, а отсутствие в двух других списках и есть требуемое поведение (замена целиком). Ничего не менять — только проверить и зафиксировать тестами 1.6 и 1.7.
- [x] 1.5 Тесты `test/config.test.ts`: объявление в проектном конфиге даёт список в объявленном порядке; необъявленный ключ — не ошибка и `undefined`; `tools: []` и `tools: ["  "]` отклоняются с ошибкой, называющей файл и ключ; `["./gradlew", "npm run"]` принимается как есть.
- [x] 1.6 Тест: `project: { tools: ["npm"] }` в глобальном конфиге отклоняется той же проверкой и тем же сообщением, что `project.check` там же (файл назван, `.stepcast/config.yml` предложен).
- [x] 1.7 Тест слияния слоёв: верхний слой заменяет список целиком (элементы нижнего не подмешиваются), а соседний ключ секции, объявленный другим слоем, не затирается.
- [x] 1.8 `stepcast config` печатает значение и его происхождение — проверить на списочном значении (прецедент есть: `env_deny`, `context.deny`) и покрыть тестом отчёта.

## 2. Ключ в документе пайплайна и в публикуемых схемах

- [x] 2.1 `src/core/pipeline/schema.ts`: `ProjectSchema` документа получает тот же ключ той же моделью — объявление в пайплайне перекрывает конфигурацию, а не заводит второй формат.
- [x] 2.2 Перегенерировать `schema/config.schema.json` и `schema/pipeline.schema.json` (`npm run schema`); убедиться, что в обеих `project.tools` описан как массив с `minItems: 1`, элемент — строка с `minLength: 1` и `pattern: "\\S"`.
- [x] 2.3 Убедиться, что `test/schema-generated.test.ts` проходит и что расхождение он ловит: временно поправить сгенерированный файл руками и увидеть отказ.

## 3. Пространство подстановки `project` пополняется именем `tools`

- [x] 3.1 `src/core/pipeline/expand.ts`: `PROJECT_NAMES` пополняется именем `tools` — перечень доступных имён в диагностике обязан его называть.
- [x] 3.2 `resolveProjectValues` получает строку `tools: document.project?.tools ?? config.project.tools` — тот же полистовой порядок слоёв, что у `check` и группы `spec`.
- [x] 3.3 Тесты `test/expand.test.ts`: `${project.tools}` раскрывается из конфигурации и из пайплайна (пайплайн перекрывает); необъявленный ключ даёт отказ, называющий оба места объявления; `${project.toolchain}` даёт отказ с перечнем имён, среди которых назван `tools`; документ без ссылки на имя раскрывается без объявления.

## 4. Раскрытие списочного значения в элемент списка

- [x] 4.1 `src/core/pipeline/interpolate.ts`: завести разбор шаблона, отвечающий на вопрос «даёт ли ровно одна подстановка этого шаблона список» — с возвратом самой подстановки и значений, чтобы обход не разбирал шаблон дважды.
- [x] 4.2 Ветка массива `interpolateTree` размножает такой элемент: по элементу на значение, окружающий текст применяется к каждому, место исходного элемента и порядок соседей сохраняются.
- [x] 4.3 Прочие подстановки размножаемого элемента раскрываются как обычно — по разу на каждую копию; отложенная (`${jobs.*}`, `${run.*}`, `${env.*}`) остаётся текстом в каждой копии и доезжает до позднего раскрытия.
- [x] 4.4 Две списочные подстановки в одном элементе — `StepcastError`, называющая поле и оба выражения; произведение не считается ни в каком виде.
- [x] 4.5 `renderValue`: подсказка при списочном значении в скалярном поле дополняется условием раскрытия («списочное значение раскрывается только в элементе списка»); прежний текст об обращении к группе вместо листа сохраняется — обе причины остаются различимы.
- [x] 4.6 Карта подстановок пишется по путям произведённых элементов (`…allow.5`, `…allow.6`, …), а не исходного индекса; убедиться, что `substitutions` раскрытого пайплайна не содержит пути, которого в документе больше нет.
- [x] 4.7 Тесты `test/interpolate.test.ts` (или ближайшего по месту): размножение по числу значений; список из одного значения даёт один элемент; порядок соседей сохранён; отложенная подстановка выживает в каждой копии; две списочные подстановки роняют разбор; скалярное поле роняет разбор с новой подсказкой.
- [x] 4.8 Тест `test/lint-paths.test.ts`: список путей, объявленный одним элементом со списочной подстановкой из `${project.*}`, проверяется на существование поэлементно — отсутствующий путь назван именно тот, что отсутствует.
- [x] 4.9 Убедиться, что раскрытие идёт до сборки прав и предикатов: `toPermissions` и `toPredicate` получают уже размноженный список, и правок в них не требуется.

## 5. Работы петли берут права из объявления

- [x] 5.1 `.stepcast/pipelines/self-improve.yml`: в секцию `project` добавляется `tools: [npm, npx, node]` — рядом с `check` и `spec`, с комментарием о том, почему объявление живёт в пайплайне, а не в `.stepcast/config.yml` (та же причина, что у соседей: заход петли этот файл завести пока не может).
- [x] 5.2 `.stepcast/jobs/implement.yml`: три права `Bash(npm *)`, `Bash(npx *)`, `Bash(node *)` заменяются одной строкой `Bash(${project.tools} *)`; права на `git` остаются литералом с коротким объяснением, почему они не переехали (инструмент движка, а не репозитория).
- [x] 5.3 `.stepcast/jobs/fix-review.yml`: та же замена, то же объяснение.
- [x] 5.4 `stepcast lint .stepcast/pipelines/self-improve.yml` проходит без новых диагностик; раскрытый пайплайн даёт обеим работам ровно те же три права в том же порядке, что и до правки.
- [x] 5.5 Прочие работы петли (`propose`, `plan`, `review`, `slots`, `merge`, `verify`, `finalize`) не трогать: выдача инструментов работе, у которой их сегодня нет, — правка поведения петли, вынесенная за границы изменения (см. Non-Goals в `design.md`).

## 6. Проверки переносимости

- [x] 6.1 `test/loop-portability.test.ts`: перечень запрещённых литералов файлов работ пополняется именем инструмента внутри права — образцы вида `Bash(npm`, `Bash(npx`, `Bash(node`. Запрещается именно право, а не всякое вхождение слова: комментарий файла работы, поясняющий пример, литералом права не является.
- [x] 6.2 Убедиться, что проверка падает до правки — на сегодняшних `implement.yml` и `fix-review.yml`, назвав оба файла.
- [x] 6.3 Чужое объявление в том же тесте (`foreignPipeline` и `RUN_PIPELINE`) пополняется `tools: [make]`; добавить утверждения: шаги `implement-a` и `fix-review-a` уходят с правом `Bash(make *)`, и ни `Bash(npm *)`, ни `Bash(npx *)`, ни `Bash(node *)` среди их прав нет.
- [x] 6.4 Тест переноса на несколько инструментов: объявление `tools: ["./gradlew", "java"]` даёт две записи в объявленном порядке — размножение работает не только на трёх значениях этого репозитория.
- [x] 6.5 Сквозной прогон против чужого объявления (третий блок теста) остаётся зелёным: объявление `tools` обязательно там, где на него ссылаются, и его отсутствие в пайплайне теста должно ронять прогон — проверить и это, отдельным утверждением.

## 7. Документы

- [x] 7.1 `docs/config.md`: раздел «Инструменты репозитория» рядом с «Команда проверки проекта» и «Практика спецификации» — что объявляется (имена, не права), почему список, почему пустой список отклоняется, почему пробел в значении допустим, запрет глобального слоя, замена списка между слоями. Пример секции `project` в начале файла дополняется ключом.
- [x] 7.2 `docs/pipeline-format.md`, «Когда что раскрывается»: состав пространства `project` пополняется именем `tools` и оговоркой, что это первое списочное имя.
- [x] 7.3 `docs/pipeline-format.md`, раздел о подстановках: правило раскрытия списочного значения в элемент списка — с примером `Bash(${project.tools} *)`, условием «ровно одна списочная подстановка на элемент» и отказом в скалярном поле.
- [x] 7.4 `openspec/changes/job-tools-declaration/status.md`: раздел «Работает» с новой строкой об объявлении инструментов и о правиле раскрытия; пересобрать `docs/status.md` командой `npm run status:build` (собранный документ руками не править).
- [x] 7.5 `README.md` править не нужно — состава секции `project` он не называет вовсе (проверено при подготовке изменения); задача только в том, чтобы не завести туда литерал при правке соседних абзацев.

## 8. Проверка целиком

- [x] 8.1 `npm run check` зелёный.
- [x] 8.2 `openspec validate "job-tools-declaration" --strict` проходит.
- [x] 8.3 Границы правок соблюдены: изменение не трогает `backlog.md` и `dist/`.

## 9. Правки по итогам ревью

- [x] 9.1 Размножение отключено на позднем раскрытии (`resolveListExpansion` отвечает `undefined` при `mode: 'late'`): выход работы — произвольный JSON, где список строк обычен, и элемент вроде `"${jobs.x.output.changed_files}"` менял бы состав прав или границ правок молча, уже после фиксации снимка. Прежнее поведение («непредставимое строкой» с объяснением про состав выхода) сохранено; закреплено тестами в `test/late.test.ts`.
- [x] 9.2 Пустой список отклоняется самим раскрытием, а не только схемами: ноль значений удалял бы элемент целиком. Тест в `test/expand.test.ts` называет поле и сообщение.
- [x] 9.3 Подсказка `renderValue` о раскрытии в элементе списка выдаётся только там, где правило действует, — на списке строк при разборе документа. Массив не из строк получает перечень допустимых значений, поздний этап — объяснение про состав выхода работы.
- [x] 9.4 Сценарий «Раскрытое значение попадает в снимок пайплайна» закрыт тестом: `serializeLock` пайплайна со ссылкой на `${project.tools}` даёт объявленные строки и ни одной подстановки.
- [x] 9.5 Сценарий «Права собираются из объявления» закрыт тестом на настоящем `.stepcast/pipelines/self-improve.yml`: `implement` и `fix-review` обеих дорожек несут прежний список прав целиком, в прежнем порядке.
- [x] 9.6 Документы и спека приведены в соответствие: `docs/pipeline-format.md` и `design.md` больше не утверждают, что отложенные пространства списков не дают; в дельту `pipeline-definition` добавлены требования и сценарии о позднем раскрытии, пустом списке и адресности подсказки.
