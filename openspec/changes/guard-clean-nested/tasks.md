## 1. Проверка чистоты обходит объявленные репозитории

- [x] 1.1 `test/lanes.test.ts`: тест-стенд «корень плюс вложенный репозиторий, игнорируемый корнем» (`gitInit` на оба, `.gitignore` корня, коммит в каждом) и первый тест по нему — грязный вложенный при чистом корне обязан отказывать. Написать и убедиться, что он падает **до** правки `tree.ts`: сегодня проверка отвечает «чисто».
- [x] 1.2 `src/core/lanes/tree.ts`: `CleanTreeOptions` пополняется объявленным составом (`nested?: readonly string[]`), комментарий у поля называет, откуда состав берётся (`project.nested_repos`) и почему ядро читает его параметром, а не конфигурацией.
- [x] 1.3 Там же: `assertCleanTree` обходит корень и каждый объявленный каталог, снимая `status --porcelain` в каждом своим вызовом `git -C`. Порядок — корень, затем состав в том виде, в каком он пришёл (конфигурация уже даёт его каноническим).
- [x] 1.4 Маршрутизация `allow`: путь приводится к пути от корня и достаётся тому репозиторию, чей объявленный каталог — самый длинный совпавший префикс; сверяется со `status` этого репозитория относительно его корня. Правило то же, что у `routeOf` в `src/core/anchor/composite.ts`, — если общий помощник напрашивается, вынести его, не дублируя тело.
- [x] 1.5 Сообщение отказа: перечисляет **все** нечистые репозитории — корень словом «корень», вложенный своим объявленным каталогом. Подсказка (`hint`) остаётся прежней по смыслу: откат стирает незакоммиченное безвозвратно.
- [x] 1.6 Отдельный отказ на объявленный каталог, которого нет либо который не является рабочим деревом git: сообщение называет каталог и отличает его от корня. Отказ корня («дерево запуска не является репозиторием git») сохраняется дословно.
- [x] 1.7 Тесты `test/lanes.test.ts`: грязный вложенный при чистом корне (1.1); имя нечистого репозитория в сообщении; нечисты сразу несколько — названы все; неотслеживаемый файл во вложенном учитывается наравне с изменённым; `allow` относится к дереву, где путь лежит (очередь в корне, очередь во вложенном); одноимённый файл в соседнем дереве исключением не прощается; объявленный каталог не репозиторий — отказ, называющий его; необъявленный состав — все действующие тесты проходят без правок.

## 2. Сведение дорожек передаёт состав проверке

- [x] 2.1 `src/core/lanes/merge.ts`: `MergeLanesOptions` получает объявленный состав и передаёт его в `assertCleanTree` рядом с `allow: [file]`. Комментарий у вызова дополняется: исключение очереди относится к тому дереву, где очередь лежит.
- [x] 2.2 `src/cli/commands/merge-lanes.ts`: состав берётся из уже читаемой `resolveConfig({ cwd })` (`config.project.nestedRepos`) и передаётся в `mergeLanes`.
- [x] 2.3 Тест `test/merge-lanes.test.ts`: на прогоне с многорепным деревом грязный вложенный при чистом корне останавливает сведение до первого наложения — дерево не тронуто, очередь не правлена, код `2`; очередь внутри вложенного репозитория сведению не мешает.

## 3. Команда `stepcast assert-clean`

- [x] 3.1 `src/cli/commands/assert-clean.ts`: разбор ключа `--allow` (перечень через запятую, как `--lanes`), чтение конфигурации, вызов `assertCleanTree` с составом, печать отказа и код возврата. Позиционных аргументов нет.
- [x] 3.2 `src/cli/main.ts`: объявление команды в `COMMANDS` (описание одной строкой, ключ `--allow` с описанием) и ветка в `run`.
- [x] 3.3 Проверить, что `src/core/index.ts` экспортирует всё, что команде нужно, и добавить недостающее рядом с действующим экспортом `assertCleanTree`.
- [x] 3.4 Новый тест `test/cli-assert-clean.test.ts` по образцу `test/cli-backlog.test.ts`: чистое дерево — код `0`; грязный корень — код `2` с именем репозитория; грязный вложенный при чистом корне — код `2` с именем каталога; `--allow` прощает названный файл в том дереве, где он лежит; проверка ничего не изменила (файлы на месте, `HEAD` не двинулся); вне репозитория git — код `2` с причиной.

## 4. Головной шаг петли зовёт команду

- [x] 4.1 `.stepcast/jobs/slots.yml`: шаг `guard-clean-tree` зовёт `"$STEPCAST_BIN" assert-clean` вместо `test -z "$(git status --porcelain)"`; комментарий над шагом объясняет, почему проверка съехала в команду (состав вложенных репозиториев не публикуется подстановкой, и файл работы прочитать его неоткуда).
- [x] 4.2 Убедиться, что литерала `git status` в `.stepcast/jobs/**` и `.stepcast/prompts/**` не осталось; если `test/loop-portability.test.ts` умеет это стеречь — дописать проверку туда, к действующим проверкам переносимости работ петли.
- [x] 4.3 Прогнать `npm run check` и убедиться, что работа `slots` по-прежнему раскрывается в дереве без `schema/` (действующий тест переносимости).

## 5. Документация и статус

- [x] 5.1 `docs/run-layout.md`: раздел о команде `stepcast assert-clean` рядом со «Сведением дорожек» (что проверяет, ключ `--allow`, коды возврата, почему ничего не правит) и уточнение абзаца «Чистое дерево — предусловие команды» — предусловие охватывает объявленные вложенные репозитории.
- [x] 5.2 `docs/config.md`, раздел «Вложенные репозитории»: состав читает ещё и проверка чистоты — назвать это рядом с уже перечисленными читателями состава.
- [x] 5.3 `openspec/changes/guard-clean-nested/status.md`: фрагмент о том, что теперь работает, и пересборка `docs/status.md` командой `npm run status:build` (собранный документ руками не править).
- [x] 5.4 Финальный `npm run check`.

## 6. Правки по ревью

- [x] 6.1 `src/core/lanes/merge.ts`: очередь внутри объявленного вложенного репозитория — отказ до проверки чистоты и до первого наложения (`nestedRepoOf`), потому что `commitAll` коммитит только корень и отметка `done` осталась бы незакоммиченной. Спека `lane-merge` и фрагмент статуса приведены к этому: сценарий «сведение начинается» заменён отказом, ограничение названо в «Пока нет».
- [x] 6.2 `src/cli/commands/assert-clean.ts`: проверяется корень рабочего дерева (`findProjectRoot`), а не буквальный каталог запуска — `resolveConfig` ищет проектный конфиг ровно в переданном каталоге, и из подкаталога команда не прочла бы состав вовсе, ответив «чисто» на грязный вложенный. Пути `--allow` остаются относительными каталогу запуска.
- [x] 6.3 `src/core/lanes/tree.ts`: отсутствующий объявленный каталог отличён от каталога без git теми же формулировками, что у `checkWorkspaceAvailability`; отказ git на снятии `status` вложенного завёрнут в названный отказ вместо «внутренней ошибки».
- [x] 6.4 `src/core/lanes/tree.ts`: дерево без объявленного состава отвечает прежним текстом отказа — слово «корень» объясняло бы состав, которого нет. Оба текста закреплены тестами.
- [x] 6.5 Тесты: связка «конфигурация → состав → предусловие» на уровне CLI `merge-lanes`; записи корня о самой части (`?? backend/` и сдвинутый gitlink) как нарушение чистоты корня; вызов `assert-clean` из подкаталога; `--allow` от каталога запуска.
- [x] 6.6 `docs/run-layout.md`: двойное отрицание в абзаце про `--allow` выправлено, отказ на очереди внутри части назван в предусловии `merge-lanes`.
