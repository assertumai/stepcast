## 1. Формат дашборда, схема и чистый модуль

- [x] 1.1 Объявить модель файла дашборда (Zod) в `src/ui/dashboardsFile.ts`: заголовок, `grid.columns`, список ячеек (`id`, `widget`, `at.column/row/width/height`, `title`, `params` со скалярными значениями); неизвестный ключ — отказ с файлом и местом (Решение 1, 12)
- [x] 1.2 Внести цель генерации `schema/dashboard.schema.json` в `scripts/schema-targets.ts`, напечатать схему (`npm run schema`) и убедиться, что `test/schema-generated.test.ts` зелёный
- [x] 1.3 Завести `src/ui/dashboards.ts` — чистый модуль без импортов Node, общий демону и браузеру: модель ячейки, подстановка `${params.<имя>}` тем же языком, что у параметров цели маршрута, приведение значения к типу манифеста после подстановки, тексты отказов (Решение 8)
- [x] 1.4 Проверки, которым довольно документа: уникальность `id` ячеек, ячейка в пределах `grid.columns`, отсутствие наложений — отказ дашборда целиком с файлом и местом (Решение 9, 12)
- [x] 1.5 `test/ui-dashboards.test.ts`: подстановка, приведение к числу и булеву, значение не того типа с названным выражением, повтор `id`, выход за колонки, наложение

## 2. Чтение слоёв, сборка и запись файла

- [x] 2.1 Прочитать два каталога слоёв в `src/ui/dashboardsFile.ts` — домашний и проектный по корню демона; отсутствие каталога — обычное состояние; `<id>` — безопасный сегмент, вложенные каталоги и чужие расширения не дашборды (Решение 1, 2)
- [x] 2.2 Перекрытие по `<id>` файлом целиком, с сохранением имени файла-источника у действующего дашборда (Решение 2)
- [x] 2.3 Отдать дашборды с диагностикой рядом с таблицей маршрутов — тем же местом, что и таблица маршрутов: `src/ui/watcher.ts` (`currentDashboards()`) и `src/ui/dashboards/row.ts` (`GET /api/dashboards`), а не `src/ui/kernel.ts` — маршруты в этом дереве тоже собираются в `watcher.ts`, не в `kernel.ts`, и дашборды заведены той же линией; отказ одного дашборда не отменяет остальные и едет причиной (Решение 10)
- [x] 2.4 Запись файла слоя через `Document` библиотеки `yaml`: комментарии, порядок ячеек и форматирование переживают правку; неразбираемый файл не переписывается; отпечаток документа сверяется с тем, от которого началась правка (Решение 11)
- [x] 2.5 `test/ui-dashboards-file.test.ts`: слои и перекрытие целиком, отсутствие каталогов, сломанный файл, запись с сохранением комментариев, отказ записи в неразбираемый файл, отказ по разошедшемуся отпечатку

## 3. Слот виджетов, каталог и манифесты

- [ ] 3.1 Объявить слот `WIDGET` (`keyed`) и типы манифеста (`WidgetManifest`, `WidgetParam`) и props виджета в `ui/src/sharedSlots.ts` — поверхность `@stepcast/slots` (Решение 4, 7)
- [ ] 3.2 Завести сервис каталога `ui/src/services/widgets.ts`: регистрация манифеста областью вкладчика (приёмом `ctx.screens.register` демона), снятие вместе с областью, снимок для конструктора (Решение 5)
- [ ] 3.3 Проверить, что виджет без манифеста — виджет без параметров, а повтор ключа отвергается обычным `duplicate-key` реестра слотов с названной причиной (Решение 4, 5)
- [ ] 3.4 `ui/test/widgets.test.tsx`: вклад встроенного и файлового виджета в один слот, регистрация и снятие манифеста, отказ по повторному ключу

## 4. Виджеты-файлы: props, манифест, загрузка по требованию

- [ ] 4.1 Передать props в модуль виджета в `ui/src/widgetHost.tsx` и прочитать его манифест именованным экспортом; отсутствие манифеста — не отказ (Решение 6, 7)
- [ ] 4.2 Завести браузерную строку `ui/src/plugins/widgets.tsx`: ключ виджета-файла (`<projectKey>/<id>`) попадает в слот и в каталог сразу по событию `widgets`, модуль импортируется по требованию — ячейкой дашборда либо открытым каталогом (Решение 6)
- [ ] 4.3 Показать в каталоге три состояния виджета-файла: манифест прочитан, ещё не прочитан, отказ компиляции — той же карточкой, что и на месте ячейки (Решение 6)
- [ ] 4.4 Снять отклонение параметров цели у вида `widget` в `ui/src/plugins/shell.tsx`; параметр, которого нет в манифесте, — названная причина на месте цели
- [ ] 4.5 Перевести `ui/src/pages/Widgets.tsx` на каталог с манифестами вместо перечня карточек

## 5. Пять встроенных виджетов

- [ ] 5.1 `runs` — список прогонов (`project`, `pipeline`, `status`, `limit`), общей с экраном «Прогоны» таблицей (Решение 14)
- [ ] 5.2 `pipeline` — состояние пайплайна: его последний либо названный прогон (`project`, `pipeline`, `run`)
- [ ] 5.3 `usage` — расход за период (`days`, `project`) запросом к `GET /api/usage`
- [ ] 5.4 `step-output` — вывод шага (`project`, `run`, `job`, `step`, `attempt`) запросом к `GET /api/step-output`
- [ ] 5.5 `run-pipeline` — кнопка запуска (`project`, `pipeline`, `label`) запросом к `POST /api/run`, с показом отказа на месте виджета
- [ ] 5.6 Убедиться, что новых маршрутов чтения ради виджетов не заведено, а общие с экранами куски вынесены в общие компоненты, а не скопированы

## 6. Вид цели `dashboard` и показ дашборда

- [x] 6.1 Добавить форму цели `dashboard: <id>` в модель строки маршрута и в `schema/routes.schema.json` (Решение 3)
- [ ] 6.2 Внести вклад в `route.target` по ключу `dashboard` новой браузерной строкой `ui/src/plugins/dashboard.tsx`, не трогая `ui/src/plugins/shell.tsx` (Решение 3)
- [ ] 6.3 Отрисовать дашборд по сетке (`ui/src/pages/Dashboard.tsx`): ячейки на объявленных местах, заголовок карточки из ячейки либо из манифеста
- [ ] 6.4 Проверить смысл ячейки на странице: неизвестный виджет, необъявленный параметр, недостающий обязательный, неверный тип — причина на месте ячейки, соседи целы (Решение 9)
- [ ] 6.5 Показать причину и предложение создать файл на маршруте, ведущем на несуществующий дашборд (Решение 11)
- [ ] 6.6 Взять название пункта меню для цели-дашборда из заголовка дашборда (`nav.title` не объявлен)

## 7. Живое применение

- [x] 7.1 Добавить часть отпечатка по двум каталогам дашбордов в `src/ui/watcher.ts` и пересобирать дашборды только по её сдвигу (Решение 10)
- [x] 7.2 Отправлять событие `dashboards` в `src/ui/screens/rows.ts` только при отличии от отправленного прежде (демон); приём в `ui/src/services/live.ts` и типы в `ui/src/api.ts` — отдельно, часть браузерной работы (не сделано)
- [ ] 7.3 Показать удалённый дашборд причиной на его месте, а не прежней картинкой (браузер, не сделано)
- [x] 7.4 `test/ui-watcher.test.ts`: часть отпечатка, отсутствие лишних перечитываний, событие только при отличии

## 8. Конструктор и запись из витрины

- [ ] 8.1 Режим правки в том же вкладе цели: выбор ячейки, добавление из каталога, удаление, перемещение и изменение размера по сетке (Решение 11)
- [ ] 8.2 Панель параметров выбранной ячейки по манифесту: тип, обязательность, умолчание, пояснение; подстановка вводится текстом
- [x] 8.3 Строка состава `src/ui/dashboards/row.ts` с `GET /api/dashboards` и `POST /api/dashboards`; демон называет файл до записи; слой — `home` либо `project`
- [x] 8.4 Внести строку в `UI_ROWS` (`src/ui/screens/rows.ts`) и проверить, что её отключение патчем убирает правку, не гася показ дашбордов
- [ ] 8.5 `ui/test/dashboard.test.tsx`: раскладка по сетке, параметры маршрута в ячейках, битая ячейка, живая правка файла, добавление/удаление/перемещение/настройка и сохранение

## 9. Запуск прогона из витрины

- [x] 9.1 Строка состава `src/ui/runLaunch/row.ts` с `POST /api/run`: проект из `projects.json`, файл пайплайна из `listPipelineFiles`, ничего сверх (Решение 13)
- [x] 9.2 Порождать отсоединённый дочерний процесс `stepcast run` с `cwd` в корне проекта; ответ 202 без идентификатора прогона; прогон приходит обзором наблюдателя
- [x] 9.3 Внести строку в `UI_ROWS` и проверить, что её отключение патчем убирает запуск, не задевая прочего
- [x] 9.4 `test/ui-server.test.ts`: `GET`/`POST /api/dashboards`, `POST /api/run` с подставным порождением процесса, отказы по неизвестному проекту и неизвестному пайплайну

## 10. Документация и образец

- [ ] 10.1 Написать `docs/dashboards.md`: формат и схема, два слоя и перекрытие целиком, маршрут как единственный адрес, подстановки и типы параметров, сетка и её проверки, отказы файла и ячейки, конструктор и запись, каталог виджетов
- [ ] 10.2 Снять в `docs/widgets.md` Non-Goal про `props` и данные, описать манифест и props виджета
- [ ] 10.3 Обновить `docs/routes.md`: третий вид цели, снятое отклонение параметров цели у виджета, название пункта от заголовка дашборда
- [ ] 10.4 Назвать каталог дашбордов среди файлов слоёв в `docs/config.md`; описать вклад виджета плагином в `docs/ui-plugins.md`
- [ ] 10.5 Положить `examples/dashboards/run.yml` — дашборд из встроенных виджетов с параметрами маршрута — и сослаться на него из `docs/dashboards.md`

## 11. Зелёная проверка

- [ ] 11.1 `npm install --no-audit --no-fund && npm run check` — зелено целиком
