## 1. Объявление источника в формате

- [x] 1.1 Добавить в `WorkspaceSchema` (`src/core/pipeline/schema.ts`) ключ
      `inherit` со значением-строкой; признавать его только в теле работы —
      на уровне пайплайна и в `defaults.workspace` отклонять с именем ключа
- [x] 1.2 Провести `inherit` через модель (`src/core/pipeline/model.ts`:
      `Workspace`) и раскрытие работ так, чтобы значение доходило до
      исполнения; сохранить его в `pipeline.lock.yml` вместе с остальным
      раскрытым пайплайном
- [x] 1.3 Перегенерировать `schema/pipeline.schema.json` и
      `schema/job.schema.json` командой `npm run schema`
- [x] 1.4 Дополнить `test/expand.test.ts`: работа с `inherit` раскрывается и
      попадает в лок; работа без него раскрывается как прежде

## 2. Статические проверки

- [x] 2.1 Добавить в `src/core/lint.ts` отказы: `inherit` называет
      несуществующую работу; `inherit` называет работу вне зависимостей этой
      работы; `inherit` объявлен при режиме `cwd`; работа в изолирующем режиме
      имеет больше одной зависимости и `inherit` не объявлен. Каждое сообщение
      называет работу и допустимые значения
- [x] 2.2 Учесть `needs: all` при проверке числа зависимостей: раскрытая
      зависимость от всех работ считается по факту, а не по форме записи
- [x] 2.3 Провести те же проверки до запуска первой работы в
      `checkWorkspaceAvailability` (`src/core/run/workspace.ts`), чтобы
      пайплайн, минующий `lint`, отказывал до исполнения шагов, а не посреди
      прогона
- [x] 2.4 Дополнить `test/lint.test.ts` пятью случаями из спецификации, включая
      принятый случай единственной зависимости без `inherit`

## 3. Разрешение источника и раскладка каталогов

- [x] 3.1 Добавить в `src/core/graph.ts` (или рядом) число потомков работы —
      величину, по которой различается цепочка и развилка
- [x] 3.2 Написать разрешение источника наследования: единственная зависимость;
      объявленный `inherit`; `none`; транзитивный переход к источнику
      источника, когда у выбранного нет зафиксированного дерева; исчерпание
      цепочки. Логику держать без ввода-вывода — как у планировщика работ,
      чтобы её можно было проверить без процессов и файлов
- [x] 3.3 Расширить `prepareWorkspace` (`src/core/run/workspace.ts`): принимать
      источник — его каталог, якорь и режим — и возвращать, продолжен каталог
      или заведён собственный. Цепочка возвращает каталог источника без
      подготовки; развилка готовит каталог как сегодня и приводит его к якорю
      источника через `restore`
- [x] 3.4 Отказ приведения к якорю проводить тем же путём, что и отказ
      подготовки директории: `spawn_failed` с названной причиной, ни одного
      исполненного шага
- [x] 3.5 Передать в `prepareWorkspace` из `src/core/run/runner.ts` каталоги и
      последние якоря завершившихся работ; хранить их в контексте прогона рядом
      с записями работ
- [x] 3.6 Убедиться, что якорь зависимой работы снимается тем же способом, что
      у источника (`manifest.anchor_kind` на прогон), и что индексные файлы
      двух работ, делящих каталог, не мешают друг другу. Проверено прогоном:
      цепочка из трёх работ в одном каталоге и копия внутри репозитория
      (`test/workspace.test.ts`) снимают якоря последовательно, каждая под
      своим `scope`-индексом, и стыковка `tree_before`/`tree_id` на границе
      работ сходится

## 4. Журнал и состояние

- [x] 4.1 Добавить в `JobRecord` (`src/core/journal/schema.ts`) поля источника:
      идентификатор работы-источника и признак продолженной директории; оба
      необязательные
- [x] 4.2 Записывать их в `runner.ts` там же, где сейчас пишется режим и путь
      рабочей директории
- [x] 4.3 Добавить событие о наследовании дерева (имя работы, имя источника,
      способ получения каталога) в перечень событий и писать его до первого шага
- [x] 4.4 Дополнить `test/journal.test.ts`: запись работы несёт источник;
      работа без наследования его не несёт; событие о наследовании в потоке есть

## 5. Поведение прогона под тестом

- [x] 5.1 Дополнить `test/workspace.test.ts` наследованием в режиме `worktree`:
      созданный, изменённый и удалённый предшественником файл виден зависимой
      работе; работа без зависимостей начинает от `HEAD`
- [x] 5.2 То же для режима `copy`, включая незакоммиченные изменения в голове
      цепочки
- [x] 5.3 Проверить раскладку каталогов: линейная цепочка из трёх работ делит
      один каталог; игнорируемый файл переходит по цепочке; развилка даёт по
      каталогу на потомка, и игнорируемый файл в них не попадает; пропуск
      одного из двух потомков цепочки не открывает
- [x] 5.4 Проверить выбор источника при нескольких зависимостях: объявленный
      `inherit` даёт дерево названной работы; `inherit: none` даёт исходное
      состояние
- [x] 5.5 Проверить пропуск по цепочке: источник пропущен по условию — берётся
      его источник; ни у одной работы цепочки дерева нет — берётся исходное
      состояние, работа исполняется
- [x] 5.6 Проверить стыковку якорей: состояние до первого шага зависимой работы
      равно состоянию после последнего шага источника

## 6. Возврат результата

- [x] 6.1 Проверить `applyRun` (`src/core/run/apply.ts`) на цепочке: наложение
      всех работ даёт итоговое состояние без пропусков и двойного применения;
      правку кода вносить только если проверка её потребует
- [x] 6.2 Убедиться, что наложение не требует завершённого прогона: работы,
      уже записавшие исход, накладываются, пока прогон идёт. Дополнить
      `test/diff.test.ts` или `test/workspace.test.ts` соответствующим случаем
- [x] 6.3 Проверить `--job` на работе из цепочки: накладывается вклад одной
      работы, а несостыковка с деревом даёт конфликт с перечнем путей, а не
      частичный результат

## 7. Петля саморазвития

Вне границ этого выполнения: правки трогали `.stepcast/*` и `scripts/*`, а
допущены только `src/`, `test/`, `docs/` и каталог самого изменения. Пункты
ниже отменены и оставлены невыполненными до отдельного захода с расширенными
границами.

- [ ] 7.1 Перевести `.stepcast/pipelines/self-improve.yml` на
      `workspace.mode: worktree`; снять комментарий о вынужденном `cwd`,
      объяснив на его месте раскладку цепочки и отдельную работу `finalize`
- [ ] 7.2 Добавить в голову цепочки работу установки зависимостей (`npm ci`) и
      объяснить в ней, почему свежее дерево `worktree` их не несёт
- [ ] 7.3 Объявить `finalize` работой в режиме `cwd` (переопределение на
      работе): она коммитит и обязана видеть настоящий репозиторий; `inherit`
      ей при этом не нужен и при `cwd` запрещён
- [ ] 7.4 Научить `scripts/finalize.mjs` накладывать результат прогона
      (`stepcast apply <run-id>`) на успешном пути до отметки пункта и коммита;
      на неуспешном — только помечать пункт, как сейчас. Идентификатор прогона
      брать из окружения шага, не из аргумента
- [ ] 7.5 Дополнить `test/finalize.test.ts`: успешный путь зовёт наложение до
      коммита; неуспешный не зовёт его вовсе; отсутствие пункта очереди
      по-прежнему ничего не делает
- [ ] 7.6 Прогнать `stepcast lint .stepcast/pipelines/self-improve.yml` и
      убедиться, что пайплайн принимается

## 8. Документация

- [x] 8.1 Дополнить раздел «Рабочая директория» в `docs/pipeline-format.md`:
      наследование дерева зависимой работой, ключ `workspace.inherit`,
      раскладка цепочки и развилки, поведение при пропущенном источнике
- [x] 8.2 Обновить `docs/status.md`: строка о рабочей директории называет
      наследование; известные ограничения пополняются двумя — неотслеживаемое
      содержимое не переносится в каталог развилки, и `apply --job` на работе
      из цепочки может не лечь на дерево без вкладов предшественников
- [ ] 8.3 Отметить в `docs/status.md`, что петля саморазвития идёт в
      изолирующем режиме, и снять оговорку о правке файла во время шага в той
      части, которая касалась петли — зависит от раздела 7, отменённого в этом
      выполнении по границам файлов
- [x] 8.4 Прогнать `npm run check` и убедиться, что типизация, линт и тесты
      зелёные

## 9. Правки по ревью

- [x] 9.1 Снимать якорь рабочей копии внутри репозитория средствами git: копия
      `.git` не содержит, и `capture` в ней отказывал — наследование в режиме
      `copy` внутри репозитория молча не работало. База объектов берётся у
      репозитория прогона (`GIT_DIR` + `GIT_WORK_TREE`), покрыто тестом
      «копия внутри репозитория фиксируется якорями»
- [x] 9.2 Отказывать до первой работы, когда граф требует засева, а состояние
      фиксируется хеш-манифестом (вне git): `restore` у манифеста невозможен
      по устройству. Якорь, не хранящий содержимого, наследование пропускает
      как отсутствующий, а не падает на нём посреди прогона
- [x] 9.3 Продолжать каталог цепочки по объявленному графу, а не по факту
      снятого якоря: отказ бухгалтерии у предшественника больше не уводит
      наследника в свежий каталог
- [x] 9.4 Не протаскивать пайплайновый `workspace.path` в работу, сменившую
      режим (`src/core/pipeline/expand.ts`)
- [x] 9.5 Убирать каталог, заведённый под неудавшийся засев, и покрыть
      сценарий «Приведение не удалось» тестом
- [x] 9.6 Развести кольцо импортов `lint.ts` ↔ `run/workspace.ts`: общие
      правила наследования переехали в `run/inherit.ts`
- [x] 9.7 Покрыть тестом отказ `workspace.inherit` на уровне пайплайна и в
      `defaults.workspace`
- [x] 9.8 Убрать гонку по времени из теста наложения на незавершённом прогоне:
      ожидание записанного исхода вместо сна на глазок
