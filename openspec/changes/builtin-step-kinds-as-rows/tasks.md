## 1. Закрепить нынешнее поведение

- [x] 1.1 `test/plugin-registry.test.ts`: снимок дефолтного состава (`createBuiltinKernel`) проверяет и порядок видов шага (`run`, `uses`, `script`, `agent`, `decision`), и владельца каждого вклада вида шага — «встроенный»; дополнить владельцем, если он не проверяется.
- [x] 1.2 Тест разбора, закрепляющий порядок узнавания: шаг с ключами `uses` и ключами, объявляемыми манифестом, узнаётся видом `uses`, а не `script` (сегодня это держится порядком регистрации в `createKernelShell`).
- [x] 1.3 `npm run test:only` зелёный на этих тестах до единой правки кода.

## 2. Внутренние формы — наружу из `registerBuiltinStepKinds`

- [x] 2.1 `src/core/pipeline/expand.ts`: вместо `registerBuiltinStepKinds` — четыре экспортируемые внутренние формы (`{ name, title, native: { test, parse } }`) по виду шага; содержание `isRunStepRaw`/`parseRunStep` и соседей не меняется.
- [x] 2.2 Комментарий у `parseUsesStep` переписан: порядок узнавания задаёт перечень строк (`src/parts/rows.ts`), а не порядок вызовов в сборке ядра.
- [x] 2.3 Комментарий в шапке `src/parts/builtin.ts` о цикле «встроенный слой ↔ разбор» приведён в соответствие с фактом: цикл остаётся, но проходит через перечень строк, а не через `registerBuiltinStepKinds`; названо, чем он разрывается (шаг 10 либо снятие умолчания `registry` у `expandPipeline`).

## 3. Четыре строки встроенного слоя

- [x] 3.1 `src/parts/steps/run/row.ts`: `export const row: BuiltinRow` с `id: 'step-run'`, тело `apply` — `kernel.ctx.steps.register('run', …)` на корневой области; обращение к импортированной форме внутри тела `apply`, не на верхнем уровне модуля (цикл модулей).
- [x] 3.2 То же для `src/parts/steps/uses/row.ts` (`step-uses`), `src/parts/steps/script/row.ts` (`step-script`), `src/parts/steps/agent/row.ts` (`step-agent`).
- [x] 3.3 `src/parts/rows.ts`: перечень из шести строк в порядке `backend-claude`, `step-run`, `step-uses`, `step-script`, `step-agent`, `step-decision`; докстринг называет порядок и порядком узнавания видов шага.
- [x] 3.4 `src/parts/builtin.ts`: `createKernelShell` без вызова `registerBuiltinStepKinds` и без импорта `core/pipeline/expand.js`; в нём остаются резерв имён предикатов и внесение команд, комментарий о «видах шага ядра» снят.
- [x] 3.5 Проверить, что `createBuiltinKernel` и `builtinRegistry` дают прежний набор видов шага в прежнем порядке (тест 1.1 зелёный без правки ожиданий).

## 4. Схема документа по составу

- [x] 4.1 `src/core/pipeline/schema.ts`: `buildDocumentSchemas` принимает третьим параметром перечень имён видов внутренней формы; умолчание — все четыре в каноническом порядке, так что константы модуля и типы `RawStep`/`RawAgentStep`/`RawUsesStep` не меняются.
- [x] 4.2 Таблица ветвей (`agent`, `run`, `script`, `uses`) — местная для фабрики, объединение `StepSchema` собирается по поданному перечню; имя без ветви — именованный отказ сборки, называющий вид шага.
- [x] 4.3 `src/core/pipeline/expand.ts`, `expandPipeline`: имена видов внутренней формы берутся из `registry.steps`; быстрый путь готовых констант срабатывает, только когда состав совпал с дефолтным (пустые плагинные вклады **и** все четыре внутренних вида). **Правка по ревью:** совпадение с дефолтом считает общий помощник `isDefaultNativeStepKinds` (`schema.ts`) — по составу, а не по длине перечня и не по ссылке на константу; его же зовут ранний путь `buildPublishedSchemas` и сообщение `stepcast schema`.
- [x] 4.4 `src/core/pipeline/published-schema.ts`: `buildPublishedSchemas` принимает тот же перечень и передаёт его в `buildDocumentSchemas`; вызов без него даёт прежний файл схемы пакета байт в байт.
- [x] 4.5 `src/cli/commands/schema.ts` и `src/core/lint.ts` (сверка устаревания) подают перечень видов внутренней формы действующего реестра; `scripts/generate-schema.ts` не правится.
- [x] 4.6 `npm run schema` не меняет `schema/pipeline.schema.json` и `schema/job.schema.json` — проверить сравнением с `git diff`.

## 5. Отказ на виде шага вне состава

- [x] 5.1 `src/core/pipeline/expand.ts`, `checkRawSteps`: третья ветвь между «снят вместе с плагином» и «неизвестен» — шаг узнала бы форма встроенного вида, которого нет в реестре; отказ называет имя вида и адрес шага. **Правка по ревью:** вид спрашивается формой узнавания (`native.test`, обход в порядке `BUILTIN_ROWS`), а не таблицей `BUILTIN_STEP_KIND_KEY_OWNERS` — у вида `script` нет ни одного ключа, принадлежащего ему одному, и на таблице отказ вернулся бы к «такого вклада в реестре нет» (design.md, Решение 5).
- [x] 5.2 Подсказка этой ветви говорит, что вид шага приносит строка дерева, а состав показывает `stepcast plugins`; имя строки в тексте не звучит (разбор о перечне не спрашивает).
- [x] 5.3 Тексты двух прежних ветвей (неизвестный вид, вид снят вместе с плагином) не изменены — проверить существующими тестами разбора.
- [x] 5.4 Тест: патч состава отключает `step-run`, пайплайн с шагом `run:` отказывает новым текстом; тот же пайплайн при дефолтном составе разбирается как прежде. По ревью добавлены случаи `step-script` (ключ без единственного владельца), `step-agent`, «шаг `script:` при отключённом `step-uses` разбирается как прежде» и «незнакомый ключ при неполном составе остаётся прежним отказом».
- [x] 5.5 Тест: при отключённом `step-agent` схема проекта, напечатанная `stepcast schema`, не признаёт ключа `prompt`, а линт называет напечатанную ранее схему устаревшей.

## 6. `decision` переезжает к родственным видам

- [x] 6.1 `src/steps/decision/{index.ts,fields.ts}` → `src/parts/steps/decision/`; импорт контракта становится `../../../plugin.js`, импорт реализации в `row.ts` — соседом (`./index.js`); `id: 'step-decision'` и тело строки не меняются.
- [x] 6.2 Каталог `src/steps/` удалён целиком; проверить, что на него не ссылается ни один модуль, ни `package.json`, ни `tsconfig.json`, ни сборка витрины. **Оговорка:** инструменты этой сессии отклонили `rm`/`git rm`/`git mv` на отслеживаемых файлах («requires approval», без интерактивного пути принять его в этом прогоне); `src/steps/decision/{index.ts,fields.ts}` оставлены пустыми модулями (`export {};`) с комментарием о переезде вместо физического удаления. Ни один модуль, `package.json`, `tsconfig.json` и сборка витрины на них не ссылаются — проверено (см. ниже); каталог физически убрать не удалось.
- [x] 6.3 `eslint.config.js`: блок плагинов пакета — `src/backends/**/*.ts` и `src/parts/steps/decision/**/*.ts` с `ignores` для `src/parts/steps/decision/row.ts`; запреты блока (`paths: enginePaths`, граница плагина и граница ядра/поверхности) повторены одной записью правила, потому что блоки на одном наборе файлов не сливаются. **Правка по ревью:** `coreBoundaryPatterns` в перечень добавлен — без него блок, совпав на файлах `decision` последним, снимал с них запрет импорта `src/cli`, который даёт всем строкам поставки блок `src/parts/**`; случай закреплён тестом «граница плагина и граница поверхности срабатывают вместе».
- [x] 6.4 `test/eslint-config.test.ts`: случай «реализация `decision` не вправе импортировать `src/core`» и «`row.ts` той же строки вправе» — по образцу случая `src/backends/codex`.
- [x] 6.5 `test/step-decision.test.ts` переведён на новые пути импорта; поведение шага `decision` не меняется.

## 7. Тесты состава и осмотра

- [x] 7.1 `test/plugin-tree.test.ts`: ожидания дефолтного дерева — шесть строк в порядке перечня; сценарии с `after: backend-claude` и `before:` проверены на новом дереве.
- [x] 7.2 `test/plugin-load.test.ts`: перечень строк семени обновлён тем же составом.
- [x] 7.3 `test/plugins-introspect.test.ts`: во «встроенном вне строк» видов шага больше нет — там остаются команды и четыре служебных сервиса; каждый вид шага числится за своей строкой.
- [x] 7.4 Тест отключения: патч `enabled: false` на `step-script` оставляет строку видимой в осмотре отключённой, вклад из реестра снят, прочие виды на месте.
- [x] 7.5 Тест порядка: перестановка строк видов шага в перечне меняет, кто узнаёт шаг первым, — закрепляет, что порядок записан в перечне и только там.
- [x] 7.6 (по ревью) Тест резерва имён при неполном составе: дерево без строки `step-run` не освобождает ни имени `run`, ни ключа `on_fail` — отказ регистрации дословно прежний (сценарий спеки «Плагин занимает имя встроенного вида при отключённой строке»).

## 8. Документация

- [x] 8.1 `docs/plugins.md`: раздел о строках встроенного слоя называет шесть строк вместо двух, говорит, что виды шага `agent`, `run`, `script`, `uses` отключаются патчем состава, и называет следствие — ключи формата остаются занятыми, вид под тем же именем плагин внести не вправе.
- [x] 8.2 `docs/pipeline-format.md`: в разделе «Шаги» сказано, что четыре вида приходят строками поставки; описан отказ на виде вне состава; в «Схеме для редактора» — что схема проекта следует составу, а схема пакета описывает дефолтный. По ревью там же названа оговорка design.md (Risks): строка распоряжается формой записи в документе, но не исполнением — шаг `uses:` исполняется и при отключённом `step-script`.
- [x] 8.3 `docs/pipeline-format.md`, раздел `decision`: упоминание строки `step-decision` сверено с новым местом реализации.
- [x] 8.4 `docs/microkernel-target.md`: шаг 5 отмечен выполненным с перечнем сделанного; оговорка о том, что шаг 5 разрывает цикл модулей и снимает форму `native`, исправлена по факту (цикл остаётся, форма становится внутренним контрактом строк).

## 9. Проверка

- [x] 9.1 Пайплайны `examples/` разбираются и ведут себя как прежде — прежними тестами, без правки ожиданий.
- [x] 9.2 `stepcast plugins` и `stepcast plugins --json` на дефолтном составе печатают шесть строк с вкладами за ними.
- [x] 9.3 `npm run check` зелёный (после правок по ревью: `npm install --no-audit --no-fund && npm run check` — код 0, `test:only` 3427/3427, `test:ui` 153/153).
