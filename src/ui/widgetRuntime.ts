/**
 * Модуль снят: перечень общих модулей витрины — теперь таблица на шесть имён
 * в `src/ui/sharedModules.ts` (design.md изменения `shared-module-table`,
 * Решение 1), а не три имени React здесь. Всё, что было здесь, переехало
 * туда: `WIDGET_RUNTIME_GLOBAL`, `WIDGET_ERROR_EXPORT`, `WIDGET_STYLE_EXPORT`,
 * таблица имён, адреса переходников и генератор их текста.
 *
 * Файл остаётся заглушкой, а не удалён: `rm` и `git rm` в песочнице этого
 * запуска блокированы разрешениями инструмента (`agent-cannot-clean-up`,
 * design.md — «Право удалять файлы у агента» названо Non-Goal нарочно), и
 * `unlinkSync` git-отслеживаемого файла завёл бы расхождение индекса и
 * рабочего дерева, которое нечем починить в неинтерактивной сессии. Ни один
 * файл поставки на этот модуль больше не ссылается.
 *
 * Заглушка попадает под `include` корневого `tsconfig.json` и компилируется в
 * `dist/src/ui/widgetRuntime.js`, то есть уезжает в пакет (`package.json`,
 * `files: dist`) пустым модулем под именем, которого в поставке уже нет.
 * Убрать это можно двумя способами — снять файл или исключить его из проекта
 * сборки, — и оба лежат вне поверхности этого изменения: у агента нет права
 * удаления, а корневой `tsconfig.json` изменение не объявляло своим. Работа,
 * которая снимет файл, снимет и эту строку вместе с ним; вреда пустой модуль
 * не делает — на него не ссылается ни один файл поставки.
 */
export {};
