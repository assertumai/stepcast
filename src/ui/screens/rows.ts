// Модуль переехал в src/ui/rows.ts: перечень строк поставки витрины называет
// не только экраны, и уровнем выше стоит рядом с каталогами, которые называет
// (row-module-convention, design.md, Решение 3). Тело строки каркаса —
// в src/ui/shell/row.ts. Файл оставлен пустым, а не реэкспортом старого
// пути, — импорт отсюда обязан стать ошибкой компиляции, а не молча
// продолжить работать через прежнее имя.
export {};
