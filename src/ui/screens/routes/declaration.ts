import type { ScreenDeclaration } from '../declaration.js';

/** Экран «Маршруты»: перечень действующих маршрутов и правка файла слоя (`ui-routes`, design.md Решение 12). */
export const declaration: ScreenDeclaration = {
  id: 'screen-routes',
  title: 'Маршруты',
  params: [],
};
