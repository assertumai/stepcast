// Модуль переехал в src/parts/builtin.ts: встроенный слой поставки — не дело
// ядра плагинов (kernel-domain-free-imports, Решение 2). Файл оставлен пустым,
// а не реэкспортом старого пути, — импорт отсюда обязан стать ошибкой
// компиляции, а не молча продолжить работать через прежнее имя.
export {};
