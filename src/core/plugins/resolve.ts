// Модуль переехал в src/parts/resolve.ts: `resolveWithPlugins` зовёт состав
// дефолта (`loadPlugins`) и потому принадлежит ему, а не ядру
// (kernel-domain-free-imports, Решение 3). Файл оставлен пустым, а не
// реэкспортом старого пути, — импорт отсюда обязан стать ошибкой компиляции.
export {};
