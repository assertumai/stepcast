// Переехало в src/parts/steps/decision/fields.ts (`builtin-step-kinds-as-rows`,
// design.md, Решение 8) — рядом с родственными видами шага. Файл оставлен
// пустым, а не удалён: инструменты этой сессии не могут снять файл, отслеживаемый
// git (`rm`/`git rm` отклонены песочницей), только заменить его содержимое.
export {};
