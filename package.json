{
  "name": "stepcast",
  "version": "0.0.0",
  "description": "Движок пайплайнов для разработки с ИИ-агентами",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "stepcast": "./dist/src/bin.js"
  },
  "exports": {
    ".": "./dist/src/core/index.js"
  },
  "files": [
    "dist",
    "schema"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src test",
    "test": "npm run build && node --test \"dist/test/**/*.test.js\"",
    "test:only": "node --test \"dist/test/**/*.test.js\"",
    "check": "npm run typecheck && npm run lint && npm run build && npm run test:only",
    "schema": "npm run build && node dist/scripts/generate-schema.js"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "execa": "^10.0.1",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "eslint": "^9.0.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.0.0"
  }
}
